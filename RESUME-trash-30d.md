# RESUME (삭제·휴지통 방) — **닫힘. 전부 나갔다** (2026-09-16 확인)

> 이 저장소에는 인계가 여럿이다. **이것은 「삭제·휴지통 방」 것이고, 이어갈 일이 없다.**
> 새 세션이 이 파일을 읽었다면 **다른 방 인계를 보라** — 여기서 할 일은 없다.
> 상세는 `docs/session-logs/2026-09-14-s23-trash-30d.md`.

## 남은 일 — **없다**

사장님 확정 «삭제-휴지통 이동-휴지통 내에 의무 보관 30일-30일 후 자동삭제-30일 전
원할시 바로 삭제 가능» + «등록되지 않은 업체(테스트한 업체)도 삭제할 수 있도록» 를
전부 만들어 냈고, **커밋 다섯 개가 모두 main 에 있다.**

```
[실측 2026-09-16 · git · merge-base --is-ancestor 로 5/5]
  966ca38   기능 ①  삭제 단추 + 휴지통 + 테스트 자리
  724ce55   기능 ②  30일 자동삭제 + 잠금 해제
  7ac3fa0f  도장     0.3.579 나갔다
  fcff10f0  도장 고침
  b6a93a38  인계 정리
  → main 0.3.593 · 기능이 나간 0.3.579 이후 열네 판이 더 지나갔다
```

**대장도 제자리다** — `docs/WORKLIST.md` 배포 대기 목록에 이 방 줄이 없고, §2 기능 표에
「삭제 · 휴지통 · 30일 자동삭제 … **완성**」으로 올라가 있다. 파일도 전부 main 에 있다
(`customers/purge.py` · `customers/trash/page.tsx` · `customers/tests/page.tsx` ·
마이그레이션 `20260914_1700_a_customer_trash.py`).

도장은 이 방이 [4/5] 에서 세 번 지고(채점 도는 사이 main 이 앞선다) 사장님 결정
«그만두고 다음 방에 맡겨» 로 넘겼는데, **다음 방이 가지를 합치면서 함께 데려갔다** —
맡긴 대로 됐다.

## 이 방이 끝내 못 잰 것 하나 (사장님 몫)

이 방은 운영 주소에 못 닿는다(egress 403). [실측 2026-09-16] `/api/health` 가 `000` —
**답이 없다는 뜻이지 「서버가 죽었다」가 아니다.** 도달했는지 **모르는** 것이다.

눈으로 재는 길: 콘솔 `/console/customers` 에서 줄마다 「삭제」, 제목 옆에 「테스트 자리」·
「휴지통」이 보이면 실제로 도는 것이다. 휴지통에 들어가면 「그냥 두면 N일 뒤 자동삭제」가
붙어 있어야 한다.

## 다음 사람이 같은 데서 안 막히게 (이 판에서 배운 것)

- **비운 대기 표는 그다음 배포를 막는다.** `claim_version` 은 대기 표에 줄(판 번호 또는
  `—`)이 있어야 번호를 물린다. 도장 커밋이 표를 비우자 배포가 [1/4] 에서 섰다.
- **[1/4] 는 절반만 고치고 설 수 있다.** `bump_version.sh` 가 판 파일을 올린 뒤
  `claim_version` 이 대장에서 멈추면 판 파일만 앞선다. 되돌리고 다시 돌린다.
- **알렘빅 head 가 갈라지는 자리.** 같은 날 두 방이 마이그레이션을 만들면 둘 다 같은
  부모를 가리켜 head 가 둘이 된다. 먼저 main 에 닿은 쪽 뒤로 잇는다(파일 이름의 시각도
  함께 옮긴다).
- **`VEO_DEPLOY_ORDER` 가 없으면 배포가 시작도 안 된다.** 사장님 원문을 그대로 인용한다.
- **채점(약 20분) 사이에 main 이 앞서면 [4/5] 에서 거절된다.** 여러 방이 동시에 나가는
  날에는 흔하다 — 합치고 다시 내면 되고, **판 번호는 main 에 먼저 닿는 쪽이 갖는다.**
- **이 컨테이너의 PostgreSQL 이 잘 내려간다.** 시험이 통째로 빨간불이면
  `pg_isready -h localhost -p 5432` 부터 본다. `service postgresql start`.
  [실측] 한 세션에 두 번 났다.
- **남의 방 줄을 지울 때는 `merge-base --is-ancestor` 로 재고 지운다** — 판단이 아니라
  잰 것이어야 한다.

## 참고

- veo-platform 은 `add_repo`(owner `recon9973-lang`) → `/home/user/veo-platform`.
- 그 저장소 대장 `docs/WORKLIST.md` · 지금 상태 `docs/STATE.md` 한 장.
- 이 저장소 현황 `PROJECT_STATE.md`, 지도 `핵심두뇌_MASTER.md`.
