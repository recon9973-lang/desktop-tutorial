# RESUME — 같은 과 방 (2026-09-23 · 0.3.650 인계 대기)

<!-- 가지: claude/practical-shannon-pcfyru -->

> 상세는 `docs/session-logs/2026-09-19-same-subject.md`.
> 현황 `PROJECT_STATE.md`, 지도 `핵심두뇌_MASTER.md`.
> **저장소 둘을 쓴다** — 고치는 곳은 `veo-platform`(ANSEO), 이 저장소는 기록만.

## 지금 상태 — **0.3.650 이 배포 대기**

사장님 「남은것 작업 시작」(2026-09-22). 남은 넷 중 **셋은 시장의 사실**이라 갈라
여쭈었고 사장님이 **「안 한다」**로 정하셨다. 남은 하나를 만들었다.

```
넘긴 것  가지 claude/practical-shannon-pcfyru · 판 0.3.650
받는 법  veo-platform:docs/HANDOFF-2026-09-23-same-subject-0.3.650-to-anseo.md
전달     **사장님이 하신다** — 방끼리 부르는 길은 없다
```

## 사장님이 정하신 것 — **「안 한다」도 결정이다**

```
피부과 ↔ 성형외과          안 묶는다        (2026-09-22)
내과 ↔ 가정의학과          안 묶는다
소아청소년과 ↔ 이비인후과   안 묶는다
한의원                     한의원끼리 — 가를 근거가 어디에도 없다
내과 건강검진센터 구분      안 가른다
```

**그냥 안 만들고 두지 않았다.** 코드와 시험의 글을 「아직 덜 물었다」에서 **「물어서
이렇게 정해졌다」**로 고쳤다(`competition_groups.py` · `test_competition_groups.py` ·
`test_our_subjects.py`). 안 그러면 다음 방이 표가 비어 보인다고 또 여쭙거나 **지어낸다.**

**바꾸려면 사장님께 다시 여쭙는다.** 묶음은 시장의 사실이지 코드가 아는 것이 아니다.

> 여쭙기 전에 재어 뒀다 — [실측 2026-09-23 · 네이버 「춘천 건강검진센터」] 돌아온
> 다섯 줄이 「병원,의원>내과」·「건강,의료>병원,의원」뿐이고 **검진 분류가 없다.**
> 심평원 30열에도 검진 칸이 없다. 그래서 선택지를 「홈페이지 글로만 가릴 수 있다」로
> 적어 여쭈었다 — **고를 수 있는 것만 내놓는다.**

## 만든 것 — 지도 오른쪽 「가까운 경쟁 다섯 곳」

사장님이 2026-09-19 에 고르신 안(「간추린 경쟁 목록 + 지도 연결」). 그 뒤 경쟁 판정
자체를 고치는 일(0.3.631~647)이 길어져 미뤄져 있었다.

```
어디   「상권」 탭 · 지도 오른쪽 칸 · 「우리 과」 줄 바로 아래
연결   줄 앞에 **지도가 찍는 바로 그 표식**(`markerImage('same')`)
       누르면 아래 전체 목록(`#competition-list`)으로 내려간다
```

**전체 표는 안 옮겼다** — `.list` 에 `min-width: 990px` 가 걸려 있고 0.3.588 에서 좁은
칸 밀림을 고치며 **일부러 가로 전체로 옮긴 것**이다. 옮기지 않고 새로 세웠다.

**같은 과가 없으면 칸이 아예 안 선다** — 칸 이름이 「경쟁」인데 경쟁이 아닌 곳으로
채우면 그 이름이 거짓이 된다(오류 231 과 같은 종류).

새 파일 `RivalsNearby.tsx` · 관문 `rivals-nearby.test.ts` 5건.

> **관문에 한 번 걸렸다.** 거리를 `toFixed`·`Math.round` 로 직접 적었다가
> `score-display.test.ts` 가 잡았다. 집에 있는 자(`formatCount`)로 바꿨다 — 그래야
> 간추린 목록과 전체 표가 **한 화면에서 같은 수**를 말한다. 관문이 옳았다.

## 바로 이어갈 작업

**① 나갔는지 확인한다 — 이 방은 배포를 안 돌린다**

관문(`scripts/deploy_room_gate.sh`)이 ANSEO 방 사본이 아니면 첫 줄에서 거부한다.
**`git config veo.deploy-room anseo` 를 이 사본에 넣지 않는다.**

**판 번호로 보지 마라** — 다섯 번 빼앗겼다(638·639·640·642·그리고 그 뒤). `origin/main` 의
`LocationTab.tsx` 에 `RivalsNearby` 가 있으면 나간 것이다.

**② 사장님께 사진 한 장** — 「상권」 탭에서 **지도와 그 오른쪽 칸이 같이 보이게.**
간추린 목록이 지도 옆에 제대로 섰는지, 줄 앞 표식이 지도의 것과 같은지 한 장이면 보인다.

**③ 그 뒤엔 붙들 것이 없다** — 새 오더를 기다린다. 남은 것은 전부 사장님이 「안 한다」로
정하셨다.

## 지나온 길 (참고)

```
0.3.631  경쟁 묶음        0.3.636  면허·갈래·층      0.3.637  네이버 플레이스
0.3.640  홈페이지 읽기    0.3.641  판정이 실제로 씀   0.3.643  왜 못 읽었는지 말함
                         ── 그 뒤 ANSEO 방이 이어감 ──
0.3.644~647  안 물었다 · 날것 `>` 를 먹었다 · 이웃도 네이버로       43 → 21 → 15
0.3.650  지도 오른쪽 간추린 목록 + 묶음 확정                        **대기**
```

## 주의·제약

- **고치는 곳은 `veo-platform`.** `add_repo` 로 붙이고 `/home/user/veo-platform` 에 clone.
  이 저장소(`desktop-tutorial`)를 고쳐서 ANSEO 가 바뀌는 일은 없다.
- **이 방은 배포를 안 돌린다**(0.3.638 관문). 검사는 여전히 이 방 몫이고, 거기서
  밟은 자리 일곱은 그대로 유효하다:
  ```
  ① Postgres 를 띄워야 한다. 안 그러면 ci-local·test-db 가 「연결 거부」
     service postgresql start · pg_hba.conf 127.0.0.1 → trust · 역할 root 생성
  ② 루트 .venv 는 3.12 로:  make setup PYTHON=/usr/bin/python3.12
  ③ **변경이력을 손으로 합치지 않는다.** 두 번 괄호를 끊었고(웹 시험 파일 둘이
     PARSE_ERROR), 한 번은 나간 항목과 안 나간 항목을 섞어 **0.3.640 항목이 둘**이
     됐다(관문이 잡았다). 안전한 방법 — `origin/main` 파일을 통째로 쓰고
     **내 항목 하나만 맨 위에 얹는다**
  ④ 배포 도중에 나무를 건드리면 미는 직전에 막힌다
  ⑤ **칸을 늘렸으면 `pnpm --filter @veo/api-client generate` 도 돌린다.** 안 돌리면
     `check-contracts` 가 「schema.d.ts is stale」로 선다
  ⑥ **Postgres 가 검사 도중에 꺼진다.** DB 시험이 무더기로 ERROR 나면 코드가 아니라
     환경이다 — `pg_isready` 로 먼저 보고 ①을 한다
  ⑦ **`git stash` 로 합치지 않는다.** 충돌 중에는 `pop` 이 막혀 작업이 stash 에
     갇힌다. 합치기 전에 **커밋부터** 한다
  ```
- **판 번호는 나갈 때 정한다.** 내 0.3.622 는 작업하는 동안 main 이 0.3.630 까지 가서
  막혔다. 먼저 `git fetch origin main` 으로 지금 판을 본다.
- **좁히기는 실패하면 바닥으로 내려간다** — 기준이 사라지면 반경 안이 전부 「그 외」가
  되어 지금보다 더 틀린다. 빈 칸은 「0 명」이 아니라 「모름」.
- **사람이 고르는 칸은 만들지 않는다** (사장님 「자동으로 분류해주고」).
- 커밋·PR·코드에 모델 ID 를 넣지 않는다. 지어낸 수치 금지, 못 잰 값은 «—».

## 덮이지 않은 것 — **한방**

> 2026-09-22 현재도 그대로다. 0.3.646 이 이웃도 네이버로 가리게 했지만 **한방은 여전히
> 못 덮는다** — 아래 까닭이 그대로이기 때문이다.

한의원은 상호에 과를 안 적고(「만지한의원」) 한방 과목엔 전문의 칸도 거의 비어, 사다리가
바닥까지 내려간다. **반경 안 한의원이 여전히 전부 「같은 과」다** — 사장님이 맨 처음
보내주신 화면 그대로다. 화면이 그 사실을 적게는 해 뒀다.

**네이버 플레이스(0.3.637~0.3.641)도 한방은 못 덮는다** — [실측] 지역검색이 한의원에
「병원,의원>한의원」이라고만 답하고, 그것은 **면허**라서 갈래로 쓰면 두 곳에서 같은
것을 정하는 꼴이 된다(`test_place_lanes.py::test_an_oriental_category_is_not_a_lane`).
한방을 가르려면 **한방 안의 경쟁 묶음**을 사장님께 여쭤야 한다 — 아직 안 여쭀다.
시험 하나가 이것이 안 덮였음을 붙들고 있다
(`test_our_subjects.py::test_an_oriental_clinic_is_not_narrowed_at_all`).

## 참고

- 시뮬레이션은 저장소에 있다 —
  `apps/api/tests/location/test_competition_across_customer_shapes.py`
  (거래처 여섯 꼴 · 17건). **한 사례만 보고 고치지 말 것** — 사장님 지적 8.
- 판정은 **서버 한 곳**에서만 난다: `location/service._neighbours_of`.
  `same_subject` 는 `veo/location/` 밖으로 안 나간다(리포트로 안 흘러감).
