# RESUME — 같은 과 방 (2026-09-20 22:1x KST)

<!-- 가지: claude/practical-shannon-pcfyru -->

> 상세는 `docs/session-logs/2026-09-19-same-subject.md`.
> 현황 `PROJECT_STATE.md`, 지도 `핵심두뇌_MASTER.md`.
> **저장소 둘을 쓴다** — 고치는 곳은 `veo-platform`(ANSEO), 이 저장소는 기록만.

## 지금까지 — **만들어 놓고 연결을 안 했다. 그것을 고쳤다.**

「같은 과」 판정이 **심평원 등록 진료과목이 하나라도 겹치면 참** 한 줄이었다
(`location/service.py` `_neighbours_of`). 의원·요양병원·병원은 표시과목을 여럿 걸어
두므로 가려내는 힘이 없었다 — 사장님 사례: 그랜드연합의원(등록 다섯 과)의 경쟁사로
반경 안 내과의원·안과의원·피부과의원이 전부 올라왔다.

```
0.3.631  경쟁 묶음        나감   근골격·통증(정형·재활·마취통증·신경외과)을 한 판으로
0.3.636  면허 · 갈래 · 층  나감   의과/치과/한방 전문의 인원수로 면허를 가른다
0.3.637  네이버 플레이스   나감   **그러나 경쟁 목록에 안 닿았다** ↓
0.3.640  길을 이었다       대기   + 홈페이지 읽기 + 설명글 바로잡기
```

## **결함 — 「만들었다」와 「닿는다」는 다른 말이다** (오류 230)

사장님이 2026-09-20 화면으로 잡으셨다: *「경쟁병원은 아직 적용안됐네. 다른 과가
경쟁병원으로 나옴. 이미 이것과 관련한 정리가 됐을텐데」*

우리 자리를 **두 곳에서 따로 재고 있었다.**

```
_center_of      lane_of(..., place_lane=…, site_lane=…)   ← 네이버·홈페이지가 들어감
_neighbours_of  lane_of(name, kind, subjects, doctors)    ← **둘 다 빠짐**
```

그랜드연합의원은 간판에도 등록 과목에도 갈래가 없어 아래쪽에서 늘 빈 갈래가 나왔고,
`same_lane` 이 「모름」을 돌려 **예전 방식으로 떨어졌다.** 0.3.637 도 0.3.639 도
**경쟁 목록에는 한 번도 닿지 않았다.**

**고침**: 목록이 우리 자리를 **다시 재지 않는다.** 화면이 적는 네 값
(`licence_ko`·`lanes_ko`·`tier_ko`·`lane_source_ko`)을 그대로 쓴다 — 어긋날 자리가
없어진다. `_geo_center`(주소로만 잡은 중심)도 같은 자리에서 갈래를 채운다. 안 채우면
그 거래처만 반경 안이 통째로 「그 외」가 되어 **반대 방향으로 틀린다.**

**관문** `test_the_list_uses_the_lane_the_screen_shows.py` — 사장님 화면을 그대로 세우고
**실물 `_neighbours_of` 를 부른다**(5건). 고침을 되돌려 2건이 깨지는 것을 확인했다.

> **왜 안 잡혔나.** 값을 재는 시험은 전부 옳았고 지금도 초록이다. 시뮬레이션
> (`test_competition_across_customer_shapes.py`)도 못 잡았다 — **셈을 옮겨 적은 것**이라
> 실물을 안 부른다. **새 근거를 만들면 그 근거가 판정까지 가는 길에 시험을 둔다.**

## 판 번호를 두 번 빼앗겼다

```
0.3.638  ANSEO 방이 배포 방 관문으로 가져감      → 내 것을 0.3.639 로
0.3.639  입지 방이 상권 칸 업종 막대로 가져감    → 내 것을 0.3.640 으로
```

**번호는 main 에 먼저 닿는 쪽이 갖는다.** 인계 중에도 움직이므로, 받는 방은 물리기
전에 `git fetch origin main` 으로 지금 판을 본다.

## 바로 이어갈 작업

**① 배포는 이 방 것이 아니다 — 확인만 한다**

0.3.638(ANSEO 방)이 **관문**을 세웠다: `scripts/deploy_room_gate.sh` 가 사본의
`git config veo.deploy-room` 을 보고 `anseo` 가 아니면 `make deploy`·`make preflight`
를 **첫 줄에서 거부**한다. 우회 변수는 없다. **그 값을 이 사본에 넣지 않는다** —
관문을 부수는 것이지 통과하는 것이 아니다.

```
넘긴 것   가지 claude/practical-shannon-pcfyru · 판 0.3.640
받는 법   veo-platform:docs/HANDOFF-2026-09-20-same-subject-0.3.640-to-anseo.md
대기 줄   veo-platform:docs/WORKLIST.md §2 「배포 대기 목록」
전달      **사장님이 하신다** — 방끼리 부르는 길은 없다([실측] ListAgents 빈 값)
```

다음 세션에서 할 일은 **나갔는지 확인**이다: `git fetch origin main` 후
`apps/api/src/veo/__init__.py` 의 판이 0.3.640 **이상**이고 `location/service.py` 의
`our_lane = Lane(` 이 main 에 있으면 나간 것이다. **판 번호로만 보지 마라** — 번호는
두 번 빼앗겼다. 안 나갔으면 사장님께 다시 알린다 — **이 방이 직접 내지 않는다.**

**② 실물 대조 — 이것이 가장 중요하다**

0.3.640 이 나간 뒤 사장님께 **사진 한 장**을 여쭌다 — 거래처 화면 「상권」 탭,
「경쟁 목록 — 같은 과」 표와 **그 위 「우리 과」 줄**(갈래·근거가 적힌 곳).

```
갈래가 「정형외과」로 적혀 있다   → 네이버가 말했다. 목록이 그것으로 센다
갈래 칸이 비어 있다             → 네이버를 못 읽었다. 목록은 과목 겹침으로 떨어지고
                                  설명글이 그렇게 적는다 (0.3.640 에서 그렇게 만듦)
```

**갈래가 비어 있으면 0.3.640 으로도 안 고쳐진다.** 그때 봐야 할 것은 코드가 아니라
**그 거래처의 네이버 열쇠**(금고)다. 이 컨테이너는 운영 주소로 못 나가 여기서는 못
잰다(egress 403). 같은 자리를 또 코드에서 찾지 않는다.

**③ 아직 안 만든 것**

```
지도 오른쪽 간추린 경쟁 목록   사장님이 고르신 안 · 전체 표를 통째로 옮기면 안 된다
                              (`.list` 에 min-width: 990px · 0.3.588 에서 일부러 넓힘)
나머지 경쟁 묶음               피부·성형 / 내과·가정의학 / 소아·이비인후 / 치과 / 한방
내과 「건강검진센터 있고 없고」  사장님이 말씀하셨는데 아직 안 만듦
전문병원 지정 목록(복지부)      공공데이터포털 15051054 · 파일 내려받기 · egress 막힘
                              0.3.640 의 홈페이지 읽기가 굿모닝·척탑을 이미 덮는다
```

## 대기/차단

- **사장님 답 대기**: 나머지 경쟁 묶음(피부·성형 등)은 **안 여쭀다 — 지어내지 않는다.**
  시험 하나가 이것을 붙들고 있다(`len(COMPETITION_GROUPS) == 1`).
- **배포 대기**: 0.3.640 — ANSEO 방이 받는다.
- **못 잰 것**: 운영 화면의 실제 경쟁 목록. egress 403.

## 주의·제약

- **고치는 곳은 `veo-platform`.** `add_repo` 로 붙이고 `/home/user/veo-platform` 에 clone.
  이 저장소(`desktop-tutorial`)를 고쳐서 ANSEO 가 바뀌는 일은 없다.
- **이 방은 배포를 안 돌린다**(0.3.638 관문). 검사는 여전히 이 방 몫이고, 거기서
  밟은 자리 넷은 그대로 유효하다:
  ```
  ① Postgres 를 띄워야 한다. 안 그러면 ci-local·test-db 가 「연결 거부」
     service postgresql start · pg_hba.conf 127.0.0.1 → trust · 역할 root 생성
  ② 루트 .venv 는 3.12 로:  make setup PYTHON=/usr/bin/python3.12
  ③ 변경이력(`changelog.ts`) 충돌을 정규식으로 풀지 않는다 — 괄호가 끊겨 웹 시험
     파일 둘이 PARSE_ERROR 로 통째로 떨어졌다. **두 번 다 같은 자리**(`items: [`)
  ④ 배포 도중에 나무를 건드리면 미는 직전에 막힌다
  ```
- **판 번호는 나갈 때 정한다.** 내 0.3.622 는 작업하는 동안 main 이 0.3.630 까지 가서
  막혔다. 먼저 `git fetch origin main` 으로 지금 판을 본다.
- **좁히기는 실패하면 바닥으로 내려간다** — 기준이 사라지면 반경 안이 전부 「그 외」가
  되어 지금보다 더 틀린다. 빈 칸은 「0 명」이 아니라 「모름」.
- **사람이 고르는 칸은 만들지 않는다** (사장님 「자동으로 분류해주고」).
- 커밋·PR·코드에 모델 ID 를 넣지 않는다. 지어낸 수치 금지, 못 잰 값은 «—».

## 덮이지 않은 것 — **한방**

한의원은 상호에 과를 안 적고(「만지한의원」) 한방 과목엔 전문의 칸도 거의 비어, 사다리가
바닥까지 내려간다. **반경 안 한의원이 여전히 전부 「같은 과」다** — 사장님이 맨 처음
보내주신 화면 그대로다. 화면이 그 사실을 적게는 해 뒀다.

**네이버 플레이스(0.3.637)도 한방은 못 덮는다** — [실측] 지역검색이 한의원에
「병원,의원>한의원」이라고만 답하고, 그것은 **면허**라서 갈래로 쓰면 두 곳에서 같은
것을 정하는 꼴이 된다(`test_place_lanes.py::test_an_oriental_category_is_not_a_lane`).
한방을 가르려면 **한방 안의 경쟁 묶음**을 사장님께 여쭤야 한다 — 아직 안 여쭀다.
시험 하나가 이것이 안 덮였음을 붙들고 있다
(`test_our_subjects.py::test_an_oriental_clinic_is_not_narrowed_at_all`).

## 참고

- 시뮬레이션은 저장소에 있다 —
  `apps/api/tests/location/test_competition_across_customer_shapes.py`
  (거래처 여섯 꼴 · 17건). **한 사례만 보고 고치지 말 것** — 사장님 지적 8.
- 판정은 **서버 한 곳**에서만 난다: `location/service._neighbours_of`.
  `same_subject` 는 `veo/location/` 밖으로 안 나간다(리포트로 안 흘러감).
