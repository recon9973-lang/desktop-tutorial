# RESUME — 같은 과 방 (2026-09-19 23:2x KST)

<!-- 가지: claude/practical-shannon-pcfyru -->

> 상세는 `docs/session-logs/2026-09-19-same-subject.md`.
> 현황 `PROJECT_STATE.md`, 지도 `핵심두뇌_MASTER.md`.
> **저장소 둘을 쓴다** — 고치는 곳은 `veo-platform`(ANSEO), 이 저장소는 기록만.

## 지금까지 (핵심만)

「같은 과」 판정이 **심평원 등록 진료과목이 하나라도 겹치면 참** 한 줄이었다
(`location/service.py` `_neighbours_of`). 의원·요양병원·병원은 표시과목을 여럿 걸어
두므로 가려내는 힘이 없었다 — 사장님 사례: 그랜드연합의원(등록 다섯 과)의 경쟁사로
반경 안 내과의원·안과의원·피부과의원이 전부 올라왔다.

**우리 과를 먼저 좁히는 사다리**(간판 ∪ 전문의 → 등록 전부 → 모름)를 세우고, 상대
쪽에는 간판을 **더한다**. **0.3.630 으로 나갔다**(ANSEO 방이 합쳐 냄 ·
[실측 22:48 KST] 서버·웹 0.3.630 · `/api/health` 3/3 200).

**그런데 실물 대조에서 안 고쳐졌다.** 화면이 「우리 과는 심평원에 등록된 과목
그대로입니다」라고 적는다 — 간판에 과가 없고(「그랜드연합의원」) 전문의로도 못 좁혔다.

```
veo-platform      0.3.630  d88dd657 · 32d520a4 · 6c4f1296   (main 에 있음)
desktop-tutorial  e65f9e9  상권 탭 이름이 「입지」로 남아 있던 줄 고침
```

## 바로 이어갈 작업

**① 네이버 플레이스 분류를 사다리 맨 위에 단다** — 사장님 오더 16 「자동으로 분류해주고」

통로·열쇠가 **이미 있다**: `apps/api/src/veo/providers/naver/search.py` 의
`Corpus = Literal["local", ...]` 이 지역검색을 부른다. 열쇠는 금고
(`datalab_for(db, organization_id=…)` — 진단이 쓰는 그것).

**단 하나 고칠 것**: `_item()` 이 네이버가 주는 **`category` 를 버리고 있다**.
한 줄 더 받으면 된다(`SearchItem` 에 칸 추가).

```
[실측 2026-09-19 23:11 KST · 이 세션에서 직접 호출]
그랜드연합의원       병원,의원>정형외과    춘천 중앙로 68  5층   grand1.co.kr
그랜드우리안과의원   병원,의원>안과        춘천 중앙로 68  2층
그랜드배내과의원     병원,의원>내과        춘천 중앙로 68  3층   grand1.co.kr
```

밟으면 안 되는 자리 둘 — **부를 때 실제로 본 것이다**:
- **동명이인 셋**(서울 관악 · 경기 의정부 · 강원 춘천) → **주소로 반드시 대조**.
  안 하면 엉뚱한 병원의 과를 우리 것으로 쓴다. 심평원 중심 좌표·주소와 맞춘다.
- **분류가 두 꼴**: `병원,의원>정형외과`(과 있음) / `건강,의료>병원,의원`(**과 없음**).
  뒤엣것은 **「모름」으로 두고 아래 칸으로 내려간다.** 못 읽은 것을 읽은 척하지 않는다.

받아 온 분류는 **저장해 두고 매번 안 부른다**(업종 쪽 `store_kind_code`·
`store_kind_source` 와 같은 꼴 — `Customer` 에 칸 둘 + 마이그레이션).

**② 경쟁 묶음** — 사장님 오더 17 (2026-09-19)

> *「정형외과, 재활의학과, 마취통증의학과, 신경외과는 엄밀히 살펴보면 진료과가
> 다르지만 일반적으로 경쟁 병원으로 인식되고 있음」*

우리 과가 그 넷 중 하나면 **넷 다** 경쟁으로 센다. 코드도 네이버도 모르는 지식이라
**표로 박는다**(나중에 한 줄씩 늘릴 수 있게 한 파일에).

이것이 ①의 짝이다 — 네이버가 「정형외과」 하나만 주는데, 묶음이 없으면 재활·통증
환자를 놓친다. **시뮬레이션이 잡았던 결함 ⓐ(간판이 과하게 좁힌다)도 묶음이 있으면
애초에 안 생긴다.**

**③ 사장님 결정 (2026-09-19 · 오더 18)**

> *「오케이 그럼 **과별로 경쟁병원을 정하는 걸로** 하자. 그랜드연합의원은 연합이니까
> **정형외과 마취통증의학과가 같이 있는 것으로 보고** 이어서 진행」*

**묶음 방식 채택.** 과마다 「이 과의 경쟁 병원은 이것들」을 표로 정한다.
그랜드연합의원은 **정형외과 + 마취통증의학과**로 본다 — ①(네이버 「정형외과」)에
②(근골격·통증 묶음)를 얹으면 자동으로 그렇게 된다. 손으로 넣지 않아도 된다.

나머지 묶음(피부·성형 / 내과·가정의학 / 소아·이비인후 / 치과 계열 / 한방 계열)은
**아직 안 여쭀다 — 지어내지 않는다.** 표를 한 줄씩 늘릴 수 있게 만들어 두고, 필요할
때 여쭌다.

**④ 그 뒤** — 지도 오른쪽에 **간추린 경쟁 목록 + 지도 연결**(사장님이 고르신 안).
전체 표를 통째로 옮기면 안 된다 — 0.3.588 에서 좁은 칸 밀림을 고치며 **가로 전체로
옮긴 것**이고 `.list` 에 `min-width: 990px` 가 걸려 있다. 되돌리면 1440px 창에서
가로로 굴러간다.

## 대기/차단

- **사장님 답 대기**: 근골격·통증 말고 **나머지 묶음**(피부·성형 등)은 아직 안 여쭀다.
- **못 잰 것**: 운영 자료에 「과목별 전문의수」가 실제로 채워져 있는지. 이 컨테이너는
  운영 주소로 못 나간다(egress 403). ①이 들어가면 이 물음 자체가 덜 중요해진다.

## 주의·제약

- **고치는 곳은 `veo-platform`.** `add_repo` 로 붙이고 `/home/user/veo-platform` 에 clone.
  이 저장소(`desktop-tutorial`)를 고쳐서 ANSEO 가 바뀌는 일은 없다.
- **배포 관문에서 밟은 자리 넷** — 둘은 환경, 둘은 절차:
  ```
  ① Postgres 를 띄워야 한다. 안 그러면 ci-local·test-db 가 「연결 거부」
     service postgresql start · pg_hba.conf 127.0.0.1 → trust · 역할 root 생성
  ② 루트 .venv 는 3.12 로:  make setup PYTHON=/usr/bin/python3.12
  ③ VEO_DEPLOY_ORDER 에 **사장님 실제 문장**을 인용한다 (요약·짐작 금지)
  ④ 배포 도중에 나무를 건드리면 미는 직전에 막힌다
  ```
- **판 번호는 나갈 때 정한다.** 내 0.3.622 는 작업하는 동안 main 이 0.3.630 까지 가서
  막혔다. 먼저 `git fetch origin main` 으로 지금 판을 본다.
- **좁히기는 실패하면 바닥으로 내려간다** — 기준이 사라지면 반경 안이 전부 「그 외」가
  되어 지금보다 더 틀린다. 빈 칸은 「0 명」이 아니라 「모름」.
- **사람이 고르는 칸은 만들지 않는다** (사장님 「자동으로 분류해주고」).
- 커밋·PR·코드에 모델 ID 를 넣지 않는다. 지어낸 수치 금지, 못 잰 값은 «—».

## 덮이지 않은 것 — **한방**

한의원은 상호에 과를 안 적고(「만지한의원」) 한방 과목엔 전문의 칸도 거의 비어, 사다리가
바닥까지 내려간다. **반경 안 한의원이 여전히 전부 「같은 과」다** — 사장님이 맨 처음
보내주신 화면 그대로다. 화면이 그 사실을 적게는 해 뒀고, **진짜 해답은 ①(네이버
플레이스 분류)** 이다. 시험 하나가 이것이 안 덮였음을 붙들고 있다
(`test_our_subjects.py::test_an_oriental_clinic_is_not_narrowed_at_all`).

## 참고

- 시뮬레이션은 저장소에 있다 —
  `apps/api/tests/location/test_competition_across_customer_shapes.py`
  (거래처 여섯 꼴 · 17건). **한 사례만 보고 고치지 말 것** — 사장님 지적 8.
- 판정은 **서버 한 곳**에서만 난다: `location/service._neighbours_of`.
  `same_subject` 는 `veo/location/` 밖으로 안 나간다(리포트로 안 흘러감).
