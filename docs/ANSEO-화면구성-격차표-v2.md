# ANSEO 화면 구성 격차표 v2 — «최대한 반영» 재판정 (2026-08-31)

> **오더**: «다시 두 화면 비교 분석해서 최대한 새로 만든 화면을 적용해줘» — 기능 존재 여부가
> 아니라 **화면 구성·배치·카드 문법** 관점으로 시안(Lovable ANSEO 콘솔 v1.1.0) 대 실물
> (veo.seokorea.org · 판 0.3.421)을 전수 재비교. 이전 격차표(08-29, G1~G18)와 이식
> 1~15판·서버 신설 4판이 소화한 것은 재확인만 하고, **이전에 보수적으로 제외한 항목을
> «최대한 반영» 기준으로 재판정**한다.
>
> **불변(재판정 대상 아님)**: ①S3 KPI 4장 = «?표시로 접기» 유지(사장님 확정 08-31)
> ②보존 2건 — 대시보드 DoNow 검색창 · 진단 탭 레이더 3장 ③무날조(잰 값만) ·
> 발행 불변 · 판 다르면 비교 금지 ④/console/geo AEO 독립 화면은 다른 방 소관.
>
> 실물 근거: 2026-08-31 저녁 전수 실측(apps/web 렌더 순서·박스 대장·패턴 grep).
> 시안 근거: `ANSEO-콘솔-디자인-워크플로우-인포그래픽-분석.md` S1~S13 + 제3·6·7부.

## 0. 이미 일치하는 것 (이번 세션까지의 이식으로 종결 — 재작업 금지)

| 시안 | 실물 |
|---|---|
| S1① 5단계 퍼널(클릭=필터) | StageFlow — 동일 문법 |
| S1⑥ 산점 사분면(크기=이슈·빨강=차단·판 밖 문구) | CompanyMapCard + 중앙값 십자 |
| S1⑧ 이슈 도넛 / S1⑨ 운영 경보 / S1⑪ 엔진 7종(사유 병기) | AlertCard + EngineRoster |
| S2①이슈 구성 바·②AEO 깔때기·③현황판(★고정·필터 칩·▲▼·«—»각주) | SeverityStacks·FunnelChart·CompanyTable |
| S3 손실 상위·판정 구성 스택 | TopLossCard·VerdictMixCard (0.3.401·402) |
| S4① 재채점 카드 | RescoreProbe (0.3.421) |
| S4② 받은/깎인/배점/달성률+4회차 스파크 | ScoreLoss 확장 (0.3.406·409·414) |
| S4③ 원점수·÷배점 병기 / S4⑦ 와플 / S4⑧ 구간표 | 0.3.413 / 0.3.404 / Headline bandTable |
| S5④ 90% 목표 바 | ScoreLoss 세로 90% 눈금 (0.3.406) |
| S9② 축×심각도 매트릭스 / S9④ 상승폭·담당 | AxisSeverityCard (0.3.405) / 0.3.419·420 |
| S8/S10 진단→발행 깔때기·발행본 도넛·내용 해시 | DiagnosisFunnelCard(0.3.408)·PublishedByCustomerCard(0.3.407)·ReportItem |
| 질문형 제목·오독 방지 각주·finding 판정 줄 | ChartCard 4슬롯(title/ask/finding/stamp) — 문법 정착 |

## 1. 격차표 v2 — 배치·구성 격차 (판단·판 배정)

| # | 시안 구성 | 실물 현황 | 격차(구성 관점) | 재판정 | 판 |
|---|---|---|---|---|---|
| V1 | **S1② 실행 큐** — 퍼널 바로 아래 **전체 폭**, 머리말 «예상 합계 +N · 산정 불가 K건», 행=번호·상태·심각도·거래처·축·metric·영향·담당·**상승폭 비례 막대** | Todo «오늘 손댈 것»이 8번째 구역·반 폭, 머리말·번호·막대 없음 | **대시보드의 주인공이 다르다** — 시안은 실행 큐가 화면의 축 | **반영** — Todo를 퍼널 바로 아래 전체 폭 «실행 큐»로 승격, 머리말+행 구성+막대(gain 실측·산정 불가 분리). 품·기한은 원천 없음 확정 제외 | **0.3.422** |
| V2 | **S1③~⑤ 축 요약 3카드**가 실행 큐 다음(화면 상반) → 산점⑥ → AEO 퍼널⑦ → 도넛⑧ → 경보⑨ | Scales(게이지2+AEO바)가 산점·경보 **뒤**(8번째) | 상반부 순서가 시안과 역전 | **반영** — 순서를 시안대로: 퍼널→큐→축 3카드→산점·슬로프·경보. DoNow는 최상단 보존 | **0.3.423** |
| V3 | **S4⑥/S5⑤ 손실 도넛** «총 손실이 어디서 나왔나» — 중앙=깎인 합계, 영역별 구성비 | 없음(손실은 막대·표만) | 손실의 **구성비** 시각이 없다 | **반영(재판정 뒤집음)** — Donut 부품 기보유, 데이터 실측(영역별 깎인 점수). SEO·GEO 탭에 각 1장 | **0.3.424** |
| V4 | **S3 진단 실행 카드** «진단일별 점수» — 완료 배지·점수·대비·수집 주소·실행자·범위·신뢰도·명세 판 | 진단 탭에 없음(SEO 탭 HistoryTable로만) | 진단 탭이 «실행 사실»을 말하지 않는다 | **반영** — 최근 실행 1건 요약 카드 신설(전 값 실측 기보유) | **0.3.425** |
| V5 | **S3 «이 점수가 덮지 못한 것»** 한계 자백 카드 — 예외 4줄+«차감하지 않았습니다» | 진단 탭에 없음(예외는 SEO/GEO 탭 와플 안에만) | **한계 자백 상설 자리**가 진단 탭에 없다 | **반영** — coverage 예외(0.3.417 API)를 진단 탭 카드로 승격 | **0.3.425** |
| V6 | **S7/S9 이슈 행의 회차별 판정 띠 4칸**(채움=판정·점선=못 잼) | IssueCard에 띠 없음(VerdictStrip은 진단 화면만) | 이슈 목록에서 «언제부터 앓던 문제인가»가 안 보인다 | **반영** — 검사별 회차 판정 API(0.3.415) 기보유 → IssueCard에 띠 배선 | **0.3.426** |
| V7 | **S4③/S5② 100점 환산의 영역 구성 스택**(칸 너비=배점·채움=득점) + «가장 크게 깎인 곳/가장 잘 지키는 곳» 두 문장 | 원점수 병기(0.3.413)까지만 — 스택·두 문장 없음 | 환산 카드가 «어느 영역이 얼마를 차지하나»를 안 그린다 | **반영** — CompositionBar 문법 재사용 + 두 문장(실측 최대·최소) | **0.3.427** |
| V8 | **값 갱신형 모션** — 게이지 700ms(stroke-dasharray)·퍼널 500ms(width)·도넛 hover 굵기+범례 연동, reduced-motion 전면 스위치 | GaugeArc·FunnelChart·Donut 트랜지션 미확인(motion.test는 @veo/ui) | «다시 접히는» 감각 부재 가능성 | **반영(실측 후)** — CSS 트랜지션 3종 + reduced-motion 가드. 이미 있으면 변경 없음 기록 | **0.3.428** |
| V9 | **아이브로우**(자간 넓힌 소형 레이블) + 질문형 제목 — 27타일 예외 0 | ChartCard에 eyebrow 슬롯 없음(제목+ask로 대체) — StageFlow 칸에만 존재 | 카드 머리 문법이 시안과 다름(질문·각주는 동등) | **부분 반영** — ChartCard에 eyebrow 슬롯 추가, 주요 인포 카드부터 단계 적용 | 0.3.429~ |
| V10 | S9① 전역 이슈 **심각도 도넛**(매트릭스와 별도) | 매트릭스만(도넛은 거래처 이슈 탭 IssueGradeCard) | 구성비 강조 카드 부재 | **부분 반영** — V3 도넛과 같은 판형 재사용, 매트릭스 위 소형 배치 | 0.3.429~ |
| V11 | S7① 이슈 탭 **상태 구성 스택**(열림~닫힘 5단) + S9③ 상태 5칸 | 등급 구성 카드만(상태 축 없음) | 상태(사람 손) 축의 구성 시각 부재 | **부분 반영** — 상태 5칸 줄(스택 or 칸) 후순위 | 0.3.429~ |
| V12 | S10 리포트 발행본 **`sha256:` 라벨** 병기 | 해시 23자만(알고리즘 라벨 없음) | 초소형 | **반영(동승)** — V7 판에 동승 | 0.3.427 |

## 2. 기각·제외 유지 (사유 — 재론 방지)

| 항목 | 사유 |
|---|---|
| S4⑤ 동심 아크 «어디에 아직 점수가 남아 있나» | **시안 분석 스스로 유일한 실패작 판정**(반지름 왜곡·§7-1 교체 권고). 남은 점수=달성률의 보수라 ScoreLoss·달성률과 같은 값 두 벌(0-D). 수평 막대 번역도 중복이라 기각 유지 |
| S3 게이지 2+관측 1 (진단 탭) | 그 자리는 **보존 확정 레이더 3장**이 차지(사장님 확정). 게이지 병설은 두 벌 금지 위반 |
| S3/S10 KPI 타일 행 | S3은 «?접기» **사장님 확정(08-31)** — 불변. S10 KPI 3장의 정보는 ChartCard 3장이 이미 나름 |
| S1② 품·기한 열 | 원천 없음 — 무날조 확정(08-31 «품 없이», 설계 메모 도장) |
| S1⑩ 대시보드 전 거래처 와플 | 커버리지 분모가 사이트 단위 — 전 거래처 합산 와플은 분모 섞기(원칙 1 위반). 탭 수준(0.3.404)으로 종결 |
| S6 AEO 탭 전체·S13 전역 AEO 화면들 | /console/geo(AEO) 독립 화면은 **다른 방 소관** |
| S11 의료 규정 | 다른 방 소관(원고 검수 트랙) |
| G14 워터폴·G15 생키 | 기존 판정 유지(실물 도형 문법 정립·데이터 누적 후) |

## 3. 판 순서 (0.3.422~)

```
0.3.422  대시보드 실행 큐 승격 — 전체 폭·머리말(합계+N·산정 불가 K)·행 구성·상승폭 막대   [V1]
0.3.423  대시보드 상반부 재배치 — 퍼널→큐→축 3카드→산점·경보 (시안 순서)               [V2]
0.3.424  SEO·GEO 손실 도넛 «총 손실이 어디서 나왔나»                                  [V3]
0.3.425  진단 탭 — 진단 실행 카드 + «이 점수가 덮지 못한 것» 한계 카드                  [V4·V5]
0.3.426  이슈 카드 회차별 판정 띠 배선                                                [V6]
0.3.427  100점 환산 — 영역 구성 스택 + 최대·최소 두 문장 + sha256 라벨                  [V7·V12]
0.3.428  값 갱신형 모션 3종 + reduced-motion                                          [V8]
0.3.429~ eyebrow 슬롯·전역 심각도 도넛·상태 구성 (후순위 묶음)                          [V9~V11]
```

매 판: verify → deploy-candidate → CI 초록 → main ff → 삼중 실측 → 도장(상시 오더).
박스 대장은 새 구역마다 사유 주석과 함께 선등재(s10 배움).
