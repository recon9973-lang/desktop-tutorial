# ANSEO 콘솔 시안(v1.1.0) — 디자인 · 기능 워크플로우 · 인포그래픽 전수 분석

> 근거: 2026-08-29 19:09~19:12 캡처 스크린샷 11장(원본 3024px 폭, 최장 17,250px), 27개 타일로 분할해 **전부** 판독.
> 시안 출처: Lovable 프로토타입(우하단 «Edit with Lovable» 배지), 푸터 «개발 VENOM · 연구 및 방법론 VEO-LAB · v1.1.0».
> 화면 7종 — ① 이번 주 실행 ② 거래처 목록 ③~⑧ 거래처 상세 6탭 중 진단·SEO·GEO·AEO·이슈·리포트 ⑨ 이슈(전역) ⑩ 리포트(전역) ⑪ 의료 규정(원고 검수).
> **화면 속 수치는 전부 테스트용 임시 데이터다.** 따라서 값의 정합성은 분석 대상에서 제외하고, **흐름·기능·디자인·구성**만 본다. 본문에 인용한 수치는 화면 구조를 설명하기 위한 예시일 뿐이다. 화면에 없는 동작은 «추정»으로 표기한다.

---

# 제1부 — 총론

## 1. 정보 구조(IA)

```
로고 영역    ANSEO  SEO · GEO · AEO · 네이버 키워드     (제품 축 4개를 로고 옆 상시 표기)
글로벌 내비  거래처 · 이슈 · 리포트 · 원고 검수 · AEO 관측 ▾ · 운영 ▾
우상단      테마 토글(해 아이콘) · «관리자» 역할 버튼
거래처 상세  진단 · SEO · GEO · AEO · 이슈 · 리포트     (탭 6개)
```

- 최상위 메뉴는 **업무 단위**(거래처·이슈·리포트·검수·관측·운영)이고, SEO·GEO·AEO는 **거래처 상세의 탭**이다. 사장님 확정사항(«SEO·GEO·AEO는 거래처 상세의 탭이고 최상위 메뉴로 안 올린다»)과 일치.
- 홈이 대시보드가 아니라 **«이번 주 실행»(작업 큐)**. 부제가 설계 선언문이다: «점수판이 아니라 작업 목록입니다. 앞 단계를 건너뛰면 뒤 단계 측정이 의미를 잃기 때문에, 작업 흐름 순서대로 놓고 그 안에서 품이 적은 것부터 세웁니다.»
- 모든 화면 상단에 **«표본» 배지**: «운영 콘솔에서 옮겨 온 표본 데이터입니다. 수집 파이프라인 연결 전까지 이 값으로 화면을 검증합니다.» — 목업임을 화면 스스로 선언. «저장됨» 배지(거래처·이슈 화면)는 «조직 데이터베이스에서 읽었습니다. 상태 변경은 이력으로 남습니다»로 저장 계층까지 구분한다.
- 푸터 고정 문구: «ANSEO가 계산하는 점수는 검색엔진과 AI 답변 엔진이 사이트를 읽을 수 있는 기술·구조 준비도입니다. 검색 순위를 예측하거나 보장하지 않으며, 수집한 값은 모두 출처와 수집 시각을 함께 표시합니다.»

## 2. 기능 워크플로우

### 2.1 핵심 루프 — 화면들이 그리는 상태 흐름

```
수집(크롤 40장) ─ 진단 실행(측정 39장) ──► 이슈 생성 ──► 실행 큐(이번 주 실행) ──► 사람이 조치
      ▲                                                                        │
      │                                                                        ▼
      └────────────── 재진단 ◄─── «재진단 대기» ◄─── «조치 중» ◄────────────────┘
                        │
                        ├─ 통과 → 이슈 자동 «닫힘» (사람이 닫을 수 없음)
                        └─ 실패 → 다시 «열림»
성공한 진단 실행 ──► 리포트 근거로 선택 ──► 발행본(불변 · sha256 · v버전) ──► 업체 전달
의료 규정 스캔(별도 축) ──► 후보 적발 ──► 사람이 «이슈로 올리기» ──► 위 루프에 합류
```

세 개의 불변 규칙이 루프를 지탱한다. 화면 문구 그대로:

1. **이슈는 사람이 닫지 않는다** — «닫힘은 사람이 누른 값이 아니라 재진단이 통과한 결과입니다.» (이슈 전역·거래처 이슈 탭·의료 규정에서 3회 반복)
2. **리포트에 수기 입력이 없다** — «숫자는 진단 실행에서만 옵니다» · «점수를 손으로 넣는 칸은 없습니다» · «발행본은 고칠 수 없습니다»(sha256·버전 고정, «발행본은 특정 진단 회차에 고정됩니다. 회차가 늘어도 이미 낸 문서의 숫자는 바뀌지 않습니다»)
3. **측정 실패 ≠ 0점** — «판정 못 한 항목은 0점으로 내리지 않고 분모에서 빼고 따로 셉니다» · «—는 아직 재지 않았다는 뜻이며 0점이 아닙니다» · «회색 칸은 0%가 아니라 진단 못 함입니다»

### 2.2 5단계 진행 퍼널 (이번 주 실행 상단 «어디가 막혀 있나»)

| 단계 | 질문 | 남은 일감 |
|---|---|---|
| 1 · 접근 | 읽을 수 있나 | **3** |
| 2 · 구조 | 무엇인지 알 수 있나 | **3** |
| 3 · 답변화 | 인용할 단위가 있나 | **2** |
| 4 · 관측 | 실제로 나오나 | 0 |
| 5 · 보고 | 설명할 수 있나 | 0 |

- «단계를 누르면 아래 목록이 걸러집니다» — 퍼널이 곧 큐의 필터(차트=내비게이션).
- 단계 의존성 각주: «1단계에 차단 항목이 있습니다. 접근이 막힌 주소가 있으면 그 뒤의 구조·답변화 점수는 **'낮다'가 아니라 '못 쟀다'** 입니다.» — 제품 제1원칙이 첫 화면 첫 카드에 있다.

### 2.3 실행 큐의 우선순위 로직

- 정렬 기준 공개: «오른쪽 막대는 **상승폭 + 품**입니다. 같은 시간을 넣었을 때 점수가 가장 많이 오르는 순서이며, 상승폭을 산정할 수 없는 항목은 0으로 내리지 않고 '산정 불가'로 둡니다.»
- 행 구성: 순번 · 문장형 제목 · 상태 배지 · 심각도 · 거래처 · 축 · metric id · 영향 주소 수 · 품(한 시간 안/반나절/여러 날) · 담당자 실명 · 기한 · 예상 상승폭.
- 집계도 분리: «예상 합계 +32.4 · 산정 불가 1건».

### 2.4 채점 모델 — SEO/GEO/AEO 3원 구조

| 축 | 성격 | 구조 | 환산 | 판정 종류 |
|---|---|---|---|---|
| SEO | 준비도 점수 | 6영역 · 배점 합계 128점 | 원점수/128×100 (41.70→32.58) | 감점·통과·판정 못 함·해당 없음 |
| GEO | 준비도 점수 | 5영역 · 배점 합계 100점 | 원점수=환산값 («배점 합계가 100이라 환산값과 같습니다») | 동일 4종 |
| AEO | **점수가 아니라 관측** | 물어봄→응답→언급→인용 4단 퍼널 | 단계마다 분모 상이, 합산 금지 | 언급됨·언급 안 됨·진단 못 함 |

- AEO 카드 제목이 «점수가 아니라 관측입니다». 분모 상이(215/215 → 208/215 → 30/215 → 22/**102**)를 매번 각주로 재설명: «출처를 주지 않는 엔진의 답변은 '인용 안 됨'이 아니라 '알 수 없음'으로 셉니다.»
- 채점 명세는 버전 관리(1.10.0)되며 «지금 명세로 다시 접기(재채점)» 버튼으로 저장된 원자료를 새 명세로 재계산한다.
- 항목마다 «판정 근거 → 실제로 본 응답·코드 조각 → 수정 내용(+기대 상승폭)»이 접힌 상세로 붙는다. openssl 핸드셰이크 출력, `<head>` 원문까지 증거로 첨부.

### 2.5 AEO 관측 절차 (탭 내 «키워드에서 AX 대응까지»)

1. **키워드 선별** — 검색량 상위 선별, 출처·수집 시각 기록
2. **질문 추출** — 실제 사용자가 물을 법한 문장으로 변환, 검수 후 확정
3. **7개 서비스 질의** — 동일 질문·동일 조건, 답변 원문 보관
4. **답변 구조화** — 출처 도메인·URL, 언급된 경쟁사, 인용 콘텐츠를 표로
5. **AX 대응 정리** — 서비스별·기간별 인용/밀림 비교 → 조치 항목 연결

- «검색량 조회는 **네이버 키워드**에서, 브랜드 식별자는 **브랜드 식별**에서 등록합니다» — 다른 모듈과의 연결 고리 명시.
- 질의 집합 규율: «발행한 집합만 매주 돕니다. 저장만 된 집합은 측정에 쓰이지 않습니다. **질문을 바꾸면 그 시점부터 추이가 갈라집니다.**»
- 답변 1건에서 구조화하는 필드: 답변 원문 / 출처 도메인·URL / 언급된 경쟁사 / 인용에 사용된 콘텐츠(페이지·문서) / 우리 거래처 인용 여부와 위치 / 서비스명·모델·실행 시각. «하나라도 얻지 못하면 0이 아니라 '진단 못 함'으로 남기고 사유를 함께 표시합니다.»

### 2.6 의료 규정(원고 검수)의 별도 축 설계

- «의료광고 심의 관점에서 별도로 세는 항목입니다. **일반 SEO 점수와 합치지 않습니다.**»
- 검사 방식이 항목마다 셋으로 구분: **자동 판정** / **후보만 수집** / **사람 확인** — 그리고 사전·페이지 요소로 판정 불가한 조항(거짓 광고·시술행위 노출·외국인환자 유치)은 «사전으로 찾을 수 없어 스캔하지 않음 = 진단 못 함»으로 정직하게 남긴다.
- 우선순위 레이블 3단: **먼저 볼 것 / 확인 필요 / 기록용**. 기계는 후보(55건)만 만들고, 사람이 «이슈로 올리기»를 눌러야 본 루프 합류. 올린 뒤에도 «사람이 닫지 않습니다. 문구를 고친 뒤 재진단이 통과해야 닫힙니다.»
- 법적 책임 경계 문구: «이 검사는 법률 자문이 아니며, 심의 소지가 있는 표현을 사람이 판단하도록 모아 줄 뿐입니다.»

## 3. 디자인 시스템

### 3.1 토대

- **다크 단일 기조**(라이트 토글 존재): 거의 순흑 배경 + 미세하게 밝은 카드 + 1px 보더 + 큰 라운드. 카드가 유일한 컨테이너 단위 — 전 화면이 카드 그리드 하나로 조립된다.
- **타이포 2원 체계**: 국문 산세리프(제목·본문) + 모노스페이스(**모든** 수치·백분율·metric id·코드·sha256). 숫자 열 정렬과 «측정값» 정체성을 타이포로 만든다.
- **컬러 시맨틱**: 파랑=우리/중립 측정, 초록=통과·양호·개선(+), 주황=중요·경고·깎임, 빨강=차단·심각·감점(-). 증감은 ▲초록/▼빨강 삼각형. 심각도 4단(차단·심각·중요·경미)과 상태 5단(열림·조치 중·재진단 대기·닫힘·판정 못 함)이 모두 이 팔레트의 배지로 처리.

### 3.2 반복 패턴 — 이 시안의 서명

1. **아이브로우 + 질문형 제목**: 모든 카드가 `자간 넓힌 소형 레이블`(예: `깎인 점수 구성`) 다음에 질문/문장형 제목(«총 손실이 어디서 나왔나» «누가 대신 인용되고 있나» «이 점수가 어디에 서 있나» «어느 AI가 우리를 부르나»). 차트 제목이 '무엇을 그렸나'가 아니라 '무슨 질문에 답하나'를 말한다. **27개 타일 전체에서 예외 0건.**
2. **각주 해설**: 카드 하단마다 회색 한 줄로 읽는 법·함정을 설명. 인포그래픽마다 «이 차트를 오독하는 법»을 선제 차단하는 문구가 붙는다(각 화면 전수 기록 참조).
3. **증거 접기**: 항목 행 → 판정 근거 + 실측 코드 조각 + 수정 방법(고치면 +N) 세트.
4. **CTA 절제**: 화면당 파란 버튼 1~2개(«+ 새 업체 등록», «이슈로 올리기», «재채점», «집계»). 나머지는 링크(«전체 이슈 보기 →», «항목별 근거 보기 →», «호출량·비용 보기 →», «칸별로 보기», «버전 열람·내보내기 →»)이거나 읽기 전용.
5. **한계 자백 카드**: «이 점수가 덮지 못한 것», «발행 리듬 — (달이 하나뿐이라 리듬은 그리지 않습니다)» 같은, 못 그린 것·못 잰 것을 **말하는 자리**가 레이아웃에 상설로 있다.

### 3.3 디자인 리스크

- **회색 보조 텍스트 대비 부족**: 순흑 위 중회색 각주·레이블 다수가 WCAG AA(4.5:1) 경계로 보인다. 각주가 이 시스템의 핵심 장치이므로 한 단계 밝혀야 한다.
- **metric id 노출**(`seo.onpage.title_present_and_unique` 등): 운영자 콘솔에선 추적성에 유리하나 업체 전달 발행본에 그대로 나가면 안 된다 — 발행 템플릿의 번역 레이어 확인 필요.
- **세로 길이**: 거래처 상세 AEO 탭 원본 10,718px, 의료 규정 17,250px. 섹션 앵커·카드 접기 없이는 후반 카드 도달률이 낮을 것.
- 의료 규정 히트맵의 빨강 농도 스케일 기준(최댓값 정규화인지 절대 구간인지)이 화면에 없다.

---

# 제2부 — 화면별 전수 기록

## S1. 이번 주 실행 (Loop Contents) — 원본 3024×7146

**표본 배지**: 2026-08-24(월)~08-30(일) · 한국 시간 · 실행 대기 8건 · 기준일 2026-08-26

**① 작업 흐름 «어디가 막혀 있나»** — §2.2의 5단계 퍼널. 각 칸: 번호·단계명·질문·남은 일감 수(빨강/주황 강조)·진행바.

**② 실행 큐 «순서대로 8건»** — 예상 합계 +32.4 · 산정 불가 1건. 8행 전체:

| # | 제목 | 상태 | 심각도 | 거래처·축·metric | 영향 | 품·담당·기한 | 상승폭 |
|---|---|---|---|---|---|---|---|
| 01 | 응답은 왔지만 문서를 읽지 못한 주소가 있습니다 | 재진단 대기 | 차단 | 대구명성요양병원 · SEO · seo.http_status_ok | 주소 1개 | 한 시간 안 · 이재훈 · 8/27 | **+6.2** |
| 02 | robots.txt 를 수집하지 못해 크롤링 허용 여부를 확인하지 못했습니다 | 판정 못 함 | 차단 | 대구명성요양병원 · SEO · seo.robots.meta_indexable | 주소 1개 | 한 시간 안 · 이재훈 · 8/27 | **— 산정 불가** |
| 03 | HTTPS 인증서가 유효하지 않아 평문 HTTP 로 측정했습니다 | 조치 중 | 심각 | 그랜드연합의원 · SEO · seo.https.certificate_valid | 주소 1개 | 반나절 · 정수민 · 8/28 | +4.8 |
| 04 | 제목 태그가 없거나 다른 페이지와 겹칩니다 | 조치 중 | 중요 | 그랜드배내과의원 · SEO · seo.onpage.title_present_and_unique | 주소 14개 | 반나절 · 김하늘 · 8/29 | +3.4 |
| 05 | 대표 URL(canonical) 선언이 자기 자신을 가리키지 않습니다 | 재진단 대기 | 중요 | 시원클리닉 · SEO · seo.canonical.self_reference | 주소 8개 | 반나절 · 정수민 · 8/29 | +2.7 |
| 06 | 구조화 데이터 선언이 없어 검증할 대상이 없습니다 | 열림 | 심각 | 굿투어 · GEO · geo.structured_data.declared | 주소 39개 | 여러 날 · 김하늘 · 9/2 | **+9.1** |
| 07 | 설명 메타가 비었거나 너무 짧습니다 | 닫힘 | 경미 | 그랜드아름다운의원 · SEO · seo.onpage.meta_description | 주소 22개 | 한 시간 안 · 박도윤 · 9/1 | +1.2 |
| 08 | 질문형 콘텐츠 블록이 없어 답변 엔진이 인용할 단위가 없습니다 | 열림 | 중요 | 베놈 · GEO · geo.answerability.faq_block | 주소 11개 | 여러 날 · 박도윤 · 9/4 | +5.0 |

- 우측 막대는 상승폭에 비례하고, 그 아래 회색 작은 수치는 품 환산치로 보인다(6.2/1.6/1.1/0.9/1.1/1.2/0.6 — 추정: 시간당 상승폭).
- 각주: «오른쪽 막대는 '상승폭+품'입니다. …산정할 수 없는 항목은 0으로 내리지 않고 '산정 불가'로 둡니다.»

**③~⑤ 축별 요약 3카드**
- SEO 게이지 **46.1점** — «진단 주소 11곳의 평균 · 최저 13.68 — 최고 83.43»
- GEO 게이지 **72.1점** — «거래처 10곳의 평균 · SEO와 같은 크롤 · 최저 59.89 — 최고 81.17»
- AEO **30 / 215건 답변에서 언급** — 4단 바: 물어본 것 215/215, 응답 받음 208/215, 우리가 언급됨 30/215, 우리가 인용됨 22/102. 각주: «30/208 · 이번 주 · 거래처 4곳 합산 · **점수가 아니라 관측입니다 — 준비도(SEO·GEO)와 분모가 달라 같은 게이지로 그리지 않습니다.**» ← 게이지 2개 + 바 1개로 시각 형식 자체를 분리한 것이 의도된 설계.

**⑥ 거래처 배치 «읽히는가·검증되는가»** — SEO(가로)×GEO(세로) 산점도. 사분면 코너 라벨: 좌상 «AI는 읽는데 검색은 못 읽음 — 구조 정비» / 우상 «양쪽 다 준비됨 — 관측으로» / 좌하 «둘 다 막힘 — 접근부터» / 우하 «검색은 되는데 AI는 못 씀 — 답변화». 각주: «못 잰 거래처 1곳은 원점이 아니라 판 밖입니다.» «점 크기는 열린 이슈 수, 빨강은 차단 항목이 있는 곳입니다. 두 축을 하나로 합치지 않고 위치로 읽습니다.»

**⑦ AEO 관측 «어디서 줄어드나»** — 퍼널 바 4단, 단계 사이 감소분을 빨간 주석으로: 응답 받음 **-7**, 우리가 언급됨 **-178**, 우리가 인용됨 **-8**. 각주: «마지막 단계는 분모가 다릅니다 — 출처를 볼 수 있었던 답변만 셉니다.»

**⑧ 이슈 구성 «열린 이슈 315건»** — 도넛: 차단 6(1.9%) · 심각 35(11.1%) · 중요 71(22.5%) · 경미 203(64.4%). «전체 이슈 보기 →»

**⑨ 운영 경보 «사람이 봐야 하는 것»** — 4행:
- 차단 이슈 **6건** — 베놈·시원클리닉·굿투어·그랜드우리안과의원·그랜드배내과의원 외 1곳
- 진단 밀린 곳(14일 넘음) **1곳** — 대구명성요양병원 14일
- 관측 안 도는 곳(발행은 끝남) **5곳** — 시원클리닉·그랜드연합의원·대구명성요양병원·그랜드우리안과의원·그랜드배내과의원
- AI 엔진 끊김(최근 이레) **1종** — «구글 AI Overview 1건 — 상대 서버에서 답을 못 받았습니다. **열쇠 만료일 수 있습니다.**» ← 원인 후보까지 제시

**⑩ 진단 커버리지 «사이트의 몇 %를 봤나»** — 와플(파랑) + 예외 4줄: ①1개 주소 응답은 받았지만 문서를 읽지 못해 채점 대상 제외 ②39장 측정, 사이트 전체 확인 안 됨 — 페이지 간 비교 검사(중복·고아 페이지·깨진 링크)는 전체를 재야 판정 ③HTTPS 인증서 무효로 1개 주소 평문 HTTP 측정 ④robots.txt 미수집. «측정 39장 / 수집 40장 · 신뢰도 73.25%»

**⑪ AI 엔진 «관측 대상 7종»** — Claude(Anthropic)·Gemini(Google)·ChatGPT(OpenAI)·Grok(xAI)·구글 AI Overview(**주황 점** = 장애)·네이버 AI 브리핑·Perplexity. «쓸 수 없는 엔진도 목록에서 빼지 않습니다. 빼면 '여기 있는 게 전부'로 읽힙니다.» «호출량·비용 보기 →»

## S2. 거래처 목록 — 3024×3482

부제: «거래처 목록과 진단 상태. 상세로 들어가면 SEO·GEO·AEO·이슈·리포트 탭이 열립니다.» / 저장됨 배지: 거래처 11곳.

**① 거래처별 이슈 구성 «많기만 한가, 심각한가»** — 수평 바 11개, 차단 보유 거래처는 빨간 바 + «차단 1» 라벨:
굿투어 43(차단1) · 그랜드연합의원 35 · 그랜드우리안과의원 33(차단1) · 그랜드배내과의원 33(차단1) · 시원클리닉 31(차단1) · 베놈 30(차단1) · 그랜드아름다운의원 26 · 법무법인 일맥 22(차단1) · 참사랑한의원 21 · 범어5층치과 21 · 대구명성요양병원 20(차단1).
각주: «열린 이슈 합계 315건. **처리 순서는 건수가 아니라 심각도·밀도로 정합니다.**»

**② AEO 진행 깔때기 «11곳»** — 거래처 등록 11/11(100.00%) → 질문 발행 9/11(81.82%) → 관측 완료 4/11(36.36%). 각주: «다음 할 일 = 질문 발행이 없는 2곳 — **깔때기의 목**이 여기입니다. SEO 진단 완료 11곳 · GEO 진단 완료 10곳.»

**③ 거래처 현황판 «맡은 곳 11곳»** — 우상단 «+ 새 업체 등록». 필터 칩: 전체 11 · 14일 지남 1 · 이슈 열림 11 · 소재지 없음 2 · 고정 5. 테이블(★=고정) 전 행:

| 거래처 | 측정 URL | SEO | GEO · AEO | 이슈 | 마지막 진단 |
|---|---|---|---|---|---|
| ★그랜드연합의원 (강원 춘천 중앙로 68 5층) | grand1.co.kr/orthopedics/ | 26.00 ▲3.70 | 66.53 · 발행됨 | 35 | 8일 전 |
| ★그랜드우리안과의원 (중앙로68 2층) | grand1.co.kr/ophthalmology/ | 25.42 0.00 | 66.62 · 발행됨 | 33 | 6일 전 |
| ★그랜드배내과의원 (중앙로68 3층) | grand1.co.kr/pediatrics/ | 25.42 ▼0.76 | 66.62 · 발행됨 | 33 | 6일 전 |
| ★베놈 (기타 · 대구 수성구) | venomad.com | 77.10 ▼2.35 | 76.79 · 측정 1회 | 30 | 1일 전 |
| ★그랜드아름다운의원 (중앙로 68 4층) | grand4.co.kr | 58.58 ▲5.04 | 72.91 · 측정 2회 | 26 | 12일 전 |
| 굿투어 (경북 울릉군) | good-tour.kr | 58.91 | 74.59 · 집합 없음 | 43 | 12일 전 |
| 시원클리닉 (소재지 없음) | siwonclinic.co.kr | 37.98 ▲0.58 | 78.64 · 발행됨 | 31 | 12일 전 |
| 법무법인 일맥 (법률사무소 · 대구 중구) | 1macklaw.co.kr | 83.43 | 81.17 · 측정 2회 | 22 | 7일 전 |
| 참사랑한의원 (소재지 없음) | chamsarang1075.com | 77.99 ▲2.10 | 76.93 · 측정 2회 | 21 | 오늘 |
| 범어5층치과 (대구 수성구 범어동) | 5fdental.com | 22.22 | 59.89 · 발행 전 | 21 | 7일 전 |
| 대구명성요양병원 (대구 달서구) | mshp.kr | 13.68 | **—** · 발행됨 | 20 | 14일 전 |

- AEO 열의 상태 어휘가 5종이나 된다: 발행됨 / 측정 1회 / 측정 2회 / 집합 없음 / 발행 전 — 깔때기 단계가 그대로 상태 어휘. GEO «—» 각주: «한 줄이 업체 한 곳입니다. **'—'는 아직 재지 않았다는 뜻이며 0점이 아닙니다.**»
- 그랜드 계열 3곳이 같은 도메인의 경로 3개(orthopedics/ophthalmology/pediatrics)로 등록 — 멀티 사이트 한 도메인 케이스를 표본에 포함해 둔 것.

## S3. 거래처 상세 · 진단 탭 (그랜드아름다운의원) — 3024×3544

헤더: 병원·의원 · 강원도 춘천시 중앙로 68 4층 · grand4.co.kr · 열린 이슈 26건. 표본: 최근 측정 2026. 8. 12. 오후 7:37 · 기준일 2026-08-26.

**KPI 4장**: SEO 준비도 **58.58 ▲5.04**(누적 2회 · 명세 1.10.0) / GEO 준비도 **72.91**(«SEO와 같은 크롤에서 함께 나옵니다») / AEO 관측 **2판**(측정됨) / 열린 이슈 **26**(차단 0건 포함)

**게이지 2 + 관측 1**: SEO 100점 환산 게이지 · «항목별 근거 보기 →» / GEO 게이지 · 깎인 점수 병기 / AEO «점수가 아니라 관측입니다» 4단 퍼널 + «준비도(SEO·GEO)와 관측(AEO)은 분모가 다릅니다. 한 숫자로 합치지 않습니다.» — 상단 KPI(요약 숫자)와 아래 게이지(환산·근거 진입점)를 분리한 2단 구성.

**«지금 점수를 가장 많이 깎는 영역»** — 두 축 합쳐 손실 상위 7, 롤리팝: 01 SEO·경쟁력 25.91점 / 02 SEO·해석 불가 22.46 / 03 SEO·대표 URL 혼란 19.62 / 04 SEO·클릭·표현 14.3 / 05 GEO·구조화 데이터 12.1 / 06 GEO·답변 가능성 8.4 / 07 GEO·개체 식별 4.8. 각주: «점수는 축별로 따로 매기되, **'먼저 손댈 곳'을 고를 때만 한 줄에 세웁니다.** 배점이 다르므로 옆의 배점과 함께 읽으세요.» — 합산 금지 원칙과 실무 필요(우선순위) 사이의 타협을 문구로 정당화.

**항목 판정 구성 «SEO 12개 + GEO 10개»** — 스택 바: 감점 13 · 판정 못 함 3 · 통과 5 · 해당 없음 1. «'판정 못 함'은 감점 0이지만 통과도 아닙니다 — 수집을 복구해야 확정됩니다.» + SEO/GEO 받은 점수 추이 스파크라인 2줄.

**진단 실행 «진단일별 점수»** (완료 배지) — 2026. 8. 12. 오후 7:37 · 점수 58.58 · 지난 회차 대비 0.00 ▲5.04 · 수집 주소 40개 · 실행자 이재훈. «진단 범위 74.08% · 신뢰도 73.25% · 채점 기준 1.10.0»

**«이 점수가 덮지 못한 것»** — 한계 4줄(S1-⑩과 동일 내용) + «확인하지 못한 항목은 차감하지 않았습니다. **차단됐다는 뜻이 아니라 확인하지 못했다는 뜻입니다.**»

## S4. 거래처 상세 · SEO 탭 — 3024×6498

표본: 명세 1.10.0 · 측정범위 74.08%.

**① 채점 명세 카드 «지금 명세로 다시 접기»** + «재채점» 버튼: «저장된 점검 항목을 명세대로 다시 접어 거래처 점수를 갱신합니다. 판정 못 한 항목은 0점으로 내리지 않고 분모에서 빼고 따로 셉니다.»

**② SEO 채점 6영역 «어느 영역에서 점수를 받았고, 어디서 깎였나»** (배점 합계 128 · 명세 1.10.0):

| 영역 | 받은 점수 | 깎인 점수 | 배점 | 달성률 |
|---|---|---|---|---|
| 경쟁력 | 2.09 | -25.91 | 28 | 7% |
| 해석 불가 | 5.54 | -22.46 | 28 | 20% |
| 대표 URL 혼란 | 1.38 | -19.62 | 21 | 7% |
| 클릭·표현 | **-0.30** | -14.30 | 14 | **-2%** |
| 기본 정비 | 2.99 | -4.01 | 7 | 43% |
| 색인 차단 | 30.00 | -0.00 | 30 | 100% |

- 바는 «옅은 칸=배점 크기(공통 눈금 30점), 진한 칸=깎인 점수»의 이중 인코딩. 4회차 추이 스파크라인 각주: «추이선은 그 영역의 '받은 점수' 최근 4회차 — 마지막 점이 지금 값이고, 선 아래 채움은 배점 대비 높이입니다(손실이 크면 거의 보이지 않습니다).»
- 음수 값(클릭·표현 -0.30, 달성률 -2%)도 그대로 그린다 — 화면이 값을 미화하지 않고 음수 바를 반대 방향 빨강으로 표현하는 문법까지 갖춰져 있다.

**③ 100점 환산 32.58/100** — 원점수 41.70 / 배점 128 · ÷128×100 · 깎인 점수 -86.30. 영역별 구성 스택(칸 너비=배점): 경쟁력 2.1/28(7%) · 해석 불가 5.5/28(20%) · 대표 URL 1.4/21(7%) · 클릭·표현 -0.3/14(-2%) · 기본 정비 3.0/7 · 색인 차단 30.0/30(100%). 하단: «가장 크게 깎인 곳 — 경쟁력 -25.91 (배점 28)» / «가장 잘 지키는 곳 — 색인 차단 30.00점». 각주: «배점 합계는 원점수 128점이라 환산값과 숫자가 다릅니다 — 거래처 목록·대시보드에 뜨는 SEO 점수는 언제나 이 100점 환산값입니다. 원점수는 '어느 영역에서 몇 점을 잃었나'를 볼 때만 씁니다.»

**④ 진단 항목 «항목 12개 — 감점 7 · 판정 못 함 2 · 통과 2»** — 필터 칩(전체 12/감점 7/판정 못 함 2/통과 2/해당 없음 1). 12행 전체:

| 판정 | 항목 | metric · 심각도 · 영향 | 점수(배점) |
|---|---|---|---|
| 통과 | 색인을 막는 선언이 있는가 | seo.robots.meta_indexable · 39개 | 0.00 (30) |
| 판정 못 함 | robots.txt 를 읽을 수 있는가 | seo.robots.txt_fetchable · 차단 · 1개 | 0.00 (4) |
| 감점 | HTTPS 인증서가 유효한가 | seo.https.certificate_valid · 심각 · 1개 | -4.01 (7) |
| 감점 | 제목 태그가 있고 페이지마다 다른가 | seo.onpage.title_present_and_unique · 중요 · 14개 | -6.40 (8) |
| 감점 | 설명 메타가 채워져 있는가 | seo.onpage.meta_description · 경미 · 22개 | -7.90 (6) |
| 감점 | 대표 URL 이 자기 자신을 가리키는가 | seo.canonical.self_reference · 중요 · 8개 | -19.62 (21) |
| 감점 | 본문 텍스트를 추출할 수 있는가 | seo.render.text_extractable · 심각 · 26개 | -22.46 (28) |
| 감점 | 제목 계층(H1~H3)이 문서 구조를 나타내는가 | seo.onpage.heading_structure · 중요 · 18개 | -3.20 (6) |
| 감점 | 주요 콘텐츠가 제때 그려지는가 | seo.perf.lcp · 중요 · 39개 | -12.10 (14) |
| 통과 | 모바일 화면에 맞춰지는가 | seo.mobile.viewport · 39개 | 0.00 (6) |
| 판정 못 함 | 같은 내용의 페이지가 중복되는가 | seo.crawl.duplicate_pages | 0.00 (—) |
| 해당 없음 | 사이트맵이 선언되어 있는가 | seo.sitemap.declared | 0.00 (—) |

- HTTPS 항목 펼침 상세: 판정 근거 «인증서 체인이 끊겨 있어 평문 HTTP 로 내려받아 측정했습니다. 이번 점수는 HTTP 기준입니다.» + TLS 핸드셰이크 코드블록(`openssl s_client -connect www.example.kr:443` / `verify error:num=20:unable to get local issuer certificate`) + 수정 내용(**고치면 +4.8**): 1. 중간 인증서(intermediate) 체인 포함 재발급·재배포 2. HTTP→HTTPS 301 리다이렉트 한 단계 정리 3. 재발급 후 재진단하면 HTTPS 기준 재채점.
- 각주: «항목을 누르면 '판정 근거 → 실제로 본 응답·코드 조각 → 수정 내용' 이 함께 열립니다. '판정 못 함'은 감점 0이지만 통과도 아닙니다 — 수집을 복구한 뒤 재진단해야 확정됩니다.»

**⑤ «어디에 아직 점수가 남아 있나»** — 동심 아크 게이지(중앙 32.6): 경쟁력 7.5% · 해석 불가 19.8% · 대표 URL 혼란 6.6% · 클릭·표현 -2.1% · 기본 정비 42.7% · 색인 차단 100.0%. «바깥 궤도일수록 배점이 큰 영역입니다. 길이는 '남은 점수 비율'이라 짧을수록 손실이 큽니다.»

**⑥ «총 손실이 어디서 나왔나»** — 도넛(중앙 86.3 깎인 점수): 경쟁력 25.91(30.0%) · 해석 불가 22.46(26.0%) · 대표 URL 혼란 19.62(22.7%) · 클릭·표현 14.3(16.6%) · 기본 정비 4.01(4.6%)

**⑦ «사이트의 몇 %를 봤나»** — 와플: 커버리지 74.08% · 신뢰도 73.25% · «전체를 재지 못하면 페이지 간 비교 검사(중복·고아 페이지·깨진 링크)는 '통과'가 아니라 '판정 못 함'으로 남습니다.» 측정 39장/수집 40장.

**⑧ «이 점수가 어디에 서 있나»** — 점수 구간표: 준비 완료 90~100 / 양호 75~89 / 주의 50~74 / **취약 25~49 ·현재** / 차단 0~24.

## S5. 거래처 상세 · GEO 탭 — 3024×7022

**KPI 3장**: GEO 준비도 72.91(«AI 가 읽고 검증할 수 있나») / 진단 범위 74.08%(측정 39장) / 신뢰도 73.25%(«확인 못 한 항목은 차감하지 않았습니다»)

**① GEO 채점 5영역 «답변 엔진이 읽어야 할 선언 중 무엇이 빠졌나»** (배점 합계 100 · 명세 1.10.0) — 영역마다 정의 부제가 붙는다:

| 영역 (정의) | 받은 | 깎인 | 배점 | 달성률 |
|---|---|---|---|---|
| 답변 가능성 (한 문단으로 완결되는 답변 단위가 있나) | 15.60 | -8.40 | 24 | 65% |
| 구조화 데이터 (선언 여부와 검증 통과 여부를 나눠 셉니다) | 9.90 | -12.10 | 22 | 45% |
| 개체 식별 (상호·주소·도메인이 일관되게 선언돼 있는가) | 15.20 | -4.80 | 20 | 76% |
| 근거 표기 (출처·갱신일이 문서에 드러나는가) | 14.80 | -3.20 | 18 | 82% |
| AI 크롤러 접근 (robots 에서 답변 엔진 크롤러를 막고 있는가) | 16.00 | -0.00 | 16 | 100% |

**② 100점 환산 71.50/100** — «배점 합계가 100이라 환산값과 같습니다» · 깎인 -28.50. 스택 구성 동일 문법. «가장 크게 깎인 곳 — 구조화 데이터 -12.10 (배점 22)» / «가장 잘 지키는 곳 — AI 크롤러 접근 16.00점». 각주: «GEO 는 콘텐츠 표현이 아니라 **선언·구조·접근을 태그 단위로 잰 결과**이며, SEO 준비도와 합산하지 않습니다.»

**③ 진단 항목 «항목 10개 — 감점 6 · 판정 못 함 1 · 통과 3»** (해당 없음 0). 10행 전체:

| 판정 | 항목 | metric · 심각도 · 영향 | 점수(배점) |
|---|---|---|---|
| 감점 | 질문에 그대로 인용될 답변 단위가 있는가 | geo.answer.self_contained_block · 중요 · 21개 | -6.10 (14) |
| 감점 | 제목 단계가 끊기지 않고 이어지는가 | geo.answer.heading_hierarchy · 경미 · 9개 | -2.30 (10) |
| 감점 | 구조화 데이터를 선언했는가 | geo.schema.declared · **차단** · 32개 | -8.40 (12) |
| 감점 | 선언한 구조화 데이터가 검증을 통과하는가 | geo.schema.validates · 중요 · 7개 | -3.70 (10) |
| 감점 | 상호·주소·전화가 사이트 안에서 일치하는가 | geo.entity.nap_consistency · 중요 · 11개 | -4.80 (12) |
| 통과 | 외부 프로필과 연결(sameAs)이 선언돼 있는가 | geo.entity.same_as · 39개 | 0.00 (8) |
| 감점 | 문서에 갱신일이 드러나는가 | geo.evidence.last_updated · 경미 · 27개 | -3.20 (10) |
| 통과 | 작성·감수 주체가 선언돼 있는가 | geo.evidence.author_declared · 39개 | 0.00 (8) |
| 판정 못 함 | 답변 엔진 크롤러를 robots 에서 막고 있는가 | geo.access.ai_user_agents · 차단 | 0.00 (10) |
| 통과 | 자바스크립트 없이 본문을 읽을 수 있는가 | geo.access.no_js_readable · 39개 | 0.00 (6) |

- 구조화 데이터 펼침 상세: «39장 중 7장만 JSON-LD 를 선언했습니다. 나머지 32장에는 선언 자체가 없습니다.» + 선언 없는 페이지 HEAD 코드(`<head><meta name="description" content="...">` `<!-- application/ld+json 없음 -->`) + 수정(**고치면 +8.4**): 1. 병원·업체 공통으로 LocalBusiness(또는 MedicalClinic)를 전 페이지 공통 head 에 2. 서비스 페이지는 Service, 안내/FAQ 페이지는 FAQPage 추가.

**④ «배점의 90%까지 얼마나 남았나»** — 영역별 바 + 90% 목표 세로 눈금: 15.60/24 · 9.90/22 · 15.20/20 · 14.80/18 · 16.00/16.

**⑤ «총 손실이 어디서 나왔나»** — 도넛(28.5): 답변 가능성 8.4(29.5%) · 구조화 데이터 12.1(42.5%) · 개체 식별 4.8(16.8%) · 근거 표기 3.2(11.2%)

**⑥ «사이트의 몇 %를 봤나»** — 와플(GEO는 **초록**; SEO·진단은 파랑 — 색으로 축 구분, 다만 범례는 없음).

**⑦ GEO 이슈 «답변 엔진이 걸려 넘어지는 지점»** — 2건: 구조화 데이터 선언 없음(심각 · 39개 · **+9.1**) «선언이 없다는 사실은 '구조화 데이터가 선언되어 있는가' 항목에서 감점했습니다.»(중복 감점 방지 설명) / 질문형 콘텐츠 블록 없음(중요 · 11개 · +5.0) «AI 답변에 인용되려면 한 문단으로 완결되는 답변 단위가 필요합니다.»

## S6. 거래처 상세 · AEO 탭 — 3024×10718 (최장 탭)

표본: 관측 대상 7개 서비스 · 2026-08-24(월)~08-30(일) · 한국 시간.

**① 집계 카드 «저장된 관측을 다시 세기»** + «집계» 버튼: «질문×엔진×검색조건 칸을 세어 언급률을 냅니다. **답을 못 받은 칸은 '언급 없음'이 아니라 분모에서 빠집니다.**»

**② 언급 비교 «우리와 자주 함께 불리는 타사»** (질문 3개 · 잰 칸 32) — 질문 필터 칩(전체/Q1/Q2/Q3). KPI 3:
- 질문 전체 언급률 **46.9% ▲5.5p** (언급 15 / 잰 칸 32 · 직전 잰 회차 대비)
- 비교 브랜드 안에서 순위 **1위/5** («선두입니다 — 2위와의 간격은 아래 '언급 현황'에서 봅니다»)
- 대상 질문·분모 **3개** (엔진 7 × 검색 켬/끔 = 질문당 14칸 · 잰 칸 32 / 최대 42칸)

**③ 언급 현황 — 우리 대 타사** — 바 5개 + 회차별 라인차트(07-14→08-25):
1 그랜드아름다운의원(우리) 46.9%(15/32) — 상승 곡선, 끝 라벨 44% / 2 목이편한의원 37.5%(12/32) — **하강** 33% / 3 강남척주의원 34.4%(11/32) — 31% / 4 네이처메드한의원 15.6%(5/32) — 14% / 5 춘천더연의원 9.4%(3/32) — 8%.
각주: «점 위에 커서를 올리면 그 브랜드·회차의 값이 나옵니다. **아무도 재지 못한 1개 회차는 축에서 뺐습니다.**»

**④ 질문별 언급 칸 수** (행을 누르면 그 질문만 봄) — 질문×5브랜드 매트릭스:

| 질문 (잰 칸 · 우리 언급률) | 우리 | 목이편한 | 강남척주 | 네이처메드 | 춘천더연 |
|---|---|---|---|---|---|
| 춘천 피부과 리프팅 잘하는 곳 추천 (11 · 45.5%) | 5 | **7** | 5 | 3 | 2 |
| 춘천 보톡스 가격 어느 정도인가요? (10 · 20.0%) | 2 | 4 | **6** | 2 | 1 |
| 그랜드아름다운의원 후기 어떤가요? (11 · 72.7%) | **8** | 1 | 0 | 0 | 0 |

각주: «분모는 우리에게도 타사에게도 같은 '잰 칸'입니다. 못 잰 칸은 어느 쪽으로도 세지 않으므로, **낮은 값이 '안 불렸다'인지 '못 쟀다'인지 헷갈리지 않습니다.**» — 총합 1위지만 일반 질문 2개에선 경쟁사에 밀린다는 사실이 바로 읽힌다(브랜드 질문이 평균을 끌어올림).

**⑤ 발행 질문 3개 · 직전 회차 대비** — 질문별 진행 바(칸 단위) + 직전 회차 세로 눈금:
- Q1 춘천 피부과 리프팅… **45.5% ▲20.5p** · 언급 5/11 · 진단 못 함 3 · 최고 순번 1번째 · 씨앗 키워드 춘천 피부과 · 월 2,410 — [펼침] 서비스별 표: ChatGPT 켬2/끔· · Gemini 4/· · Claude 1/· · Perplexity 3/**—**(«이 회차에 검색 끔 조건을 지원하지 않았습니다») · Grok ·/· · 네이버 AI 브리핑 2/**—**(«검색 끔 조건이 없는 서비스입니다») · Google AI Overview ·/**—**. 답변이 제시한 출처 5건: grand4.co.kr/lifting · blog.naver.com/grand4/223 · grand4.co.kr/ · map.naver.com/p/entry/place/1234567 · mokhuri.com/skin. «숫자 칸은 답변 안에서 우리가 등장한 순번입니다. 점선 '—' 칸은 언급 안 됨이 아니라 재지 못한 칸이라 분모에서 빠집니다.»
- Q2 춘천 보톡스 가격… **20.0% ▲11.7p** · 언급 2/10 · 진단 못 함 4 · 최고 순번 4번째 · 월 880 · «칸별로 보기»
- Q3 그랜드아름다운의원 후기… **72.7% ▲14.4p** · 언급 8/11 · 진단 못 함 3 · 최고 순번 1번째 · 월 320 · «칸별로 보기»
- 각주: «언급률은 '잰 칸 중 우리가 언급된 칸'의 비율입니다. 막대 위 세로 눈금이 직전 회차 값입니다.»

**⑥ 주간 추이 «회차별 언급률»** (최근 7회차) — 라인+영역: «최신 회차 44.4% · **관측 없는 주는 선을 끊고 아래 점으로만 남깁니다.**»

**⑦ 엔진 분포 «어느 AI가 우리를 부르나»** — 방사형 동심 아크(중앙 46.9% 전체 언급률): ChatGPT 50.0% · Gemini 33.3% · Claude 50.0% · Perplexity 100.0% · Grok 16.7% · 네이버 66.7% · AI Overview 50.0%. 각주: «분모는 엔진별로 '잰 칸'입니다. 검색 끔 조건이 없는 서비스는 그만큼 분모가 작습니다.» ← **유일하게 실패한 차트**(제3부 참조).

**⑧ 질문×엔진 히트맵 «어느 칸에서 불렸나»** (칸 값=언급률%):

| | ChatGPT | Gemini | Claude | Perplexity | Grok | 네이버 | AI Overview |
|---|---|---|---|---|---|---|---|
| Q1 | 50 | 50 | 50 | 100 | · | 100 | (빈칸) |
| Q2 | · | · | 50 | 100 | · | · | **—** |
| Q3 | 100 | 50 | 50 | 100 | 50 | 100 | 100 |

**⑨ 관측 커버리지 «몇 칸이나 실제로 쟀나»** — 와플: «전체 칸 42개 중 잰 칸 32개. **회색 칸은 0%가 아니라 '진단 못 함'입니다.**»

**⑩ 언급 결과 구성 «잰 칸이 어디로 떨어졌나»** — 도넛(42 전체 칸): 언급됨 15(35.7%) · 언급 안 됨 17(40.5%) · 진단 못 함 10(23.8%)

**⑪ AEO — AI 답변 관측 «관측 2판 완료»** — 4단 퍼널 재등장(215/215 100.00% · 208/215 96.74% · 30/215 13.95% · 22/102 21.57%) + «분모가 단계마다 다릅니다. 출처를 주지 않는 엔진의 답변은 '인용 안 됨'이 아니라 '알 수 없음'으로 셉니다.»

**⑫ 관측 절차 «키워드에서 AX 대응까지»** — §2.5의 5단계 넘버 리스트.

**⑬ 발행된 질문 집합 «핵심 질문 1 · 발행 8/18»** — 발행된 질문의 원문 목록 카드. 각주: «발행한 집합만 매주 돕니다. 저장만 된 집합은 측정에 쓰이지 않습니다. **질문을 바꾸면 그 시점부터 추이가 갈라집니다.**» — 질문 집합의 «발행» 개념(저장≠측정 대상)과 추이 단절 규칙을 기능으로 명시.

**⑭ AI 엔진 상태 «쓸 수 있는 엔진 7개»** — Claude 사용 가능 / **구글 AI Overview: 최근 이레 실패 1건 — 상대 서버에서 답을 못 받았습니다. 열쇠 만료일 수 있습니다** / Gemini·네이버·ChatGPT·Perplexity·Grok 사용 가능. «쓸 수 없는 엔진도 사유와 함께 그대로 보여 줍니다. 목록에서 빼면 '여기 있는 게 전부'로 읽히기 때문입니다.»

**⑮ AI별 누적 답변 «엔진마다 몇 번 물었나»** — ChatGPT 90 · Claude 90 · Gemini 90 · Perplexity 45 · Grok 90 · 네이버 AI 브리핑 30 · 구글 AI Overview 15. 각주: «언급 30건 전부 검색 켬에서 나왔습니다 — **켬 30/128 (23.43%) · 끔 0/80**.» ← 검색 미연동 답변에선 전혀 안 불린다는 발견이 각주 한 줄에.

**⑯ «무엇을 구조화하나»** — §2.5의 6필드 리스트 + «하나라도 얻지 못하면 0이 아니라 '진단 못 함'으로 남기고 사유를 함께 표시합니다.»

**⑰ «누가 대신 인용되고 있나»** — 답변에서 함께 나온 도메인 표: mokhuri.com 등장 11 · 인용 7 · «정형외과 브랜드 네트워크» / kangchuh.com 8 · 5 · «척추 전문 브랜드» / naturemed.co.kr 6 · 2 · «한방 병원».

## S7. 거래처 상세 · 이슈 탭 — 3024×1804

표본: 열린 이슈 26건(표본 1건 표시).

**① 상태 구성 «사람이 지금 뭘 하고 있나 · 기계가 뭐라 했나»** — 우측 «고치면 +1.2점». 스택 바(전체 초록) + 범례 «닫힘 1» + «닫힘 — 재진단에서 같은 항목이 통과해 자동으로 닫혔습니다.»

**② 이슈 «사람이 닫지 않습니다 — 재진단이 통과하면 닫힙니다»** — 표본 이슈 1건 풀 카드:
설명 메타가 비었거나 너무 짧습니다 · 배지 «닫힘» · 경미 · SEO · seo.onpage.meta_description · 영향 주소 22개 · 예상 상승 +1.2 · 처음 발견 2026-07-16 · 판단 맥락 문구 «클릭·표현 영역 감점입니다. **색인에는 영향이 없지만 표시 문구를 검색엔진이 임의 생성합니다.**» + 진행 바 + **회차별 판정 띠 4칸**(빨·빨·초·초). 각주: «띠 한 칸이 한 회차입니다 — 채운 칸은 판정, 점선 칸은 판정 못 함. 이슈를 누르면 조치 이력과 근거가 열립니다.»

## S8. 거래처 상세 · 리포트 탭 — 3024×2418

표본: 발행본 0건 · 아직 안 낸 진단 8회차.

**① 진단→리포트 «어디서 문서가 되지 않고 멈춰 있나»** — 3단 감소 바: 진단 실행 12/12(100.0%) → 근거로 쓸 수 있는 회차 4/12(33.3%) **-8** → 발행본 0/12(0.0%) **-4**. «발행본은 특정 진단 회차에 고정됩니다. 회차가 늘어도 이미 낸 문서의 숫자는 바뀌지 않습니다.»

**② 발행된 리포트 «발행본은 고칠 수 없습니다»** — «이 거래처로 발행한 리포트가 없습니다. 발행은 **리포트 화면**에서 합니다.»(발행 진입점을 전역 화면 한 곳으로 고정)

**③ «숫자는 진단 실행에서만 옵니다»** — 근거로 쓸 수 있는 진단 4건: 2026. 8. 26. 오후 9:41 · 128페이지 / 8. 24. 오후 2:56 · 200페이지 / 8. 14. 오전 3:54 · 200페이지 / 8. 14. 오전 2:54 · 200페이지 — 모두 SUCCEEDED. «그 실행의 점수·판정·근거가 그대로 문서가 됩니다. 점수를 손으로 넣는 칸은 없습니다.»

## S9. 이슈 (전역) — 3024×3178

부제: «진단이 찾아낸 개선 항목을 영향도·난이도 순으로 정리합니다.» / 저장됨: 이슈 8건.

**① 심각도 구성 «열린 이슈 7건»** — 도넛: 차단·심각·중요·경미 4단 구성비(절대값+% 병기).

**② 축×심각도 «어느 축에 무엇이 몰려 있나»** — 3×4 매트릭스: SEO 차단2·심각1·중요2·경미1 / GEO 심각1·중요1 / AEO 전부 빈칸. «칸 값은 대표 표본 8건 기준입니다. 빈 칸은 '없음', 점선 칸은 '판정 못 함'입니다.»

**③ 상태 «지금 무엇이 사람 손에 있나»** — 5칸: 열림 2 · 조치 중 1 · 재진단 대기 3 · 닫힘 1 · 판정 못 함 1. «'닫힘'은 사람이 누른 값이 아니라 재진단이 통과한 결과입니다. '판정 못 함'은 감점하지 않았고 점수에도 들어가지 않았습니다.»

**④ 처리 순서 «위에서부터 처리하면 점수가 가장 빨리 오릅니다»** — 8행(S1-② 큐와 동일 항목의 전역판): 각 행 = 상태 배지 + 제목 + 심각도·축·metric·거래처·영향 주소 + **회차별 판정 띠 4칸** + 상승폭(+9.1 / +6.2 / +5.0 / +4.8 / +3.4 / +2.7 / +1.2 / robots.txt —). 마지막 행(판정 못 함)의 띠는 점선 4칸. «이슈는 사람이 닫지 않습니다. 재진단이 통과하면 자동으로 닫히고, 그 근거가 함께 남습니다.»

## S10. 리포트 (전역) — 3024×2908

부제: «버전이 찍힌 리포트를 발행하고 공유 링크를 만듭니다. 발행본은 사후 수정되지 않습니다.» / 표본: 발행본 2건 · 아직 안 낸 진단 8회차.

**KPI 3장**: 발행본 **2건**(«발행 후에는 고칠 수 없습니다») / 아직 안 낸 진단 **8회차**(주황 강조 · «근거로 쓸 수 있는 성공 실행») / 발행 리듬 **—**(«**달이 하나뿐이라 리듬은 그리지 않습니다**») ← 데이터가 모자라면 차트를 그리지 않는다는 원칙의 극단 사례.

**① «어디서 멈춰 있나»** — 성공한 진단 실행 12/12(100.0%) → 리포트 근거로 고른 실행 4/12(33.3%) -8 → 발행된 문서 2/12(16.7%) -2. «줄어드는 폭이 '일이 밀린 자리'입니다. **실행은 있는데 문서가 없으면 업체는 아무것도 못 받은 것과 같습니다.**»

**② «거래처별 발행본»** — 도넛(2 발행본): 참사랑한의원 2(100.0%). «한 거래처에 몰려 있으면 나머지 거래처는 진단만 돌고 결과를 못 받은 상태입니다.» — 편중 자체를 경고로 읽게 하는 각주.

**③ 발행된 리포트 «업체에 전달하는 문서»** — 참사랑한의원 v1 · 만든 날 2026. 8. 19. 오전 11:54 · 최신 버전 동일 · `sha256:a11fc70989030227…` · «버전 열람·내보내기 →» / 참사랑한의원 테스트 진단 v1 · 2026. 8. 17. 오전 3:53 · `sha256:bf9f54582ce53e7c…`

**④ «어느 진단으로 만들까요 — 숫자는 진단 실행에서만 옵니다»** — S8-③과 동일한 실행 4건 목록 + «점수를 손으로 넣는 칸은 없습니다. 고른 실행의 점수·판정·근거가 그대로 문서가 되고, 발행본은 못 고칩니다.»

## S11. 의료 규정 (원고 검수) — 3024×17250 (최장 화면)

부제: «의료광고 심의 관점에서 별도로 세는 항목입니다. 일반 SEO 점수와 합치지 않습니다.» / 표본: 대상 거래처 9곳 · 검사 항목 4개.

**KPI 4장**: 적발 합계 **55건**(«사람이 판단할 후보 수») / 가장 잦은 항목 **절대적 표현**(항목별 합계 기준) / 대상 거래처 **9곳**(«업종이 의료로 등록됨») / 진단 못 함 **9칸**(«페이지를 못 읽어 세지 못한 칸»)

**① 거래처×항목 히트맵 «어디에 무엇이 몰려 있나»** — 9행×4열 전체:

| 거래처 | 전후 사진 | 절대적 표현 | 환자 후기 | 비급여 고지 |
|---|---|---|---|---|
| 그랜드연합의원 | 1 | 2 | 0 | 1 |
| 그랜드우리안과의원 | 3 | 5 | 4 | 2 |
| 그랜드배내과의원 | 0 | 1 | 0 | 0 |
| 그랜드아름다운의원 | **7** | 4 | 2 | 0 |
| 굿투어 | 2 | 3 | **6** | 1 |
| 시원클리닉 | 5 | 2 | 1 | 0 |
| 참사랑한의원 | — | — | — | — |
| 범어5층치과 | — | — | — | — |
| 대구명성요양병원 | 0 | 2 | 1 | — |

각주: «점선 '—' 칸은 위반이 없다는 뜻이 아니라 해당 페이지를 읽지 못했다는 뜻입니다. **0과 구분해서 읽습니다.**» (— 9칸 = KPI의 «진단 못 함 9칸»과 일치)

**② 실행 큐 «사람이 볼 후보를 이슈로 올린다»** (적발 20칸) — 각 행 = 거래처×항목 + 우선순위 + 적발 수 + 조문 + «이슈로 올리기» 버튼. **먼저 볼 것** 그룹: 그랜드아름다운·전후 사진 7건(의료법 제56조 제2항 제2호) / 그랜드우리안과·절대적 표현 5건(제8호) / 시원클리닉·전후 사진 5건 / 그랜드아름다운·절대적 표현 4건 / 그랜드우리안과·전후 사진 3건 / 굿투어·절대적 표현 3건 / 그랜드연합·절대적 표현 2건 / 굿투어·전후 사진 2건 / 시원클리닉·절대적 표현 2건 / 대구명성·절대적 표현 2건 / 그랜드연합·전후 사진 1건 / 그랜드배내과·절대적 표현 1건. **확인 필요** 그룹(환자 후기): 굿투어 6건 / 그랜드우리안과 4건 / 그랜드아름다운 2건 / 시원클리닉 1건 / 대구명성 1건. **기록용** 그룹(비급여 고지 · 제13호+제45조): 그랜드우리안과 2건 / 그랜드연합 1건 / 굿투어 1건. 각주: «올린 이슈는 사람이 닫지 않습니다. 문구를 고친 뒤 재진단이 통과해야 닫힙니다.»

**③ 표현 스캔 «조문별로 무엇이 걸렸나»** (스캔 가능 13/16개 조항 · 후보 41건 · 페이지 못 읽음 3곳) — 16조항 전체(각 행에 검사 방식 표기):

| 조항 | 근거 | 검사 방식 | 건수 |
|---|---|---|---|
| 비급여 진료비용 할인·고지 | 제56조 제2항 제13호 · 제45조 | 자동 판정 | 9건 |
| 과장 광고 | 제8호 | 자동 판정 | 7건 |
| 부작용 정보 누락 광고 | 제7호 | 자동 판정 | 6건 |
| 환자 유인행위 | 제27조 제3항 | 후보만 수집 | 5건 |
| 치료경험담 등 치료효과 오인 광고 | 제2호 | 자동 판정 | 5건 |
| 전문병원 명칭 사용 | 제3조의5 제1항 | 자동 판정 | 3건 |
| 기사·전문가 의견 형태 광고 | 제10호 | 후보만 수집 | 2건 |
| 미평가 신의료기술 광고 | 제1호 | 사람 확인 | 1건 |
| 미심의 광고/심의내용과 다른 광고 | 제11호 · 제57조 | 자동 판정 | 1건 |
| 비교 광고 | 제4호 | 자동 판정 | 1건 |
| 상장·인증·보증·추천 광고 | 제14호 | 자동 판정 | 1건 |
| 비방 광고 | 제5호 | 후보만 수집 | 0건 |
| 법적 근거 없는 자격·명칭 표방 | 제9호 | 자동 판정 | 0건 |
| 거짓 광고 | 제3호 | 사람 확인 · 사전으로 찾을 수 없어 스캔하지 않음 | **진단 못 함** |
| 시술행위 노출 광고 | 제6호 | 상동 | **진단 못 함** |
| 외국인환자 유치 국내광고 | 제12호 | 상동 | **진단 못 함** |

각주: «'진단 못 함'은 위반이 없다는 뜻이 아니라 사전이나 페이지 요소로 판정할 수 없는 조항이라는 뜻입니다. …사실 확인이 필요한 항목은 사람이 직접 봐야 합니다.»

**④ 스캔→실행 큐 «스캔에서 걸린 문구를 거래처별로 올린다»** (후보 26칸) — ②와 달리 조문 단위 + **실제 적발 위치·문구 예시**가 붙는다: 그랜드아름다운·환자 유인행위 3건 «예: /event — 이벤트가» / 그랜드아름다운·부작용 정보 누락 3건 «예: /event — 통증 없» / 그랜드우리안과·부작용 누락 2건 «예: /lasik — 부작용 없» / 그랜드우리안과·과장 2건 «예: /lasik — 완치» / 그랜드아름다운·치료경험담 2건 «예: /event — 후기» / 그랜드아름다운·과장 2건 «예: /event — 1위» / 시원클리닉·과장 2건 «예: /clinic — 유일» / 그랜드연합·미평가 신의료기술 1건 «예: /treatment/knee — 국내 최초 도입»(사람 확인) / 그랜드우리안과·환자 유인 1건 «예: /lasik — 이벤트가» / 그랜드우리안과·치료경험담 1건 «예: /lasik — 체험담» / 그랜드아름다운·미심의 1건 «예: /about — 심의필 번호 형식 불일치» / 굿투어·환자 유인 1건 «예: /package — 패키지 할인» / 굿투어·치료경험담 1건 «예: /package — 후기» / 굿투어·과장 1건 «예: /package — 최고» / 시원클리닉·치료경험담 1건 «예: /clinic — 전후 사진» / 시원클리닉·부작용 누락 1건 «예: /clinic — 부작용 안내 문단 없음» / 그랜드아름다운·전문병원 명칭 2건 «예: /about — 전문병원» / 그랜드연합·비교 1건 «예: /treatment/knee — 타 병원 대비» / 그랜드아름다운·기사·전문가 의견 1건 «예: /about — 인터뷰» / 그랜드아름다운·상장·인증·보증·추천 1건 «예: /about — 수상» / 굿투어·기사·전문가 의견 1건 «예: /package — 언론 보도» / 시원클리닉·전문병원 명칭 1건 «예: /clinic — 교정 전문» / 그랜드우리안과·비급여 할인·고지 3건 «예: /lasik — 이벤트가» / 그랜드아름다운·비급여 3건 «예: /event — 할인» / 굿투어·비급여 2건 «예: /package — 할인» / 그랜드연합·비급여 1건 «예: /treatment/knee — 비급여 고지표 없음». 각주: «한 칸은 '거래처×조문'입니다. **같은 조문이 여러 페이지에서 걸리면 한 이슈로 묶어 올립니다.**»

**⑤ 항목별 «무엇을 따로 세나»** — 4개 검사 항목의 정의·조문·상태: 전후 사진 18건/5곳(«부작용·주의사항 문구가 같은 화면에 있는지» · 제2호 · 먼저 볼 것 · 진단 못 함 2곳) / 절대적 표현 19건/7곳(«'최고·유일·100%·완치' 등 단정 어휘» · 제8호 · 먼저 볼 것 · 진단 못 함 2곳) / 환자 후기 14건/5곳(«체험담을 검색 노출 목적으로 배치했는지» · 제2호 · 확인 필요 · 진단 못 함 2곳) / 비급여 고지 4건/3곳(«항목·금액·갱신 시각이 함께 게시됐는지» · 제13호·제45조 · 기록용 · 진단 못 함 3곳)

**⑥ 거래처별 «적발 건수 순위»** — 롤리팝 9행: 그랜드우리안과 14 · 그랜드아름다운 13 · 굿투어 12 · 시원클리닉 8 · 그랜드연합 4 · 대구명성 3 · 그랜드배내과 1 · 참사랑 0 · 범어5층 0. 각주: «건수는 위험의 크기가 아니라 '사람이 볼 분량'입니다. **한 건이 심의 위반이고 열 건이 문구 정리일 수 있습니다.**»

**⑦ 근거 법령 «어떤 조문으로 걸러내나»** (16개 조항) — 출처 명기: «의료법 법률 제21524호(2026-04-07) · 의료법 시행규칙 보건복지부령 제1177호(2026-06-12) · 보건복지부 의료광고 가이드라인 2판(2024-12, 발간등록번호 11-1352000-100026-01). 이 검사는 법률 자문이 아니며, 심의 소지가 있는 표현을 사람이 판단하도록 모아 줄 뿐입니다.» 조항마다: 정의 문장 + 우선순위 + 검사 방식 + **가이드라인 페이지 번호**(P.48/P.51/P.61/P.64/P.68/P.71/P.73/P.74/P.77/P.81/P.83/P.86/P.89/P.93/P.96/P.98) + **신호 어휘 사전**(예: 과장 광고 — «최고·유일·1위·100%·완치·최저가·세계 최초·가장 안전», 환자 유인 — «이벤트가·무료 시술·친구 추천 시·패키지 할인·본인부담금», 자격·명칭 표방 — «○○전문의(비전문과목)·명의·지정의·권위자») + 집계 상태(«현재 집계 중» / «아직 자동 집계 안 함»).

**⑧ 대상 «업종이 의료로 등록된 거래처»** — 9곳 목록(전부 병원·의원 라벨): 그랜드연합·그랜드우리안과·그랜드배내과·그랜드아름다운·굿투어·시원클리닉·참사랑·범어5층치과·대구명성요양병원. — 목록 11곳 중 베놈(기타)·법무법인 일맥(법률사무소)이 자동 제외됐음을 역으로 보여준다.

## S12. /console/dashboard — 소스 코드 검증 (Lovable 커넥터 경유)

> 프리뷰 URL 은 이 작업 환경의 네트워크 정책이 차단하지만(아래 «접근 확인 로그»), Lovable 커넥터로 프로젝트 소스에 접근해 **코드 자체를 판독**했다(커밋 `216233be`). 그 결과 `/console/dashboard` 는 **S1 «이번 주 실행» 화면의 구현체**임이 확인됐다 — 라우트 head 타이틀이 «이번 주 실행 — ANSEO 콘솔», 부제도 S1 과 동일. 아래는 스크린샷(정지 화면)으로는 볼 수 없던 것들이다.

### S12.1 인터랙션 레이어 (코드에서만 보이는 동작)

- **단계 퍼널 = 실제 필터**: `StageFlow` 클릭 → 실행 큐가 그 단계 항목으로 걸러지고, 큐 제목이 «{단계명} 단계 N건»으로 바뀌며, 우상단에 «전체 보기» 해제 버튼이 나타난다. 같은 단계 재클릭 = 해제(토글).
- **상태 반응형 각주**: 퍼널 하단 문구가 조건 분기한다 — 차단 항목이 있으면 «…'낮다'가 아니라 '못 쟀다'입니다», **없으면 «접근 단계가 비었습니다 — 뒤 단계 점수를 그대로 신뢰할 수 있습니다.»** 오독 방지 각주가 정적 캡션이 아니라 상태를 읽는 문장.
- **큐 행의 이중 링크**: 제목 → 이슈 상세(`/console/issues/$issueId`), 거래처명 → 거래처 상세. 실행 큐가 곧 내비게이션 허브.
- **정렬 기준의 최신 표기**: 각주가 «상승폭 **÷** 품»으로 코드에 있다(스크린샷 판은 «상승폭+품»). 막대(`LeverageBar`)는 큐 내 최대 leverage 로 정규화.
- **AEO 카드의 형식 분리는 코드 주석으로도 선언**: `{/* AEO — 점수가 아니라 관측 횟수. 0~100 게이지를 쓰면 준비도와 같은 척도로 오해된다. */}` — 디자인 원칙이 주석으로 코드에 박제. AEO 카드 안에 4단 미니 퍼널 바가 인라인으로 들어가고, 언급·인용 단만 ember 색으로 구분.
- **사분면 점에는 라벨 데이터가 이미 전달됨**(`label: c.name`) — §7-2 권고(라벨 표기)는 `Quadrant` 컴포넌트 렌더링만 열면 되는 상태. 점 색은 3值: 차단 보유=destructive, 관측 돌아감(aeoRuns>0)=mint, 그 외=signal — 스크린샷에서 «빨강/파랑»으로만 보이던 것이 실은 3단계.
- **동적 head 메타**: 라우트별 title·description·og 태그 지정 — 콘솔 자체도 자기 제품의 원칙(메타 채우기)을 지킨다.
- **조직명 개인화**: 제목이 `{membership.organizationName} · 이번 주 실행` — 멀티테넌트 전제.

### S12.2 IA 의 단일 소스 — `src/lib/navigation.ts`

내비게이션이 파일 하나로 강제되며, 머리 주석에 규칙이 명문화돼 있다: «화면 코드가 메뉴 이름을 따로 적지 않는다 — 이름·차례가 어긋나는 사고를 막기 위해 헤더·계정 메뉴·거래처 탭이 전부 이 파일 하나를 읽는다.»

**자리(place) 5종 분류**:

| place | 규칙 | 항목 |
|---|---|---|
| BRAND | 로고 자체가 그 화면. 메뉴에 안 건다 | 대시보드(=이번 주 실행) |
| MENU | 헤더에 이름 그대로 | 거래처 · 이슈 · 리포트 |
| TOOL | 헤더+계정 메뉴 양쪽 | 원고 검수(**의료 업종 조직에만 노출** — `industry: "MEDICAL"` 게이트) |
| GROUP | 묶음 메뉴 | AEO 관측(키워드 · 경쟁 도메인 · AI 답변 검수) / 운영(사용량·비용 · 채점 기준 버전 · 외부 연결 열쇠 · 팀원 관리 · 사이트맵·파이프라인 · 변경 이력) |
| ABSORBED | 헤더에 안 뜸, 다른 화면 링크로만 진입 | 거래처 등록 · 프로젝트 · 내 계정 — **각각 `enteredFrom`(실제 입구) 명기 의무** |

- **원칙 0-E가 주석으로 선언돼 있다**: «부를 수 없는 기능은 없는 기능이다. ABSORBED 항목은 반드시 enteredFrom 에 실제 입구를 적어 둔다. …비어 있으면 죽은 화면이다.» — 죽은 라우트(고아 화면)를 데이터 구조 차원에서 금지.
- 거래처 상세 탭 6개(진단/SEO/GEO/AEO/이슈/리포트)도 같은 파일의 `CUSTOMER_TABS` 가 순서·이름의 단일 소스.

### S12.3 라우트 전모 (스크린샷 11장 밖의 화면들)

소스의 라우트 파일로 확인된 전체 화면: 스크린샷으로 본 7종 외에 **키워드**(질문을 뽑는 검색량 표본) · **경쟁 도메인**(AI 답변에서 우리 대신 인용되는 곳 — S6-⑰의 전역판) · **AI 답변 검수**(사람이 확인해야 할 관측 답변) · **사용량·비용**(S1-⑪ «호출량·비용 보기» 링크의 목적지) · **채점 기준 버전**(명세 1.10.0 의 관리 화면) · **외부 연결 열쇠**(«열쇠 만료일 수 있습니다» 경보의 해결 화면) · **팀원 관리 · 사이트맵·파이프라인 · 변경 이력 · 거래처 등록 · 프로젝트 · 내 계정**, 그리고 콘솔 밖의 **`/results/$token`(업체에 주는 공유 결과 링크)** · **`/invite/$token`(초대 가입)** · **`/login`**(공개 가입 없음 — 초대 전용, 로그인 화면 좌측에 «측정 원칙» 5문장을 전시). 스크린샷의 경보·링크가 가리키던 목적지 화면이 전부 실재한다 — **매달린 링크 없음.**

### S12.4 소결

대시보드 코드는 시안 분석(제1~3부)의 결론을 그대로 확인시킨다: 질문형 제목·오독 방지 각주·형식 분리(게이지 vs 퍼널)·CTA 절제가 컴포넌트와 주석 수준까지 내려가 있고, IA 는 단일 소스 + 진입점 의무화로 구조적 부패(이름 불일치·죽은 화면)를 데이터 타입으로 막는다. 스크린샷 판과 어긋난 유일한 지점은 정렬 각주 표기(«+» → «÷»)로, 코드 쪽이 의미상 정확하다(같은 시간 대비 상승).

---

# 제3부 — 인포그래픽 문법 분석

## 4. 유형 전수 (18종)

| # | 유형 | 위치 | 인코딩 | 평가 |
|---|---|---|---|---|
| 1 | 단계 퍼널 카드 5칸 | S1 | 남은 건수+진행바, 클릭=필터 | ◎ 차트가 곧 내비게이션 |
| 2 | 큐 리스트+상승폭 막대 | S1·S9 | 막대=상승폭+품 | ◎ 정렬 기준을 각주로 공개 |
| 3 | 반원 게이지 | S1·S3 | 점수/100 | △ 공간 대비 정보량 낮음. S1(평균)과 S3(개별)의 의미 차이가 형태로 구분 안 됨 |
| 4 | AEO 퍼널 바 | S1·S3·S6 ×3회 | 파랑 바+감소분 주석(-7·-178·-8), 분모 병기 | ◎ 제품 대표 차트. 분모 상이를 시각+문구 이중 방어 |
| 5 | 산점도 사분면 | S1 | 위치=SEO×GEO, 크기=이슈 수, 빨강=차단 | ○ 취지 정확. 결함: 11곳에 라벨 없음, 동좌표 점 겹침, «판 밖 1곳»이 그림엔 안 보임 |
| 6 | 도넛 | S1·S4·S5·S6·S9·S10 ×6회 | 중앙 합계+범례 절대값·% | ○ 표준. 범례 병기로 도넛 약점 보완 |
| 7 | 와플 그리드 | S1·S4·S5·S6 ×4회 | 칸=페이지/관측 칸 | ○ «회색=진단 못 함» 각주가 핵심. 파랑(SEO)/초록(GEO) 색 구분에 범례 없음 |
| 8 | 영역 구성 스택 바 | S4·S5 | 칸 너비=배점, 채움=득점, 어두움=깎임 | ◎ 가중치와 득점을 한 줄에 — 좋은 발명 |
| 9 | 채점표(바+수치+스파크라인) | S4·S5 | 받은/깎인/배점/4회차/달성률 | ◎ 표·차트 결합의 모범. 추이선 읽는 법 각주까지 |
| 10 | 90% 목표선 바 | S5 | 바+세로 눈금 | ◎ 목표 대비 잔여를 눈금 하나로 |
| 11 | 방사형 동심 아크 | S6-⑦ | 엔진별 %를 아크 길이로 | ✕ **유일한 실패작.** 안쪽 궤도일수록 같은 %가 짧게 그려지는 반지름 왜곡 + 분모 다른 %(Perplexity 100% 분모↓ vs ChatGPT 50%)가 같은 무게로 보임. 수평 막대+분모 병기(«n/N») 교체 권고 |
| 12 | 멀티라인 추이+끝 라벨 | S6 ×2회 | 5브랜드 색, 우측 끝 이름+% | ◎ 범례 왕복 제거. «못 잰 회차는 선을 끊고 점으로» — 결측 처리 정석 |
| 13 | 질문×엔진 히트맵 | S6-⑧ | 셀=언급률, 점선=못 잼 | ○ 값 병기로 판독 가능 |
| 14 | 거래처×항목 히트맵 | S11-① | 농도=적발 수, —=못 읽음 | ○ 0/— 구분 방어. 농도 스케일 기준 미표기 |
| 15 | 축×심각도 매트릭스 | S9-② | 3×4 건수 | ○ 작지만 질문에 정확히 답함 |
| 16 | 회차별 판정 띠 | S7·S9 | 칸=회차, 채움=판정, 점선=못 잼 | ◎ «언제부터 앓던 문제인가»를 4칸으로 압축 |
| 17 | 랭킹 롤리팝 | S11-⑥ | 점+선+건수 | ○ «건수는 위험이 아니라 분량» 각주가 백미 |
| 18 | 3단 감소 깔때기 | S2·S8·S10 ×3회 | n/12+감소분(-8·-4/-2) | ◎ 병목=감소 폭 직독. «깔때기의 목» 언어까지 |

## 5. 인포그래픽 문법의 3원칙 (전 차트 공통)

1. **분모가 다르면 절대 합치지 않는다** — AEO 퍼널 마지막 단(22/102), 게이지·바 형식 분리, «한 숫자로 합치지 않습니다» 반복.
2. **빈 값 3종 구분** — `0`(측정했고 없음) / `—`(재지 않음·분모 제외) / `점선 칸`(판정 못 함). 표·히트맵·와플·띠 전부에 일관 적용, «0과 구분해서 읽습니다» 명문화.
3. **모든 차트에 «오독 방지 각주»** — 18종 중 각주 없는 유형이 없다. 극단 사례: 발행 리듬 KPI는 «달이 하나뿐이라 리듬은 그리지 않습니다»로 차트 자체를 거부.

---

# 제4부 — 종합 평가

## 6. 이 시안의 자산 (본 구현에 이식 가치)

1. 질문형 카드 제목 + 오독 방지 각주의 짝 — 화면 문해력을 만드는 최상위 장치.
2. 빈 값 3종(0 / — / 판정 못 함)과 «측정 실패 ≠ 0점» 원칙의 전면 적용.
3. 실행 큐의 «상승폭+품» 정렬, 산정 불가 분리 집계, 품·담당·기한 병기.
4. AEO 퍼널의 분모-감소 주석 표기법과 «점수가 아니라 관측» 형식 분리.
5. 발행 깔때기(감소 폭=병목)와 발행본 불변성 UI(sha256·버전·«고칠 수 없습니다»).
6. 증거 접기(판정 근거→실측 코드→수정 방법+기대 상승폭) — 컨설팅 리포트의 신뢰 골격.
7. 사용 불가 엔진도 사유와 함께 목록 유지(«빼면 '여기 있는 게 전부'로 읽힙니다»).
8. 의료 규정의 검사 방식 3분류(자동 판정/후보만 수집/사람 확인)와 신호 어휘 사전·가이드라인 페이지 인용.
9. 한계 자백 카드(«이 점수가 덮지 못한 것», 발행 리듬 —).

## 7. 고칠 것 (디자인·구성 관점 · 우선순위순)

1. **[차트] 방사형 아크(S6-⑦) 교체** — 안쪽 궤도일수록 같은 값이 짧게 그려지는 반지름 왜곡 + 분모가 다른 %가 같은 시각 무게로 보이는 문제. 수평 막대 + 엔진별 분모(n/N) 병기로.
2. **[차트] 산점도(S1-⑥) 라벨** — 거래처가 십여 곳이면 점에 이름을 직접 붙일 수 있다. 동좌표 겹침 처리(지터/묶음) + «판 밖(못 잰 곳)»의 시각 표기 자리도 필요.
3. **[접근성] 회색 보조 텍스트 대비 상향** — 각주가 이 시스템의 핵심 장치인데 순흑 배경 위 중회색이 AA 경계. 한 단계 밝게.
4. **[범례] 색·스케일 기준 명시** — 와플의 파랑(SEO 계열)/초록(GEO 계열) 색 구분, 의료 규정 히트맵의 농도 스케일 기준이 화면에 없다.
5. **[UX] 초장문 화면 항해** — AEO 탭(약 10,700px)·의료 규정(약 17,000px)에 섹션 앵커 또는 카드 접기. 없으면 후반 카드 도달률이 떨어진다.
6. **[구성] 게이지의 의미 구분** — 이번 주 실행의 게이지(전 거래처 평균)와 거래처 상세의 게이지(개별 환산)가 같은 형태라 스캔 시 혼동 여지. 평균 게이지에는 분포(최저—최고)가 부제로만 있으니 형태 차이(예: 범위 밴드)를 주는 것도 방법.
7. **[발행] metric id·내부 용어의 고객용 번역 레이어** — `seo.onpage.*` 같은 id는 콘솔에선 추적성 자산이지만, 업체 전달 발행본 템플릿에서는 일반어로 번역돼야 한다.

## 8. 부록 — 코드 분석: 브랜드 식별 관문 (IdentityGate / IdentityCard / IdentityBadge)

시안과 함께 공유된 React 컴포넌트(`@/data/anseo-identity` 소비)를 같은 잣대로 분석한다.

### 8.1 워크플로우상의 위치 — «관측 앞의 관문»

파일 머리 주석이 설계 의도를 그대로 말한다: «AEO 언급률보다 앞에 놓인다 — 식별이 안 된 거래처의 언급률은 '낮은 값'이 아니라 '셀 수 없는 값'이기 때문이다. 그래서 여기서는 **점수를 그리지 않고**, 몇 곳이 잴 수 있는 상태인지와 무엇이 비어서 못 재는지만 보여 준다.»

- 시안의 제1원칙(측정 실패 ≠ 0점)을 **화면 이전 단계(게이트)로 끌어올린 구현**이다. 5단계 퍼널의 «앞 단계가 막히면 뒤는 못 잰 것» 논리가 AEO 안에서도 반복된다: 식별 → 관측.
- 상태 3단(측정 가능 / 일부 등록 / 등록 안 됨) + 파생 지표 «식별 못 해 검수로 넘어간 언급(pending)» — 등록 미비의 비용을 «검수 대기 적체»라는 작업량으로 번역한다. 점수가 아니라 일감으로 보여 주는 이번 주 실행의 어법과 동일.
- «인용에 나왔는데 등록 안 된 곳(unregisteredCitedDomains)» KPI는 S6-⑰ «누가 대신 인용되고 있나»와 맞물린다 — 관측이 발견한 미등록 도메인이 등록 후보로 되돌아오는 **피드백 루프**가 데이터 모델에 있다.
- IdentityCard 하단 «검수 대기 N건 — **등록을 채우면 자동 판정으로 내려갑니다**» — 시안의 «고치면 +N» 어법(조치→효과 예고)과 같은 마이크로카피 문법.

### 8.2 디자인 시스템 일치도

| 시안의 패턴 | 코드에서의 구현 |
|---|---|
| 아이브로우+질문형 제목 | `eyebrow` 클래스 + «이 그림이 답하는 것 — 지금 이대로 재면 무엇이 보류로 쌓이나» |
| 오독 방지 각주 | 카드 하단 «한국 병원 상호는 겹칩니다 — '서울치과'는 수십 곳입니다. …낮게 나온 언급률은 '성과가 없다'가 아니라 '가릴 수 없다'입니다.» |
| 빈 값 문법(점선=못 잼) | 미입력 필드 = `border-dashed` 투명 필, 입력 = `bg-signal` 채움 — 와플·히트맵의 점선 칸 문법을 필드 게이지로 이식 |
| 컬러 시맨틱 토큰 | `mint`(가능)/`ember`(경고)/`unknown`(못 잼)/`signal` — 토큰 이름 자체가 차트 문법을 인코딩. `text-mint` 같은 하드코딩 hex 없음 |
| 모노스페이스 수치 | `num`·`metric` 유틸 클래스 — 2원 타이포 체계 유지 |
| 상태 배지 | `IdentityBadge` 점+라벨, `Record<status, …>` 완전 매핑(타입이 상태 추가 시 갱신 강제) |
| 한계·이유 병기 | 필드마다 `f.why`(왜 필요한가)와 가중치 `f.weight` 노출, 빈 필드에 «비어 있음 — {why}» |

구성도 시안의 카드 문법 그대로: 제목 → 시각(스택 바) → 범례 → KPI 3분할(문제 우선 배치: ember→unknown→mint) → 각주.

### 8.3 코드 관찰 (품질·견고성)

잘 된 것:
- `STATUS`를 `Record<IdentityRecord["status"], …>`로 선언 — 상태 열거 누락을 컴파일 타임에 잡는다.
- 분모 0 가드(`|| 1`), 0건 세그먼트는 바에서 숨기되 **범례에는 유지**(범례 안정성 — 시안의 «빼면 전부로 읽힌다» 원칙과 상통).
- 세그먼트 경계를 `border-background`로 처리해 어느 테마에서든 분리선이 유지되는 트릭.

다듬을 것:
1. **`values` 매핑의 타입 느슨함** — `Record<string, string[]>` + `values[f.key] ?? []`라서 `IDENTITY_FIELDS`에 필드가 추가되면 조용히 «비어 있음»으로 렌더된다(데이터 형상 불일치가 은폐됨). `Record<(typeof IDENTITY_FIELDS)[number]["key"], string[]>`로 좁히거나 `satisfies`로 강제 권고.
2. **status와 채움 게이지의 이중 근원** — 배지 `record.status`와 `filledFields(record)`가 따로 온다. 파생 가능하면 한쪽에서 유도하거나 불일치 시 개발 모드 경고를.
3. **툴팁 의존** — 세그먼트 수치·필드 사유(`f.why`)가 `title` 속성에만 있어 터치 기기·스크린리더에서 소실. 시안의 «각주» 원칙과 어긋나는 유일한 지점 — 바에 `role="img"`+`aria-label`, why는 접히는 텍스트로.
4. **세그먼트 위 숫자 대비** — `text-background`(다크에서 짙은 색)를 mint/ember 위에 얹는 방식은 라이트 테마에서 background 토큰이 뒤집힐 때 대비가 무너질 수 있다. 세그먼트 전경색 토큰을 따로 두는 편이 안전.
5. **`collisionRisk` 이분 처리** — `high`가 아니면 전부 «고유한 상호»로 표기. 타입에 중간 단계가 있다면 스위치로 전개해야 한다(STATUS처럼 Record 매핑 권장).
6. 소소한 것: `seg`의 라벨·색이 `STATUS`와 중복 선언(한쪽에서 파생 가능), `text-[11px]` 반복은 캡션 토큰화 여지, 라벨 열 `86px` 고정폭은 긴 한글 라벨에서 줄바꿈 확인.

### 8.4 소결

이 컴포넌트는 시안의 4원칙 중 «측정 실패 ≠ 0점»을 데이터 진입 단계로 확장한, 문법적으로 가장 충실한 구현 사례다. 헤더 주석·마이크로카피·토큰 명명까지 제품의 목소리가 코드 레벨에 일관되게 내려와 있다. 다듬을 것은 전부 국소적(타입 좁히기, 이중 근원, 툴팁 접근성, 테마 대비)이고 구조 변경은 필요 없다.

## 9. 총평

«점수판이 아니라 작업 목록» · «점수가 아니라 관측» · «사람이 닫지 않고 재진단이 닫는다» · «숫자는 진단 실행에서만 온다» — 네 문장이 IA·워크플로우·차트 문법·마이크로카피 전부를 지배하는, 원칙 일관성이 대단히 높은 시안이다. 27개 타일 전수에서 흐름·기능·구성의 구조적 결함은 발견되지 않았고, 고칠 것은 전부 국소적이다(차트 1종, 라벨·범례, 대비, 장문 항해). 특히 «측정하지 못한 것»을 다루는 규율(3종 빈 값, 분모 제외, 한계 자백 카드)은 대시보드 설계 일반에서 보기 드문 수준으로, ANSEO 실물 구현의 기준 문서로 삼을 만하다.

---

# 제5부 — 확장 흐름 대비 현 콘솔 구조 분석

> 사용자(제품 오너)가 밝힌 목표 흐름과 담당 체계를 기준으로, 현 콘솔(스크린샷 11장 + 소스)의 화면들이 어디까지 덮고 어디가 비어 있는지를 매핑한다. AEO 관측·운영 두 묶음 메뉴는 **타이틀만 있고 내용은 작업 중**임이 확인된 전제다.

## 10. 목표 흐름 7단계와 현 화면의 매핑

목표 흐름: **진단 → 관측 → 콘텐츠 추출 → 콘텐츠 분석 → 콘텐츠 제안 → 재관측 → 진단** (순환)
차별점: 자사 콘텐츠와 AI 가 인용하는 콘텐츠를 분석해 **«다음에 어떤 콘텐츠로 작업할지»까지** 보여 주는 것.

| 목표 단계 | 현 콘솔의 대응물 | 상태 |
|---|---|---|
| ① 진단 | 거래처 상세 진단/SEO/GEO 탭, 채점 명세·재채점, 이슈 생성 | **완성** — 시안·코드 모두 갖춰짐 |
| ② 관측 | AEO 탭(질문×엔진 관측), 대시보드 AEO 퍼널, 키워드→질문 절차 | **완성**(개별 거래처 수준). 전역 화면(키워드·경쟁 도메인·AI 답변 검수)은 껍데기 |
| ③ 콘텐츠 추출 | 부분 소재만 존재: AEO 탭 «답변이 제시한 출처 N건»(URL 목록), «누가 대신 인용되고 있나»(도메인 표), 인용에 사용된 콘텐츠 필드(관측 구조화 6필드 중 하나) | **화면 없음** — 데이터는 이미 잡고 있으나 «인용된 콘텐츠를 모아 보는 자리»가 없다 |
| ④ 콘텐츠 분석 | 없음. 가장 가까운 것은 GEO 탭의 답변 가능성/구조화 점검(자사 콘텐츠의 «형식» 분석)뿐 — «내용» 분석(주제·질문 커버리지·인용 콘텐츠와의 비교)은 부재 | **화면 없음** |
| ⑤ 콘텐츠 제안 | 없음. 실행 큐는 기술·구조 조치(태그·인증서·스키마)의 큐이지 콘텐츠 제작 큐가 아니다 | **화면 없음** — 차별점의 핵심인데 현 IA 에 자리 자체가 없다 |
| ⑥ 재관측 | 집계(저장된 관측 다시 세기), 회차별 추이(질문을 바꾸면 추이가 갈라진다 규칙) | **완성** — 다만 «제안한 콘텐츠가 나간 뒤 언급률이 움직였나»를 잇는 고리는 없다 |
| ⑦ 재진단 | 재진단 대기 → 자동 닫힘 상태 머신, 재채점 | **완성** |

**구조적 결론**: 현 콘솔은 ①②⑥⑦ — 즉 **«재는 쪽» 반원은 완성**돼 있고, ③④⑤ — **«만드는 쪽» 반원이 통째로 비어 있다.** 이것은 결함이 아니라 작업 순서(측정 기반을 먼저 세움)로 읽히지만, 차별점이 ⑤(콘텐츠 제안)에 있다면 다음 설계는 아래를 채워야 한다:

1. **콘텐츠 추출 화면** — 관측이 이미 수집하는 «인용된 콘텐츠(페이지/문서)»를 거래처·질문·엔진 축으로 모아 보는 자리. 경쟁 도메인 화면(껍데기)의 자연스러운 확장: 도메인 수준 → **문서 수준**으로 한 단계 내려가면 된다.
2. **콘텐츠 분석 화면** — 자사 문서 vs 인용된 타사 문서의 비교 축이 필요하다. 현 문법에 맞는 형태: «이 질문에서 AI 가 인용한 콘텐츠는 무엇을 갖췄고 우리 문서엔 무엇이 없나»를 GEO 항목 문법(감점/통과/판정 못 함)으로 재사용.
3. **콘텐츠 제안 큐** — 실행 큐와 같은 문법(우선순위·기대 효과·품·담당·기한)으로 «만들 콘텐츠» 목록을 세우는 화면. 기존 «상승폭 ÷ 품» 정렬이 그대로 이식 가능하며, 제안→발행→⑥재관측으로 잇는 상태 머신(이슈의 열림→조치→재진단→닫힘과 동형)을 붙이면 흐름이 닫힌다.
4. **재관측과의 연결 고리** — 제안된 콘텐츠에 «발행일» 마커를 추이 차트에 찍어, «질문을 바꾸면 추이가 갈라진다»와 같은 규율로 «콘텐츠가 나간 뒤»를 읽게 한다.

현 5단계 퍼널(접근→구조→답변화→관측→보고)과 목표 7단계는 충돌하지 않는다 — 퍼널의 «답변화»가 ③④⑤로 세분되는 관계다. 대시보드 퍼널을 7칸으로 늘리기보다, «답변화» 칸을 누르면 콘텐츠 추출→분석→제안 하위 흐름이 열리는 2계층이 현 문법(단계 클릭=필터)에 더 맞는다.

## 11. 담당 체계 대비 현 화면

목표 체계: **최고 관리자 1~3명**(전체 진단·관측 책임) + **콘텐츠 담당자 1명당 거래처 10곳**.

현 콘솔에서 확인되는 것:
- 실행 큐·이슈에 **담당자 개인 이름**(이재훈·정수민·김하늘·박도윤)이 항목 단위로 붙는다 — 항목 단위 배정 모델.
- 헤더에 «관리자» 역할 표시, 팀원 관리 화면 존재(내용 미확인), 대시보드 제목의 조직명 개인화(멀티테넌트).
- 거래처 목록의 «맡은 곳 11곳» 표현과 ★고정 5곳 — «내 담당»의 씨앗은 있으나 담당자별 필터는 화면에 없다.

목표 체계와의 간극:
1. **역할 2종의 화면 분리가 없다.** 최고 관리자(전 거래처 진단·관측)와 콘텐츠 담당자(내 10곳)는 첫 화면부터 달라야 한다 — 관리자는 지금의 대시보드(전 거래처 관제탑)가 맞고, 콘텐츠 담당자는 «내 10곳의 콘텐츠 제안 큐»가 첫 화면이어야 한다. 현 IA 의 BRAND(로고=대시보드) 규칙에 역할 분기를 넣을 자리가 필요하다.
2. **배정 단위의 전환** — 현재는 이슈(항목) 단위 담당인데, 목표는 거래처 단위 담당(1인 10곳). 거래처에 담당자 필드를 두고 이슈 담당은 그로부터 상속하는 쪽이 목표 체계와 맞는다. «맡은 곳 N곳» 카운트도 조직 전체가 아니라 «내» 것이 된다.
3. **10곳 규모에 맞는 화면 밀도** — 콘텐츠 담당자 화면은 거래처 11곳 목록이 아니라 10곳 × 콘텐츠 파이프라인 상태(추출됨/분석됨/제안됨/발행됨/재관측 중)의 보드형이 자연스럽다. 관리자 화면의 «사람이 봐야 하는 것» 문법을 담당자용으로 바꾸면 «오늘 만들 것»이 된다.
4. **책임의 분모 표기** — 이 제품의 문법대로라면 담당자 화면에도 분모가 붙어야 한다: «10곳 중 관측이 도는 곳 7 · 제안 대기 3» 식으로, 관리자의 진단·관측 책임과 담당자의 콘텐츠 책임이 서로의 분모를 침범하지 않게.

---

# 제6부 — 구조 해부 (소스 전 계층 판독 · 커밋 216233be)

> Lovable 커넥터로 진입점 → 데이터 계층 → 도메인 로직 → 공용 컴포넌트 → 라우트 순으로 소스 전체를 읽었다. 목적: 구조가 어떻게 짜였는지와 **실물 구현에 가져갈 부분**의 추출. 스택: TanStack Start(라우터+서버 함수) + Tailwind v4 + Supabase(인증·DB).
> ※ 한 가지 사실 정정 — «AEO 관측·운영 묶음은 타이틀만»이라는 전제와 달리, 이 커밋 시점에는 대부분 표본 데이터로 **구현돼 있다**(자리표시는 `/results/:token` 상세 연결 · `/invite/:token` · 리포트 상세 3곳뿐). 그 사이 작업이 진행된 판으로 보인다.

## 12. 계층 구성 — 단일 소스 6개가 전부를 지배한다

```
①정체성  brand.ts          제품명·태그라인·고지문·측정 원칙 5개조·판번호·CHANGELOG·NOT_MEASURED("—")
②IA      navigation.ts     메뉴 이름·차례·자리 5종(BRAND/MENU/GROUP/TOOL/ABSORBED)·거래처 탭 6개
③채점    scoring-spec.ts   «점수를 만드는 유일한 곳» — 순수 함수 + SPEC_VERSION
④관측    aeo.ts            엔진 7종(+citesSources 플래그)·관측 5단계·수집 필드 6종
⑤자기문서 site-map.ts       화면 실측표(live/shell) + 파이프라인 7단계 + 불변 규칙 5조
⑥토큰    styles.css        oklch 색 토큰 전부 — «컴포넌트에 hex를 쓰지 않는다»
            ↓ 표본: anseo-sample.ts · anseo-identity.ts
저장소    repo.ts           DB 우선, 비면 표본 폴백 — source("db"|"sample")를 화면 배지로 그대로 노출
서버      diagnosis.functions.ts  채점·집계는 서버 함수에서만 (zod 검증 + 인증 미들웨어)
부품      primitives / sample / issue-life / infographics / charts / check-list / …
라우트    routes/*          조립만 한다 — 계산·이름·색을 스스로 만들지 않는다
```

**코드 주석으로 명문화된 아키텍처 결정** (전부 원문 실측):

| 결정 | 근거 문구(주석) |
|---|---|
| 점수는 서버에서만 | «점수를 브라우저에서 계산해 저장하면 '화면마다 다른 점수'가 생긴다. 여기서만 계산하고 여기서만 쓴다.» |
| 채점은 순수 함수 | «같은 입력이면 언제 어디서 돌려도 같은 점수가 나와야 하고, 판정 못 한 항목은 0점이 아니라 '못 잼'으로 따로 센다» — verdict 4종(pass/fail/**unknown**/not_applicable)이 타입에, unknown은 분모 제외+unmeasured 별도 집계, 잴 배점이 없으면 normalized가 0이 아니라 **null** |
| 저장소 폴백의 정직성 | «데이터베이스가 비어 있으면 '값이 없다'가 아니라 표본으로 되돌아가고, **그 사실을 source로 알려 준다**» → 화면의 저장됨/표본/명세 3종 배지(DataBanner)와 직결 |
| 이슈 닫기는 기계만 | `HUMAN_STATES = ["open","in_progress","awaiting_rescan"]` — «사람이 바꿀 수 있는 상태는 두 가지뿐이다. 닫기는 재진단이 한다.» 모든 전이·생성이 issue_events 감사 이력으로, 의료→이슈 승격은 slug 멱등(같은 칸 두 번 안 올라감) |
| 게이트는 단정하지 않는다 | «세션 복원이 끝나기 전에는 아무 것도 단정하지 않는다 — 잠깐 깜빡였다가 로그인으로 튕기는 사고를 막는다» + 조직 미배정(membershipMissing)을 별도 상태로 |
| 역할 4종 | admin(관리자)/manager(운영자)/analyst(분석가)/viewer(열람자) + `hasRole`, 업종 게이트(industry=MEDICAL)와 별개 축 |
| 로그인 보안 카피 | «계정 존재 여부를 흘리지 않는다» — 오류 문구 단일화 |
| SSR 하이드레이션 방어 | 아크 좌표를 `toFixed(3)`으로 고정 — «서버·브라우저의 부동소수 끝자리 차이로 생기는 하이드레이션 불일치를 없앤다» |

**site-map.ts의 파이프라인 7단계** (0등록→1수집→2채점→3키워드·질문→4AI 관측→5실행→6보고)는 단계마다 들어오는 것/하는 일/남는 것/보이는 화면/실행 위치를 데이터로 갖고, «각 단계는 앞 단계의 산출물만 먹습니다»가 명시돼 있다 — 제5부의 목표 흐름 7단계와 정확히 겹치는 뼈대이며, 콘텐츠 추출·분석·제안은 이 파이프라인의 4(관측)와 5(실행) 사이에 끼워 넣는 그림이 된다.

## 13. 대시보드 인포그래픽 3블록 — 시각 인코딩·애니메이션 실측

테마 전제: **라이트가 기본, 다크가 옵션**(«Light is the default; dark is the option») — 스크린샷 11장은 다크 모드였다. 폰트 3벌: Bricolage Grotesque(제목) · Inter Tight(본문) · JetBrains Mono(수치, `zero` 슬래시드 제로 + tabular-nums), Pretendard 폴백. 경쟁 브랜드 전용 계열색 `--rival-1~4`(«좋고 나쁨의 뜻 없이 구분만»), «진단 못 함» 전용 무채색 `--unknown`.

### 블록 A — 3축 상태 카드 (GaugeArc ×2 + AEO 인라인 퍼널)

- **GaugeArc**: SVG 반원(r=52), 트랙은 `stroke-border` 전체 아크, 채움은 `strokeDasharray = 원주×비율`. **`transition-[stroke-dasharray] duration-700`** — 값이 바뀌면 700ms에 걸쳐 아크가 차오른다(마운트 시가 아니라 값 변경 시 발동). null이면 채움 path 자체를 그리지 않는다 — «못 잰 값은 채우지 않고 눈금만 남긴다».
- AEO 카드: `metric` 대형 수치 + 4단 미니 바(파랑, 언급·인용 단만 ember). 게이지를 쓰지 않는 이유가 코드 주석에: «0~100 게이지를 쓰면 준비도와 같은 척도로 오해된다.»

### 블록 B — 사분면 + 깔때기 + 도넛

- **Quadrant**: 점은 absolute % 배치(4~96% 클램프로 판 밖 이탈 방지), 크기 8px+이슈 수, `ring-1 ring-background`로 겹친 점 사이 흰 테두리 분리. **hover 시 `group-hover` 툴팁**으로 «이름 · x/y 좌표»가 뜬다 — 즉 라벨은 존재하되 호버 전용(§7-2의 상시 라벨 권고는 유지). 못 잰 거래처 수는 컴포넌트가 자동으로 «원점이 아니라 판 밖입니다» 문구로 렌더.
- **FunnelShape**: 바 폭 **`transition-[width] duration-500`** — 집계를 다시 돌리면 바가 미끄러지듯 조정된다. 단계 사이 감소분을 «−N» 빨강 주석으로, 마지막 단만 mint.
- **Donut**: 세그먼트 사이 1° 간격, **hover 시 strokeWidth 14→18 (`transition-[stroke-width]`)** + 범례 행 배경 하이라이트 — 도넛↔범례 **양방향 hover 연동**(어느 쪽에 올려도 서로 반응).

### 블록 C — 운영 (경보 리스트 + 커버리지 와플 + 엔진 DotMatrix)

- **Waffle**: 100칸 grid, `ratio: null`이면 전 칸 회색 — «못 잼»을 0%와 시각적으로 분리. 애니메이션 없음(정적).
- **DotMatrix**: health 3톤(ok=mint/degraded=ember/disabled=unknown) 점 + 사유는 title 툴팁.

### 페이지 로드 오케스트레이션 (styles.css)

```css
@keyframes rise  { from { opacity:0; transform:translateY(14px) } to { opacity:1 } }
@keyframes sweep { from { transform:scaleX(0) } to { transform:scaleX(1) } }
@utility stage    { animation: rise  0.62s cubic-bezier(0.22,1,0.36,1) forwards }  /* ease-out-quint 계열 */
@utility sweep-in { animation: sweep 0.7s  cubic-bezier(0.22,1,0.36,1) forwards; transform-origin:left center }
@media (prefers-reduced-motion: reduce) { * { animation-duration:.01ms !important; transition-duration:.01ms !important } }
```

- «one page-load sequence, not scattered effects» 지향의 유틸 2종이 정의돼 있으나, **대시보드 라우트 마크업에는 아직 배선돼 있지 않다**(정의만 존재 — 카드 등장 시퀀스는 미적용 상태). `flow` 키프레임(이동 점선)은 리믹스 전 CFO 테마의 Sankey 잔재.
- **reduced-motion 전면 무효화 스위치**가 있어 모든 모션이 접근성 설정을 존중한다.

### 그 밖의 마이크로 인터랙션 (컴포넌트 실측)

| 위치 | 동작 |
|---|---|
| TrendLine(주간 추이) | 점 hover 시 반지름 2.8→4.6 + `chart-tip` 카드(회차·값·**직전 대비 ±p**). 각 점에 r=9 투명 히트 영역을 겹쳐 작은 점도 쉽게 잡힌다. 결측 회차는 선을 끊고 축 아래 회색 점만 |
| RadialBars(동심 아크) | hover 시 해당 궤도 굵기 +1.5, 나머지 opacity 0.35로 디밍, 범례 행과 양방향 연동. 주석에 레이더 기각 사유: «7각형은 축 사이 면적이 실제 값보다 커 보인다. 같은 데이터를 한 축 위 길이로 옮기면 길이끼리 직접 비교된다» — §7-1의 교체 권고는 유지하되, 설계가 무근거는 아니었음을 병기 |
| Sparkline | Catmull-Rom→베지어 스무딩(«값은 지나가는 점 그대로»), **천장 점선=배점**(«선이 천장에서 얼마나 먼지가 곧 남은 점수»), 그라디언트 면, 마지막 점만 강조 링, 점 하나면 선 없이 점만(«추이가 없다는 뜻») |
| Heatmap | null 칸=점선 테두리 «—», 값 칸 opacity 0.15+정규화×0.85(0은 0.06), **농도>0.55면 글자색 자동 반전** |
| CheckList | 필터 칩(판정별 카운트), 펼침 화살표 rotate-90, 근거 코드가 **정규식 토크나이저로 자동 색칠**(태그=파랑·값=민트·상태코드/실패=빨강·«←» 주석=주황) — «색은 장식이 아니라 '무엇을 보라'는 지시다» |
| StageFlow | 선택 시 border-signal+bg-signal/5, transition-colors |
| QueryDeltaTable | RateBar에 **직전 회차를 세로 눈금으로 겹침**, CellStrip(칸 상태를 언급→미언급→못 잼 순 정렬해 나열), 펼치면 QueryMatrix(엔진×켬/끔 셀: 숫자=등장 순번·`●`=순번 미상·`·`=미언급·`—`=못 잼+사유) |
| 도넛/게이지 공통 | 값 갱신형 애니메이션(트랜지션)이지 등장형(마운트 키프레임)이 아니다 — 재채점·집계 버튼을 눌렀을 때 화면이 «다시 접히는» 감각을 만든다 |

## 14. 신규 화면 기록 (S13.1~S13.12)

**S13.1 키워드(/console/keywords)** — Dumbbell(PC↔모바일 검색량, «두 점 사이 길이가 기기 편차. 모바일 쪽이 멀수록 지역·즉시 탐색 성격») · Lollipop(질문 후보 순위, «위쪽 키워드가 AEO 질문 집합의 씨앗») · 검색량 표(PC/모바일/합계/경쟁/클릭률) · 조회 이력(«누가 무엇을 언제 봤나») · 출처 표기 규칙 카드. «네이버 검색광고 열쇠가 없으면 이 표는 0이 아니라 '진단 못 함'으로 비워 둡니다.»

**S13.2 경쟁 도메인(/console/competitors)** — 제목이 «브랜드 식별 · 경쟁 도메인». 상단에 **IdentityGate/IdentityCard**(부록 §8의 그 컴포넌트가 여기 배선됨), 아래 거래처별 Dumbbell(등장↔인용)+표. «다섯 항목 중 도메인 하나만 결정적» · «출처를 주지 않는 엔진의 답변은 두 값 모두 세지 않습니다.»

**S13.3 AI 답변 검수(/console/review)** — 자동 판정 UNKNOWN 답변의 사람 검수 큐. 4-Stat(대기/맞음/다른 업체/보류) · 대기열 행: 사유·질문·**답변 조각(인용 블록)**·근거 URL(자사=민트 점/외부 구분)·**동명 업체 N곳 확인됨** · 판정 3버튼(이 고객 맞음/다른 업체/**보류**) · «미판정만» 필터 · 진행 도넛(미판정=unknown 톤). «판정 전 항목은 '언급 안 됨'이 아니라 '진단 못 함' — 두 값을 합치면 언급률이 낮게 보입니다.» 판정은 로컬 상태뿐이며 그 사실을 빈 상태 문구가 자백.

**S13.4 사용량(/console/usage)** — 4-Stat(AI 호출·수집 페이지·키워드 조회·비용 $x/$y) · 엔진별 Lollipop+도넛 · **질문 커버리지 «덜 물은 자리 N건»**(최다 호출 엔진을 만점 눈금으로, «호출 0은 '답이 없었다'가 아니라 '묻지 않았다'») · 상한 와플(상한 0이면 ratio=null — «칸이 비어 있는 것은 '안 썼다'가 아니라 '상한이 없어 실행 자체가 없었다'») · «상한을 올리면 다음 회차부터. 지난 회차는 소급해 채우지 않습니다.»

**S13.5 채점 기준 버전(/console/scoring-versions)** — «채점 기준은 코드가 아니라 발행된 명세입니다.» Dumbbell(직전 판↔현재 판 배점 이동) · 판 이력 타임라인(사유+**체크섬**) · «배점이 바뀐 영역은 점수 변화가 '개선'인지 '기준 변경'인지 구분되지 않습니다. 판이 다르면 총점을 나란히 놓지 않습니다.»

**S13.6 외부 연결 열쇠(/console/credentials)** — 상태 3종만(켜짐/열쇠 없음/열쇠가 틀림), 값 재노출 없음(«값 노출: 없음» 행까지) · **«지금 막혀 있는 것» — 열쇠가 막는 화면 역링크** · «플레이스홀더 값([SENSITIVE], changeme)은 '설정됨'이 아니라 '열쇠가 틀림'으로 셉니다. **설정된 척하는 값이 가장 오래 사람을 속입니다.**» · 열쇠 상태≠엔진 상태 구분(«열쇠가 있어도 상대 서버가 답을 못 주면 '끊김'»).

**S13.7 팀원 관리(/console/team)** — Supabase 라이브(DataBanner «저장됨»). 역할 4종 라벨, 초대 현황은 admin만(비관리자에겐 «권한이 없습니다»).

**S13.8 사이트맵·파이프라인(/console/sitemap)** — site-map.ts를 그대로 렌더: 화면 실측표(경로·구현/자리표시 칩·실측 구성·기능·데이터 출처·진입 경로) + 파이프라인 7단계 + 불변 규칙 5조. 하단: «화면을 고치면 이 표도 같이 고쳐야 합니다 — 출처는 src/data/site-map.ts 한 곳입니다.» **콘솔이 자기 자신을 실측 문서화하는 화면.**

**S13.9 거래처 등록(/console/customers/new)** — 부제: «여기서 정한 주소·범위·브랜드 표기가 이후 모든 점수의 **분모**가 되므로, 나중에 바꾸면 이전 회차와 비교할 수 없게 됩니다.» 검증 로직 실측: URL 파싱 실패/비HTTP 스킴/사설·로컬 IP는 오류, http는 «첫 진단에서 차단 이슈로 잡힙니다» 경고, **같은 도메인 기존 진단 감지 시 «수집 상한(시간당 450장)을 나눠 씁니다» 경고**, 브랜드 표기 필수(«AEO 언급을 세려면 최소 하나»)+1개뿐이면 약칭 권고, 의료 선택 시 규정 검사 예고. 우측 «등록 전에 짚는 것»+실시간 미리보기. 통과해도 «저장됨이라고 말하지 않음 — 실제로는 없는 기록을 있다고 하는 셈».

**S13.10 프로젝트(/console/customers/projects)** — «프로젝트는 '같은 도메인'입니다.» 도메인 묶음 표(진단 대상·SEO 평균·열린 이슈·관측 도는 곳·가장 오래된 진단·http 스킴 경고·차단 문구) · 도메인별 이슈 Lollipop(«도메인 단위로 한 번에 고치는 편이 품이 적습니다») · 경로 분리 카드(«robots.txt·인증서·리다이렉트는 도메인 하나가 공유 — 한 곳을 고치면 함께 닫힙니다»).

**S13.11 공유 결과(/results/$token)** — **콘솔 리포트 상세와 같은 ReportDocument 컴포넌트**, `variant="shared"`로 내부 링크·이전 판 비교만 숨김. noindex/nofollow, «목록도 조직 정보도 다른 거래처도 절대 노출되지 않는다», 만료 90일 문구, 링크 실패 화면에도 측정 원칙 5개조를 배치. 인쇄 CSS로 접힌 항목 전부 펼쳐 출력.

**S13.12 초대(/invite/$token)** — 자리표시. «토큰은 원문이 아니라 해시로만 저장 — 대조·계정 생성은 서버에서만.» 그 전까지 관리자가 직접 계정 생성한다고 자백.

## 15. 가져갈 것 — 실물 구현 이식 목록 (파일·컴포넌트 지명)

**A. 아키텍처 패턴 (그대로 이식 권장)**
1. `scoring-spec.ts` — 채점을 SPEC_VERSION 붙은 **순수 함수 한 벌**로: verdict 4종, unknown=분모 제외+별도 집계, 잴 것 없으면 null. 화면·서버·리포트가 같은 함수를 먹는 구조.
2. `repo.ts` — 저장소 계층의 **source 노출**(db/sample) + `DataBanner`의 3종 배지(저장됨/표본/명세). 데이터 출처가 화면 문법이 된다.
3. `diagnosis.functions.ts` — 점수 계산의 서버 단일화(«화면마다 다른 점수» 방지).
4. `HUMAN_STATES` + `issue_events` — 사람이 만질 수 있는 상태의 화이트리스트화 + 전 전이 감사 이력 + 승격 멱등(slug).
5. `navigation.ts` — 자리(place) 5종 + 원칙 0-E(`enteredFrom` 의무) + 탭 단일 소스.
6. `site-map.ts` — 화면 실측표+파이프라인의 **데이터화**(자기 문서화 화면 포함). veo-platform의 WORKLIST·IA 문서를 코드로 옮기는 그림.
7. `brand.ts` — 제품명·고지문·측정 원칙·판번호·CHANGELOG의 단일 소스(푸터 판번호→변경 이력 링크).

**B. 컴포넌트 (재작성 없이 개념 이식)**
8. `Metric`/`QualityChip`(primitives) — 값 품질 4종(측정/해당 없음/진단 못 함/추정)을 **타입으로** 강제, «—» 폴백 내장.
9. `CheckList` + `CodeBlock` 토크나이저 — 판정 근거 코드 조각의 정규식 자동 색칠(파랑=태그·민트=값·빨강=실패·주황=주석).
10. `ReportDocument` — 콘솔/공유가 **한 컴포넌트**(variant 분기) + 인쇄 CSS. 발행본 이중 구현 방지의 정석.
11. `QueryDeltaTable`(aeo-matrix) — RateBar의 직전 회차 세로 눈금, CellStrip, 셀 4기호(순번/●/·/—)의 관측 문법.
12. `Sparkline` — 천장 점선=배점, 단일점=점만. `TrendLine` — 결측 구간 선 끊기+투명 히트 영역+chart-tip.
13. `IdentityGate`/`IdentityCard`(§8 부록) + `console.review`의 검수 3판정 — 식별 관문→검수→언급률 확정의 전체 사슬.
14. `Heatmap` — null 점선 칸, 농도 자동 텍스트 반전.

**C. 모션 규격**
15. 값 갱신형 트랜지션만: 게이지 700ms(stroke-dasharray) · 퍼널 500ms(width) · hover 디밍/굵기(도넛·동심 아크·추이선). 등장형은 `stage`(rise 0.62s)/`sweep-in`(0.7s) 유틸로 준비돼 있으나 미배선 — 배선 시 «한 번의 페이지 로드 시퀀스» 원칙 유지. `prefers-reduced-motion` 전면 스위치 필수 동반.

**D. 도메인 규칙 (기획 이식)**
16. 등록 검증 — 사설 IP 차단, 도메인 공유 시 수집 상한(450장/시간) 경고, 브랜드 표기=AEO 분모 강제, «등록은 분모 확정».
17. 자격증명 3상태 + «플레이스홀더=틀림» 규칙 + 열쇠가 막는 화면 역링크.
18. 사용량의 «덜 물은 자리»(최다 엔진 기준 커버리지)와 «상한 0=실행 없음» 구분.
19. 채점 판 비교 금지 규칙(배점 이동 시 총점 병렬 비교 차단) + 판 체크섬.
20. 공유 링크 규격 — 토큰 1건 1리포트, noindex, 90일 만료, 실패 화면에도 읽는 법.

---

# 제7부 — 원형(CFO Command Center) 히어로 인포그래픽 3종 추출·분석

> 이 프로젝트는 «Finance Dashboard» 템플릿의 리믹스다. ANSEO 재구현(8/27) 때 CFO 화면 코드는 삭제됐지만 **첫 커밋 `b0cb2b8`(Remix pages snapshot)에 원형이 그대로 남아 있어** 소스를 추출했다. 대상: ① 생키 머니플로우(SankeyFlow) ② 13주 현금 팬 차트(CashFanChart) ③ 예산 대비 워터폴(VarianceWaterfall) + 보조로 KPI 티커 스트립(KpiStrip)과 모션 라이브러리(motion.ts). 현 ANSEO 테마(oklch 토큰·num/eyebrow 유틸·rise/sweep·flow 키프레임)는 전부 이 원형에서 물려받은 것이다.

## 16. 모션 라이브러리 — `src/lib/motion.ts` (3개 훅이 전체 모션의 뼈대)

| 훅 | 역할 | 수치 |
|---|---|---|
| `useCountUp(target, duration, delay)` | 마운트 시 0→목표 카운트업. rAF 기반이라 «페이지 로드 시퀀스와 박자가 맞는다». **최초 정착 후 목표가 바뀌면 처음부터가 아니라 현재값→새 값으로 520ms 글라이드** — 시나리오 토글 때 숫자가 리셋되지 않고 미끄러진다 | 기본 1400ms · ease-out cubic(하우스 커브 «the house curve») |
| `useStage(delay)` | delay 경과 후 true — **단일 페이지 로드 시퀀스를 조립하는 원시 부품**. 컴포넌트마다 delay를 층층이 줘서 «흩어진 효과가 아니라 한 번의 오케스트레이션»을 만든다 | setTimeout 1개 |
| `useMeasure()` | ResizeObserver로 요소 폭 실측 → 모든 SVG 차트가 고정 viewBox가 아니라 **실폭 렌더** | — |

## 17. 히어로 ① — SankeyFlow (머니플로우 생키)

**데이터→기하**: `sankey-layout.ts`가 d3-sankey로 좌표만 계산하고 **순수 스냅샷(Snapshot)으로 직렬화** — 노드·리본을 key 맵으로 갖는 불변 프레임. 수익 4갈래→Total Revenue→COGS/Gross Profit→비용 6갈래→Operating Income. **적자면 그래프 방향이 바뀐다**: «손실은 어딘가에서 메워져야 한다 — 준비금에서 들어오는 것으로 그린다(Funded from Cash Reserves), 그래야 다이어그램이 수지 균형을 이룬다». 라벨 숫자는 레이아웃 값이 아니라 별도 `display` 필드 — «기하와 무관하게 재무적으로 맞는 숫자를 인쇄».

**모프 애니메이션 (rAF 수제 트위닝)**:
- 첫 등장: `collapse(target)` — 모든 리본 폭 0, 노드는 중심선 한 점으로 접힌 시작 프레임 → 목표 레이아웃까지 **1150ms**. 이후 월/시나리오 토글(stateKey 변경) 시엔 현재 프레임→새 레이아웃 **760ms**. 이징은 easeInOut cubic. 매 프레임 `interpolate(from,to,t)`로 노드·리본 좌표 전부를 lerp — CSS가 아니라 **프레임 보간**이라 리본 폭·경로가 동시에 자연스럽게 재배치된다.
- **이동 점선(travelling dash)**: 리본 중심선에 `strokeDasharray "3 26"` + `flow` 키프레임. 주석이 정책을 밝힌다 — «**등장 신호이지 실시간 신호가 아니다**: 모프가 진행되는 동안 흐르고, 정착 후 **6초 뒤 멈춘다(park)**. 호버로 추적 중인 경로만 계속 흐른다.» 흐름 속도도 상태별: 강조 경로 1.6s/기본 4.2s, `animationPlayState`로 정지.
- **경로 추적(hover/포커스)**: 리본에 올리면 `tracePath`가 그 링크의 **상류 끝~하류 끝 전체 경로**를 집합으로 계산, 해당 리본 `--ribbon-lit`(0.8~0.88), 나머지 `--ribbon-dim`(0.07~0.09)으로 디밍 — opacity 220ms 트랜지션. 노드 호버는 그 노드를 지나는 모든 경로를 켠다. 툴팁: 출발→도착, 금액, **총매출 대비 %**.
- 히트 영역: 리본 폭을 최소 9px로 부풀린 투명 path — 가는 리본도 잡기 쉽게.

**접근성 — 이 컴포넌트의 백미**: 다이어그램 전체가 **탭 스톱 1개 + 로빙 커서**(`role="application"` + `aria-activedescendant`). 화살표 키가 DOM 순서가 아니라 **흐름을 따라간다** — ←/→는 상류/하류(노드→나가는 리본→다음 노드), ↑/↓는 같은 위치의 형제(같은 깊이 노드, 같은 출발 리본), Esc로 이탈. 커서 이동 시 가로 스크롤을 자동 추적(scrollTo smooth). 리본·노드마다 aria-label에 금액·비중 문장. sr-only 사용법 안내 + 전체 요약 문장(ariaSummary) 자동 생성.

**반응형**: 700px 미만이면 축소가 아니라 **가로 스크롤러로 전환** — «좌→우 흐름이 생키의 전부라서, 라벨을 세로로 쌓아 흐름을 목록으로 부수느니 패닝으로 읽기를 보존한다». 라벨은 컴팩트 모드에서 축약 사전(shortLabel)으로 치환, 어떤 리본 위에서도 읽히도록 **텍스트 헤일로**(stroke=surface, paintOrder:stroke).

## 18. 히어로 ② — CashFanChart (13주 현금 팬 차트 + 드래그 what-if)

**인코딩**: d3-shape `curveMonotoneX`로 3겹 — P10~P90 외곽 밴드(`--band-outer`), P25~P75 내곽 밴드(`--band-inner`), 중앙 기준선(`--signal` 1.75px). 불확실성이 미래로 갈수록 밴드가 벌어지는 전형적 팬 차트. 데이터가 모자라면 차트 대신 점선 박스 문구(«Not enough cash activity…») — 빈 값 정직성 원칙의 원형.

**등장 애니메이션**: 밴드+선을 감싼 `<g>`에 **`clip-path: inset(0 100% 0 0)` → `inset(0 0 0 0)`, 1.5s cubic-bezier(0.22,1,0.36,1) 0.15s 지연** — 차트가 왼쪽에서 오른쪽으로 «그려지며» 나타난다. 페이지 로드 게이트(`active`)와 연동.

**호버**: 포인터 x를 `x.invert`로 주차(週次)에 스냅 → 세로 점선+중앙선 위 링 마커+툴팁(주차·기준값·P10–P90 구간). 축 라벨도 호버 주차만 전경색으로.

**드래그 what-if (HiringDrag)**: 채용 지출 슬라이더(0~+$900K/mo, 1만 단위 스냅). 커스텀 트랙: `setPointerCapture`+윈도우 pointermove로 드래그, 드래그 중 핸들 **scale 1.15 + 22% 시그널 색 글로우**(box-shadow 0 0 0 7px color-mix). 키보드 완비: ←/→ ±2만, Home/End, `role="slider"`+aria-valuetext. 값이 바뀌면 `buildForecast`가 즉시 재계산 → 밴드·**런웨이 소진 날짜**(18개월 미만이면 ember 경고색)가 실시간 갱신. 런웨이 기준일은 고정 날짜가 아니라 «데이터의 마지막 마감 회차로부터 전진» — 데이터 교체에도 맞게.

## 19. 히어로 ③ — VarianceWaterfall (예산→실적 워터폴)

**인코딩**: `buildSteps`가 예산 OI(앵커)→항목별 유불리 델타(누적 커서)→실적 OI(앵커)로 스텝 생성. 색: 앵커=signal-soft, 유리=mint, 불리=ember. **모든 막대가 공통 y 스케일 하나** — «One common scale for every bar»(막대별 과장 방지). 0선은 실선, 나머지 그리드는 점선.

**순차 등장 애니메이션**: 막대마다 `scaleY(0)→scaleY(1)` **520ms cubic-bezier(0.22,1,0.36,1) + i×130ms 스태거**, transform-origin이 **증가 막대는 바닥, 감소 막대는 꼭대기**(자라는 방향이 값의 방향과 일치). 연결 점선은 140+i×130ms, 수치 라벨은 240+i×130ms, 축 라벨은 200+i×130ms에 페이드 — 한 스텝 안에서도 막대→연결선→숫자 순으로 시차.

**선택 인터랙션**: 막대 클릭=코멘터리 패널 토글(aria-pressed), 선택 시 나머지 막대 opacity 0.28 디밍+선택 링. 막대 아래 점 마커로 **코멘터리 출처 구분**(작성됨=외곽선 점 / AI 생성=채운 점) — aria-label에도 «Authored/AI-generated commentary available» 명시. 포커스 링은 DOM 순서 의존을 피해 상태 기반으로 그린다(생키 노드 링과 같은 방식).

## 20. 보조 — KpiStrip (카운트업 티커 + 3D 플립 카드)

- 수치는 `useCountUp(value, 1500, 220+index×110)` — 카드마다 110ms 시차로 차례로 차오르고, 카드 자체는 `stage` 유틸에 `animationDelay 140+index×90ms` — **등장(rise)과 카운트업이 이중 스태거**.
- **플립 모델(FLIP MODEL 주석)**: 카드 뒷면에 지표 정의. 뒤집는 트리거 3개를 독립 관리 — hover는 **마우스 포인터일 때만**(`pointerType==="mouse"` — 터치 탭이 카드를 뒤집힌 채로 남기지 않게), 포커스는 **`:focus-visible`일 때만**(탭 클릭이 플립을 걸어 잠그지 않게), 탭/클릭은 pinned 토글(터치 경로). «카운트업은 이 상태보다 위에 살아서 **플립해도 티커가 재시작되지 않는다**.» 회전: rotateY 180°, 520ms, 하우스 커브, perspective 900px, backface-visibility로 양면.
- 값 없음은 0이 아니라 `NOT_AVAILABLE` 문자 — ANSEO의 «—» 규칙의 원형이 여기 있다.

## 21. 원형→ANSEO 계승 관계와 이식 판단

**이미 계승된 것**: oklch 토큰 팔레트·num/eyebrow/metric 유틸·rise/sweep/flow 키프레임·shadow/hairline·reduced-motion 스위치·«값 없음≠0» 표기(NOT_AVAILABLE→NOT_MEASURED)·chart-tip 계열 툴팁.

**원형에만 있고 ANSEO가 아직 안 쓰는 것 — 이식 가치 순**:
1. **`useCountUp`/`useStage`(motion.ts)** — ANSEO의 `stage` CSS 유틸이 미배선인 이유가 이 훅 세트를 안 가져와서다. 대시보드 KPI·재채점 결과 수치에 «최초 카운트업+이후 글라이드» 패턴을 붙이면 «다시 접히는» 감각이 완성된다. 비용 낮고 효과 큼.
2. **워터폴의 방향 있는 스태거**(scaleY+origin 분기+i×130ms, 막대→연결선→숫자 3층 시차) — ANSEO 실행 큐·손실 구성에 그대로 이식 가능한 등장 문법.
3. **생키의 경로 추적 디밍 모델**(lit/dim/rest 3단 opacity 토큰 — 토큰은 이미 styles.css에 살아 있다: `--ribbon-rest/lit/dim`) — 향후 «콘텐츠 추출» 화면에서 질문→엔진→인용 문서 흐름을 생키로 그릴 때 통째로 재사용.
4. **키보드 로빙 커서**(흐름을 따라가는 화살표 탐색 + aria-activedescendant) — 복잡 다이어그램 접근성의 모범 답안.
5. **팬 차트의 clip-path 좌→우 드로잉**(1.5s) + **드래그 what-if 핸들**(pointer capture·스냅·실시간 재계산·glow) — «상승폭 시뮬레이션(이 이슈를 고치면 점수가 얼마나 오르나)» 같은 반사실 UI에 어울리는 부품.
6. **KPI 플립의 입력 수단별 트리거 분리**(mouse hover/focus-visible/tap pin 독립) — 툴팁 의존을 지적했던 §8.3-3의 정답 패턴이 원형에 이미 있다.
7. 이동 점선의 «등장 신호, 6초 후 정지» 정책 — 장식 모션을 정보 신호와 구분하는 규율.

**버릴 것**: 생키 자체를 지금 ANSEO 지표에 쓰는 것(3축 합산 금지 원칙과 충돌 — 돈처럼 «보존되는 흐름»이 아닌 점수에 생키는 부적합). 단, 위 3번처럼 **관측 데이터의 흐름**(질문→엔진→인용)에는 보존량 구조가 성립하므로 유효.

---

# 제8부 — Lovable 인포그래픽 전수 카탈로그 (대시보드 3종 + ANSEO 합산)

> 워크스페이스의 나머지 리믹스 2종 소스를 추출해 판독했다 — **Sales Attribution(«Meridian Attribution»)** `6a7cf747…`, **RevOps(«Crestline Cloud»)** `cb49243c…`. 둘 다 차트 라이브러리 없이 수제 SVG(Attribution의 표 재정렬만 framer-motion). 이로써 Lovable에서 확보된 인포그래픽 소스는 4벌: ANSEO(18종, §4) + CFO 원형(제7부) + 아래 2종.

## 22. Sales Attribution — «데이터를 보간하는» 대시보드

**전환 아키텍처가 핵심 발명이다.** 히어로 인터랙션(First touch/Linear/Last touch 3모델 토글)에서 차트를 애니메이션하지 않고 **데이터를 애니메이션한다**:
- `useTransitionSnapshot(key, 720ms)` — 모델이 바뀌면 rAF로 t 0→1(ease-in-out cubic). **전환 도중 또 바꾸면 처음이 아니라 현재 지점에서 새 전환 시작.**
- `interpolateChannels(from, to, t)` — 두 모델의 **전 지표(share·revenue·roas·conversions·cpa·12주 trend 배열까지)를 lerp한 가상 데이터**를 만들어 모든 패널에 공급. 패널들은 정적 렌더만 한다 → 레일·워터폴·산점·표가 **같은 t로 한 호흡에** 재배치된다. `safeLerp`는 NaN(«—» 셀)을 t=0.5에서 스냅.
- 데이터 불변식이 카피로 명시: 총 매출은 모델 불변 — 레일 각주 «**The bar is always the same length. Changing the model only moves the cuts.**»

**패널 전수**:

| 패널 | 인코딩 | 애니메이션·디테일 |
|---|---|---|
| ① Revenue Rail (히어로) | 고정 길이 100% 스택 바, 채널=색 고정 | 세그먼트의 정렬 슬롯(index·누적 start)을 A·B 레이아웃 간 lerp — 잘림이 미끄러져 이동. 라벨은 실측폭 ≥78px일 때만(잘리면 통째 숨김). 세그먼트 경계는 **inset box-shadow 헤어라인**(바 외곽 길이를 수학적으로 보존). hover 디밍 0.3/200ms + 툴팁에 **다른 두 모델에서의 share 병기**. role=list, reduced-motion이면 t=1 고정 |
| ② 기여 워터폴 «Where the credit lands» | 채널 막대가 좌→우로 누적, Total 기둥으로 수렴 | 막대 x·시작 높이 lerp로 **순위 재편이 미끄러짐**. **«no credit» 빈 슬롯**: 크레딧 0이면 1px 조각 대신 점선 테두리 슬롯 — 주석 «an empty slot reads as a finding, a one-pixel bar reads as a broken render». Total 기둥 채움은 **채널색 그라디언트(기여 순)** — «합»임을 색으로. 계단 연결 점선, 머니 틱은 총액 분할이 아니라 $500K 라운드 스텝, 최소 막대 6px+전체 열 투명 히트 영역 |
| ③ Spend vs Return 산점 | x=지출, y=ROAS, 버블 반경=√예산 | **모델별 응답 곡선**: 현재 지출까지 실선, 외삽 구간 점선(32 샘플). 4× 타깃 수평 점선. y 도메인이 보간 데이터를 따라 함께 easing(«세 모델 최악값으로 얼려두면 기본 화면 위가 빈다»는 주석). 라벨 헤일로(paintOrder stroke), 버블 화면 내 클램프. **무지출 채널은 조용히 버리지 않고 «No media spend» 각주 행으로 유지** |
| ④ 전환 여정 «The journeys behind the credit» | 채널 칩+화살표 행, 여정은 모델 불변 | 선택 모델에서 **크레딧 받는 스텝만 칩이 점등**(색·테두리·12% 배경, transition-all 500ms). ShareBar는 first/last에선 단색, linear에선 **경로 색 분할 그라디언트**. 아이브로우 «Journeys are the same in every model» |
| ⑤ 채널 상세 표 | 정렬 가능 표+스파크라인 | **framer-motion `layout`으로 행 재정렬 720ms [0.65,0,0.35,1]** — 모델 전환 시 행이 미끄러져 자리 교환. 스파크라인은 **전 채널 공통 도메인**(«작은 채널의 평평한 선이 자기만의 자동 스케일 이야기가 아니라 '작음'으로 읽히게»). NaN 셀은 정렬에서 항상 바닥 |

## 23. RevOps — 버터플라이 퍼널과 «정직한 기하» 프리미티브

**히어로: FunnelShape.tsx — 거울형 퍼널-생키(«butterfly»).** 머리 주석이 규약 문서 수준:
- 중앙 트렁크가 생존 코호트를 위→아래로 운반, 매 단계에서 **dropped는 왼쪽, pending은 오른쪽 리본**으로 유출되고 트렁크는 정확히 그만큼 좁아진다 — «트렁크의 좁아짐이 주장이 아니라 **원인으로** 그려진다».
- **단일 units-per-record 스케일**: 트렁크 폭 = intake÷최초×420, 리본 두께 = frac×420. **리본은 트렁크 접점에서 가장 두껍고**(그 두께가 인코딩 값) 바깥으로 좁아지며 페이드 — «외측 테이퍼는 스타일일 뿐, 아무것도 나르지 않는다».
- **라벨 배치 엔진**: 밴드 라벨이 실측 폭+양쪽 패딩에 들어가면 내부 → 안 되면 shortLabel(예: "SQO") 시도 → 그래도 안 되면 외부 레일+점선 리더. «충돌을 살아남으라고 텍스트에 외곽선을 주는 일은 없다.» 폭 추정은 의도적 과대평가(«넘치는 라벨이 일찍 나간 라벨보다 나쁘다»).
- **이유 스트랜드**: 유실 리본이 사유별 가닥으로 중첩(같은 구간 점유라 교차 불가 by construction). 레일에는 최상위 사유 1줄만, 상위 3개+«other reasons (N)» **명시적 잔여 풀링** — «Nothing is silently dropped». 전체 분해는 hover 툴팁.
- 사이드 인플로우(퍼널 위를 거치지 않은 유입)도 같은 스케일의 리본으로 트렁크에 합류. 터미널 캡 «leaves the funnel here».
- 경로 추적: hover 시 그 단계 리본 `--ribbon-lit`(0.95), 나머지 `--ribbon-dim`(0.12/다크 0.09) — 불투명도가 라이트·다크 **별도 튜닝된 토큰**. 트렁크·리본 위 이동 점선 `rf-current` 4.2s — «**reveal cue, not live-feed cue**».
- 접근성: role=group+상세 aria-label, 단계 블록은 role=button+키보드, **sr-only 등가 테이블** 동봉. 모바일은 같은 frac 값으로 세로 칩 렌더(«**no second geometry**» — 제2의 기하를 만들지 않음).

**모션 시스템(rf-*)** — «One-shot staging only. No loops, no ambient motion.»:

| 유틸 | 동작 | 시간 |
|---|---|---|
| rf-rise | 카드 등장(6px 상승+페이드) | 420ms cubic-bezier(0.22,0.65,0.25,1) |
| rf-open | 드릴다운 펼침 | 240ms |
| rf-split / rf-grow-y | 바 자람(scaleX/scaleY 0→1) | 520ms |
| rf-draw | 선 드로잉(dashoffset 1200→0) | 720ms |
| rf-band / rf-wing | 퍼널 밴드·리본 scaleX 0.04→1, **transform-box: fill-box**(SVG 도형이 자기 중심으로 스케일) | 420/380ms, wing 스태거 i×70+150+wi×40ms |
| rf-current | 이동 점선 | 4.2s linear 무한(유일한 루프, reduced-motion 시 정지) |

핵심 규율: count↔ARR 모드 전환 시 **animKey 재키잉으로 애니메이션을 «정확히 한 번» 재생** — 연속 트랜지션이 아니라 재등장. Attribution의 «데이터 보간」과 정반대 전략으로, 두 프로젝트가 값 변경 모션의 두 학파를 보여준다.

**Chart.tsx 프리미티브** (라이브러리 0): LineChart — **성숙 전(maturing) 구간을 점선+빈 마커**로 갈라 그리고 `MaturingBand` 음영에 «still maturing» 라벨 / BarChart·StackedBarChart(rf-grow-y) / ScatterChart — **중앙값 사분면 힌트선**+코너 문구, **면적 스케일 반경**(«ARR 2배가 점 4배로 읽히지 않게» √) / WeeklyMetricChart — 주석 «**ONE METRIC, ONE CHART**», 타깃 점선, 우축 오버레이 1개까지, **`defined:false`면 선을 끊음**(그 주에 분모가 없음) / `niceMax` 라운드 틱 / 시리즈 색 5종은 «카드·침강면 위 ≥3:1(그래픽 객체 AA)»을 주석으로 문서화.

## 24. 확보 유형 총목록 — ANSEO 18종(§4) 외 신규 15종

| # | 유형 | 출처·파일 | 핵심 인코딩 | 애니메이션 | ANSEO 이식 적합성 |
|---|---|---|---|---|---|
| 19 | 생키 머니플로우 | CFO·SankeyFlow | 보존 흐름 분기/합류 | rAF 모프 1150/760ms·경로 추적·이동 점선 6s 정지 | △ 점수엔 부적합, 관측 흐름(질문→엔진→인용)엔 ○ |
| 20 | 팬 차트(불확실성 밴드) | CFO·CashFanChart | P10-90/P25-75 밴드 | clip-path 좌→우 1.5s | ○ 추이 예측 붙일 때 |
| 21 | 드래그 what-if 핸들 | CFO·HiringDrag | 슬라이더→실시간 재계산 | 핸들 확대+글로우 | ◎ «고치면 +N» 시뮬레이터 |
| 22 | 분산 워터폴(예산→실적) | CFO·VarianceWaterfall | 앵커+델타, 방향 있는 자람 | scaleY 520ms·i×130ms 스태거 3층 | ◎ 회차 간 점수 변화 분해 |
| 23 | 카운트업 티커+3D 플립 | CFO·KpiStrip/motion.ts | 숫자 램프, 뒷면=정의 | 1400ms+글라이드 520ms·rotateY 520ms | ◎ KPI 카드 전반 |
| 24 | 고정 길이 스택 레일 | Attr·revenue-rail | 분모 불변 + 잘림 이동 | 슬롯 lerp 720ms | ◎ «잰 칸 불변» AEO 구성비에 최적 |
| 25 | 기여 워터폴+no credit 슬롯 | Attr·waterfall-panel | 누적 기여→합 기둥(그라디언트) | 순위 재편 lerp | ◎ 빈 슬롯 문법은 «산정 불가» 표기로 |
| 26 | 버블+응답 곡선 산점 | Attr·scatter-panel | 실선(실측)/점선(외삽) | 도메인 easing·디밍 | ○ 키워드 지출·효율 화면 |
| 27 | 여정 칩 시퀀스 | Attr·paths-panel | 칩+화살표, 조건부 점등 | 점등 500ms | ◎ AEO 인용 경로·콘텐츠 추출 화면 |
| 28 | 레이아웃 재정렬 표 | Attr·channel-table | 정렬·모델 전환 시 행 슬라이드 | framer-motion layout 720ms | ○ 거래처 현황판 정렬에 |
| 29 | **데이터 보간 전환**(패턴) | Attr·use-transition-snapshot+interp | 차트가 아니라 데이터를 lerp | 720ms 공유 t | ◎◎ 명세 전환·필터·재채점의 표준 전략 후보 |
| 30 | 버터플라이 퍼널(거울 생키) | RevOps·FunnelShape | 트렁크+좌우 유출 리본, 단일 스케일 | rf-band/wing 스태거·추적 디밍 | ◎◎ 관측 disposition(언급/미언급/못 잼)·이슈 상태 흐름에 정확 대응 |
| 31 | 성숙 구간 라인(maturing) | RevOps·LineChart | 확정=실선, 미성숙=점선+빈 마커+밴드 | rf-draw 720ms | ◎ «아직 안 여문 회차» 표기 |
| 32 | 중앙값 사분면 산점 | RevOps·ScatterChart | 중앙값 십자+코너 힌트, 면적 반경 | rf-rise | ○ S1 사분면의 통계적 대안 |
| 33 | 단일지표 주간 차트+오버레이 | RevOps·WeeklyMetricChart | 바+타깃선+우축 비율(끊김 허용) | rf-grow-y·rf-draw | ◎ 주간 관측 리듬 화면 |

*(±: 24·25·30은 기존 §4의 스택 바·깔때기·퍼널을 대체·상위호환하는 유형)*

## 25. 이식 우선순위 (제8부 결론)

1. **데이터 보간 전환 패턴(#29)** — ANSEO의 «재채점», «채점 명세 판 전환», 필터 변경에 그대로: 모든 카드가 한 호흡으로 다시 접히는 감각을 코드 ~60줄로. 두 학파 중 ANSEO에는 이쪽이 맞다(RevOps의 재키잉 one-shot은 모드 전환처럼 «다른 그림»이 될 때만).
2. **버터플라이 퍼널(#30)** — AEO 관측의 disposition(언급됨/언급 안 됨/**판정 못 함**=pending 날개)과 이슈 생애(닫힘/재진단 대기/열림), 검수 큐 흐름이 이 기하와 1:1이다. 이유 스트랜드+잔여 풀링(«other reasons (N)»)은 «Nothing is silently dropped» — ANSEO 제1원칙의 시각화 완성형.
3. **고정 길이 레일(#24)** — «분모는 변하지 않는다»는 메시지를 형태 자체로 말하는 유일한 차트. AEO 잰 칸 구성비에.
4. **no-credit 빈 슬롯(#25)·maturing 구간(#31)·defined:false 선 끊기** — ANSEO 빈 값 3종(0/—/판정 못 함) 문법의 차트 어휘 확장판.
5. **what-if 핸들(#21)+분산 워터폴(#22)** — «이 이슈를 고치면 점수가 얼마나 오르나» 반사실 UI 세트.
6. 보조 규율: 라벨 배치 엔진(내부→축약→레일+리더), 공통 스파크라인 도메인, 면적 스케일 버블, 시리즈 색 ≥3:1 문서화, 그리고 «모든 모션은 one-shot, 루프는 reveal cue 하나뿐» 원칙.

## 접근 확인 로그

- 새 세션에서도 lovable.app 차단 (2026-08-29 11:31 UTC · curl 프록시 CONNECT 403 + WebFetch EGRESS_BLOCKED)
- **해결**: Lovable 커넥터 연결(2026-08-29 11:45 UTC) → 프로젝트 소스 직접 판독 가능해짐. `/console/dashboard` 는 소스 기준으로 분석 완료(S12). 프리뷰 URL 자체는 여전히 차단이나, 소스 접근이 더 정확하므로 실질 제약 없음.
