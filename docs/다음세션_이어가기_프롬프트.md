# VENOM 프로젝트 이어가기 (핸드오프 프롬프트)

VENOM 병원 마케팅 사이트 작업을 이어간다. 시작 전 `핵심두뇌_MASTER.md`와
`docs/VENOM_전체_작업문서-2026-07-13.md`를 먼저 읽어 현재 구조를 파악할 것.

## 컨텍스트
- 저장소: `recon9973-lang/desktop-tutorial`, 사이트 코드 `venom-wordpress/preview/`
- 배포: venom-new-site.vercel.app (`main` 자동배포)
- 작업 브랜치: `claude/session-review-prep-an0to9` (현재 origin/main과 동기, HEAD=5a18faf 이후 최신 fetch)
- 파이프라인: implement → 검증(JS 구문 + Playwright) → commit → PR → squash-merge → 브랜치 origin/main 리셋
- 커밋 트레일러 필수:
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
  `Claude-Session: https://claude.ai/code/session_019LE9CVMNiehQWEAmqwUtsK`
- 금지: 커밋/PR/코드/문서에 모델 ID·비밀키·허위 통계 삽입
- 샌드박스 egress: `vercel.app` 차단 → 배포 API 호출·이미지 자체저장은 GitHub Actions로 우회
- 다른 세션(hospital-bot/SEO)이 같은 main에 계속 푸시함 → **작업 시작 시 `git fetch origin main` 후 리셋/리베이스로 동기화**

## 직전 세션 완료분 (참고)
- Gemini 재시도 하드닝(PR #136) → AI 노출 재측정 측정불가 0건(run 29172884932)
- 관리자 'AI 노출 실측' 패널 연결(PR #144): `content/ai-expose-latest.json` ← 워크플로 자동 스냅샷
- 문서화(PR #145 세션기록, #146 전체 작업문서)
- 용어사전 확장(PR #158 AI 26→163, #159 SEO 97→216) + '자세히 보기' UX 개편(상세 모달·바텀시트·'더 보기' 페이지네이션). 원본 CSV 백업 `content/glossary/`

## 다음 작업 후보 (원하는 것 지시)
1. **클리닉 대시보드 실측 연결** — `/clinic` 원장님_앱 `dashboard-live.json`을 실측 파이프라인에 연결
2. **키워드툴 시뮬→실측 배지 전환** — admin '키워드 현황'의 "⚠ 시뮬레이션 모드"를 네이버 검색광고 실측 연동 시 자동 '실측'으로
3. **AI 노출 실측 정기 스케줄** — `ai-expose-check.yml`에 월 1회 cron 추가(현재 workflow_dispatch만)
4. **관리자 AI 노출 패널 고도화** — 추이(월별) 차트, 업체 추가, "지금 측정" 버튼(workflow_dispatch 트리거)
5. **클라이언트 제출용 산출물** — 전체 작업문서/AI 노출 리포트를 PDF/HTML 아티팩트로
6. **hospital-bot ↔ 사이트/ERP 연계** 심화

## 운영 팁
- AI 노출 재측정: GitHub Actions → *AI Exposure Check* → Run → 관리자 자동 반영
- 대상/질문 변경: `venom-wordpress/preview/content/ai-expose-input.json`
- PDF 리포트 재생성: `scratchpad/append_ai.py`의 RESULTS 갱신 후 실행
- "어디 있지?"는 항상 `핵심두뇌_MASTER.md` 먼저
