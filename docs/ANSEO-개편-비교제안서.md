# ANSEO 개편 비교 제안서 (v3)

> 2026-08-22 · 대상 `recon9973-lang/veo-platform` **main a864261 (v0.3.293, 08-22 22:33 KST 푸시)**
> 시안 5종 포함 전체 문서(아티팩트): https://claude.ai/code/artifact/029271b3-7486-4303-a2e0-8ea2… → **동일 URL로 v2 갱신됨**
> 선행: `docs/GPTO-벤치마크-ANSEO-적용리포트.md`


## v3 — 지적 5건 반영 (2026-08-22)

| 지적 | 확인 결과 (코드 검증) | 반영 |
|---|---|---|
| ① 스트립에 네이버·구글 오버뷰 없음 | **놓친 것도 뺀 것도 아닌 로직 공백.** 7개 엔진은 코드에 정의 완료(`ENGINE_LABELS`: NAVER_AI_BRIEFING·GOOGLE_AI_OVERVIEW, 짧은코드 NAV·OVW)·프로바이더 구현됨(`naver_briefing.py`·`google_ai_overview.py`, SerpAPI 경유). 스트립 데이터원 `engine_answer_totals`가 **기록 있는 엔진만 GROUP BY 반환** → 관측 0건 엔진이 조용히 사라짐. 격자는 같은 문제를 이미 잡았음(`every-known-ai-has-a-row.test`, 08-21 C-1) — 스트립 미적용. v2 시안이 5개만 그린 것도 오류 | M1: 로스터 고정 7줄, 무기록은 「측정 전」 |
| ② 모든 그래프 XYZ축 상세 | 규칙 채택: 본 그래프는 축 제목·단위·눈금 상시 기입, 행 내 스파크라인 등 **썸네일형만 예외** | M2 히어로 축 적용 + 규칙 명문화 |
| ③ AI 7개 적용 | 코드 로스터와 일치. 단 네이버 AI 브리핑·구글 AI Overview는 **「검색 끔」 조건이 없음**(검색 화면 요약) — 켬/끔 비교에서 켬 열만, 끔 칸은 「해당 없음」 분모 제외 | M1·M3 전부 7개 (짧은코드 GPT GEM CLA PER GRK NAV OVW) |
| ④ 질문 월검색은 추측 | **맞음 — v2 표기 오류.** 검색량은 키워드 단위만 실측(네이버 검색광고, 못 잰 값 null·0 금지). 질문 문장 검색량은 측정 수단 없음. 재설계: 키워드 조립형=「근거 키워드+그 검색량」, 수집형=「실제 질문 · 지식iN」 배지(+원문 링크). 질문 자체 검색량은 표기하지 않음 | M4 재설계 |
| ⑤ 안내/등록하러 가기/확인방법 혼재 | 원칙 채택: **행 버튼 = 동사 하나** — 미등록→「등록」, 확인필요→「확인」, 등록됨→「열기」. 설명은 구역 머리 ? 팝오버 하나(PageHelp 문법). 동일 원칙으로 「+ 질문 추가」·「저장」 | M5 수정 + 전 시안 문구 원칙 |

실행 순서 1번이 확장됨: 스트립 개수 전환 + **로스터 고정 7줄** (반나절~1일).

## v1 → v2 정정

v1의 「최상위 13개 평면 메뉴」「설명이 상단 점유」는 **IA 재설계(2026-08-16) 이전 화면** 기준의 오판이었다. 원격 브랜치 대조 결과 main이 최신이고(`work/customers-100`은 v0.3.168로 더 오래된 잔존 브랜치), 현재 화면 재실측 결과:

| v1 진단 | 현재 실측 (v0.3.293) | 판정 |
|---|---|---|
| 최상위 13개 평면 메뉴 | 상단바 = ANSEO 로고(=대시보드)+위치칸+거래처+계정. 나머지는 `place:'ABSORBED'` — 라우트 26→화면 9 재설계 완료 | **이미 해결** |
| SEO·GEO·AEO 화면 미분리 | 거래처 탭 = 진단·SEO·GEO·AEO·이슈·리포트 6탭 구현 완료 (`CUSTOMER_TABS`) | **이미 해결** |
| PageHelp 상단 점유 | 이미 물음표 버튼 → `<details>` 팝오버 (`packages/ui/PageHelp`) | **이미 해결** |
| 키워드-질문 분리 | IA상 「키워드=AEO 1단계」 확정, PromptSetForm이 검색량·수집질문 import 완료. **프리필만 미구현** | 부분 |
| 상단 누적 글자수 | `answer_chars` 표시, `answers`는 툴팁에만 | **유효** |
| 지도 등록 없음 | CompanyForm 4필드 그대로 | **유효** |

**정정 후 결론: 구조(IA)는 이미 스스로 고쳤다. v1의 「4메뉴 강제 재편」 제안은 철회.** 남은 것은 구조가 아니라 **입구 4곳** — 스트립 단위(개수), 질문 입구(프리필+추가 버튼), 업체 입구(지도 4종), 각 화면 첫 그림(축별 종합 인포그래픽).

## 요구 9건 재판정

| # | 지시 | 상태 | 남은 일 |
|---|---|---|---|
| 1 | 3목적(진단·데이터·컨설팅) | 부분 | 진단 결과 하단 「이 결과로 리포트 만들기」 고정 |
| 2 | 보기 쉽게·설명 간략 | 대부분 해결 | 축별 첫 그림 통일 |
| 3 | 메인=인포그래픽·서브 중복금지 | 부분 | 시안 M2(벤토)·M3 + 서브 자리 제거 규칙 |
| 4 | 질문: 등록자료+검색량 | **필요** | 시안 M4 — 업체 선택 시 초안 프리필 |
| 5 | 새 질문 추가 버튼 | **필요** | 시안 M3 — 목록 상단 고정 + 인라인 행 |
| 6 | 지도 4종 등록·안내 | **필요** | 시안 M5 — 3상태(pass/fail/unknown)+안내+인용 대조 |
| 7 | 4영역 구성 | 구조는 해결 | (v1 재편안 철회) 축별 그림만 |
| 8 | 누적 글자수→개수 | **필요** | 시안 M1 — 본문↔툴팁 교체. **주석의 옛 지시(08-22 "데이터의 양")도 갱신** |
| 9 | 비교 제안서 | 완료 | 이 문서 v2 |

## 시안 5종 (아티팩트에 렌더 포함 — ANSEO 실제 토큰 사용)

토큰 실측: 보드 `#0d1417` · 면 `#182226` · 액센트 `#00ed64`(채움 전용) · 상태 5색(pass/warn/fail/na/unknown) · 알약 버튼 · tabular-nums (`packages/ui/tokens.css`)

- **M1 상단 스트립**: 참고 이미지(OptiAnalytics) 문법 — 상태점+제목 | 전체 누적 N회 | 엔진 색점+개수 1단 | 집계 시작일+접기. 숫자=`answers`, 글자수는 툴팁. 못 읽으면 「못 읽음」(ADR 0002 유지).
- **M2 대시보드 벤토**: 타일 크기=중요도(AEO 히어로 3단 타이포, SEO/GEO 중형, 할일 소형) + 거래처 F1 행(1행=1곳). **합산 점수 없음** — README 원칙 유지. 자체 리서치(08-17: Bloomberg 밀도·벤토 비대칭·F1 행문법)의 이행.
- **M3 AEO 탭**: 질문 카드 3요소 + `+ 새 질문 추가` 고정 버튼 + **엔진 격자(칸 명도=반복 3회 중 언급 수, 회색=답변없음·분모 제외 — IA 지적 2 확정안 그대로)** + 참조 출처(엔진 귀속 배지+복사/새창).
- **M4 질문 프리필**: 업체 칩 → 초안 8개(체크박스+질문+월 검색량+조립 근거 태그) → 균형·월 비용 푸터. 재료(`keyword-volume`·`question-sources`·`regionLadder`)는 이미 전부 import돼 있음 — 조립 규칙과 화면만 신규.
- **M5 지도 4종**: 네이버 플레이스/구글 비즈니스/카카오맵/빙 플레이스 × 3상태 칩 + 안내 링크. 저장 URL은 `citation_channels` 인용 대조로 연결.

## 타사 리서치 (WebSearch 2026-08-22)

기존 자체 리서치(`docs/design/2026-08-17-design-RESEARCH.md`)의 결론(밀도=기능·벤토 비대칭·F1 행·mono 숫자·기능색만)은 **살린다.** 그 위에 AEO 전문 도구 5개:

| 도구 | 화면 문법 | 가져올 것 |
|---|---|---|
| Profound | 인용 점유 시계열, 최심 인용 분석 | 도메인 단위 인용 점유 시계열 |
| Peec AI | share-of-voice 경쟁 라인차트 | 경쟁 언급 시계열(자사 흰점) |
| Otterly.AI | 프롬프트 단위 모니터링, 링크 위치 추적 | 질문=1급 시민 구조(M3 근거) |
| Ahrefs Brand Radar | 7개 인덱스, **언급/인용 별도 지표** | 분리 표기 — 데이터는 이미 구분함 |
| Semrush AI Toolkit | 게이지 vs 업계평균, 프롬프트=키워드(난이도·볼륨) | M4의 검색량 병기. "업계평균"은 자사 거래처 풀로 한정(날조 방지) |

공통 패턴: ①질문 단위 추적 ②언급/인용 구분 ③경쟁 벤치마크 ④인용 출처 분석. ANSEO는 ①②④ 데이터 보유, ③만 데이터부터 없음. **격차는 측정이 아니라 표현.**

## 브레인스토밍 (자산 유무 판정 포함)

- 인용 채널 크로스탭(채널×엔진) — `citation_channels` 보유, 시각화만
- 관측 사다리에 지도 0단계 — `PublishLadder` 보유
- 질문 포트폴리오 균형 스택 — `FUNNELS/INTENTS` 보유
- 언급 vs 인용 이중 지표 — 데이터 보유, 표기만
- AI 경유 유입 증빙(`utm_source=openai` 태깅) — 신규·소형
- 거래처 풀 평균 벤치마크 — 신규, "업계 평균" 날조 없이 가능
- 경쟁 브랜드 로스터 — 유일하게 데이터부터 신규(표기 정규화 難)
- **깨어남 단계는 보존** — 타사에 없는 고유 문법, 벤토 타일로 승격

## 실행 순서 (v1의 구조 재편 삭제)

1. M1 스트립 개수 — 반나절
2. M3 새 질문 버튼 — 1~2일
3. M5 지도 4종 — 2~3일
4. M4 프리필 — 1주
5. M2 벤토 + 축별 첫 그림 — 1~2주
6. 참조 출처 집계 + 크로스탭 — 1주
7. 경쟁 로스터 PoC — 2주

---
*근거: veo-platform main a864261 — `lib/navigation.ts` · `EngineTotalsStrip.tsx` · `ConsoleNav.tsx` · `CompanyForm.tsx` · `PromptSetForm.tsx` · `packages/ui/tokens.css` · `docs/design/2026-08-16-IA-REDESIGN.md` · `2026-08-17-design-RESEARCH.md` · GPTO 역설계 리포트 · 첨부 참고 이미지*

## 화면 전수 점검 (2026-08-22 추가)

라우트 파일 전수: **콘솔 23 + 공개 5.** 진입 경로는 링크 grep 실측 — **0-E(부를 수 없는 기능) 위반 0건.**
(리포트 목록은 `lib/dashboard.ts`·`lib/client-dashboard.ts`가 링크 생성, 원고 검수는 계정 메뉴 TOOL로 도달 확인)

- 콘솔 운영·진단: 대시보드 / 진단(/seo: 실행·이력·결과 7구획·재검사·실행비교·캡처·noindex) / AEO(/geo: 질문·관측·결과·엔진상태·지난관측) / 키워드 / AI 답변 검수 / 브랜드 식별 / 이슈(목록·상세) / 리포트(목록·버전·판·공유)
- 콘솔 거래처: 목록(일괄진단·핀) / 업체 등록 / 상세(탭 6 + 월별밴드·의료사실·반사실·심평원 가져오기) / 수정 / 프로젝트
- 도구·관리: 원고 검수 / 사용량·비용 / 채점버전 / 열쇠 / 팀원 / 변경이력 / 내 계정
- 공개: 랜딩 / 로그인 / 초대 / 공유 리포트(/results/[token]) / VEOBot 안내

**결론: 빠진 화면 없음.** 새로 만들 화면은 정확히 둘(M4 프리필 1단계, M5 지도 구역)이고 M1은 스트립 로직 공백. 시안이 안 다룬 화면은 전부 「유지」 판정(검토 누락 아님). 남는 후속 1건: 거래처 상세의 진단·SEO·GEO 탭 첫 그림 — M2 확정 후 같은 문법으로 후속 시안.

## 시안 색 검증 (dataviz 검증기 실측)

브랜드 연상색 7종은 검증기 **FAIL**(Gemini 파랑↔ChatGPT 청록 정상시력 구분 미달, Grok 회색 무채색) →
검증된 다크 7색을 엔진 연상에 배정해 로스터 순서로 재검증: GPT `#199e70` GEM `#3987e5` CLA `#d95926` PER `#9085e9` GRK `#d55181` NAV `#008300` OVW `#c98500` — **전 항목 통과**(다크 서피스 #111a1e). 네이버↔오버뷰 색각 경고 1건은 색점+이름 상시 병기로 규정상 합법.
