# RESUME (경고색 방) — **닫힘. 전부 나갔다** (2026-09-19 확인)

<!-- 가지: claude/compassionate-newton-kyz1sm -->
<!-- 고친 것은 **ANSEO(veo-platform)** 다. 이 저장소(베놈 마케팅 사이트)에는 코드가 없다 -->

> 사장님 2026-09-19 «이방 삭제 할 예정이니 정리해줘» — 닫는다.
> 상세는 `docs/session-logs/2026-09-19-s01-gap-color.md`.
> **이 방에서 이어갈 작업은 없다.** ANSEO 대기 표에도 이 방 줄은 없다.

## 한 줄

사장님 오더 셋을 끝내 **셋 다 내보냈고**, 나간 것이 실제로 도는 것까지 재서 확인했다.

```
[실측 2026-09-19 14:40 KST · 러너 「도는 판 확인」 실행 35424600863]
  웹 · 진단 서버 · 워커 1대  셋 다 0.3.620 (뒤처진 워커 0)
  발행본  GEO 1.10.0 · SEO 1.15.0
```

## 무엇이 나갔나

| 판 | 무엇 | 화면 |
|---|---|---|
| **0.3.612** | 그래프의 「아직 못 채운 쪽」이 한 색 — SEO·GEO·AEO **열 자리** | **바뀜** |
| **0.3.617** | 배포 담당을 「만든 방」으로(사장님 «만든 방이 바로 배포해») | 무변경 |
| **0.3.620** | 대기 표에서 이미 나간 다섯 줄 지움 | 무변경 |

**0.3.612 의 알맹이** — 부족분 토큰 `--veo-gap-fg`(값을 새로 안 만든다: 「실패」와 같은 색을
가리킨다) · `--veo-gap-step-1..4`(조각을 갈라야 하는 자리의 밝기 단). 그 한 색으로 모은 열:
깎인 배점 막대 채움 · 100점 환산의 못 채운 칸 · 남은 점수 아크의 빈 꼬리 · 손실 도넛 조각 넷 ·
커버리지 와플 빈 칸 · 레일의 게이지·영역별 막대·점검 진행률 · AEO 관측 4단의 줄어든 만큼 ·
AEO 4단계 중 안 켠 단계 · 「우리 것이 0건인 채널」 칸과 범례 점.
**못 잰 것은 안 칠한다**(ADR 0002). 관문 `shortfall-is-one-colour.test.ts`(16건)가 지킨다.
붉은 세기를 바꾸려면 **토큰 한 곳**만 고친다 — 화면 코드는 안 건드린다.

## 떠도는 것 — **없다**

```
veo-platform   안 올린 것 0건 · 가지 끝 47127726 · 대기 표에 이 방 줄 0
desktop-tutorial  안 올린 것 0건
```

가지에 마지막 커밋(`47127726` · 도장 + 배포 오더 한 줄)이 **아직 안 나갔다.** 문서만이고
대기 표를 비우는 내용이라, 다음에 배포하는 방이 main 을 합치면 함께 나간다. 안 나가도
운영에는 아무 영향이 없다 — 재서 확인한 사실을 적어 둔 글이다.

## 다음 사람 몫 (임자 없음 — `veo-platform` 대장 §4-A)

둘 다 이 방이 **겪어서 찾은 것**이고 닫는 법까지 적어 뒀다. 시험을 먼저 써야 하는 일이라
이 방이 손대지 않았다.

1. **배포 전 점검 ⑤(지출 한도)가 클라우드 작업 방에서는 늘 「못 쟀다」로 지나간다.**
   `scripts/preflight.sh` 가 `scripts/gh_fallback.sh` 를 안 불러서다. 부르게 하되 흉내를 두 군데
   넓혀야 한다 — ⑤ 는 `--repo` 없이 `gh run list` 를 부르고(흉내는 거절), `--jq` 가 아니라
   `-q` 를 쓴다(흉내가 버린다). `tests/release/` 에 두 모양을 먼저 적는다.
   → 2026-09-19 에 이것 때문에 점검 15분을 다 쓰고 CI 에서야 예산 막힘이 드러났다.
2. **판 물림이 「이미 나간 항목」을 못 알아보는 구멍.** `claim_version._refuse_to_relabel` 이
   내 맨 위 항목을 `origin/main` 의 **지금** 맨 위와 견준다 — 가지를 딴 뒤 main 이 한 판 더
   나가면 관문이 지나가고 **이미 나간 항목에 새 번호가 달린다**(실제로 0.3.614 → 0.3.616).
   견줄 자리를 **merge-base 이후 main 이 낸 항목 전부**로 넓히면 닫힌다.

## 이 방이 겪어서 남기는 것 둘 (다음 방이 같은 데 빠지지 않게)

- **번호를 세 번 뺏겼다.** 방이 여럿이라 CI 8분 사이에 남이 먼저 main 에 닿는다.
  `[4/5]` 거절은 **배포 실패가 아니다** — main 부터 보고, 내 커밋이 들어갔는지 확인한 뒤
  한 칸 물러난다. 0.3.617 로 §2 에 적어 둔 그 규칙이 바로 다음 회차부터 세 번 쓰였다.
- **변경이력을 손으로 합치면 깨진다.** 갈래 표시가 항목 한가운데를 갈라 닫는 줄과 쉼표를
  먹었고 시험 파일 여덟이 못 읽혔다. **손으로 잇지 않는다** —
  `git show origin/main:apps/web/src/lib/changelog.ts` 를 통째로 가져와 내 항목만 얹고
  항목 수·제목을 main 과 대조한다(그때 596 → 597 · 빠진 것 0).

## 참고

- 현황 `PROJECT_STATE.md` · 지도 `핵심두뇌_MASTER.md` ·
  세션 기록 `docs/session-logs/2026-09-19-s01-gap-color.md`.
- ANSEO 현황 `veo-platform` 의 `docs/STATE.md` · 대장 `docs/WORKLIST.md`.
