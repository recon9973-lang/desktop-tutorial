# RESUME (경고색 방) — **닫힘. 화면에 나갈 것은 전부 나갔다** (2026-09-19)

<!-- 가지: claude/compassionate-newton-kyz1sm -->
<!-- 고친 것은 **ANSEO(veo-platform)** 다. 이 저장소(베놈 마케팅 사이트)에는 코드가 없다 -->

> 사장님 2026-09-19 «이방 삭제 할 예정이니 정리해줘» — 닫는다.
> 상세는 `docs/session-logs/2026-09-19-s01-gap-color.md`.

## 한 줄

**사장님 오더 셋은 전부 끝나 나갔다.** 마지막에 남은 것은 **대장 정리 한 건**뿐이고,
그것만 다섯 번 연달아 번호를 뺏겨 못 나갔다. 운영에는 영향이 없다(문서만).

## 나간 것 — 실측으로 확인함

| 판 | 무엇 | 화면 |
|---|---|---|
| **0.3.612** | 그래프의 「아직 못 채운 쪽」이 한 색 — SEO·GEO·AEO **열 자리** | **바뀜** |
| **0.3.617** | 배포 담당을 「만든 방」으로(사장님 «만든 방이 바로 배포해») | 무변경 |
| **0.3.620** | 대기 표에서 이미 나간 다섯 줄 지움 | 무변경 |

```
[실측 2026-09-19 15:20 KST · 러너 「도는 판 확인」 실행 35426319052]
  웹 · 진단 서버 · 워커 1대  셋 다 0.3.621 (뒤처진 워커 0)
```

**0.3.612 의 알맹이** — 부족분 토큰 `--veo-gap-fg`(값을 새로 안 만든다: 「실패」와 같은 색을
가리킨다) · `--veo-gap-step-1..4`. 그 한 색으로 모은 열: 깎인 배점 막대 채움 · 100점 환산의
못 채운 칸 · 남은 점수 아크의 빈 꼬리 · 손실 도넛 조각 넷 · 커버리지 와플 빈 칸 · 레일의
게이지·영역별 막대·점검 진행률 · AEO 관측 4단의 줄어든 만큼 · AEO 4단계 중 안 켠 단계 ·
「우리 것이 0건인 채널」 칸과 범례 점. **못 잰 것은 안 칠한다**(ADR 0002).
관문 `shortfall-is-one-colour.test.ts`(16건). 붉은 세기는 **토큰 한 곳**만 고치면 된다.

## ⚠ 못 나간 것 하나 — 다음에 배포하는 방이 가져가면 된다

```
가지   claude/compassionate-newton-kyz1sm   끝 6d23156f  (veo-platform)
내용   ① 대기 표에서 이미 나간 세 줄 지움 — 0.3.619 · 0.3.620 · 0.3.621
       ② 빠진 배포 오더 한 줄(14:22 `1565709b` «내보내») — main 에 아직 없다
크기   문서만 · 코드 무변경 · 화면 무변경 · `apps/web` 2,770 통과
```

**다섯 번 연달아 [4/5] 에서 번호를 뺏겼다**(0.3.617 → 0.3.619 → 0.3.620 → 0.3.622 →
0.3.623). 방이 여럿이라 CI 채점 8분 사이에 늘 다른 방이 먼저 main 에 닿는다. 배포가
실패한 적은 **한 번도 없다** — 점검도 CI 도 매번 초록이었다.

**가져가는 두 가지 길** (어느 쪽이든 5분이면 된다):
1. 이 가지를 합친 뒤 제 배포에 얹는다. 겹치는 곳은 대장 세 파일뿐이다.
2. 그냥 다시 한다 — 러너 `rollout-check.yml` 로 재서 **도는 것이 확인된 줄만** 지우고,
   위 오더 한 줄을 `DEPLOY-ORDER-LOG.md` 에 끼운다(시각순).

**서두를 일은 아니다.** 대기 표에 나간 줄이 남아 있으면 다음 사람이 이미 끝난 일을 또
내보낼 위험이 있을 뿐, 운영은 멀쩡하다.

## 다음 사람 몫 (임자 없음 — `veo-platform` 대장 §4-A)

둘 다 이 방이 **겪어서 찾은 것**이고 닫는 법까지 적어 뒀다. 시험을 먼저 써야 하는 일이다.

1. **배포 전 점검 ⑤(지출 한도)가 클라우드 작업 방에서는 늘 「못 쟀다」로 지나간다.**
   `preflight.sh` 가 `gh_fallback.sh` 를 안 불러서다. 부르게 하되 흉내를 두 군데 넓혀야
   한다 — ⑤ 는 `--repo` 없이 `gh run list` 를 부르고(흉내는 거절), `--jq` 가 아니라 `-q` 를
   쓴다(흉내가 버린다). → 09-19 에 이것 때문에 점검 15분을 다 쓰고 CI 에서야 예산 막힘이 드러났다.
2. **판 물림이 「이미 나간 항목」을 못 알아보는 구멍.** `claim_version._refuse_to_relabel` 이
   내 맨 위 항목을 `origin/main` 의 **지금** 맨 위와 견준다 — 가지를 딴 뒤 main 이 한 판 더
   나가면 관문이 지나가고 **이미 나간 항목에 새 번호가 달린다**(실제로 0.3.614 → 0.3.616).
   견줄 자리를 **merge-base 이후 main 이 낸 항목 전부**로 넓히면 닫힌다.

## 이 방이 겪어서 남기는 것 셋

- **`[4/5]` 거절은 배포 실패가 아니다.** main 부터 보고 내 커밋이 들어갔는지 확인한 뒤
  한 칸 물러난다. 0.3.617 로 §2 에 적어 둔 규칙이고, 그 뒤 **다섯 번 다** 이 규칙으로 풀었다.
- **거절된 회차에도 배포는 판을 물려 커밋해 둔다.** 다음 판을 잡기 전에 `__init__.py` 를
  **직접 읽어** 확인한다 — 모르고 덮으면 번호가 엉킨다(09-19 에 실제로 겪었다).
- **변경이력을 손으로 합치면 깨진다.** 갈래 표시가 항목 한가운데를 갈라 닫는 줄과 쉼표를
  먹었고 시험 파일 여덟이 못 읽혔다. **손으로 잇지 않는다** —
  `git show origin/main:apps/web/src/lib/changelog.ts` 를 통째로 가져와 내 항목만 얹고
  항목 수·제목을 main 과 대조한다.

## 참고

- 현황 `PROJECT_STATE.md` · 지도 `핵심두뇌_MASTER.md` ·
  세션 기록 `docs/session-logs/2026-09-19-s01-gap-color.md`.
- ANSEO 현황 `veo-platform` 의 `docs/STATE.md` · 대장 `docs/WORKLIST.md`.
