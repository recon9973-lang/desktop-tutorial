# RESUME (자동 진단 방 · AEO) — 2026-09-16 갱신

> **이 파일은 「자동 진단 방」 것이다.** 루트의 `RESUME.md` 는 **다른 방(진단 오진 방)**
> 것이니 덮지 않는다. 상세는 `docs/session-logs/2026-09-09-s21-aeo-grand.md`.

> 현황 `PROJECT_STATE.md`, 지도 `핵심두뇌_MASTER.md`.

## 지금까지 (핵심만)

사장님 첫 물음 «AEO 관련해서 그랜드아름다운의원 최신 진단 자료를 기반으로 조사해줘» 에서
시작해 **조사 → 결함 여섯 고침 → 배포**까지 갔다.

```
veo-platform  main 6e9e6684 · 판 0.3.558  ← 나갔다 (도는지는 «—» 못 쟀다)
  ① 미분류를 맨 뒤로(차례를 한 곳에서)      ② 미분류 몫이 경쟁사 등록에 안 사라지게
  ③ 분모를 교차표 제 셈으로                ④ 기간 탭이 채널 카드도 데려온다
  ⑤ 몇 곳에 물었나를 접기 전에(화면·서버)   ⑥ 임대형 홈페이지 자사 선언에 경로 요구
  덤: 판 물림 둘 · 대장 줄 상한 1,200→1,300 · 관문 12건
```

- **배포는 다섯 번 만에 나갔다.** 관문이 **진짜 결함 둘**을 잡았고(대장 줄 상한 ·
  판 물림이 대기 줄을 **첫 줄만** 옮기던 것 — 여섯 묶음이 한 판으로 쌓인 날 드러났다),
  Postgres 가 세 번 죽었고, 한 번은 CI 를 통과하고도 채점 20분 사이에 옆 방이 먼저
  main 에 닿아 졌다.
- **조사 결과**(문서 `docs/2026-09-09-그랜드아름다운의원-AEO-진단-조사.md`):
  자사 몫 209분의 2 = **0.96%** · 미분류 **85.2%**(운영 평균 47%의 1.8배) ·
  네이버 계열 **0건** · 엔진은 **일곱 중 하나**만 물었다.

## 바로 이어갈 작업 — **둘 다 사장님 손이다. 코드로 되는 것은 없다**

[실측 2026-09-16 · main `c9ecf707` · 판 0.3.594] 갈라 보았고, 이 방에서 되는 것이 없음을
**재서** 확인했다.

### ① 미분류 도메인 목록 — 사장님 몫 (한 줄)

> **콘솔 → 대시보드 → 「AI 가 어디를 보나」 카드 → 접힌 줄 「미분류 안에 무엇이 있나 —
> 도메인 N곳」을 펼쳐 목록을 복사해 주십시오.**

- 그 줄은 **12곳까지** 보이고 나머지는 개수만 나온다(`UNCLASSIFIED_SHOWN = 12`).
  전 거래처 합산이다.
- 그랜드아름다운의원 **한 곳만** 보려면 거래처 AEO 탭의 「채널 등록 현황」에 상위 **4곳**이
  건수와 함께 적혀 있다.
- **왜 내가 못 하나** — 운영 DB 열쇠가 이 방에 없다(`VEO_DATABASE_URL` 미설정). 운영 주소는
  프록시가 CONNECT 에서 막는다. 저장소 안에도 근거가 없다: `강남언니·바비톡·여신티켓`을
  코드·자료 전체에서 찾아봤고 **0건**이다.
- 받으면 그 자리에서 규칙판 4 를 낸다. 넣는 법은 대장 **§4-B** 에 적어 뒀다.

### ② AEO 실행 다섯 — 사장님 몫 (콘솔·외부)

일곱 엔진 재진단 · 자사 선언 경로 확인 · 춘천 경쟁사 6곳 등록 · 병원 포털 등재 ·
CI 를 필수 검사로. **이 방은 실서비스로 못 나간다** — 「못 돌린다」이지 「결함」이 아니다.

- **CI 를 필수 검사로**는 [실측 2026-09-16] 가지 보호 창구가 이 방 열쇠에 **403**(읽기도
  거절)이다. 저장소는 **개인 소유·비공개**다 — `deploy.sh` 머리말의 2026-08-04 기록은
  그 조합에서 규칙이 **적용되지 않는다**(Pro 필요)고 적어 두었다. 다만 내가 받은 403 은
  권한 거절이라 **요금제 탓인지까지는 못 쟀다**. 화면에서 한 번 보시는 것이 확실하다.

### 내가 더 할 수 있던 것 — **둘 다 만들었다** (2026-09-16 · 사장님 «자사 선언 경로 그거 만들어» · «이어서 진행»)

**둘 다 커밋만 했다. 배포는 안 했다** — 대기 표에 번호 없이(`—`) 쌓았다.

```
439dabae  경로 없이 임대형 홈페이지를 문 자사 선언을 화면이 찾아낸다
          rented_host_declarations · GET /api/brands/rented-host-warnings
          대시보드 「오늘 손댈 것」 → /console/competitors?project=…
          → 위 ②의 「자사 선언 경로 확인」이 사람 손에서 빠진다

(다음 커밋) 미분류 도메인을 통째로 넘기는 자리 — CitedChannels 「전부 넘기기」
          펼친 목록은 열둘에서 끊기는데, 규칙판에 넣을 것을 고르려면 전부가 필요하다
          → 위 ①의 「목록을 복사해 주십시오」가 열둘이 아니라 **전부**가 된다
```

**①은 아직 사장님 손이 한 번 필요하다** — 운영 DB 열쇠가 이 방에 없어 내가 직접 못 읽는다.
다만 이제 **끊기지 않고 통째로** 복사된다(도메인 · 인용 수, 칸으로 나눈 한 줄씩).

만들다 저장소 관문이 진짜 결함 둘을 잡았다(오류 대장 **217·218**) — 로그인 없이 열리던
새 창구(`from __future__ import annotations` + 이름표보다 위) · 자물쇠 검사를 안 받던
이어붙임. 그리고 **내가 한 번 단언하고 정정했다**: 「관문이 `www.` 로 뚫린다」고 코드
주석에까지 적었는데 아니었다 — 저장 길은 `normalise_own_domain` 이 이미 떼어 낸다.
**통과하는 시험이 바로 앞에 있었는데 안 폈다.**

### 끝난 것 (2026-09-10)

- **코드 여섯 + 덤 셋이 나갔다.** [실측 2026-09-10 18:43 KST · 다른 방] 운영 서버·워커
  **0.3.561** — 내 0.3.558 의 자손이므로 **내 코드는 돌고 있다**.
- **도장** — 위 실측으로 갈음됐다(이 방은 운영 주소로 못 나간다).
- **이관서·패치** — 이관은 없던 일이 됐다(이 방이 직접 냈다). 패치는 지웠고 이관서에는
  결말을 박았다 — **나간 일을 「할 일」로 남기면 다음 사람이 다시 낸다.**
- **0.3.562 도 나갔다**(main `1b7c70b4` · 미배포 0건) — 다섯 번의 시도 기록 ·
  규칙판 4(§4-B) · **「커밋·배포」 관문 고침**.

### 배포를 내면서 관문이 드러낸 것 셋 — 다음 방이 또 만난다

셋 다 **여러 방이 한 저장소를 쓰기 시작하며** 생긴 자리다. 혼자 쓸 땐 안 보였다.

```
대장 줄 상한        main 만으로 이미 1,200줄 — 어느 방이든 대기 줄 하나면 걸렸다 → 1,300
판 물림             그 판의 대기 줄을 **첫 줄만** 옮겼다(방이 하나일 때만 맞던 셈) → 전부
「커밋·배포」 관문    이름은 「대기 자리」인데 **대기 표를 한 줄도 안 봤다**
                    (§2 뒤 `###` 이 없어 4,000자로 물러나 있었다) → 끝을 `## 2-A.` 로
```

**여기서 한 번 틀렸다** — 셋째를 고치다 다른 방의 §4-A 오류 기록 한 줄을 고쳐서
관문을 통과시켰다. 그 관문이 스스로 「옛 기록은 안 본다」고 적어 둔 자리다. 되돌리고
경계를 고쳤다. **관문을 통과시키려고 기록을 고치면 안 된다.**

## 대기/차단

- **사장님 몫 (AEO)** — ① 일곱 엔진으로 재진단(특히 네이버 AI 브리핑)
  ~~② 자사 선언 경로 확인~~ → **화면이 대신한다**(439dabae · 나가면 대시보드가 걸린 곳을
  이름으로 세운다) ③ 춘천 경쟁사 6곳 등록(목록은 조사 문서 §3) ④ 병원 포털 등재 정비
  (인용 8.1%인데 우리 것 0건) ⑤ **CI 를 필수 검사로**(대장 §5).
- **규칙판 4** — 미분류 도메인 목록을 받으면 그 자리에서 낸다(넣는 법은 대장 §4-B).
  이제 대시보드에서 **끊기지 않고** 복사된다.
- **이 방 권한** — veo-platform 푸시 권한 요청은 관문이 거절했으나 `git push` 와
  `make deploy` 는 실제로 됐다. 워크플로 디스패치(`gh workflow run`)는 403.

## 주의·제약

- 이 가지 외로 푸시 금지(desktop-tutorial). veo-platform 은
  `claude/geo-mosaic-unclassified-last`.
- **배포는 `make deploy` 만.** 오더 없이 밀지 않고, 나갈 때 **대기 표를 통째로 보여 드리고
  한 번 여쭙는다**. `VEO_DEPLOY_ORDER` 에 사장님 원문을 인용한다.
- **판 번호는 나갈 때 배포가 정한다.** 사람이 미리 맞추지 않는다.
- 대장 머리말과 대기 표의 미배포 범위가 **일치**해야 한다(`worklist.test.ts`).
- **Postgres 가 자주 죽는다.** `service postgresql start` 로 되살리고, 긴 검사 앞에는
  15초마다 살피는 지킴이를 붙인다. 역할 `root`/비번 `veo` · DB `veo_test`.
- 못 잰 값은 «—». 지어낸 수치 금지. 의료광고법 준수. 모델 ID 는 커밋 트레일러에만.
- 커밋 트레일러: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` +
  `Claude-Session: https://claude.ai/code/session_013hnfFKDdKTpoJnZ6hRix8Y`

## 참고

- veo-platform 환경: `PYTHON=/usr/bin/python3.12 make setup` · `pnpm install` ·
  `service postgresql start` · `VEO_DATABASE_URL=... alembic upgrade head` ·
  `gh` 는 릴리스 tarball 로 설치(`/usr/local/bin/gh`).
- 검사: `make preflight`(약 12분) · `make ci-local` · `apps/web` 에서 `npx vitest run`.
- 이 방은 운영 주소로 못 나간다 — `make deploy` [5/5] 의 오류 22 는 **실패가 아니라
  확인을 못 한 것**이다.
