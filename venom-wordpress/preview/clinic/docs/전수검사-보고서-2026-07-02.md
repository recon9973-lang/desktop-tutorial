# 전수검사 보고서 — 2026-07-02

검사 범위: `hospital-marketing/` 전체 (HTML 7, 파이프라인 py 11, 문서, 스키마)
검사 방식: `/검사` 스킬 절차 — 자동 스크립트 검증 + Playwright 실측 렌더 + 수동 검토
표기: ✅ 통과 · 🔧 이번 검사에서 수정 · ⚠️ 결정 필요

## 요약

**10개 영역 중 9개 통과.** 발견된 문제 4건은 전부 경미했고 즉시 수정했습니다(🔧 4건).
결정이 필요한 항목은 3건(⚠️)으로, 모두 코드가 아니라 운영 확정 사항입니다.

| 영역 | 상태 | 비고 |
|---|---|---|
| 1. SEO | ✅ | title 35자·desc 90자(적정), canonical, OG 6종, JSON-LD(Organization+FAQPage) 파싱 검증, h1 1개, 헤딩 계층 정상(H1>H2>H3), 내부 페이지 noindex, sitemap/robots 일치 |
| 2. 의료법 문구 | ✅ | 금지 표현 매칭 15건 전수 문맥 확인 → **전부 "하지 않습니다" 부정 고지·FAQ·키워드 사전의 금지 라벨** (위반 0건). 필수 고지(내부 참고용·전재 금지·당사 산식·보장 없음·출처/기준일) 4개 화면 모두 존재. "미노출 = API 응답 상위 5건 밖" 정의 표기 확인 |
| 3. 콘텐츠 조건 | ✅ | 신청폼 필수 2(required)/선택 4(optional) 분리 정상, 채널 체크박스 3개, 허니팟 존재, 점수 명칭 "검색정보 운영 진단 점수" 통일, 스냅샷 열 없음, 리뷰/사진 병원 직접 입력 표기 |
| 4. 오탈자·일관성 | 🔧 | 구 브랜드 "메디랭크" 잔재 1건(파이프라인 docstring) 수정. 화면 노출 문자열에서 오탈자 미발견. 내부 패키지 코드명 `medirank`는 유지(아래 ⚠️-3) |
| 5. 링크 매칭 | ✅ | 전체 HTML의 상대 링크·fetch 경로 스크립트 검증 — 깨진 링크 0건 |
| 6. 이미지·webP | ✅ | **래스터 이미지 0장, `<img>` 태그 0개** — 전 화면이 인라인 SVG/CSS 벡터라 webP 변환 대상 없음. 향후 병원 사진 사용 시 `pipeline/convert_webp.py`(메타데이터 제거 포함)로 변환 |
| 7. 코드 최적화 | ✅ | 파이프라인 단위 테스트 13건 통과. 죽은 코드 없음. esc()·게이지·팔레트가 페이지마다 중복되나 이는 "단일 파일·의존성 0" 배포 원칙의 의도적 트레이드오프(아래 ⚠️-3) |
| 8. UI/UX | 🔧 | 5개 페이지 × 데스크톱(1200px)/모바일(390px) = 10개 조합 실측: JS 에러 0건, 가로 스크롤 0건, 표는 컨테이너 내 스크롤 정상. 발견 1건 수정: 모바일에서 브랜드명 "(주)베놈" 줄바꿈 갈라짐 → nowrap 처리 |
| 9. 보안 | 🔧 | XSS 이스케이프 적용 지점 재확인, .env 미커밋 확인, 관리자단·내부 화면 noindex 확인. 발견 1건 수정: 법무 설명자료 HTML에 CSP 누락 → 추가. 관리자단 무인증 프로토타입 경고 배너 유지 중 |
| 10. 처리방침·제공조건 | ⚠️ | 제공조건 7개 항목 모두 존재, 법무 문서 간 모순 없음. 처리방침 placeholder 13개 미기입(아래 ⚠️-1) |

## 🔧 이번 검사에서 수정한 것 (4건)

1. `pipeline/medirank/__init__.py` — 구 브랜드 문구를 베놈 표기로 수정 (패키지명은 코드명으로 유지)
2. `docs/법무-검토-설명자료.html` — CSP·referrer·noindex 메타 추가 (유일하게 누락됐던 페이지)
3. `landing/index.html` — 모바일(390px)에서 브랜드명 줄바꿈 갈라짐 수정
4. (검사 준비 중 확인) 스킬이 세션 재시작 후부터 `/검사`로 호출 가능함을 확인 — 절차는 본 검사와 동일

## ⚠️ 결정 필요 (대표님)

1. **처리방침 placeholder 13개** — 시행일, 수탁자 4곳(문자·메일·클라우드·CRM), 개인정보 보호책임자
   이름·연락처. 업체·담당자 확정 시 기입해야 게시 가능합니다.
2. **GA4 측정 ID** — `G-XXXXXXXXXX` placeholder 상태(스니펫은 준비 완료, ID 없으면 미로드로 안전).
3. **코드 중복 정리 여부** — 공용 CSS/JS 파일로 추출하면 유지보수가 쉬워지지만 "파일 하나만 복사하면
   되는" 단일 파일 원칙이 깨집니다. 현재 규모(페이지 5개)에서는 **현행 유지 권장**, 페이지가 8개를
   넘어가면 추출 권장.

## 다음 검사 권장 시점

- 접수 서버 연동 직후 (신규 코드의 입력 검증·이스케이프 재검)
- 처리방침 게시 직전 (placeholder 소진 확인)
- 이후 월 1회 정기 (`/검사` 호출)
