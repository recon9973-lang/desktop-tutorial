# RESUME (입지 방) — **가지 하나가 CI 통과한 채 서 있다** (2026-09-18)

<!-- 가지: claude/hospital-location-analysis-plan-6kbmqo -->

> 이 방은 거래처 상세 **「입지」 탭**을 만들었다. ANSEO(`veo.seokorea.org`) 쪽 일이고
> 코드는 `veo-platform` 저장소에 있다 — 이 저장소(desktop-tutorial)에는 문서만 있다.

## 지금 상태

- **나간 것**: 「입지」 다섯 축(경쟁 · 교통 · 인구 · 상권 · 방문자)과 **머리 요약**(0.3.555)이
  운영에서 돌고 있다. 머리 요약 위에 다른 방이 9/12 에 이어 붙이기도 했다.
- **못 나간 것 하나** — `veo-platform` 가지 **`claude/anseo-location-v0501`**:
  ```
  8036538  인구 네 칸의 짝 · 연령 피라미드에 총·남·여 (v0.3.609)
  52f7e2f  판 물림 0.3.609 → 0.3.610 (배포 도구가 붙인 것)
  ```
  **CI 를 이미 통과했다**(실행 35351440498). main 에 미는 마지막 한 걸음만 남았다.

## 왜 못 나갔나 — 코드 탓이 아니다

네 번 시도해 **네 번 다 같은 자리**에서 거절됐다: 검사 초록 → CI 통과 → **main 에 미는 순간**
`! [rejected] (non-fast-forward)`.

까닭은 구조다. 우리 배포는 후보 가지에서 **CI 20분 채점 → 그 뒤 main 밀기**인데, ANSEO 방이
그 20분 안에 main 을 계속 민다(실측 2026-09-18: 채점 한 번 도는 사이 6 커밋). 속도로는 못 이긴다.

**지름길은 쓰지 않았다.** 채점 뒤에 main 것을 섞어 밀면 아무도 채점하지 않은 나무가 나가는
셈이고, 그것이 이 저장소가 **오류 135** 로 못 박은 사고다.

## 이어받는 사람이 할 일

1. **사장님이 ANSEO 방에 「30분만 main 을 비워 달라」**고 해 주시면 그 창에 한 번에 끝난다.
   또는 그 방이 자기 배포에 이 가지를 얹어 같이 내보내면 물릴 일이 없다.
2. 스스로 다시 밀 때 — **rebase 하지 말 것.** 문서마다 충돌이 난다. 대신:
   ```
   git fetch origin main
   git diff --name-only <옛 base> origin/main -- <우리 코드 파일>   # main 이 건드렸나 (여태 한 번도 안 건드림)
   git checkout -B claude/anseo-location-v0501 origin/main
   git checkout 8036538 -- <우리 코드 파일>
   make bump-version TO=<main 판 +1>  ·  문서 넷은 새로 쓴다
   VEO_DEPLOY_ORDER="<사장님 문장>" make deploy      # 검사+배포를 붙여 창을 좁힌다
   ```
   **우리 코드 파일** (`apps/web/src/app/(console)/console/customers/[customerId]/`):
   `LocationTab.tsx` · `two-pyramids-stand-side-by-side.test.ts`

## 사장님이 하실 것

1. **「입지」 탭 1km 고리 한 번 열어 확인** — 0.3.547 뒤 첫 확인이 아직이다(task #32).
   상권 상자에 숫자가 뜨면 그 축은 산 것이다. 또 문장이 뜨면 이번엔 **응답 맨 위 칸 이름**이
   함께 적히니 그 캡처 한 장이면 고친다.
2. 결정 둘: **카카오맵 열쇠**(#15) · **「입지」 이름 겹침**(#9 · 이 방 「상권」 / ANSEO 「자리」 / 그대로).

## 이 방이 남긴 규율 (다음 사람이 헛디디지 않게)

- **못 잰 값은 «—»** — 0 을 그리지 않는다. 안 적재된 갈래를 0 으로 놓고 더하지 않는다
  (지하철 미적재면 「정류장·역」 합도 «—» 다 — 그 합은 없는 값이 아니라 **틀린 값**이 된다).
- **합산 점수 금지**(대장 §1). 축마다 잰 자리도 단위도 다르다.
- **잰 자리를 숫자 옆에 적는다** — 반경 안인지 시군구 전체인지. 사장님이 걸리신 곳이 전부
  그 자리였다(방문자 683만은 시군구 연인원 · 인구 이동은 창원시 전체 · 3km 를 골라도 상권은 2km).
- **반올림 금지** — 자른다(`.toFixed`·`Math.round` 관문).
- **자동 물림(`claim_version.py`)은 이력 절 제목을 `### … (v0.3.6xx …)` 꼴로 찾는다.**
  옆 항목을 따라 `## … 0.3.6xx` 로 쓰면 배포가 거기서 멈춘다.
- **변경이력 충돌은 두 방 글을 한 항목으로 붙인다**(판 번호 줄이 같아서다). 항목 둘로 갈라야
  한다. 대기 표를 고친 뒤에는 **줄 수를 세어** 저쪽 줄이 빠지지 않았는지 본다.
- **커밋 전 반드시** `grep -rln '^<<<<<<< |^>>>>>>> '`.
- **이 컨테이너의 PostgreSQL 이 자주 죽는다**(로그가 종료 기록 없이 끊긴다). 긴 검사에는
  감시를 붙인다: `while kill -0 $PID; do sleep 20; pg_isready -q … || pg_ctlcluster 16 main start; done`
- **`ruff format` 을 저장소 전체에 돌리지 말 것** — `ruff check` 만 쓴다.

## 참고

- 세션 로그 `docs/session-logs/2026-09-09-s22.md` · `-2026-09-08-s21.md` ·
  이 방 것만 `2026-09-04-s16-입지방.md` · `-s17-입지방.md`
- 조사 보고서 `docs/plans/anseo-floating-population-sources.md`
- 현황 `PROJECT_STATE.md` · 지도 `핵심두뇌_MASTER.md`
