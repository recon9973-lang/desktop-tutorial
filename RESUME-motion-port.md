# RESUME — 모션 방 (2026-09-14 · s23 이어서)

> 이 저장소에는 인계가 여럿이다. **이 파일은 모션 방 것이다.** 상세는
> `docs/session-logs/2026-09-13-s23-motion-port.md`. 다른 방 RESUME 은 건드리지 않는다.

## 지금까지 (핵심만)

Lovable 시안(ANSEO 콘솔)을 **디자인 원본으로 확정**하고, 거기 정해진 `motion-*` 체계를
`veo-platform` 으로 옮겼다. **시간·굴곡은 `tokens.css` 토큰 한 벌**, **움직임은 새 파일
`packages/ui/src/motion.css` 전역 유틸리티**로 갈라 옮겼다.

덮인 범위 — 공용 그래픽 8 · `components/infographic` 13 · 거래처(고리·점·피라미드·곡선·
게이지) · AEO(언급 점유·출처 행렬) · 대시보드 · 리포트 · 공용 조각(머리줄 두 메뉴·엔진
점·진단서) · 공유 리포트 · 콘솔 화면 등장 차례. 상권 화면의 **빈 상자 850px** 와 목록 칸
글자 단위 절단도 함께 고쳤다. 죽은 규칙 14(화면 7 + 유틸리티 7)를 지웠다.

**2026-09-13 23:0x KST 에 판 0.3.574 로 나갔다**(가지 `claude/anseo-motion-port` →
CI 초록 → main `a8589c4` · 도장 `07dbd05`). 한 판에 옆 방의 오류 210 수정이 함께 나갔다.

## 바로 이어갈 작업

### 0. 먼저 — **이 방이 낼 것은 없다. 전부 나갔다**

```
[실측 2026-09-16 13:17 KST]
  이 가지가 main 보다 앞선 커밋   0건
  main 끝                        c9ecf70 · 판 0.3.594
  대장 배포 대기 표              이 방 줄 없음 (WORKLIST-HISTORY.md 로 옮겨졌다)
```

이 방의 커밋은 화면 고침(대시보드 지휘 줄 · 로그인 격자 · 숨김 글자 여섯 ·
스크롤 틀 · 고르는 칸)과 재는 도구(가짜 서버 채움 · 좁은 폭 재기) **전부 main 에
들어가 있다.** 배포는 이 방이 돌린 것이 아니라 **다른 방이 합쳐 냈다**
(`6f2bff7 모션 방 가지 4커밋을 합친다` → 0.3.589, 그 뒤 0.3.594 까지).

배포 담당은 ANSEO 방이다 — 이 방은 `make deploy` 를 돌리지 않는다. **새로 만든 것이
없으면 대기 표에 아무것도 적지 않는다.**

### 1. 시안의 새 판(배치·구조) — **대조는 끝났다. 남은 것은 셋뿐이다**

시안 계획 열두 항목을 우리 화면과 하나씩 맞춰 보니 **아홉은 이미 지키고 있었다**
(AEO 점수 게이지 없음 · 샘플 수치 없음 · 「진단 못 함」 글자 · 실행 큐 자리 ·
한글 줄바꿈 등). 시안의 「완료」를 그대로 옮겼다면 아홉 번 헛일할 뻔했다.

적용한 것 하나 — **마우스 반응 절제**(칸은 테두리로, 줄은 바탕색으로, SVG 점 1.4→1.2).

```
남은 셋 → 전부 손댔다. 남은 것은 **사장님 눈·기기** 쪽이다.
  ①  대시보드 상단 지휘 화면    했다(04bb4dc) — 살아 있는 콘솔에서 눈 확인만
  ②  좁은 폭 점검              공개 화면 끝 · **콘솔 28개도 쟀다** — 표에 줄을 채우고도 넘침 0(아래)
  ③  로딩 애니메이션 멈춤       재현 안 됨. **원인 안 가리는 고침 둘을 배포**했다(5a21bf9)
```

**③ — 1초는 이 방이 닿는 어느 자리에서도 안 나온다.** 앞선 세 고침 모두 실서비스에
2026-09-11 04:55Z 배포 성공(사장님이 09-13 에 지적하시기 전이다). 크로뮴 실측:
시작 2ms · 되감기 최대 1.63배 · 블러 전환 33ms · mp4 구조 정상(elst 0 · B프레임 없음)
· **webm·mp4 둘 다 등속 164칸 41.7ms**(stts 를 직접 읽었다 — 여태 안 본 자리였다).
남은 축은 **사파리·아이폰**이고 이 컨테이너는 H.264 를 못 푼다.

사장님 «니가 알어서 좀 해자» → **원인을 안 가리는 고침 둘을 내보냈다**(PR #248 · main `5a21bf9`
· Production success 2026-09-14 14:18Z).

```
① mp4 를 먼저 고르게      iOS 17 사파리는 VP9 을 「틀 수 있다」고 답하는데 아이폰에
                          VP9 하드웨어 디코더가 없다 — 640×640 을 CPU 로 푼다.
                          mp4(H.264 High 3.1)는 모든 아이폰이 칩으로 푼다.
                          **그림은 한 픽셀도 안 바뀐다**(같은 164칸) · 471KB→601KB
② 영상이 아예 못 뜨면      한 프레임도 못 낸 채 1.6초가 지나면 셰이더로 넘긴다.
   스스로 건진다           저전력 모드가 자동재생을 막는 자리다. 셰이더는 끝나는
                          지점이 없어 멈출 수 없다. 필요할 때만 내려받는다(평소 0KB).
```

**중간 멈칫으로는 안 넘긴다 — 한 번 되돌린 판단이다.** 처음엔 900ms 멈추면 갈아타게
만들었다가 물렸다. 셰이더는 나란히 놓으면 확실히 못하다(실 가닥 단면 봉우리 **8.8 → 0.7**
· 같은 틴트에서 더 흐리다 — 사장님이 «실 같은 디테일이 없어지던데» 로 먼저 짚으셨다).
**한 번 멈칫한 대가로 남은 내내 못한 링을 보는 것은 손해다.**

되돌리기: `watchStall(st)` 한 줄 + 그 블록 삭제 · `<source>` 두 줄 순서 원복.
**여전히 멈춘다면 원인은 이 둘 다 아니다** — 그때 `/ring-check.html` 숫자를 받는다.

그래서 `venom-wordpress/preview/ring-check.html` 을 만들었다 — 사장님이 폰에서 열고
단추를 누르면 실제 링을 25초 돌리며 rVFC 로 재고 표를 낸다(어느 파일을 틀었나 ·
모션 최소화 켜졌나 · 첫 프레임까지 · 가장 오래 멈춘 칸이 영상 몇 초 자리인지).
noindex · 어디에도 연결 안 함 · 제품 코드는 한 줄도 안 건드림.

```
프리뷰 https://venom-new-site-kjtu04whe-75cold-7724s-projects.vercel.app/ring-check.html
```

**그 숫자를 받기 전에 고치지 마라.** 링 로더 방이 「사장님이 말한 문제를 악화시키는
안을 만들고 나서야 쟀다」고 제 인계에 적어 두었다. 셰이더 전환(`ring-shader.js`,
만들어만 두고 안 켬)은 영상 자체를 없애 이 문제 계열을 통째로 지우지만 **생김새가
똑같지 않다** — 사장님 결정 사항이고, 아직 안 받았다.

**①에서 시안의 낱말을 하나 바꿔 썼다.** 시안은 «진단 기준 주간» 이라 했지만 이
화면에서 주 단위로 묶이는 값은 **AEO 관측 하나뿐**이다 — 그대로 적으면 SEO·GEO 도
그 주에 잰 것으로 읽힌다(시안 자신이 금지한 합산). 값에 «AEO 관측 · 9/8~9/14 ·
판 3개» 로 무엇의 주간인지를 달았다. **뜻은 지키고 낱말만 우리 사실에 맞춘 것**이니,
사장님이 「시안대로 적어라」 하시면 되돌리기 전에 이 이유부터 말씀드려라.

위험 건수는 아래 실행 큐와 **같은 함수**(`openIssues`)를 지난다 — 따로 세면 머리와
큐가 다른 건수를 말하는 날이 온다(오류 192). 시험이 그 어긋남을 잡는다.

**우리 세 단은 720 · 960 · 1100 이다**(`breakpoints-are-shared.test.ts`). 앞선 판에
이 자리에 「1240」이라고 적어 두었는데 **틀렸다** — 1240 은 대시보드 파일 하나에만
남아 있는 예외지 우리 단이 아니다. 시안의 623px 도 우리 숫자가 아니다.

**②에서 실제로 찾은 것** — 로그인 화면이 폰에서 옆으로 흘렀다. `1fr` 의 바닥은
`auto`(min-content)라 칸이 제 그릇보다 넓어진다. [실측 크롬] 320px→73px · 360px→33px ·
390px→3px. `minmax(0, 1fr)` 로 바닥을 풀어 **320~1440 전 구간 0**. 접는 폭 860→960.

**콘솔 28개도 쟀다 — 넘침 0건.** 이 방이 「콘솔을 못 연다」고 믿어 온 것이 틀렸다:
`apps/web/test/smoke/` 에 **이미 여는 길이 있었다**(가짜 진단 서버 + 가짜 쿠키 · 비밀번호
안 씀 · 운영에 닿는 길 자체가 없음). `shoot.mjs` 가 그 길로 화면을 찍고 있었는데 폭을
재는 쪽만 없었다. 그래서 `narrow.mjs` 를 그 옆에 만들어 넣었다.

```
다시 돌리기   make narrow-web   (= pnpm build && pnpm narrow)
결과          화면 28개 × 폭 390/720/960/1100 = 112번 전부 넘침 0
빈 쪽이 아니다 28개 전부 h1 이 떴다 · 마디 69~6,917
```

**표에 줄을 채우고도 다시 쟀다 — 역시 넘침 0.** 가짜 서버에 **채움 모드**를 더했다
(`SMOKE_FILL_ROWS`, **기본 꺼짐** — 연기 시험이 재는 「자료 없어도 뜨나」를 안 건드린다).
배열 12줄 · 글자는 실제로 있을 법한 가장 긴 것(긴 병원 상호 · 질의문 붙은 132자 URL ·
긴 한국어 문장) · 숫자 1~97 · **불리언 참**(거짓이면 `registered` 같은 깃발에서 채운 줄이
도로 걸러져 표가 다시 빈다 — 첫 판에서 실제로 그랬다).

```
채우기 전   전체 20,108 마디 · 표에 줄 선 화면 1개
채운 뒤     전체 24,732 마디 · 이슈 554→5,029(목록 290·표 6줄) · 휴지통 290줄
결과        화면 28개 × 폭 넷 = 112번 전부 넘침 0
```

**거래처 목록까지 채우니 넘침이 나왔다 — 16건·화면 일곱.** 안 차던 원인은 전부 내 채움
코드의 결함이었다(① 자르는 기준이 전체 깊이 → 봉투만으로 7 을 먹어 목록이 통째로 빔
② `is_registered` 의 `default:false` 가 먼저 걸림 ③ id 가 전부 같아 조인이 한 줄로 뭉침).
고친 뒤 전체 20,108→**41,007 마디**, 표에 줄 선 화면 1→**9개**.

```
390px   competitors 541 · customers 490 · customers/tests 497 · dashboard 6572
        geo 6614 · reports 35 · usage 279
720px   competitors 211 · customers 160 · customers/tests 167 · dashboard 6242 · geo 11535
960px   dashboard 6002 · geo 6303        1100px  dashboard 5862 · geo 9523
```

### 갈랐다 — 한 번에 하나씩만 바꿔서 다시 쟀다 (2026-09-15)

```
줄 수 탓            대시보드  6572→1856(390px) · 6242→1526(720px)   [줄 12→3]
글자 길이 탓         리포트 35→사라짐 · 사용량 279→사라짐 · 경쟁 브랜드 541→84  [글자 짧게]
내 가짜 값 탓        AEO 막대 — 코드가 brands[0] 을 최댓값으로 믿는다(서버가 내림차순으로
                    준다는 전제). 내 숫자가 정렬이 안 돼 퍼센트가 100 을 넘었다.
                    MentionShareBoard.tsx:159
줄도 글자도 아닌 것   거래처 목록 490·160 **그대로** — 자료와 무관하다. 여기가 남은 실마리.
```

### 도구의 거짓 양성을 고쳤다 — 이것이 이번 판의 알맹이다

`overflow-x: auto|scroll|hidden` 인 조상이 있으면 그 안에서 스크롤될 뿐 **문서를 못 민다.**
안 걸렀더니 거래처 목록의 표가 범인으로 찍혔는데, [실측 720px] 그 표는 `tableWrap` 안에서
**624px 로 제대로 가둬져** 있었다. 오른끝 상위 여섯이 전부 그 표라 진짜 범인이 가려졌다.
빼고 다시 재니 거래처 목록 720px 의 범인은 `<div class="group">` 하나만 남았다.

> **가둬진 것을 범인으로 세지 마라.** 폭을 잴 때 가장 쉽게 빠지는 함정이다.

### 실마리 둘을 팠다 — **둘 다 내가 틀린 자리였다** (2026-09-15)

추측을 그만두고 **인과로 쟀다**: 요소를 하나씩 `display:none` 해 보고 문서 폭이 줄어드는
자리를 좁혀 들어갔다.

```
body.scrollWidth 720 · html.scrollWidth 880     ← 흐름 밖이라는 뜻
<span .srOnly> absolute · 폭 1px · 왼 879       ← 이것 하나 숨기면 720
```

**화면 낭독기 전용 글자 1px 이 쪽을 밀고 있었다.** `.srOnly` 는 `position:absolute` 인데
`left` 가 없어 **정적 위치**(896px 표의 오른쪽 끝)에 놓이고, `.tableWrap` 이
`position:static` 이라 **담는 그릇이 아니어서** 스크롤 틀을 그냥 빠져나갔다.
틀의 `overflow-x:hidden` 도 표의 `width:100%` 도 안 먹혔다 — 자손이 애초에 틀의 좌표계
밖이었기 때문이다.

**고침 한 줄 — `.tableWrap { position: relative }`**
```
customers/tests  497(390) · 167(720)  →  둘 다 사라짐
customers        160(720)             →  사라짐        390px 490 → 249
전체             16건                  →  13건
```

> **오른끝으로 범인을 찾지 마라.** 이번에 그렇게 해서 두 번 틀렸다 — 표를 범인으로
> 찍었다가(가둬져 있었다) 테마 단추를 범인으로 찍었다(밀려난 피해자였다).
> **숨겨 보고 문서가 줄어야 범인이다.**

### 남은 것을 끝까지 갈랐다 (2026-09-15)

```
고쳐서 없앤 것   대시보드 넷 + 거래처 셋
                 ① `.tableWrap { position: relative }`  ② `.srOnly { left: 0 }` 여섯 자리
글자 길이 탓     customers 249 · reports 35 · usage 279 → 흔한 길이로 바꾸니 **셋 다 사라짐**
                 (내가 넣은 132자 URL 탓이었다)
확정 가짜        AEO 넷 — 숲 그림이 `left: ${rate.value * 100}%` 로 점을 놓는데
                 축은 0~100% 다. 내 채움이 정수 1~97 을 넣어 최대 9700% 자리에 찍혔다.
                 **실제 값은 0~1** (RateForest.tsx:75,78)
진짜 후보 하나   competitors — 흔한 길이로도 390px 에서 249px 이 밖으로 나간다
```

**누적: 16건 → 9건(고침 둘) → 그중 여덟이 내 가짜 자료 → 진짜 후보 하나.**

### `competitors` 도 잡았다 — 고르는 칸이었다

줄 열둘이 **똑같이 866px** 이라 한 줄만 숨겨선 안 줄어들었다(격자 칸 폭에 늘어난 것).
**같은 종류를 모든 줄에서 한꺼번에** 숨기니 범인이 나왔다.

```
931 → 606   <select>(회차 고르는 칸) 열셋을 한꺼번에 숨기면
931 → 606   그것을 담은 <label> 에 min-width: 0 을 주면 — 같은 값
```

`<select>` 는 **가장 긴 선택지만큼** 넓어지고 그 아래로 안 줄어든다. 담은 `.label` 도
격자 칸이라 바닥이 `auto` — 그 폭이 그대로 쪽을 밀었다. **`.label { min-width: 0 }`**
→ 390px 541→**216** · 720px 211→**사라짐** · 전체 9→**8건**.

> **한 번 헛짚었다** — `.input`(셀렉트 자신)에 먼저 넣었다가 한 픽셀도 안 변해 되돌렸다.
> 실험 결과를 읽을 때 **「무엇에 적용했는지」를 안 보고 이름만 봤다.**

### 재는 법 (이 세션에서 세운 것)

```
make narrow-web                    28개 화면 × 폭 390/720/960/1100
SMOKE_FILL_ROWS=12 (기본)          표에 줄을 채운다. 0 이면 빈 상태
혼자 돌려라                        다른 서버와 겹치면 CPU 를 다퉈 거짓 빨강이 난다
범인 찾기                          **오른끝으로 찾지 마라.** 하나씩 display:none 해 보고
                                   문서 폭이 줄어드는 자리를 따라 내려간다
가르는 법                          줄 수(12→3) · 글자 길이(길게→짧게)를 **한 번에 하나씩만**
                                   바꿔 다시 재면 원인이 갈린다
```

**채움의 값 범위 — 고쳤다 (2026-09-15).** 예전엔 모든 숫자에 1~97 을 넣어, 비율(0~1)을
받아 자리를 잡는 그림이 최대 9700% 자리에 점을 찍었다. 이제:
```
① minimum·maximum 이 적혀 있으면 그대로   (숫자 칸 1,355 중 45)
② 안 적힌 자리만 이름으로                 비율 0~1 · 점수 0~100 · 횟수 0~50 · 돈 0~500
③ 이름은 낙타 등을 끊어 토막으로          RatePayload → [rate, payload]
④ 신뢰구간은 low ≤ value ≤ high
```
③ 이 없으면 `ratepayload` 한 낱말이라 «rate» 와 안 맞아 비율 자리에 2 가 들어가고,
토막으로 안 끊으면 반대로 `operations` 안의 «ratio» 가 걸린다 — **둘 다 실제로 잡혔다.**

**이름표 붙는 코드 자리도 짧게** — 화면이 `LABELS[code] ?? code` 로 그리는 곳이 많아,
거기 긴 문장이 들어가면 짧아야 할 칸이 부푼다(AEO 의 「인용 N회 · 엔진」 칸이 1,608px).
`engine·provider·state·kind·code·type·severity·role` 은 `CHATGPT` 같은 짧은 코드로.

```
[실측] 비율 칸이 0~1 밖 102 → 0
       AEO 넘침 9,523 → 620 → **넷 다 사라짐**
       전체 8건 → **4건**  competitors 216 · customers 249 · reports 28 · usage 279
                            — 넷 다 **390px 폰 폭에서만** 난다
```

### 남은 넷도 전부 재는 자리였다 — **넘침 0** (2026-09-15 · s24)

넷을 하나씩 파 보니 **같은 모양 하나**였다 — 짧아야 하는 이름표 칸에 내가 문장이나
병원 상호를 넣었고, 그 칸이 `white-space: nowrap` 이라 한 칸이 쪽을 옆으로 흘렸다.

```
competitors 216   identity_strength 가 꼬리표에 그대로 찍혀 522px   ← 계약이 글로만 적어 둔 값
reports      28   band_label_ko 가 «label» 에 걸려 병원 상호 297px
usage       279   grade 가 아무 규칙에도 안 걸려 문장 554px
customers   249   region 도 문장이 되어 지역 칩 591px
```

**① 계약이 글로 적어 둔 값을 읽는다.** `identity_strength` 는 `enum` 없이 설명의
**첫머리**에만 «SUFFICIENT | PARTIAL | INSUFFICIENT» 라고 적혀 있다 — 그런 칸이
**열여섯**이고 `enum` 이 붙은 칸은 마흔이다. 화면은 `LABELS[값] ?? 값` 으로 그리니
모르는 값이 그대로 찍힌다. 설명 **가운데**의 `A | B` 는 값이 아니라 글이라 안 읽는다.

**② 짧은 이름표 무더기를 따로 뒀다.** 등급·구간(`grade·band·tier`)과
지역(`region·district·sido·sigungu`). 실제로 오는 값은 「취약」·「좋음」·「강남구」다.

```
[실측 2026-09-15] 화면 28개 × 폭 넷 = 112번 전부 **넘침 0**
                  연기 시험 28/28 그대로 · 웹 2,616 초록
커밋  2ccb614 짧아야 하는 이름표 자리를 짧게
      d80bede 다 재고도 안 끝나던 것을 끝나게 한다
```

**범인 찾는 법을 하나 더 배웠다.** 오른끝 목록이 세 화면 **모두** `.group`(밝기 고르는
칸 · 189px)을 맨 위로 올렸는데, 숨겨 보니 **셋 다 원인이 아니었다.** 맞는 길은 하나뿐이다 —
**위에서 아래로 한 자식씩 숨겨 내려가고, 문서가 줄어드는 쪽으로만 내려간다.** 갈림길
(둘 이상이 같이 넓힌다)에서 멈춰 그 자리의 자식 폭을 눈으로 본다.

**재는 도구가 다 재고도 안 끝나던 것도 고쳤다** — `narrow.mjs` 가 가짜 서버를 안 닫아
듣는 소켓이 이벤트 루프를 붙잡았다. 네 폭을 다 재고 **21분**을 매달려 있었고 출력은 한
줄도 안 나왔다(파이프로 묶여 있었다). `make narrow-web` 이 CI 에서 영영 안 돌아온다는
뜻이었다. 연기 시험은 이미 닫고 있었다.

**내가 틀린 고침 하나** — `.tableWrap` 에 `min-width: 0`. [실측] 화면별 값까지 대조해
**한 픽셀도 안 변했다.** 되돌렸다. 그 길은 아니다.

### 조각 주소 여섯도 자료가 찬 채로 쟀다 (2026-09-16 · s25)

**여태 껍데기만 잡힌 데는 까닭이 있었다.** 가짜 서버는 주소에 `/smoke-` 가 있으면
**일부러 404** 로 답한다 — 없는 것에 없다고 답해야 화면이 제 「없음」 길을 타고, 연기
시험이 그 길을 잰다. 폭 재기는 정반대가 필요한데 같은 식별자를 쓰고 있었다. 그래서
**식별자를 두 벌로 갈랐다**(`DYNAMIC_FIXTURES` / `DYNAMIC_FIXTURES_FILLED`).

자료를 넣자 **오류 하나와 넘침 둘**이 드러났고, 셋 다 재는 자리였다.

```
리포트 버전  오류판   TypeError: Cannot read properties of null (reading 'rule_id')
업체 여정    175px   가로축 눈금이 464px 로 축 밖으로 (position:absolute · nowrap)
이슈 상세     11px   「조치 담당 …」 칸이 334px
```

**① 널만 든 목록을 안 만든다.** 깊이 자름(`MAX_DEPTH`)에 걸린 줄이 `null` 로 나오는데
그것을 목록에 그대로 담고 있었다 — 계약 예시 **204 자리**가 같은 꼴. 실제 서버라면
절대 안 주는 답이고, 화면은 그 줄을 펴다 죽는다. 못 만들면 빈 목록이 맞다.

**② 형식 칸이 비어도 날짜인 자리가 있다.** `week.window.monday` 는 `format` 없이
설명으로만 날짜(«… YYYY-MM-DD»)라 문장이 들어갔고, `shortLabel` 이 「9/12」로 못 쪼개
원본을 그대로 넘겼다. 계약의 날짜 칸 **176 중 159 는 형식이 적혀 있고 17 은 없다** —
이름(`_at`·`_on`·`monday`)과 설명으로 읽는다(걸리는 열여섯을 전부 눈으로 확인했다).
**줄마다 다른 날**로 만든다 — 같은 날이면 추이 그림의 열두 주가 한 줄로 뭉친다.

**③ `…_owner` 는 사람 이름이 아니라 직군 코드다.** 화면이
`OWNER_LABELS_KO[값] ?? 값` 으로 그리고 계약도 한 자리에 `default: DEVELOPER` 를 적어
뒀다. 접미사만 코드로 돌렸다 — `owner_name`·`owner_ko` 는 사람 이름 자리라 안 건드렸다.

```
[실측 2026-09-16 · 390px] 여섯 전부 200 · 본문이 왔다
  customers/[customerId]           974 마디      edit        1,316 마디
  customers/[customerId]/journey   602 마디      issues/[id] 1,360 마디
  reports/[reportId]             2,008 마디      …/[version] 1,372 마디
  화면 28개 × 폭 넷 = 112번 전부 **넘침 0** · 연기 시험 28/28 · 웹 2,616 초록
커밋  a9938cf 드러난 셋을 고친다      39652f6 식별자를 두 벌로 가른다
```

**이제 콘솔에 껍데기로 재는 화면이 없다.** 남은 것은 값이 계약 표본이라는 것뿐이다 —
모양은 실물이고 값은 실물이 아니다.

**겹쳐 돌리지 마라.** 같은 판을 세 번 돌려 0·0·54건이 나왔고, 54건이 나온 판만 다른
Next 서버와 브라우저가 함께 돌고 있었다. CPU 를 다투면 배치가 안 끝난 중간 상태가
찍힌다 — **화면 결함이 아니라 재는 자리의 결함이다.**

**「공통 화면 머리 부품화」는 목록에서 뺐다.** 재 보니 할 일이 아니었다 —
머리를 쓰는 화면 **32곳 중 31곳이 이미 공용 판을 쓰고 있다**(`@/styles/page.module.css`).
앞 방이 «28개가 각자 쓴다»고 보고한 것이 틀렸다(오류 **213**). 남은 차이는 CSS 가
아니라 JSX 배치뿐이라, 부품을 뽑는 일은 **화면을 보면서** 판단할 몫이다.

다만 그 착각이 **결함 둘을 가리고 있었다** — `mine` 의 단서 넷과 `datasources` 의
머리말이 **없는 클래스를 불러 맨 글자로** 서 있었다. 둘 다 고쳤다.

### 2. 관문 하나가 눈을 감고 있었다 — 다음 방도 이 버릇을 조심하라

`styles-are-not-undefined.test.ts` 는 2026-09-01 에 **바로 그 결함을 막으려고**
세운 관문인데, 첫 import 하나만·점으로 시작하는 경로만·홑따옴표만 읽고 있었다.
**공용 판을 쓰는 화면 서른한 개가 처음부터 관문 밖**이었고, 관문은 그 사실을
말하지 않고 초록이었다.

이제 「무엇을 보고 있나」를 스스로 잰다(짝 수·별칭 경로·둘째 import·따옴표 두 꼴).
**교훈은 관문 하나에 그치지 않는다** — 관문이 있다는 것은 관문이 본다는 뜻이 아니다.
믿기 전에 그 관문이 무엇을 읽는지 먼저 읽는다.

### 3. 못 끝낸 확인 하나

**실서비스가 0.3.574 를 서비스하는지 이 방은 못 쟀다** — `veo.seokorea.org` 응답 000
(egress 차단). 콘솔 화면 아래 판 표시가 0.3.574 인지, 화면이 제목 → 지표 → 패널 차례로
뜨는지 **사람 눈으로** 한 번 보면 끝난다. (사장님 «확인했어» 2026-09-13 — 원문이 그 한
마디뿐이라 판 표시까지 보신 것인지는 이 방이 단정하지 않는다.)

이번에 고친 두 줄도 같은 처지다 — **토큰 다섯이 `tokens.css` 에 있는 것까지만 쟀고**,
화면에서 어떻게 서는지는 못 봤다.

## 대기/차단

- **실서비스 관측을 이 방에서 못 한다** — `veo.seokorea.org`·Railway 전부 egress 차단.
  판 확인은 사장님 화면이거나, 닿는 방에서만 된다.
- 배포는 `VEO_DEPLOY_ORDER="사장님 원문"` 없이는 안 나간다(관문).

## 주의·제약

- 작업 저장소는 **`recon9973-lang/veo-platform`**(`add_repo` 로 붙인다 → `/home/user/veo-platform`).
  이 저장소(desktop-tutorial)에는 기록만 남긴다. 가지 `claude/tender-bell-wtempg`.
- **veo-platform 환경 세우기**: `PYTHON=/usr/bin/python3.12 make setup` · `pnpm install
  --frozen-lockfile` · `service postgresql start` · `su postgres -c "psql -c \"CREATE ROLE
  root LOGIN SUPERUSER PASSWORD 'veo'\""` · `PGPASSWORD=veo make db-test-create`.
  **postgres 는 세션 중에 죽는다** — 점검이 빨간불이면 먼저 `pg_isready`.
- **방이 여럿이라 main 이 작업 중에 움직인다.** 배포 직전 `git fetch` 로 보고, 밀기 전이면
  멈추고 합친다. 오류 대장 번호도 자주 겹친다 — 먼저 올라간 쪽이 그 번호를 갖는다.
- 못 잰 값은 «—». 지어낸 수치 금지. 모델 ID 는 커밋 트레일러에만.

## 참고

- 규칙: veo-platform `docs/WORKLIST.md` **§1-C23**(화면 모션 확정) · 현황 `docs/STATE.md`
- 이 방이 적은 오류: `docs/CORRECTIONS.md` **209**(화면 차례) · **212**(없는 화면에 걸었다고 보고)
- 시안: Lovable 프로젝트 `c99930c9-cf5b-4586-855c-f9a913d79f15` (korean's Lovable · 「Remix of
  Finance Dashboard」 라는 이름이지만 내용은 ANSEO 콘솔이다). MCP 도구로 `list_edits` ·
  `read_file` 하면 계획서와 커밋을 그대로 읽을 수 있다. 편집기
  https://lovable.dev/projects/c99930c9-cf5b-4586-855c-f9a913d79f15
