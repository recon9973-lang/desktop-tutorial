<!-- 가지: claude/confident-archimedes-f2eegb -->

# RESUME (지도 순위 방) — 2026-09-21 00:2x KST

> **이 방의 일감은 이 저장소가 아니라 ANSEO(`veo-platform`)에 있다.**
> 여기(`desktop-tutorial`)는 베놈 마케팅 사이트다 — 코드는 한 줄도 안 건드렸다.
> 이번 세션 기록: `docs/session-logs/2026-09-19-map-rank.md` (오더 43건 · 1~6부)

## 지금까지 (핵심만)

```
나감  0.3.627  지도 첫 페이지 — 네이버·카카오·구글 순위       [합치기 #9  · c244bd85]
나감  0.3.629  거래처 화면 한 줄 요약 · 주 1회 저절로 재기    [합치기 #10 · 58321e61]
나감  0.3.632  지도 1페이지→경쟁사 후보 · 지난 판과 견주기    [합치기 #11 · 8b6b83f6]
                · 빈 자리 셈 고침
(판번호 없음)  통로 재는 도구 + 러너 일감                     [합치기 #12 · #13]
나감         지도 자체를 읽는 길(Apify) 재는 갈래            [합치기 #14 · 1b8962a3]
나감         칸을 제목 아닌 이름으로 고른다                 [합치기 #15 · dd93e223]
나감         「FAILED」 한마디로 안 끝낸다                   [합치기 #16 · 4c2ac236]
나감         네이버 길이 열렸다 — 실측 기록                 [합치기 #18 · e6dea84c]
나감         **네이버 순위를 실제로 읽는다 — 제품에 붙임**   [합치기 #19]
대기         **자리를 흩지 않는다 — 중심 한 곳에서만**       [합치기 #21]

카카오 ✅ 잰다   구글 ✅ 잰다   빙 ⛔ 통로 닫힘
네이버 ✅ **붙였다(#19) · 열쇠도 넣었다 — 남은 것은 Railway 「Deploy」 단추 하나**
```

## 네이버 — 다섯 번 재서 알아낸 것 (이 방의 가장 큰 결론)

```
1차 05:22  DataForSEO 403(까닭 안 적힘) · SearchApi 200 인데 장소 묶음 없음
2차 13:19  DataForSEO 403 "Please verify your account" ← 열쇠·요금제 아님, **본인 확인**
           SearchApi naver_maps 400 "Unsupported engine" ← **지도 전용 통로가 없다**
3차 15:12  사장님 본인 확인 뒤 DataForSEO **200** — 다만 온 것은 웹문서 20줄
4차 15:17  「강남역맛집」 → local_pack 옴. 1위 민직(별점 4.87·리뷰 3,088·광고 아님)
5차 15:20  「강남치과」·「강남미용실」·「춘천정형외과」 → **장소 묶음 없음**
```

**통로 탓이 아니다.** 네이버 통합검색 화면 자체가 **병원 검색어에는 장소 묶음을 안 띄운다.**
맛집은 띄운다. 그러니 통합검색을 읽는 어떤 통로를 사서 붙여도 거래처(병원) 순위는 못 본다.
값은 한 번 $0.0012(≈1.7원)로 싸지만 **살 이유가 없다.** `maps/naver_places.py` 머리말에 박았다.

## 네이버 길이 열렸다 — **이제 붙이는 일만 남았다**

[실측 2026-09-20 22:23 KST · **사장님 네이버 지도 화면과 한 줄씩 대조**]
Apify `fetch_cat~naver-map-places-scraper` · 「춘천정형외과」

```
순위  Apify                    화면        리뷰 수
 —    (안 줌)                  춘천드림…    **광고 — 빼고 준다**
 1    더봄365정형외과의원       같음        242+858   = 1,100  같음
 2    강남리본정형외과의원      같음        19+2,091  = 2,110  같음
 3    하나의원                 같음        50+1,098  = 1,148  같음
 4    서울진통증의학과의원      같음        135+1,357 = 1,492  같음
 5    **그랜드연합의원**        같음        (거래처)
```

**차례도 리뷰 수도 한 자리까지 같다. 광고는 빼고 준다.** 값은 열 번 재고 $0.0001 대.
기록은 합치기 **#18**(probe 머리말 10차 · naver_places 머리말 · 대장 ⑬ → 「붙이기 대기」).

### 함께 오는 칸

`rank` · **`placeId`** · 좌표 · 주소 · 전화 · 홈페이지 · 영업상태 · 리뷰 수.
**`placeId` 가 이름 비슷한 곳을 가르는 열쇠** → `AMBIGUOUS` 문제까지 푼다.

## 막혀 있는 한 가지 — **Railway 「Deploy」 단추**

[실측 2026-09-21 · 사장님 Railway 화면] 열쇠는 **제대로 들어가 있다** — 이름
`VEO_APIFY_TOKEN` · 자리 `veo-platform`(API 서버) · 다른 열쇠들과 같은 목록. 그런데
왼쪽 위에 **「Apply 1 change」· 보라색 Deploy** 가 떠 있었다.

> **Railway 는 변수를 넣어도 「Deploy」를 누를 때까지 반영하지 않는다.**

그래서 서버가 옛 환경으로 돌고 화면에 「열쇠가 없어 이 지도는 재지 않았습니다」가 났다.
**Deploy 를 누르고 2~4분 뒤 「지금 재기」** 를 하면 네이버 줄에 순위가 뜬다.

`veo-worker` 에는 **안 넣어도 된다** — 주 1회 저절로 재기도 API 서버에서 돈다
(`api/app.py` 가 `start_map_rank_scheduler` 를 부른다).

**새 코드가 도는지 아는 법**: 네이버 줄 잔글씨가 **「네이버 지도 · 광고 뺀 차례」** 면
새 코드다(옛 코드는 「네이버 통합검색」). 판 번호로는 알 수 없다 — 이 판들은 번호를
안 물렸다.

## 자리를 흩지 않는다 — 합치기 **#21** (검사 중)

사장님 지시(2026-09-20): *"동서남북 등 방위가 의미 없어. … 결과값에는 순서가 이미
정해져 있어 거리와 상관없이."* **맞는 말씀이고 그날 잰 것이 증거다** — 네이버 지도를
좌표 없이 물었는데 사장님 화면과 차례까지 같았다.

| 자리 | 무엇 |
|---|---|
| `maps/service.py` | `BEARINGS_BY_POINTS` → **`CENTRE_ONLY`** |
| `customers/schemas.py`·`router.py` | 창구 입력 `points` 삭제(계약 재생성) |
| `MapRanks.tsx` | 「재는 자리」 고르는 칸 삭제 |
| `map-ranks.ts` | 문장을 **「1위 · 상위 10곳 안」** 하나로 |
| `maps/schedule.py` | **정기 재기가 터질 뻔했다** — 아직 `points=` 를 넘기고 있었다 |

**몫**: 한 거래처 한 주에 6회 → **1회**(15곳이면 월 390회 → 65회).
**되돌림 방지**: 카카오 호출이 한 번을 넘으면 시험이 빨간불. 자리를 도로 늘리려면
**먼저 재라** — 지역 이름 없는 검색어에서 정말 다른지 확인한 다음이다.
**옛 판은 그대로 읽는다** — DB 에 아홉 자리로 쌓인 줄을 지난 판 견주기가 쓴다.

## 붙인 것 — **합치기 #19** (사장님 지적 «적용이 안된것 같은데»)

앞선 판 다섯은 전부 **기록**이었고 제목에도 「화면 안 바뀜」이라고 적혀 있었다. 그런데
내가 「다 됐다」고 말해 사장님이 화면을 보러 가셨다. **기록 저장과 제품은 다른 일이다** —
앞으로 「됐다」는 **화면에서 값이 보일 때만** 쓴다.

| 한 일 | 자리 |
|---|---|
| 네이버 **지도**를 읽는 갈래 | `maps/apify_places.py` (신설) |
| 재는 자리를 통합검색 → 지도 | `maps/service.py` — `naver_map_fetcher` |
| 열쇠 | `core/settings.py` — `apify_token`(`VEO_APIFY_TOKEN`) |
| 통로 이름 | `maps/channels.py` — `APIFY_NAVER_MAP` |
| 화면 글 | `map-ranks.ts` — 「네이버 지도 · 광고 뺀 차례」 |
| 대장 | 대기 표에 **판 번호 없이** · 가지 적음(배포는 ANSEO 방) |

**지킨 것 셋**: ① 광고를 우리가 **다시** 거르고 차례를 **다시 매긴다** ② 열쇠 없으면
「안 쟀다」 ③ **격자가 안 되는 것을 숨기지 않는다**(네이버만 한 판).

### 막혀 있는 한 가지 — **사장님 몫**

> 운영(**Railway**) → API 서버 → `Variables` → `New Variable`
> 이름 **`VEO_APIFY_TOKEN`** · 값은 apify.com 토큰

**저장소 비밀값은 검사용이라 운영 서버가 못 본다.** 안 넣으면 네이버 칸은 계속 «—» 다.
넣고 나서 AEO 화면에서 「지금 재기」를 한 번 누르면 네이버 줄에 순위가 뜬다.

## 이번에 내가 두 번 틀렸다 — 다음 방이 같은 데서 안 넘어지게

**① 건너뛴 것을 초록으로 셌다.** 「api 전체 초록」이라고 보고했는데 그때 작업 자리
PostgreSQL 이 죽어 **DB 시험이 통째로 건너뛰어졌다**(`s` 는 초록이 아니다). GitHub 검사가
둘을 잡았고, 둘 다 내 변경 탓이 맞았다(가짜 SerpAPI 로 네이버를 흉내 내던 시험).

> **DB 시험은 이렇게 돌려야 실제로 돈다:**
> ```
> VEO_SCORING_SPECS_DIR=<repo>/packages/scoring-specs \
> VEO_TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@127.0.0.1:5432/veo_test \
> VEO_DATABASE_URL=<같은 값> \
>   .venv/bin/python -m pytest apps/api/tests -q -m requires_postgres
> ```
> 그냥 `pytest tests` 는 **조용히 건너뛴다.** 명세 폴더를 빠뜨리면 41건이 수집 단계에서
> 죽는다(`SpecNotFoundError`).

**② CI 가 PR 에 저절로 안 붙는 일이 있다.** #19 에서 실제로 그랬다(앞선 판들은 몇 초
만에 붙었다). Actions 자체는 살아 있었다. **`workflow_dispatch` 로 손으로 걸면** 같은
검사를 받는다 — 그 덕에 ①을 잡았다.

## 그다음 (순서대로)

1. **지도 1페이지 추이** — 지금은 우리 순위만 견준다. 1페이지 목록 자체가 어떻게 바뀌었나
   (누가 새로 들어오고 누가 빠졌나)는 아직 안 했다. `map_rank_readings.listings` 에 쌓여 있다.
2. **구글 지도가 약하다** — [실측] 그랜드연합의원은 카카오 1위인데 구글은 10곳 밖이다.
   거래처에 줄 「할 일」(구글 비즈니스 프로필 채우기)로 이어질 자리.

## 대기 / 차단

- **배포**: 이 방에서 `make deploy` 가 막힌다(「Production Deploy」). **합치기 요청**을 만들고
  사장님이 「Merge」 — 이 길이 정착됐다(#9·#10·#11·#12·#13). 우회하지 않는다.
- **바깥 통로가 이 방에서 전부 막혀 있다(403)**: serpapi.com · dapi.kakao.com ·
  api.dataforseo.com · searchapi.io · apify.com · veo.seokorea.org.
  **러너(GitHub Actions)는 나갈 수 있다** → 재는 일은 전부 러너에 맡긴다(`probe-naver-vendors.yml`).
  웹검색만 이 방에서 된다.
- **러너 일감은 main 에 있어야 목록에 뜬다**(`workflow_dispatch`). 가지에만 있으면 안 보인다.

## 주의 · 제약

- **열쇠·비밀번호는 저장소 비밀값으로만.** 대화창·일감 입력칸·문서·로그 어디에도 값을 옮겨
  적지 않는다(입력칸 값은 실행 목록에 그대로 남는다). DataForSEO 비밀번호는 한 번 대화창을
  거쳤으니 **나중에 바꾸시는 편이 좋다**.
- **판 번호를 미리 잡지 않는다.** 이번에도 0.3.631 을 물렸다가 같은 과 방이 먼저 닿아
  0.3.632 로 물러났다. 합칠 때 **변경이력은 두 항목을 다 남긴다**(덮으면 나간 판이 대장에서
  사라진다 — 0.3.574~577 이 그렇게 비었다).
- **대장 관문 둘**: §2 머리말은 `미배포 **X~Y**`(또는 `없음`·`N건`) 꼴이어야 하고 **범위 양 끝은
  대기 표에 줄이 있어야** 한다.
- **화면 글은 60자 상한**(관문 `screen-text-stays-short`). 줄일 때 **사실을 남기고 까닭을 버린다.**
- **작업 자리 PostgreSQL 이 자주 죽는다**(이번 세션 세 번). `pg_ctlcluster 16 main start` ·
  포트 **5432** · DB `veo_test` · postgres/postgres. 죽으면 수백 건이 「연결 거부」로 무더기
  실패한다 — 코드 문제와 헷갈리지 말 것.
- **mypy 가 람다 기본인자를 못 읽는다** — 닫힘함수로 바꾼다(이 방에서 두 번 걸렸다).
- **한마디짜리 실패는 실패가 아니라 물음이다.** 「403」·「FAILED」만 보고 멈추면 아무것도
  못 정한다. **까닭을 읽게 만들어야** 다음이 정해진다 — 이 방에서 두 번 같은 자리에 섰고,
  두 번 다 까닭을 읽자마자 풀렸다.
- **받아 보기 전에는 모른다.** 저쪽 문서에 적힌 것과 실제로 오는 것은 다르다. 그래서
  **돈 안 드는 판부터** 돌린다(크레딧 보기 → 무엇을 받나 → 시켜야만 돌리기).
- **가지를 main 으로 되돌리기 전에 안 합쳐진 커밋이 있는지 보라.** [사고 2026-09-21]
  `git checkout -B <가지> origin/main` 으로 체크포인트 커밋 하나를 덮었다. 원격에 남아
  있어 되살렸지만, 안 밀었으면 잃었다. **`git log origin/<가지>..HEAD` 로 먼저 센다.**
- **CI 가 PR 에 저절로 안 붙는 일이 잦다**(#19·#21 에서 실제로). Actions 자체는 살아
  있다 — `workflow_dispatch` 로 **손으로 걸면** 같은 검사를 받는다.
- 커밋은 **이 가지에만.** main 에 직접 밀지 않는다. 모델 ID 는 어디에도 안 적는다.

## 이 방이 정한 것 (다시 묻지 않는다)

- **주소는 후보를 만드는 데만 쓴다** — 통합시일 때만 옛 시 이름을 후보로 더 낸다.
- **고르는 것은 사람이다.** 검색수를 하나도 못 쟀으면 추천하지 않는다.
- **사람이 누르면 아홉 자리, 자동은 다섯 자리.**
- 못 잰 자리도 줄로 남긴다 · 이름이 비슷하기만 하면 순위로 세지 않는다(`AMBIGUOUS`).
- **아무 곳도 없던 자리는 「1페이지 밖」이 아니다** — 분모에서 빼되 몇 자리였는지 함께 적는다.
- 경쟁사 자동 등록은 **AI 답변 쪽과 같은 열쇠**를 쓴다(두 줄로 안 쌓이고, 껐던 곳은 안 켜진다).
- **직접 긁기(브라우저)는 권하지 않는다** — 약관·차단·캡차. Aside.com 도 제품엔 안 맞는다
  (사람 컴퓨터에서 도는 AI 브라우저 · 좌표 지정 불가). 남의 손(Apify)을 빌리는 쪽이 낫다.

## 참고

ANSEO: `docs/WORKLIST.md` 배포 대기 표 · `docs/WORKLIST-HISTORY.md` 2026-09-19 절 ·
`docs/STATE.md`(⑫ 이 방 · ⑬ 네이버 돈 판단) · `apps/api/src/veo/maps/probe_naver_vendors.py`
머리말(실측이 전부 적혀 있다). 이 저장소 현황은 `PROJECT_STATE.md`, 지도는 `핵심두뇌_MASTER.md`.
