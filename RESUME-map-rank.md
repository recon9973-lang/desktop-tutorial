<!-- 가지: claude/confident-archimedes-f2eegb -->

# RESUME (지도 순위 방) — 2026-09-19 19:1x KST

> **이 방의 일감은 이 저장소가 아니라 ANSEO(`veo-platform`)에 있다.**
> 여기(`desktop-tutorial`)는 베놈 마케팅 사이트다 — 코드는 한 줄도 안 건드렸다.
> 이번 세션 기록: `docs/session-logs/2026-09-19-map-rank.md` (오더 12건)

## 지금까지 (핵심만)

「상권」 탭에 **«지도 첫 페이지»** 를 만들어 **나갔고**(0.3.627), 그 뒤 남은 일감 둘을 만들어
**합치기 요청 #10 에 올려 두었다**(0.3.629 · 사장님이 「Merge」 를 누르면 나간다).

```
나감   0.3.627  지도 첫 페이지 — 대표 키워드 1개로 네이버·카카오·구글 순위
                [실측 2026-09-19 18:44 KST · 합치기 #9 · 커밋 c244bd85]
대기   0.3.629  거래처 화면 한 줄 요약 · 주 1회 저절로 재기  → 합치기 요청 #10
카카오 ✅ 잰다      구글 ✅ 잰다      네이버 ⛔ 확인 전      빙 ⛔ 통로가 닫혔다
검색광고 열쇠 ✅ 사장님이 넣으심 — 후보 펼 때 월 검색수·추천이 붙는다
```

## 바로 이어갈 작업

1. **합치기 요청 #10 이 나갔는지 확인.**
   `git fetch origin main && git show origin/main:apps/api/src/veo/__init__.py`
   에 **0.3.629**(또는 그 위)면 닿은 것이다. 닿았으면 `docs/STATE.md`·`WORKLIST.md`
   대기 표에서 그 줄을 내리고 §2 머리말의 미배포 범위도 같이 줄인다(관문이 본다).
   아직이면 검사 상태를 보고 사장님께 한 줄로 알린다.
2. **네이버 실측** — 이 방에서는 못 한다(바깥이 막혔다). 열쇠 있는 자리에서 한 줄:
   `VEO_SERPAPI_KEY=… python -m veo.maps.probe_naver_places`
   · 장소 묶음이 오면 `maps/naver_places.PLACE_KEYS` 를 그 열쇠 **하나로** 줄인다.
   · 안 오면 유료 제3자 통로(DataForSEO 등)를 쓸지 **사장님께 여쭐 일**(돈 · 대장 §5).
3. **지도 1페이지 업체를 경쟁사 후보로** — 첫 보고에서 약속한 자리인데 아직 안 했다.
   `competitors/auto_register.py` 가 지금은 AI 답변에 나온 이름만 본다. **지도 1페이지야말로
   진짜 옆집 경쟁사**다. `map_rank_readings.listings` 에 이미 상위 열 곳이 쌓인다.
4. **순위 추이** — 지난 판과 견줘 「지난주보다 두 계단」. 표(`map_rank_runs`)에 이미 쌓인다.

## 대기 / 차단

- **배포**: 이 방에서 `make deploy` 가 막힌다(「Production Deploy」). **합치기 요청**을 만들고
  사장님이 「Merge」 두 번 — 이 길이 정착됐다. 우회하지 않는다.
- **네이버 순위**: 실측 전까지 화면은 «—» 와 「이 통로는 지도 목록을 주지 않습니다」.
  **열쇠 탓으로 적지 않는다.**
- **운영 확인**: 이 방은 veo.seokorea.org 에 403 이다. 「도는 판」은 못 쟀다고 적는다.

## 주의 · 제약

- **판 번호를 미리 잡지 않는다.** 이 세션에서만 620 → 625 → 626 → 627 → 628 → 629 로
  다섯 번 밀렸다. 나갈 때(합치기 직전) 물린다.
- **대장 관문 둘**: §2 머리말은 `미배포 **X~Y**` 꼴이어야 하고 **그 범위의 양 끝은 대기 표에
  줄이 있어야** 한다. 나간 판은 표에서 내리고 범위도 줄인다.
- **화면 글은 60자 상한**(관문 `screen-text-stays-short`). 줄일 때 **사실을 남기고 까닭을
  버린다**.
- 작업 자리 시험 DB(로컬 PostgreSQL 5433)가 전체 시험 도중 **자주 죽는다**. 죽으면 1,300건이
  「연결 거부」로 무더기 실패한다 — 코드 문제와 헷갈리지 말고 다시 띄우고 돌린다.
- 커밋은 **이 가지에만.** main 에 직접 밀지 않는다. 모델 ID 는 어디에도 안 적는다.

## 이 방이 정한 것 (다시 묻지 않는다)

- **주소는 후보를 만드는 데만 쓴다** — 통합시(창원시 마산회원구)일 때만 구 어간 앞 두 글자를
  후보로 더 낸다(「마산」). 「동대문구」에는 안 쓴다.
- **고르는 것은 사람이다.** 검색수를 하나도 못 쟀으면 추천하지 않는다.
- **사람이 누르면 아홉 자리, 자동은 다섯 자리.** 자동은 사람이 안 볼 때 나간다.
- 못 잰 자리도 줄로 남긴다 · 이름이 비슷하기만 하면 순위로 세지 않는다(`AMBIGUOUS`).

## 참고

ANSEO: `docs/WORKLIST.md` 배포 대기 표 · `docs/WORKLIST-HISTORY.md` 2026-09-19 절 ·
`docs/STATE.md`. 이 저장소 현황은 `PROJECT_STATE.md`, 지도는 `핵심두뇌_MASTER.md`.
