<!-- 가지: claude/confident-archimedes-f2eegb -->

# RESUME (지도 순위 방) — 2026-09-19 17:4x KST

> **이 방의 일감은 이 저장소가 아니라 ANSEO(`veo-platform`)에 있다.**
> 여기(`desktop-tutorial`)는 베놈 마케팅 사이트다 — 코드는 한 줄도 안 건드렸다.
> 이번 세션 기록: `docs/session-logs/2026-09-19-map-rank.md`

## 지금까지 (핵심만)

「상권」 탭에 **«지도 첫 페이지»** 줄을 만들었다 — 대표 키워드 하나로 **네이버·카카오·구글**
지도에서 누가 1페이지에 있고 우리가 몇 번째인지 잰다. 대표 키워드는 주소와 상호로 후보를
만들고 **사람이 고른 것만** 저장한다(검색수가 있으면 가장 큰 것을 제안한다).

```
ANSEO 가지   claude/confident-archimedes-f2eegb · 커밋 b28d9ce6 · 판 0.3.626
카카오       ✅ 잰다 (공식 창구 · 이미 있는 REST 키)
구글         ✅ 잰다 (SerpAPI 지도 통로 · 자리마다 몫 1회)
네이버       ⛔ 확인 전 — 통로가 플레이스 목록을 주는지 아직 못 봤다
빙           ⛔ 통로가 닫혔다(2025-08-11) · 순위 줄에서 뺐다(등록 줄에는 그대로)
검색광고 열쇠 ✅ 사장님이 넣으셨다(09-19) — 후보 펼 때 월 검색수·추천이 붙는다
관문         서버·일꾼 7,968 · 화면 2,787 · tsc · next build · 드리프트 0 — 전부 초록
```

## 바로 이어갈 작업

1. **배포 확인.** 사장님이 **다른 자리에서 `make deploy`** 를 돌리기로 하셨다(이 방은 작업
   자리 분류기가 「Production Deploy」로 막는다 — 우회하지 않는다).
   나갔는지 보는 법: `git fetch origin main && git show origin/main:apps/api/src/veo/__init__.py`
   에 **0.3.626**(또는 그 위)이 있으면 닿은 것이다. 안 나갔으면 사장님께 한 번 여쭙는다.
2. **네이버 실측** — 열쇠 있는 자리에서 한 줄:
   `VEO_SERPAPI_KEY=… python -m veo.maps.probe_naver_places`
   · 장소 묶음이 오면 `maps/naver_places.PLACE_KEYS` 를 그 열쇠 **하나로** 줄인다.
   · 안 오면 유료 제3자 통로를 쓸지 **사장님께 여쭐 일**(돈 · 대장 §5).
3. **거래처 화면 한 줄 요약** — 「지도 등록」 줄 옆에 `네이버 — · 구글 7위 · 카카오 2위`.
4. **정기(주 1회) 자동 측정** — 지금은 사람이 「지금 재기」를 눌러야 잰다. 기존 정기 진단
   장치(`jobs/`)에 얹는다.

## 대기 / 차단

- **배포**: 사장님 손에 있다(위 1번).
- **네이버 순위**: 실측 전까지 화면은 «—» 와 「이 통로는 지도 목록을 주지 않습니다」로 둔다.
  **열쇠 탓으로 적지 않는다** — 열쇠로 풀리는 일이 아니다.

## 주의 · 제약

- **판 번호를 미리 잡지 않는다.** 이 세션에서만 0.3.620 → 0.3.625 → 0.3.626 으로 두 번
  밀렸다(다른 방이 먼저 main 에 닿았다). 배포가 나갈 때 스스로 물린다.
- 작업 자리 시험 DB(로컬 PostgreSQL 5433)가 전체 시험 도중 **두 번 죽었다**. 죽으면 1,300건이
  「연결 거부」로 무더기 실패한다 — 코드 문제와 헷갈리지 말고 다시 띄우고 돌린다.
- 이 방은 카카오·serpapi·네이버로 **못 나간다.** 응답 모양은 문서 기준이고, 다르면 실패한다
  (비슷한 칸에 밀어 넣지 않는다).
- 커밋은 **이 가지에만.** main 에 직접 밀지 않는다. 모델 ID 는 어디에도 안 적는다.

## 이 방이 정한 것 (다시 묻지 않는다)

- **주소는 후보를 만드는 데만 쓴다** — 통합시(창원시 마산회원구)일 때만 구 어간 앞 두 글자를
  후보로 더 낸다(「마산」). 「동대문구」에는 안 쓴다(「동대」가 선다).
- **고르는 것은 사람이다.** 검색수를 하나도 못 쟀으면 추천하지 않는다.
- **아홉 자리에서 잰다.** 못 잰 자리도 줄로 남긴다.
- 이름이 비슷하기만 하면 순위로 세지 않는다(`AMBIGUOUS`).

## 참고

ANSEO 쪽 자세한 것: `docs/WORKLIST.md` 배포 대기 표 0.3.626 줄 ·
`docs/WORKLIST-HISTORY.md` 2026-09-19 절 · `docs/STATE.md` 미배포 표.
이 저장소 현황은 `PROJECT_STATE.md`, 지도는 `핵심두뇌_MASTER.md`.
