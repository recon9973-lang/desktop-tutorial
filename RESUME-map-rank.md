<!-- 가지: claude/confident-archimedes-f2eegb -->

# RESUME (지도 순위 방) — 2026-09-21 17:xx KST

> **이 방의 일감은 이 저장소가 아니라 ANSEO(`veo-platform`)에 있다.**
> 여기(`desktop-tutorial`)는 베놈 마케팅 사이트다 — 코드는 한 줄도 안 건드렸다.
> 기록: `docs/session-logs/2026-09-19-map-rank.md`(1~6부 · 오더 43건) ·
> `docs/session-logs/2026-09-21-map-rank.md`(**7부 · 오더 44~52**)

## 이 방의 일은 **끝났다** — 화면으로 확인했다

```
[실측 2026-09-21 14:56 KST · 사장님 화면]
네이버 지도   5위   5위 · 상위 10곳 안 · 「네이버 지도 · 광고 뺀 차례」
카카오맵      1위   1위 · 상위 10곳 안
구글 지도      —    1페이지 밖
```

**세 지도가 한 화면에 같이 나온다.** 시작할 때 네이버는 아예 못 읽던 자리였다.

## 나간 판 (이 방 것만)

```
0.3.627  지도 첫 페이지 — 네이버·카카오·구글 순위           [합치기 #9]
0.3.629  거래처 화면 한 줄 요약 · 주 1회 저절로 재기         [합치기 #10]
0.3.632  지도 1페이지→경쟁사 후보 · 지난 판과 견주기         [합치기 #11]
(번호없음) 통로 재는 도구 + 러너 일감                       [합치기 #12·#13]
(번호없음) 지도 자체를 읽는 길(Apify) 재는 갈래             [합치기 #14·#15·#16·#18]
0.3.640  **네이버 순위를 실제로 읽는다 — 제품에 붙임**       [합치기 #19]
0.3.641  자리를 흩지 않는다 — 중심 한 곳에서만              [합치기 #21]
0.3.642  가운뎃자리도 자리 수를 안 보낸다(빨간 글 고침)      [합치기 #22]
0.3.644  네이버 지도 열쇠도 「외부 연결 열쇠」 화면에        [합치기 #23]
```

**미배포 없음.** 가지는 main(`467bede4`)과 한 글자도 다르지 않다.

## 바로 이어갈 작업

사장님이 새 오더를 주실 때까지 **손댈 것이 없다.** 남은 것은 아래 둘이고, 둘 다
**급하지 않다** — 먼저 여쭤 보고 시작한다.

1. **지도 1페이지 목록 자체의 추이** (TODO #1)
   지금은 우리 순위만 지난 판과 견준다. 1페이지 목록이 어떻게 바뀌었나(누가 들어오고
   누가 빠졌나)는 아직 안 냈다. 자료는 `map_rank_readings.listings` 에 이미 쌓여 있다.
   [실측 2026-09-21] 「춘천정형외과」 2·3등이 **하루 만에 뒤바뀌었다** — 이 일감의 값.

2. **구글 지도가 약하다 → 거래처 할 일** (TODO #2)
   카카오 1위 · 네이버 5위인데 구글만 1페이지 밖이다. 코드 문제가 아니라 **거래처가
   구글 비즈니스 프로필을 안 채운 것**일 가능성이 크다. **가설부터 재고** 나서 잇는다.

## 대기 / 차단

- 없다. Railway 열쇠(`VEO_APIFY_TOKEN`)는 들어가 있고 **작동 중**이다(5위가 증거).
- **배포**: 이 방에서 `make deploy` 가 막힌다. **합치기 요청**을 만들고 사장님이 「Merge」 —
  이 길이 정착됐다(#9~#23). 우회하지 않는다.
- **바깥 통로가 이 방에서 전부 막혀 있다(403)**: serpapi.com · dapi.kakao.com ·
  api.dataforseo.com · searchapi.io · apify.com · **veo.seokorea.org**.
  **러너(GitHub Actions)는 나갈 수 있다** → 재는 일은 전부 러너에 맡긴다.
  운영이 살아 있나 보려면 **`rollout-check.yml` 을 손으로 건다**(판·`/api/health` 를 준다).

## 이번 세션에서 배운 것 — 다음 방이 같은 데서 안 넘어지게

**① 화면과 엔진을 같이 고칠 때는 셋이다 — 화면 · 가운뎃자리 · 엔진.**
자리 수를 엔진·화면에서 빼고 **중간 창구**(`apps/web/src/app/api/map-ranks/route.ts`)를
빠뜨려, 그 창구가 스스로 9 를 채워 보내 사장님 화면에 빨간 글이 났다. 웹 창구 53개 중
시험이 있는 것은 11개뿐이다 — **아무도 안 재는 자리가 사고가 난다.**

**② 빨간 글을 보면 먼저 「언제」를 본다.**
「서버에 연결하지 못했습니다」가 났는데 엔진이 **다시 올라오던 중**이었다(uptime 299초).
**날짜 줄이 안 바뀐 것**이 증거였다 — 화면에 보이던 값은 옛 판이었다. 코드부터 의심했으면
한 세션을 날렸다.

**③ 좋은 소식 바로 뒤에 「그런데」를 붙이지 않는다.**
「다 됐습니다」 뒤에 Apify 가 목록에 없다는 얘기를 꺼냈더니 사장님이 **고장 난 줄** 아셨다
(«다 넣어서 작동 중인데 무슨 말이야 갑자기?»). 매듭을 짓고 **한 박자 쉰 뒤** 꺼낸다.

**④ 관문을 놓았으면 되돌려서 잡히는지 확인한다.**
이 세션에서 관문 둘을 놓고 둘 다 일부러 되돌려 빨간불을 봤다. 안 해 보면 장식이다.

**⑤ 이름을 시험에 옮겨 적지 않는다.** 옮기면 **옮긴 그 순간의 사실만** 지키고 새로
더해지는 것을 못 본다. 바깥 근거(엔진 파일·계약 문서)를 읽게 만든다.

## 주의 · 제약 (이 방의 고정 규칙)

- **「됐다」는 화면에서 값이 보일 때만 쓴다.** 기록 저장과 제품은 다른 일이다.
- **DB 시험은 이렇게 돌려야 실제로 돈다** (그냥 `pytest` 는 **조용히 건너뛴다**):
  ```
  VEO_SCORING_SPECS_DIR=<repo>/packages/scoring-specs \
  VEO_TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@127.0.0.1:5432/veo_test \
  VEO_DATABASE_URL=<같은 값> .venv/bin/python -m pytest apps/api/tests -q
  ```
  명세 폴더를 빠뜨리면 41건이 수집 단계에서 죽는다. `-p no:warnings` 를 붙이면
  `filterwarnings` 표지를 못 읽어 수집이 터진다. **`s`(건너뜀)는 초록이 아니다.**
  마지막 전체 판: **8,039건 초록 · 실패 0** · 건너뜀 56건은 전부 까닭이 적힌 것.
- **mypy 는 `apps/api` 안에서 돌려야 맞다**(밖에서 돌리면 없는 오류 3건이 나온다).
  `ruff format --check` 는 CI 가 안 돌린다 — 526개가 「고칠 것」으로 나와도 정상.
- **작업 자리 PostgreSQL 이 자주 죽는다**: `pg_ctlcluster 16 main start` · 포트 5432 ·
  DB `veo_test` · postgres/postgres.
- **CI 가 PR 에 저절로 안 붙는 일이 잦다**(#19·#21·#22·#23). `workflow_dispatch` 로
  **손으로 걸면** 같은 검사를 받는다(`ci.yml` · `ref=<가지>`).
- **판 번호를 미리 잡지 않는다.** 대기 표에 `—` 로 쌓고 **자기 가지를 적는다.**
  **대장 관문 둘**: §2 머리말이 대기 표와 같은 범위를 말해야 하고, 번호 없는 줄이
  있으면 머리말에 **「번호 미정 N건」**을 적어야 한다(관문이 문구까지 알려 준다).
- **대장은 자주 부딪힌다** — 다른 방이 같은 줄을 고친다. **저쪽의 배포 사실을 살리고
  내 줄만** 남긴다. 합쳐진 뒤라면 main 것을 통째로 가져오는 쪽이 깨끗하다.
- **가지를 되돌리기 전에 `git log origin/<가지>..HEAD` 로 먼저 센다.**
  되돌리는 밀기(force push)는 막혀 있고, **막는 것이 맞다** — 부수지 않는 길로 간다.
- **열쇠·비밀번호는 저장소 비밀값으로만.** 대화창·일감 입력칸·문서·로그 어디에도 값을
  옮겨 적지 않는다(입력칸 값은 실행 목록에 그대로 남는다). DataForSEO 비밀번호는 한 번
  대화창을 거쳤으니 **나중에 바꾸시는 편이 좋다**.
- 커밋은 **이 가지에만.** main 에 직접 밀지 않는다. 모델 ID 는 어디에도 안 적는다.
- **화면 글은 60자 상한**(관문 `screen-text-stays-short`). 줄일 때 **사실을 남기고
  까닭을 버린다.**

## 이 방이 정한 것 (다시 묻지 않는다)

- **네이버 통합검색은 병원 검색어에 장소 묶음을 안 띄운다**(실측 5회 · 맛집은 띄운다).
  통합검색을 읽는 통로를 **사도 소용없다.** 그래서 **지도 화면 자체**를 읽는다.
- **네이버 지도는 Apify `fetch_cat~naver-map-places-scraper` 로 읽는다.** 광고는 저쪽도
  빼고 주지만 **우리도 다시 거르고 차례를 다시 매긴다**(조용한 거짓이 가장 나쁘다).
  값은 열 번 재고 $0.0001 대. 주택용 회선을 쓰는 도구(`abotapi~…`)는 무료 요금제로 안 돈다.
- **자리를 흩지 않는다** — 중심 한 곳에서 한 판. 사장님 지시(«방위가 의미 없어»)이고
  **재서 확인했다**(좌표 없이 물어도 차례가 같았다). 되돌리려면 **먼저 재라.**
- **주소는 후보를 만드는 데만 쓴다** · **고르는 것은 사람이다** · 못 잰 자리도 줄로 남긴다 ·
  이름이 비슷하기만 하면 순위로 세지 않는다(`AMBIGUOUS` · `placeId` 가 이것을 푼다).
- **직접 긁기(브라우저)는 권하지 않는다** — 약관·차단·캡차. 남의 손을 빌리는 쪽이 낫다.

## 참고

ANSEO: `docs/WORKLIST.md`(대기 표·§2 머리말) · `docs/WORKLIST-HISTORY.md` ·
`docs/STATE.md` · `apps/api/src/veo/maps/apify_places.py` 머리말(실측 대조표가 있다) ·
`apps/api/src/veo/maps/probe_naver_vendors.py` 머리말(통로 재기 열 판이 다 적혀 있다).
이 저장소 현황은 `PROJECT_STATE.md`, 지도는 `핵심두뇌_MASTER.md`.
