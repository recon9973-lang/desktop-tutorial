// 지표 도움말 — 대시보드·사이트·공유리포트에서 마우스를 올리면 뜨는 설명.
// 표시 문구의 단일 기준(single source of truth). 비개발자(고객사 포함)가 읽는 글이므로
// 전문용어 없이, 계산 방식과 한계를 있는 그대로 적는다.
//
// ⚠️ 여기 문구를 고칠 때는 반드시 실제 코드와 대조할 것. 그럴듯하지만 틀린 설명은
//    지표가 조용히 거짓말하는 것과 똑같은 사고를 만든다(docs/08 함정 ⑩·⑩-2 참고).
export const METRIC_HELP = {
  // ── 사이트 개요 ──
  // src/app/api/collect/route.ts — sessionKey 단위. t.js: sessionStorage + 30분 TTL
  sessions:
    "방문 한 번을 뜻합니다. 한 사람이 여러 페이지를 봐도 한 번으로 셉니다. 30분 넘게 아무 활동이 없다가 다시 움직이면 새 방문으로 셉니다. 브라우저 탭을 닫아도 그 방문은 끝납니다.",

  // collect/route.ts: isBounce = !(pageViews > 1 || click·cta_click·conversion·form_submit)
  bounce:
    "한 페이지만 보고, 아무것도 누르지 않은 채 나간 방문의 비율입니다. 스크롤만 내린 것도 이탈로 봅니다. 높으면 첫 화면이 방문자가 기대한 내용과 다르다는 신호일 수 있습니다.",

  // metrics.ts: avg(Session.maxScrollPct)
  avgScroll:
    "방문자가 페이지를 평균 몇 %까지 내려봤는지입니다. 이 숫자가 낮으면, 중요한 내용이 방문자가 보지 않는 아래쪽에 있다는 뜻입니다.",

  // t.js: window.flowlens.track("이름") 을 사이트에 직접 심어야만 발생
  conversions:
    "예약 완료·문의 접수처럼 '성공'으로 칠 행동의 횟수입니다. 이 값은 사이트에 전환 신호를 직접 심어야 잡힙니다. 계속 0으로만 나온다면 아직 심지 않은 것이지, 성과가 없다는 뜻이 아닙니다.",

  // ── 클릭 ──
  clicks: "링크·버튼처럼 실제로 눌리는 것을 누른 횟수입니다.",

  // t.js: isInteractive() — a/button/input/select/textarea/role=button/onclick 을 조상 4단계까지 확인
  deadClick:
    "눌렀는데 아무 일도 일어나지 않는 곳을 누른 횟수입니다. 이미지나 글씨가 버튼처럼 보일 때 생깁니다. 다만 자바스크립트로만 동작하는 버튼은 실제로는 잘 눌려도 여기 잡힐 수 있으니, 숫자가 높으면 히트맵에서 어디를 눌렀는지 직접 확인해 보세요.",

  // t.js: 0.5초 내 5% 이내 근접 3회 이상
  rageClick:
    "0.5초 안에 같은 자리를 세 번 넘게 연타한 횟수입니다. 버튼이 느리거나 눌리지 않을 때 나오는 답답함의 신호입니다.",

  // metrics.ts: CTA를 1개 이상 본 세션 ÷ 전체 세션 (t.js가 가림 여부까지 확인)
  ctaViewRate:
    "예약·문의 같은 핵심 버튼을 화면에서 실제로 본 방문자의 비율입니다. 팝업에 가려져 있었다면 본 것으로 치지 않습니다. 이 숫자가 낮으면 핵심 버튼이 너무 아래에 있거나 무언가에 가려져 있다는 뜻입니다.",

  mobilePct: "휴대폰으로 들어온 방문의 비율입니다. 이 값이 높으면 개선은 모바일 화면을 기준으로 판단해야 합니다.",

  // ── 대시보드(전체) ──
  totalSessions: "관리 중인 모든 사이트의 방문을 합친 수입니다.",
  sitesManaged: "추적 스크립트가 설치되어 실제로 데이터를 모으고 있는 사이트 수입니다.",
  suggestions:
    "쌓인 데이터가 정해진 기준을 넘으면 자동으로 만들어지는 개선 제안의 개수입니다. 사람이 하나씩 쓴 것이 아니라 규칙에 따라 계산된 것이므로, 데이터가 적을 때는 신뢰도 표시를 함께 보세요.",
  totalConversions: "모든 사이트의 전환을 합친 수입니다. 전환 신호를 심은 사이트만 집계됩니다.",
} as const;
