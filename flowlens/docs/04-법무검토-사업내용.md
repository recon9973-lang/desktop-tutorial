# FlowLens 법무 검토용 사업·데이터 처리 개요

작성일: 2026-07-14 · 개정: 2026-07-19 (사업자 정보 확정, 구현 현황 갱신, 시스템 설계 보강)
목적: **개인정보 보호 전문가/법률 자문에게 검토를 의뢰하기 위한 사실 정리 문서**입니다. 본 문서는 서비스가 실제로 무엇을·어떻게 수집·처리하는지를 정확히 기술하며, 마지막에 검토 요청 쟁점과 시스템 설계를 정리합니다.

> ⚠️ 본 문서는 법률 의견이 아니라 **사업자가 사실관계를 정리한 자료**입니다. 최종 판단(개인정보 해당 여부, 동의 요건, 계약 형태 등)은 전문가 검토로 확정해 주세요.

---

## 0. 사업자 정보

- 상호/법인명: **주식회사 베놈** (VENOM Inc.)
- 대표자: **김보형**
- 사업자등록번호: **291-86-02777**
- 주소: **대구광역시 수성구 용학로25길 54, 4층**
- 개인정보 보호책임자(CPO): **이재훈** / 이메일 **venomad@naver.com** / 연락처 **1661-4142**
- 고객 문의: venomad@naver.com (CPO와 동일)
- 서비스명: **FlowLens** (웹 행동 분석 SaaS)
- 서비스 도메인: **https://flow.seokorea.org** (운영 중)

---

## 1. 사업 개요

FlowLens는 웹사이트 운영자(특히 **마케팅 대행사** 및 중소 쇼핑몰·병원/상담·랜딩페이지 사업자)가 자신 또는 고객사의 웹사이트에 **추적 스크립트 한 줄**을 설치하면, 방문자의 클릭·스크롤·체류·좌절클릭·모바일 제스처 등 **행동 데이터**를 수집·분석하여 히트맵·리포트·개선 제안을 제공하는 **월 구독형(SaaS) 도구**입니다.

- 핵심 가치: "방문자가 어디서 망설이고 이탈하는지"를 시각화하고 개선 과제를 한국어로 제안.
- 개인 식별이 목적이 아니며, **개인정보 최소 수집**을 설계 원칙으로 합니다.
- 유사 서비스: Microsoft Clarity, Hotjar, Mouseflow, 뷰저블(Beusable), Amplitude 등.

### 사업 모델
- 요금제(현행 3티어, 월 구독): **무료(0원, 14일 체험)** / **그로우(19,800원)** / **에이전시(88,000원)**. 한도 초과 시 종량 과금 또는 수집 정지 선택.
- 결제: **토스페이먼츠(PG) 연동** 예정. 카드정보는 PG가 처리하며 FlowLens는 카드번호를 저장하지 않음.

---

## 2. 데이터 주체(정보주체)는 두 종류입니다 — **구분이 핵심**

| 구분 | 대상 | 관계 |
|---|---|---|
| **가) 서비스 회원** | FlowLens에 가입한 대행사/사업자(직접 계약자) | FlowLens와 직접 계약 |
| **나) 방문자** | 회원(또는 그 고객사)의 **웹사이트를 방문한 일반 이용자** | FlowLens와 직접 관계 없음, 웹사이트 운영자를 통해 데이터 발생 |

> 법적으로 가장 중요한 쟁점: **나) 방문자 행동 데이터**에 대해 FlowLens가 "수탁처리자"인지, "제3자 제공받는 자"인지, 동의 획득 의무가 누구에게 있는지입니다. (→ 6장·9장 참고)

---

## 3. 수집하는 데이터 (정확한 목록)

### 가) 서비스 회원(가입 사업자)
- 이메일, 이름, 대행사/회사명
- 비밀번호: **평문 미저장**, scrypt 해시(salt 포함)로만 저장
- 요금제, 가입일시
- 로그인 세션: 서명된 쿠키(HMAC, 사용자 ID 서명값). 별도 추적 아님.
- 결제정보: **토스페이먼츠가 처리**. FlowLens 서버는 카드번호·유효기간 등을 저장하지 않음(결제 승인 결과의 주문번호·금액·승인시각 정도만).

### 나) 웹사이트 방문자(행동 데이터)
수집 이벤트 종류: `page_view, click, rage_click(좌절클릭), dead_click(무반응클릭), scroll, pointer_sample(마우스 이동 샘플), form_focus, form_submit, cta_view, cta_click, conversion, session_end, double_tap, zoom, swipe`

각 이벤트에 저장되는 필드:
- 이벤트 종류, 페이지 URL·경로
- **좌표: 문서 대비 상대값(0~1)** — 절대 픽셀 미저장
- 뷰포트 크기(가로/세로 px), 스크롤 도달률(%)
- 클릭 대상 라벨(`targetLabel`): 버튼/링크의 짧은 텍스트 — **전송 전 마스킹 처리**(아래)
- 제스처 방향(줌 in/out, 스와이프 상하좌우) 등 부가정보(JSON)
- 발생 시각

세션 단위 정보:
- 세션키: 브라우저 `sessionStorage`에 저장되는 **임의 난수**(30분 무활동 시 만료). 개인 식별자 아님, 쿠키 기반 크로스사이트 추적 아님.
- 디바이스 종류(모바일/데스크톱/태블릿), 유입 경로 대략 분류(직접/검색/광고/소셜/추천), 최대 스크롤 도달률, 페이지 수, 이탈 여부

### ★ 수집하지 **않는** 데이터 (설계상 원천 차단)
- **폼 입력값**(이름·전화·이메일·주소 등)과 **비밀번호**: 코드 경로에 존재하지 않음(value에 접근하지 않음)
- **원본 IP 주소**: 저장하지 않음
- **절대 픽셀 좌표**: 상대값(0~1)만 저장
- **화면 녹화/DOM 스냅샷**: 세션 "리플레이"는 DOM을 녹화하지 않고 수집된 이벤트를 시간순 재생하는 방식 → 화면에 표시된 개인정보가 캡처되지 않음

### ★ 민감정보 자동 마스킹
클릭 대상 라벨 등 텍스트에 대해 **전송 전(클라이언트)** 정규식으로 이메일·전화번호·카드번호·주민등록번호 패턴을 `***`로 치환. 비밀번호 입력 영역 근처의 이벤트는 좌표·라벨 모두 미수집.

### ★ 봇 제외
`navigator.webdriver`가 감지되면 수집 중단.

---

## 4. 데이터 처리 흐름

1. 웹사이트 운영자가 자신/고객사 사이트에 추적 스크립트(`t.js`)를 설치(HTML 삽입 또는 워드프레스 플러그인 등).
2. 방문자가 페이지를 이용하면 스크립트가 이벤트를 모아 주기적으로 FlowLens 수집 서버(`/api/collect`)로 전송(HTTPS).
3. 서버가 사이트 식별키(siteKey)로 사이트를 확인하고 이벤트를 데이터베이스에 저장.
4. 회원(대행사)이 대시보드에서 히트맵·리포트·개선 제안을 조회.
5. 회원은 **읽기 전용 공유 링크**(토큰 기반, 로그인 불필요)를 생성해 고객에게 리포트를 공유할 수 있음. 공유 리포트에는 **집계 지표와 개선 제안만** 표시(개별 개인정보 없음).

---

## 5. 보관 및 파기

- 원본 이벤트 보관기간(현행 3티어): **무료 14일 / 그로우 90일 / 에이전시 원시 90일 + 집계 스냅샷 365일**(사이트별 `retentionDays` 필드로 강제). 보관기간이 지난 원시 이벤트는 히트맵 격자 스냅샷으로 롤업 후 삭제되어, 개별 좌표·세션은 사라지고 익명 집계만 남습니다.
- 회원 정보: 탈퇴 시 파기(법령상 보존의무 있는 항목 제외). 대행사 삭제 시 소속 고객사·사이트·세션·이벤트가 연쇄 삭제(`onDelete: Cascade`)됩니다.
- 방문자/세션 단위 삭제: 사이트별 "데이터 관리" 탭에서 기간·경로·세션키·전체 단위 즉시 삭제 지원.

> **현재 구현 상태 (2026-07-19)**: 보관기간 초과분 삭제가 **Vercel Cron으로 자동화 완료**되었습니다. `vercel.json`의 크론이 매일 03:00(KST) `/api/cron/cleanup`을 호출해 전 사이트를 롤업 후 삭제합니다. 이 엔드포인트는 `CRON_SECRET` Bearer 토큰으로 보호되며(운영 환경변수 설정 완료), 미인증 호출은 401로 거부됩니다.

---

## 6. 위탁·제3자 제공·국외 이전

- **웹 호스팅·서버리스 컴퓨팅**: **Vercel Inc.**(미국 법인). 서비스 화면 제공 및 수집·처리 로직 실행. 함수 실행 리전은 **sin1(싱가포르)**.
- **데이터베이스(저장)**: **Neon Inc.**(PostgreSQL 관리형). 리전 **아시아·싱가포르**. 행동 데이터·회원 정보의 실제 저장소.
- → 위 두 수탁사로 인해 데이터가 **국외(싱가포르)에서 처리·보관**됩니다. `/privacy` 방침 5장에 위탁·국외이전 사실을 명시했습니다. **국외이전 시 고지·동의 요건**은 법무 검토 쟁점 4번 참조.
- **결제대행**: 토스페이먼츠(결제정보 처리) — 연동 예정. 카드번호 등은 PG가 처리하며 FlowLens는 저장하지 않음.
- **AI 리포트(선택, 현재 미활성)**: 향후 LLM API로 리포트 문장을 다듬는 기능을 붙일 수 있으며, 이 경우 처리 내용·수탁사·국외이전 여부를 별도 고지 예정. **현재는 룰 엔진(자체 서버 내 결정론적 로직)만 사용하며 외부 AI에 데이터를 보내지 않음.**
- 그 외 제3자 제공 없음(법령 근거·동의 예외 제외).

---

## 7. 현재 적용 중인 보호조치

- 전송 구간 HTTPS
- 폼 입력값·비밀번호 원천 미수집, 민감정보 **클라이언트 + 서버 2중 마스킹**(이메일·전화·카드·주민번호)
- 원본 IP 미저장, 좌표 상대값(0~1)만 저장
- **URL query/hash 제거**(SDK·서버 2중), **referrer는 호스트명만** 저장
- **수동 전환 meta는 allowlist(dir만)** 로 제한하고 민감 키 차단
- **민감영역 수집 제외**: 고객이 `data-fl-ignore`/`.fl-sensitive` 지정 시 해당 영역 이벤트 미수집, `data-fl-label`로 안전 라벨 지정 가능
- **수집 API 도메인 검증**: Origin/Referer 호스트가 등록 도메인과 불일치하면 조용히 무시
- 비밀번호 scrypt 해시 저장, **운영 시크릿 강제**(기본값 사용 시 서버 기동 실패), 로그인 쿠키 **Secure(운영)**
- 멀티테넌시 격리: 모든 조회를 로그인 회원의 소속(대행사) 범위로 제한
- 공유 리포트는 집계값만 노출·읽기전용·**추측 불가 랜덤 토큰**·**기본 30일 만료**
- **보관기간 초과 이벤트 자동 삭제**(Vercel Cron 매일 실행, `CRON_SECRET` 인증 — 설정 완료)
- 봇 트래픽 제외(`navigator.webdriver`)

- **추적 거부 신호 존중**: GPC(Global Privacy Control)·DNT·방문자 opt-out(`window.flowlens.optOut()`, localStorage) 감지 시 수집 완전 중단
- **방문자 데이터 삭제 도구**: 기간·경로·세션키·전체 단위로 즉시 삭제(삭제요청 대응). 사이트별 `/데이터 관리` 탭
- **감사 로그**: 로그인·데이터 삭제·공유링크 생성·요금제 변경·가입을 시각과 함께 기록

**2026-07 보안 감사(해킹 시나리오 포함) 반영 — 추가 구현·검증 완료:**
- **결제 우회 차단**: 요금제 상향(유료 전환)은 결제 성공 경로에서만 반영되도록 서버에서 차단(무료 가입 후 임의 상향 불가). 하향만 셀프 허용.
- **행동데이터 열람 인증**: 히트맵 좌표 조회(`/api/overlay`)에 로그인 + 본인 대행사 사이트 검증 적용, 캐시 no-store. 크롬 확장 등 외부 뷰어는 공개 `siteKey`와 분리된 비밀 `overlayToken` 사용.
- **수집 API 남용 방지(rate limit)**: 공개값인 `siteKey`로 위조 이벤트를 대량 주입하는 것을 막기 위해 IP·사이트 기준 고정윈도우 제한(10초당 20요청), 요청당 이벤트 100개 상한. 원문 IP는 저장하지 않고 해시(`RateLimit`/`LoginThrottle`/`DiagnoseQuota` 모두 해시만)로만 사용.
- **도메인 검증 fail-closed**: Origin/Referer 헤더가 없거나 등록 도메인과 불일치하면 수집 거부(과거 헤더 부재 시 통과하던 우회 제거).
- **이벤트 시각 위조 방어**: 클라이언트가 보낸 미래 시각으로 보관기간 삭제를 회피하지 못하도록 서버에서 시각을 검증·보정.
- **세션 토큰 만료**: 로그인 쿠키에 만료(7일) 포함 + 상수시간 비교. 쿠키 유출 시 영구 유효 문제 제거.
- **로그인 무차별 대입 방어**: 15분 내 10회 실패 시 잠금(`LoginThrottle`), 실패 응답은 계정 존재 여부를 노출하지 않음.

> 위 항목은 법무·보안 검토의 즉시 보완 권고를 반영해 구현·검증한 것입니다. 잔여 과제(법무 검토 시 함께 판단 요청): 삭제요청 **접수 창구/신원확인 절차** 표준화(현재는 운영자가 세션키로 삭제), 고위험 업종(의료·금융·법률) 설치 **정책·약관 분리**, CMP "동의 전 스크립트 미로드" 옵션, 감사로그 **보존기간·열람권한** 정책.

---

## 8. 사업자가 준비/확정해야 할 문서

- [x] 개인정보처리방침 — `/privacy` 페이지, **회사정보·CPO·수탁사·국외이전 기재 완료(v1.0)**. 전문가 검토만 남음.
- [x] 이용약관 — `/terms` 페이지, **회사정보 기재 완료(v1.0)**. 전문가 검토만 남음.
- [x] 국외 이전 고지 — 방침 5장에 Vercel(미국)·Neon(싱가포르) 기재 완료. 동의 요건은 검토 필요.
- [ ] 개인정보 처리위탁 계약서/DPA (회원사와, 그리고 클라우드/PG 수탁 관련)
- [ ] 쿠키·행태정보 동의 문구 및 배너 — 랜딩에 동의 배너(CookieConsent) 구현됨. 문구 적정성 검토 필요.
- [ ] 고객사(회원)가 자사 사이트에 게시할 "FlowLens 사용 고지" 문구 템플릿

---

## 9. 법무 검토 요청 쟁점 (질문 리스트)

아래는 전문가께 판단을 요청드리는 항목입니다.

1. **개인정보 해당성**: 상대좌표+임시세션키+디바이스+유입경로+URL의 조합으로 수집되는 방문자 행동 데이터가 개인정보/가명정보/익명정보 중 무엇에 해당하는지? (개인 식별 목적 없음, IP·입력값 미수집 전제)
2. **동의 주체·요건**: 방문자 행동 데이터 수집에 대해 동의가 필요한지, 필요하다면 **동의 획득 의무가 웹사이트 운영자(회원)에게 있는지 FlowLens에게 있는지**? 쿠키/행태정보 동의 수준(맞춤형 광고 목적은 아님)?
3. **계약 형태**: FlowLens와 회원(웹사이트 운영자)의 관계가 개인정보 **처리위탁(수탁자)**인지, 제3자 제공인지, 공동처리인지? 표준 위탁계약/DPA 필수 여부와 필수 조항?
4. **국외 이전**: 해외 클라우드/향후 해외 LLM API 사용 시 고지·동의·문서화 요건?
5. **민감정보/특수업종**: 병원·상담 등 **의료 관련 사이트**에 설치될 때 추가 보호조치나 사용 제한이 필요한지? 금융·법률 사이트는?
6. **미성년자**: 만 14세 미만 대상 사이트에 대한 제한·법정대리인 동의 이슈 처리 방향?
7. **세션 리플레이**: 화면(DOM)을 녹화하지 않고 이벤트만 재생하는 방식이 개인정보/민감정보 노출 리스크를 충분히 낮추는지에 대한 확인.
8. **공유 링크**: 로그인 없이 열람 가능한 읽기전용 집계 리포트(개인정보 미포함)의 적정성 및 권고 보완사항.
9. **해외 규제**: EEA/UK/미국 방문자를 서비스 대상에 포함할 경우 GDPR/UK GDPR/CCPA 대응(사전 동의, GPC 등) 필요 범위.
10. **보관·삭제**: 요금제별 보관기간 정책의 적정성과, 방문자 삭제요청 처리 절차 요건.

---

## 10. 시스템 설계 (아키텍처·데이터 흐름·데이터 모델)

법률 검토 시 "무엇을 수집하는가"뿐 아니라 "어디에 어떻게 저장·격리·삭제되는가"를 함께 판단하실 수 있도록 실제 구현 기준으로 정리합니다.

### 10.1 기술 스택 및 배포 구조
- **프레임워크**: Next.js(App Router) — 프론트 화면과 백엔드 API가 한 코드베이스. 서버 컴포넌트 중심.
- **실행 환경**: Vercel 서버리스 함수, 리전 **sin1(싱가포르)**.
- **데이터베이스**: Neon 관리형 PostgreSQL, **싱가포르 리전**. ORM은 Prisma.
- **추적 스크립트**: 정적 파일 `public/t.js`(약 590줄, 외부 의존성 없음). 고객사 페이지 `<head>`에 한 줄 삽입 또는 GTM·워드프레스로 배포.
- **전송 구간**: 전부 HTTPS.

### 10.2 데이터 흐름 (수집 → 저장 → 조회 → 파기)

```
[방문자 브라우저]                         [FlowLens (Vercel/sin1)]        [Neon DB/싱가포르]
   t.js 실행                                                                   
   ├─ webdriver/GPC/DNT/opt-out 확인 → 해당 시 완전 중단                       
   ├─ 클릭·스크롤·체류·이동 등 이벤트 버퍼링                                    
   ├─ 클라이언트 1차 마스킹(이메일·전화·카드·주민번호 → ***)                   
   └─ 주기 전송(HTTPS) ──POST /api/collect──▶  siteKey로 사이트 확인            
                                              도메인(Origin/Referer) 검증      
                                              rate limit(IP·사이트 해시)       
                                              서버 2차 마스킹·URL query 제거    
                                              좌표 0~1 클램프, 시각 검증 ──────▶ Session/Event/MovePath 저장
                                                                                (원문 IP·폼값 컬럼 없음)
   [회원(대행사) 브라우저] ──로그인──▶ 대시보드(히트맵/리포트) ◀── 본인 대행사 범위만 조회
   [공유 링크 열람자] ──토큰──▶ 읽기전용 집계 리포트(개인정보 미포함)
                                              매일 03:00 KST cron ───────────▶ 보관기간 초과분 롤업 후 삭제
```

### 10.3 데이터 모델 (개인정보 관점 표기)

| 모델 | 용도 | 개인정보 관련성 |
|---|---|---|
| `Agency` | 대행사(최상위 격리 단위) | 회사명 등 사업자 정보 |
| `User` | 대행사 멤버(로그인 계정) | 이메일·이름·**scrypt 해시 비번**(평문 미저장) |
| `Client` / `Site` | 고객사·추적 대상 사이트 | 사이트명·도메인·공개 `siteKey`·비밀 `overlayToken` |
| `Session` | 방문 세션(구분용) | **임시 세션키**(sessionStorage 난수), 기기·유입채널·스크롤·이탈. 개인 식별자 아님 |
| `Event` | 개별 행동 이벤트 | 종류·경로·**상대좌표(0~1)**·뷰포트·스크롤%·**마스킹된 라벨**·시각. **IP·폼입력값 컬럼 자체가 없음** |
| `MovePath` | 마우스 이동 경로(압축 1줄) | `"x,y|…"` 0~1000 정수 상대좌표. 개인정보 없음 |
| `HeatmapSnapshot` | 보관초과분 롤업(격자 카운트) | 개별 세션 사라진 **익명 집계**만 |
| `Screenshot`/`DemoScreenshot` | 히트맵 배경(사이트 페이지 캡처) | 사이트 공개 화면 이미지(방문자 개인정보 아님) |
| `ShareLink` | 읽기전용 공유 | 추측불가 랜덤 토큰·기본 30일 만료 |
| `AuditLog` | 민감행위 감사 | 회원 이메일·행위·시각 |
| `LoginThrottle`/`RateLimit`/`DiagnoseQuota` | 남용 방지 카운터 | **원문 IP 미저장, 해시만** |

### 10.4 추적 스크립트(t.js) 개인정보 설계
- **입력값 원천 차단**: input/textarea/select/`[type=password]`의 `value`를 코드상 절대 읽지 않음.
- **민감영역 제외**: 고객이 `data-fl-ignore`/`.fl-sensitive` 지정 영역, 비밀번호 필드 근처 이벤트는 좌표·라벨 모두 미수집. `data-fl-label`로 안전 라벨 지정 가능.
- **세션 식별**: 쿠키·IP가 아닌 `sessionStorage` 난수(30분 무활동 만료). 크로스사이트 추적 아님.
- **거부 신호**: `navigator.webdriver`(봇), `globalPrivacyControl`, DNT, `fl_optout` 감지 시 즉시 중단.

### 10.5 멀티테넌시 격리
모든 데이터 조회는 로그인 회원의 소속 `Agency` 범위로 제한됩니다(대행사 간 데이터 접근 불가). 수집 API는 사이트별 공개 `siteKey`로만 쓰기 하되, 히트맵 열람 등 읽기는 로그인+본인 소속 검증을 거칩니다.

### 10.6 참고 소스
더 깊은 기술 사양은 `docs/03-기획.md`(데이터 모델·API·수집 원칙), `docs/08-프로젝트-현황과-작업기록.md`(구현·보안 이력), 실제 소스 `public/t.js`·`prisma/schema.prisma`·`src/lib/sanitize.ts`(마스킹)·`src/app/api/collect/route.ts`(수집 서버)를 참조하실 수 있습니다.
