# FlowLens — 프로젝트 현황 & 작업 기록

최종 갱신: **2026-07-17**
이 문서 하나만 읽으면 프로젝트 전체를 파악할 수 있도록 정리했습니다.

---

## 1. 한 줄 요약

**FlowLens** — 웹사이트 방문자의 클릭·마우스이동·스크롤을 분석해 **무엇을 고쳐야 하는지** 알려주는 행동 분석 SaaS.
**대행사(에이전시)가 여러 고객사를 관리**하는 구조. 개인정보(IP·입력값)를 수집하지 않는 것이 설계 원칙.

---

## 2. 지금 살아있는 것 (Live)

| 항목 | 값 |
|---|---|
| **서비스 주소** | https://flow.seokorea.org |
| 호스팅 | Vercel (팀 `76cold-2381s-projects`, 프로젝트 `flowlens`) |
| **실행 리전** | **sin1 (싱가포르)** — DB와 같은 리전 (중요, 아래 5-③) |
| DB | Neon Postgres (`neon-red-elephant`, ap-southeast-1, **database: `flowlens`**) |
| 도메인 | seokorea.org(WordPress.com 등록) DNS에 `flow` CNAME → `cname.vercel-dns.com` |
| 추적 중인 사이트 | **good-tour.kr** (GTM `GTM-NGPZ4BF2` 경유, siteKey `cmrlowlbb0005l104nu05s6oa`) |
| 배포 방법 | `npx vercel@latest --prod` (GitHub 없이 CLI 직접 배포) |

### 환경변수 (Vercel Production)
| 이름 | 용도 | 상태 |
|---|---|---|
| `DATABASE_URL` | Neon `flowlens` DB (pooled) | ✅ 설정됨 |
| `FLOWLENS_SECRET` | 세션 서명 · IP 해시 | ✅ 설정됨 |
| `CRON_SECRET` | 크론 인증 | ❌ **미설정 → 자동 정리 안 돌아감** |
| `TOSS_SECRET_KEY` | 결제 | ❌ 미설정 (결제 미연동) |

---

## 3. 기능 구성

### 대시보드 (대행사)
- **개요** — 세션·이탈률·모바일 비중·전환
- **히트맵 (5종)** — 클릭맵 / 무브맵 / 셀렉터 / 스크롤맵 / 제스처
  - 배경 = **실제 사이트 화면**(기기별 자동 캡처), 기간 필터(전체/7/30/90일)
  - 진하기 슬라이더 · 집중(딤) 모드
- **퍼널 · 세션 리플레이 · 전후 비교 · 개선 제안 · 설치/공유 · 데이터 관리**

### 랜딩 (잠재고객)
- **무료 진단** — URL 입력 → 페이지 점검 + **실제 화면 위 데모 히트맵** + **플랫폼 자동 감지**

### 시각화 규칙 (리서치 근거: Clarity/Hotjar/Crazy Egg)
- **클릭맵 = 컬러** 열지도 (파랑→초록→노랑→빨강)
- **무브맵 = 블랙** 궤적(곡선) + 체류 점
- 배경은 **원색 유지** (블러·무채색 안 씀), 가독성은 **오버레이 투명도**로 조절

---

## 4. 요금제 (2026-07 확정)

| | 무료 (14일 체험) | 그로우 | 에이전시 |
|---|---|---|---|
| 월 요금 | 0원 | **19,800원** | **88,000원** |
| 월 세션 | 500 | 20,000 | 100,000 |
| 초과 | 수집 정지 | 1만당 9,900원 | 1만당 8,800원 (기본 단가와 동일) |
| 사이트 | 1 | 3 | 30 |
| 보관 | 14일 | 90일 | 원시 90일 + 집계 1년 |
| 분석 | 클릭맵만 | 5종 + 퍼널 + 세션 리플레이 | 그로우 전부 |
| 대행사 전용 | — | — | **진단(개선제안) · 화이트라벨 · 공유링크 · 전후비교** |

- 정의 위치: `src/lib/plans.ts` (**표시·제한의 단일 기준**)
- 실제로 **코드에 제한이 걸려 있음** (사이트 수·보관일·기능 게이팅·세션 한도·체험 만료)
- 수익성: **공헌이익률 94~95%**, BEP = 그로우 3명 or 에이전시 1명 → `07-수익성-분석.md`

---

## 5. ⚠️ 반드시 알아야 할 함정 (하드코딩된 교훈)

이 프로젝트에서 **실제로 사고가 났던** 것들. 잊으면 또 당합니다.

### ① GTM은 `data-site` 속성을 지운다
GTM 맞춤HTML로 넣으면 `data-*` 속성이 **제거**되어 추적이 조용히 실패.
→ **GTM에서는 반드시 `?site=KEY`** 형태를 쓸 것. (`t.js`는 data-site / `window.FLOWLENS_SITE` / `?site=` 3경로 지원)

### ② 사이트에 실제 붙어있는 GTM 컨테이너를 확인할 것
good-tour.kr은 `GTM-PVL5HQDD`가 아니라 **`GTM-NGPZ4BF2`** 였음. 엉뚱한 컨테이너에 넣으면 아무 일도 안 일어남.
→ 확인법: 페이지 소스에서 `GTM-` 검색.

### ③ Vercel 함수 기본 리전은 미국(iad1)
DB가 싱가포르인데 함수가 미국이면 **요청마다 태평양 왕복** → 704ms.
→ `vercel.json`에 **`"regions": ["sin1"]`** 지정 → **121ms (6배 개선)**.
→ 진단법: `x-vercel-id` 가 `엣지::함수리전::id` 형식 (`icn1::sin1::...`).

### ④ 마우스 이동을 낱개 이벤트로 저장하면 적자
90ms 샘플링 = 세션당 400건 → **세션당 150KB** → 에이전시 30곳이면 **월 $400**.
→ **세션·페이지별 "경로 1줄"**(`MovePath`, `"x,y|x,y|…"`)로 저장 → **8KB (19배↓)**, 월 $10.

### ⑤ `prisma db push`가 파괴적 변경을 요구할 때 `--accept-data-loss`를 쓰지 말 것
unique 인덱스 변경·추가 시 발생. 그 플래그를 빌드에 넣으면 **이후 모든 배포에서 데이터가 조용히 날아갈 수 있음**.
→ **Neon SQL로 수동 실행** 후 배포(그러면 db push가 in-sync로 통과). 전례:
```sql
-- Screenshot에 device 추가 시 실제로 사용한 SQL
ALTER TABLE "Screenshot" ADD COLUMN IF NOT EXISTS "device" TEXT NOT NULL DEFAULT 'DESKTOP';
DROP INDEX IF EXISTS "Screenshot_siteId_path_key";
CREATE UNIQUE INDEX IF NOT EXISTS "Screenshot_siteId_path_device_key" ON "Screenshot"("siteId","path","device");
```

### ⑥ 일부 한국 사이트는 해외 IP의 일반 fetch를 WAF로 차단
good-tour.kr은 우리 서버(싱가포르)의 `fetch`에 **빈 페이지를 200으로** 반환 → 진단이 "분석도구 없음·33점"이라는 **틀린 결과**를 냄.
(헤드리스 크롬은 통과, 일반 fetch는 UA를 바꿔도 차단 — TLS 지문 차이 추정)
→ **틀린 점수 대신 정직한 실패**로 처리 중.

### ⑦ Prisma `Bytes`는 `Uint8Array<ArrayBuffer>` 요구
`Buffer`를 그대로 넣으면 TS 에러 → `Uint8Array.from(buf)`.

### ⑧ 캐시된 이미지는 `onLoad`가 안 뜬다
기기 전환 시 배경 비율이 갱신 안 돼 **이미지 아래 빈 스크롤**이 생김.
→ `useEffect([bgUrl])`에서 `img.complete`면 `naturalWidth/Height`를 직접 읽어 재계산.

### ⑨ `meta` 필드는 300자로 잘린다
보안상 allowlist + 길이 제한. 큰 데이터(좌표 경로 등)는 **전용 컬럼/테이블**로.

---

## 6. 2026-07-17 세션 작업 내역

### 저장·비용
- **마우스 이동 저장 구조 최적화** — 세션당 150KB → **8KB** (`MovePath` 모델). 적자 → 흑자.
- **스크린샷 WebP 전환** — 1,786KB → **309KB** (5.8배↓)
- **롤업(집계 스냅샷)** — 보관기간 지난 원시데이터를 격자 카운트로 압축 후 삭제. 히트맵은 계속 표시됨.

### 성능
- **함수 리전 sin1 이동** — DB 조회 704ms → **121ms**

### 기능
- 히트맵 **실제 화면 배경**(기기별 자동 캡처) · 진하기 · 집중모드
- **무브맵** 신규 (블랙 궤적 + 곡선 보간)
- **전후비교 시각화**, **세션 리플레이 실제 화면**
- **기간 필터**, **사이트 등록 UI**(없었음), **비밀번호 변경**
- **플랫폼 자동 감지** (워드프레스/카페24/아임웹/윅스 등) → 전용 설치 안내

### 사업
- **요금제 확정 + 코드에 제한 전면 구현**
- 수익성 분석 (`07-수익성-분석.md`), 설치 매뉴얼 (`06-설치-매뉴얼.md`)

### 보안 (감사 후 수정)
| 발견 | 조치 |
|---|---|
| 🔴 **결제 우회** — 누구나 `plan=AGENCY` 전송으로 전 기능 무단 사용 | 유료 전환 차단(다운그레이드만 셀프) |
| 🔴 **히트맵 무단 열람** — 공개된 siteKey만으로 `/api/overlay` 조회 가능 | 인증 필수 + 본인 대행사 사이트만 |
| 🟠 세션 쿠키 영구 유효 | 만료(7일) 포함 서명 + 상수시간 비교 |
| 🟠 로그인 무차별 대입 무방비 | 15분 10회 실패 시 잠금 (IP는 해시만 저장) |
| 🟡 비밀번호 정책 불일치 | 8자로 통일 |

---

## 7. TODO (2026-07-17 기준)

### 🔴 지금 바로 — 사장님만 할 수 있음 (계정 필요)
| 항목 | 왜 급한가 | 방법 |
|---|---|---|
| **`CRON_SECRET` 설정 + 재배포** | 안 하면 **자동 정리·롤업이 안 돌아감** → 데이터가 쌓이기만 하고 보관기간이 안 지켜짐(고지 불일치) | ① 값 생성: `openssl rand -hex 24`<br>② Vercel → flowlens → Settings → Environment Variables → Add → `CRON_SECRET` (Production) → Save<br>③ **Redeploy** |
| **임시 비밀번호 교체** | 임시로 발급된 비밀번호를 쓰는 중이면 본인만 아는 값으로 교체 | 설정 → 비밀번호 변경 |

> ⚠️ **이 저장소에 시크릿을 절대 적지 마세요.** (`.env`·환경변수·비밀번호·DB URL)
> 원격 `github.com/recon9973-lang/desktop-tutorial` 에 연결돼 있어 푸시되면 그대로 노출됩니다.

### 🟠 판매를 시작하려면 필수
| 항목 | 상태 |
|---|---|
| **결제(토스) 연동** | 지금은 보안상 **유료 전환 자체가 막혀 있음**. 결제 성공 웹훅에서 plan을 반영하도록 구현해야 실제 판매 가능 |
| **법무 문서 확정** | `privacy`·`terms`·`docs/04`는 **초안** — 전문가 검토 + 실제 회사정보 기입 필요 |
| **good-tour.kr 실데이터 확인** | 추적은 살아있음. 며칠 쌓인 뒤 히트맵·개선제안이 쓸만한지 검증 |

### 🟡 여유 될 때
| 항목 | 비고 |
|---|---|
| 무료 남용 방지 강화 | 이메일 1회 외 **도메인당 1회**도 (gmail+1@ 우회 가능) |
| collect rate limit | siteKey 공개라 위조 이벤트 주입 가능 (Hotjar 등도 동일한 업계 공통 한계) |
| 테스트 데이터 정리 | `@example.com` 임시 계정 다수. 격리돼 **무해** → 급하지 않음. 정리 SQL은 아래 |
| 스크린샷 장수 상한 | 사이트·페이지 늘어나면 저장 증가 (지금은 재사용 중이라 문제 없음) |

```sql
-- 테스트 계정 일괄 삭제 (실계정은 @example.com이 아니라 안전)
DELETE FROM "Screenshot" WHERE "siteId" IN (SELECT s.id FROM "Site" s JOIN "Client" c ON s."clientId"=c.id JOIN "User" u ON u."agencyId"=c."agencyId" WHERE u.email LIKE '%@example.com');
DELETE FROM "HeatmapSnapshot" WHERE "siteId" IN (SELECT s.id FROM "Site" s JOIN "Client" c ON s."clientId"=c.id JOIN "User" u ON u."agencyId"=c."agencyId" WHERE u.email LIKE '%@example.com');
DELETE FROM "Agency" WHERE id IN (SELECT "agencyId" FROM "User" WHERE email LIKE '%@example.com');
```

### ⏸️ 보류 (결정됨)
| 항목 | 결정 |
|---|---|
| **크롬 확장** | **접어둠.** 코드·보안수정은 완료됐고 `Site.overlayToken` SQL만 실행하면 살아남.<br>이유: 대시보드가 이미 같은 일을 더 잘함 + 설치 장벽(개발자 모드·토큰)이 GTM보다 높음. **고객용으로 밀 가치 없음** |
| **설치 마찰 줄이기** | 플랫폼 자동감지까지 완료. 다음 후보: "설치 요청 메일 보내기" 버튼 / 설치 대행 패키지(셋업비 수익화) |
| **진단 정확도(WAF 차단 사이트)** | 지금은 정직하게 실패 처리. 근본 해결은 진단도 헤드리스 크롬 사용(1회 25초·비용) 또는 함수를 icn1로 이동(DB 지연 6배 악화) — **트레이드오프라 보류** |

### 다음에 집중할 것 (제안)
비용·기능·보안은 정리됐습니다. **남은 병목은 "실제 고객 확보"** 입니다.
1. good-tour.kr 데이터로 **실제 개선 제안이 쓸만한지** 검증 (제품 가치 증명)
2. 그 결과를 **첫 고객사 영업 자료**로 사용
3. 결제 연동 → 실제 과금 시작

---

## 8. 개발 참고

```bash
cd flowlens
npm run dev          # 로컬 (SQLite, http://localhost:4311)
npm run build        # 배포 전 반드시 확인
npx vercel@latest --prod   # 배포 (vercel-build가 sqlite→postgres 전환 + db push)
node scripts/cleanup.mjs   # 보관기간 초과분 롤업+삭제 (크론이 할 일)
```

- **로컬은 SQLite / 운영은 Postgres** — `vercel-build`가 sed로 provider 전환
- 스키마 변경 시 파괴적 경고가 뜨면 → **위 5-⑤** 방식으로
- 문서: `docs/01~08`
