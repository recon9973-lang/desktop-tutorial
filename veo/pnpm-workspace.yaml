packages:
  - "apps/web"
  - "packages/*"

# These ship prebuilt native binaries and need their install scripts to run:
# sharp powers Next.js image optimisation, unrs-resolver backs the ESLint
# TypeScript import resolver.
onlyBuiltDependencies:
  - sharp
  - unrs-resolver
