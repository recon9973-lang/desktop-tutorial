# 작업 목록 — 확정된 것 · 남은 것 · 사장님 몫

**2026-08-09 작성.** 사장님 지시: *"리스트업좀 하자. 이것도 이미 모두 확정했잖아."*

이 파일이 생긴 이유는 하나다. **이미 결정된 것을 내가 다시 물었다**
(`CORRECTIONS.md` #22). 결정이 `observation-engine.md` §9, `HANDOFF-NEXT-SESSION.md` §3,
`PLAN-2026-08-total-review.md` §4 세 곳에 흩어져 있었고 나는 세 곳 다 안 열었다.

**규칙: 사장님께 무엇을 여쭙기 전에 이 파일을 먼저 연다.** 여기 있으면 묻지 말고
인용한다. 여기 없으면 `docs/` 를 찾고, 찾은 것을 여기 올린다.

---

## 1. 확정 — 다시 묻지 않는다

### 1-A. AI 답변 관측 설계

출처: `docs/observation-engine.md` §9 — *"확정된 운영 설계 (사장님 결정 2026-08-08)"*

```
주기      핵심 질문 주 1회 · 확장 질문 월 1회
기본값    핵심 5~8개 · 확장 20개 안팎 (화면에서 수정)
엔진      5개 동일 횟수. 비중 조절 금지 — 구성비가 전체 언급률을 좌우한다
반복      3회
모드      검색 켬 + 끔 (Perplexity Sonar 는 끔이 없다)
수동측정  kind=MANUAL 로 별도 저장 · 추이 그래프에서 제외
모델      관측에는 gpt-5 를 쓴다 (gpt-4o 계열은 검색 호출 금액을 낼 수 없다)
```

**이 설계의 정밀도** [계산] `MoE = 1.96 × √(p(1−p)/n)`, `p = 0.5`:

| 보는 값 | n | 오차 폭 | 어떻게 읽나 |
|---|---|---|---|
| 핵심 5개 전체 언급률 | 150 | **±8.0pp** | 거래처에 보고해도 되는 값 |
| 핵심 8개 전체 언급률 | 240 | **±6.3pp** | 〃 |
| 질문 하나 (전체 엔진) | 30 | ±17.9pp | 방향만. 소수점 못 붙인다 |
| 질문 × 엔진 한 칸 | 6 | ±40.0pp | 이 칸 하나로 "이 모델이 안 뜬다" 를 말하면 안 된다 |

**비용** (`observation-engine.md` §8-C):
```
질문 1개 1회 측정 = $0.61 ≈ 854원
거래처 1곳  약 $20~28/월 (2.8~4만 원)
거래처 8곳  약 22~32만 원/월
```

### 1-B. 일하는 순서

출처: 사장님 2026-08-09 —
*"진단 업체 등록 - 자동 진단 - 데이터 수집 - 업체별 대시보드에 수집 진단 결과 노출
(이 단계에 출력 시스템 붙임)."*

리포트 발행은 **독립 과제가 아니라 4단계에 붙는 것**이다. 내가 P1 로 따로 세웠던 것은
틀렸다.

### 1-C. 측정값 불가침

출처: 사장님 2026-08-09 — *"측정값은 절대 수정하거나 추측하면 안돼"* ·
*"모든 점수는 반올림이나 절삭 없다"* · *"모든 값 동일하게 자르기로"*

```
저장·API   잰 그대로 (round() 0곳, 시험이 막는다)
화면       소수점 두 자리까지 **자르기**. 반올림 금지. 점수·비율·금액·횟수 전부
못 잰 값   0.00 이 아니라 —
```
관문: `tests/release/test_measurement_integrity.py` · `lib/score-display.test.ts`

### 1-D. 배포

출처: 사장님 2026-08-09 · `docs/audit/2026-08-09-actions-minutes.md`

```
초록불이어도 주문 없이 밀지 않는다
make deploy 만 쓴다 (CI 관문 우회 금지)
하루 2회 상한 — 2026-09-01 자동 해제
```

### 1-E. 손대지 않기로 한 것

| 무엇 | 왜 |
|---|---|
| #72 경쟁사 비교 화면 (엔드포인트 3) | **내가 만들어 낸 과제**였다. `competitors/INTEGRATION_REQUEST.md` §0 이 붙이지 말라고 적고 있다 (`CORRECTIONS.md` #17) |
| `url_records` | 죽은 유산 |
| #33 flow.seokorea.org 지역 · #35 ERP 소스 · #36 journeymap 백업 · #38 시험 없음 | 범위 밖 |

---

## 2. 지금 상태 [실측 2026-08-09]

```
운영 버전    0.3.74      (GET /api/health)
오늘 배포    0 / 2 회
미배포       v0.3.75 · 0.3.76 · 0.3.77 · 0.3.78 · 0.3.79   ← 다섯 판이 쌓여 있다
```

| 판 | 무엇 | 상태 |
|---|---|---|
| 0.3.75 | 리포트 본문 화면 | 커밋됨 |
| 0.3.76 | 홈페이지에서 브랜드 식별 읽기 · 거래처 소재지 · 한글 도메인 오탐 수정 · 명세 1.9.1 | 커밋됨 |
| 0.3.77 | 반올림·절삭 전면 제거 + 소수점 두 자리 자르기 | 커밋됨 |
| 0.3.78 | 이슈 닫는 재측정 · **거래처별 현황 화면** | 커밋됨 |
| 0.3.79 | **대시보드 거래처 레일** · 백분율 반올림 수정 | 커밋됨 |

**축별로 무엇이 도나** [실측 2026-08-09 운영 DB, 이전 세션 조회]:
```
SEO·GEO 진단   47회 · 판정 2,303 · 근거 4,068      ✅ 돈다
이슈           165건 — 닫는 경로가 v0.3.78 에 생겼다 (아직 미배포)
AI 답변 관측   observation_runs 0 · prompt_sets 0 · ai_engines 0   ⬜ 한 번도 안 켬
키워드         6개 표 전부 0행
리포트         0건 (만들 수 있는 진단 43회 대기)
공개 리드      0건 (무료 진단 72회는 있었음)
```

---

## 3. 남은 것 — 네 묶음

### 3-A. 지금 코드만으로 되는 것

| 무엇 | 근거 | 규모 |
|---|---|---|
| ~~거래처별 현황 화면~~ | v0.3.78 · 업체 관리에서 업체명을 누르면 열린다 | 끝남 |
| ~~대시보드 거래처 레일~~ | v0.3.79 · `PLAN` P0-5 의 "거래처 레일" | 끝남 |
| ~~백분율 반올림 새던 자리~~ | v0.3.79 · `toLocaleString` 이 소수를 반올림하고 있었다. 관문 확장 | 끝남 |
| 참사랑한의원 3중 등록 정리 | 2026-07-29 등록, 중복 방지 관문(2026-08-08)보다 앞선다 | 작음 |
| 검색 켬/끔 2모드를 한 실행에 반영 | `observation-engine.md` §10 ⬜ | 중간 |
| 수동 측정 별도 저장 | 〃 (확정 설계에는 있는데 미구현) | 중간 |
| ~~E7 리포트 버전 열람 화면~~ | **v0.3.75 로 끝났다** — `reports/[reportId]/[version]/` 에 본문 화면과 공유 링크가 있다. `PLAN` P0-3 이 아직 미완으로 적혀 있는 것은 그 문서가 그 판보다 앞선 것이다 | 끝남 |
| E1 폐쇄 `POST /seo/scan` | `PLAN` P0-1, 소비자 0 | 작음 |
| E3 네이버 키워드 공개 도구 배선 또는 메뉴에서 제거 | `PLAN` P0-2 | 작음 |
| ~~E8 목록 200 하드캡~~ | **이미 끝나 있었다** — `readAllPages` 가 25쪽까지 이어 읽는다(5,000행). 남은 것은 5,000행에서 조용히 잘린다는 것뿐 | 끝남 |
| #59 토큰 과소계산 보정 | 우리가 읽는 값이 실제의 64% | 중간 · 아래 3-D 도 필요 |

### 3-B. 서버는 있는데 화면이 없는 것 (24건, 묶으면 넷)

출처: `docs/audit/2026-08-08-server-ui-gap.md` §B

| 묶음 | 엔드포인트 | 없는 것 |
|---|---|---|
| **키워드** | 8 | 저장 · 다시 열기 · CSV 내보내기. 지금은 열 때마다 처음부터 다시 친다 |
| 자격증명 | 4 | = #69, 아래 3-D 와 같은 건 |
| 경쟁사 비교 | 3 | **손대지 않는다** (1-E) |
| 낱개 | 9 | 프로젝트 수정 · 진단 원자료 보기 · 채점 재현 등 |

키워드 묶음이 값이 제일 커 보이지만 **사장님이 원하시는지 아직 답을 못 들었다**
(`HANDOFF-NEXT-SESSION.md` §3 에도 같은 말이 적혀 있다).

### 3-C. 로드맵 (`PLAN-2026-08-total-review.md` §4)

```
P1   작업 큐 이행 · 정기 재진단 + 자동 diff · 리드 DB · Sentry
P2   화이트라벨 거래처 리포트 자동화  ← 3개 조사가 모두 1순위로 지목
     의료광고법 자동 검수 (규제 해자) · JS 렌더링 크롤 · GSC 연동 · 알림톡 경보
P3   한국어 AI 인용 모니터링  ← "네이버 AI + 한국어 ChatGPT 는 세계 공백"
     GEO 명세 1.4.0 근거 등급 공시 · 플레이스 순위(법률 검토 선행)
```

### 3-D. 사장님 몫 — 내가 못 하는 것

| # | 무엇 | 왜 내가 못 하나 | 내가 먼저 한 것 |
|---|---|---|---|
| **#63** | 첫 관측 실행 (venomad.com 소액) | **돈이 나간다.** 승인 필요 | 단가·월 비용 실측, 설계 확정 확인 |
| **#64** | 엔진 열쇠 3개 (Anthropic·Gemini·Perplexity) + Grok 어댑터 | 외부 콘솔에서 발급 | 어댑터 4종 코드 확인, OPENAI 만 살아 있음 확인 |
| **E10** | Railway `VEO_ALERT_WEBHOOK_URL` | 외부 콘솔 클릭 | 코드 완성 확인 — **이 값 하나로 경보 2종이 살아난다** |
| **재진단** | Railway `VEO_RESCAN_AFTER_DAYS` | 외부 콘솔 클릭 · 주기는 운영 판단 | `rescan.py` 확인 — 기본 0(꺼짐), 문서 예시는 `7` |
| **#59** | OpenAI 사용량 Cost 탭 확인 | 로그인 화면 | 우리 계산이 실제의 64% 라는 것까지 실측 |
| **키워드 8건** | 만들지 말지 | 사업 판단 | 엔드포인트 8개·표 6개 전부 0행 확인 |

**#63 · #64 · E10 · 재진단 넷은 서로 독립이다.** 하나가 막혀도 나머지는 갈 수 있다.

---

## 4. 이 파일을 고치는 규칙

* 사장님이 결정하시면 **1장에 옮기고 출처(날짜·원문)를 적는다.** 3-D 에서 지운다.
* 내가 끝내면 3장에서 지우고 `changelog.ts` 에 판을 올린다.
* **여기 없는 것을 사장님께 여쭙기 전에, 없는 것이 맞는지 `docs/` 를 먼저 찾는다.**
