# 실도메인 8개 검증 — VEO 1.8.0 실측과 구글 대조 (2026-08-01)

v3 구현에 들어가기 전, 발행 명세 1.8.0 과 실코드 파이프라인을 실제 도메인 8개로
검증했다. 사용자가 지정한 도메인이며, VEO 는 전체 크롤(상한 100장) + PageSpeed
실험실 상위 5장, 구글은 Lighthouse(= PSI 웹 UI 그 점수) 랜딩 1장 기준선이다.

측정 자료 원본: 세션 스크래치패드 `results/`·`results_lighthouse/` (JSON, 검사
단위 판정 포함). 이 문서의 모든 숫자는 그 파일에서 나왔다.

---

## 1. 결과표

| 도메인 | VEO (전체 크롤) | 구글 SEO (랜딩 1장) | 구글 성능 | 크롤 | 비고 |
|---|---:|---:|---:|---:|---|
| next-t.co.kr | **88.0** | 92 | 54 | 100장 | 경쟁사(NXT) 자사 사이트 |
| 1macklaw.co.kr | **87.4** | 92 | 78 | 100장 | |
| seo.co.kr | **83.4** | **100** | 55 | 100장 | |
| venomad.com | **83.3** | 73※ | 97 | 100장 | ※구글 측정 오염 (§4) |
| chamsarang1075.com | **76.4** | **100** | 59 | 100장 | |
| markepick.com | **52.7** | 73※ | 100 | 100장 | 상한 60 적용 |
| good-tour.kr | **50.5** | 73※ | 92 | 100장 | 상한 60 적용 |
| grand1.co.kr | **31.5** (http) | **측정 불능** | — | 100장 | §5 |

- marketing.com 은 잘못 전달된 주소였다(맨 도메인에 DNS A 레코드 없음). markepick.com 으로 정정.
- 진단 시간: 크롤 2~52초 + 성능 10~32초. **59개 검사 100장이 병목이 아니라는 실측 재확인.**
- PageSpeed 한도 사용: 이 검증 전체로 약 50회 (하루 25,000 중).

## 2. 알고리즘 완성도 — 검증 3종

표본 8개로 지도학습식 ML 은 수학적으로 무의미하다(0-C: 과적합·기준 정답 부재).
대신 성립하는 검증을 돌렸다.

### A. 재현성 — 8/8 완전 일치

저장된 검사 판정을 실코드 평가기에 다시 넣으면 **소수점 여섯째 자리까지 같은 점수**가
나온다. 점수가 결정적(deterministic)이라는 계약의 실측 확인.

### B. 교차 구현 — 독립 구현 2벌의 일치

같은 판정을 v3 프로토타입(실코드와 별도로 작성된 참조 구현)에 넣었다.
8개 도메인 최대 차이 **0.005점** — 프로토타입이 점수를 소수 둘째 자리로 반올림해
생기는 표시 차이가 전부다. 산식 자체는 두 구현이 일치한다.

### C-1. 강건성 — 배점 ±25% 몬테카를로 교란 1,000회

배점이 "그 값이어야만" 성립하는 순위인지 검사했다. 49개 검사 배점 각각에
±25% 균등 잡음을 넣어 1,000회 재채점:

```
평균 Kendall tau      0.958   (1.0 = 순위 완전 보존)
순위 완전 동일         47%
가장 자주 뒤집힌 쌍    seo.co.kr ↔ venomad.com (428/1000회)
                      → 점수차 0.14점. 뒤집히는 게 정상인 동점 수준
```

**결론: 순위는 배점의 미세 조정에 강건하다.** 뒤집히는 쌍은 전부 점수차 2.3점
이하의 근접 쌍이고, 그 이상 벌어진 쌍은 1,000회 중 한 번도 뒤집히지 않았다.
배점 하나하나의 정밀도가 아니라 **구조(관문·단계·범위)가 순위를 결정한다**는 뜻 —
구글 근거 대조(§5.3, SEO_SCORING_V3_PAGES.md)에서 "다음 판 후보"로 남긴 배점
조정이 순위를 흔들 위험도 낮다.

### C-2. 구글과의 순위 상관 — ρ = 0.286, 그리고 그 숫자를 믿으면 안 되는 이유

Spearman 상관이 낮게 나왔다. 그러나 §4 의 측정 오염을 감안해야 한다 — 7개 중
3개(venomad·good-tour·markepick)의 구글 점수는 사이트의 성질이 아니라 **호스팅
봇 차단이 구글의 측정을 방해한 결과**였다. 오염을 빼면 표본 4개라 상관계수를
말하는 것 자체가 무의미하다(0-C). 이 숫자는 결론이 아니라 기록이다.

## 3. 구글 100점 대 VEO 76점 — #39 영업 논거의 실측 재확인

```
chamsarang1075.com   구글 SEO 100 (랜딩 1장·10항목)
                     VEO 76.4 (100장·59항목)
                       - canonical 문제      40/100장
                       - title 중복          55/100장
                       - 메타설명 품질        60/100장
                       - 고아 페이지          43/96장
seo.co.kr            구글 SEO 100
                     VEO 83.4 (고아 페이지 56/101장, h1 없음 60/104장)
```

**구글이 틀린 것이 아니라 본 페이지 수가 다르다** — 이 문장이 다시 실측으로
확인됐다. 구글은 랜딩 1장에서 10개 감사를 통과시켰고 실제로 그 1장은 깨끗하다.
나머지 99장의 문제는 구글 점수에 존재하지 않는다.

## 4. 구글 점수가 오염된 3개 도메인 — 73점의 정체

venomad·good-tour·markepick 세 도메인이 **똑같은 감사 3개**(meta-description ·
http-status-code · robots-txt)에서 실패하며 나란히 73점을 받았다. 우연이 아니다.
구글이 실제로 잰 최종 URL 에 단서가 있다:

```
venomad_com    → https://www.venomad.com/?ckattempt=3
markepick_com  → https://markepick.com/?ckattempt=2
good-tour_kr   → https://www.good-tour.kr/?ckattempt=3
```

`ckattempt` 는 호스팅의 쿠키 검사(봇 차단) 리다이렉트다. Lighthouse 가 차단
페이지를 잰 것이고, 세 감사의 실패는 그 차단 페이지의 성질이다. 직접 확인:
venomad 의 www robots.txt 는 200 text/plain 으로 멀쩡하다. VEO 크롤러는 정상
통과해 100장을 받았다.

**의미: 랜딩 1장 측정은 그 1회의 fetch 가 오염되면 점수 전체가 오염된다.**
100장 크롤은 한두 장의 fetch 실패가 coverage 로 희석된다. LIGHTHOUSE_COMPARISON
§5 가 문서로 지적한 "구글은 잴 때마다 흔들린다"의 세 번째 실측 사례.

## 5. grand1.co.kr — 구글은 침묵하고 VEO 는 진단했다

HTTPS 인증서가 자체 서명이라 모든 브라우저가 경고를 띄운다. 구글 Lighthouse 는
https 로도 http 로도 `lighthouseUserError` — **아무 점수도 내지 못했다.**

VEO: https 는 크롤 거부(정직한 502), http 로 진단하면 **31.5점 + 상한
`https_or_mobile_failure`(60) 적용** — https_valid FAIL(1/1), canonical 전 페이지
문제, 메타데이터 중복 105/106장, 모바일 뷰포트 없음 98/106장. 고객에게 건넬 조치
목록이 나온다. **측정 불능과 낙제점은 다르고, 후자가 팔 수 있는 진단이다.**

## 6. 이 검증이 드러낸 VEO 자신의 결함 — 구현 ① 의 실측 근거

8개 도메인 **전부** 크롤이 100장 상한에 잘렸다(`discovery_exhausted=False`).
그 상태에서:

```
crawl.no_broken_internal_links = PASS   ← 8개 도메인 전부
```

**본 것은 100장뿐인데 "깨진 내부 링크가 없다"고 단정했다.** 0-A 위반이 실제
데이터에 8건 나온 것이다 (다른 부재형 검사 3개는 이번 표본에서 위반이 '발견'되어
FAIL 이라 이 결함이 드러나지 않았을 뿐, 같은 경로를 쓴다).

v3 설계(SEO_SCORING_V3_PAGES.md §2)가 정확히 이 결함의 수정을 구현 1순위로
잡고 있다. 이 검증으로 그 우선순위가 이론이 아니라 실측이 됐다.

## 7. 하지 못한 것 (정직하게)

- **NEXT-T 진단 도구와의 직접 대조** — 로그인·유료 계정이 필요하고, 로그인 화면에
  비밀번호를 넣지 않는 것이 규칙이다. 대신 그들의 자사 사이트(next-t.co.kr)를 VEO
  로 쟀고(88.0), 이전 참사랑한의원 12항목 나란히 대조(세션 기록, 오탐/누락/얕은
  판정 3건 전부 VEO 우세)를 근거로 남긴다.
- **지도학습 ML 검증** — 표본 8개, 정답 순위 없음. 하면 그럴듯한 숫자가 나오고
  그 숫자는 거짓말이다. 몬테카를로 강건성·교차 구현 일치·재현성으로 대체했다.
- **Lighthouse 점수의 분산** — 랜딩 1장 1회 측정이다. 구글 실험실 점수는 실행마다
  흔들린다는 것을 우리가 먼저 문서화했으므로(1.00↔0.92), 위 구글 점수도 ±수 점의
  잡음을 가진 단일 표본으로 읽어야 한다.

## 8. 결론

| 질문 | 답 |
|---|---|
| 점수가 재현되는가 | 8/8 완전 일치 |
| 구현이 산식대로인가 | 독립 구현 2벌 차이 ≤ 0.005 |
| 배점에 과민한가 | 아니다 — ±25% 교란에도 tau 0.958 |
| 전체 크롤이 실효가 있는가 | 있다 — 구글 100점 사이트에서 실결함 다수 검출 |
| 랜딩 1장 측정의 위험 | 실증됨 — 8개 중 3개에서 구글 점수 자체가 오염 |
| VEO 에 남은 구멍 | 잘린 크롤의 부재 단정 8건 — v3 구현 ① 이 고친다 |

---

# 추가 분석 (사용자 제공 스크린샷 대조) — 구글의 점수 분할과 NXT 71·42의 해부

사용자가 제공한 세 화면: 구글 PSI(성능 60·접근성 95·권장사항 81·SEO 100·
에이전트형 2/2)와 NXT "SEO 점수 체크" — 참사랑한의원 71점, 그랜드연합의원 42점.
PSI 수치가 우리 실측(성능 59·SEO 100)과 일치하므로 같은 대상·같은 시기다.

## 9. 구글은 왜 점수를 나누는가 — 그리고 VEO 는 무엇을 합산하는가

### 관측된 사실 (LIGHTHOUSE_COMPARISON §1, 실측)

구글은 **다섯 카테고리에 다섯 개의 독립 점수**를 주고, 합산한 단일 총점을
**어디에도 제공하지 않는다.** 감사 158개 중 배점이 있는 것은 51개뿐이고, 나머지
107개는 "점수에 포함되지 않음" 배지를 달고 진단 정보로만 나열된다.

### 왜 나누는가 (관측에서 추론 — 구글이 "왜"를 문서로 설명하지는 않는다)

| 카테고리 | 답하는 질문 | 고치는 사람 |
|---|---|---|
| 성능 | 방문자가 얼마나 기다리는가 | 프런트엔드 개발자 |
| 접근성 | 장애가 있는 사용자가 쓸 수 있는가 | 마크업·디자인 |
| 권장사항 | 개발 위생(폐기 API·콘솔 오류 등) | 개발자 |
| SEO | 검색엔진이 읽을 수 있는가 | SEO 담당 |
| 에이전트형 | AI 에이전트가 조작할 수 있는가 | (신설, 개발 중) |

질문과 책임자가 다르면 합산은 정보를 파괴한다 — **접근성 95가 성능 60을 가린다.**
평균이 치명 결함을 가리는 문제이고, VEO 가 상한(cap)과 관문을 만든 것과 같은
이유에서 구글은 아예 합치지 않는 쪽을 택했다.

### VEO 는 "5개 합산 점수"인가 — **아니다. 의도적으로 부분집합이다**

| 구글 카테고리 | VEO SEO 점수 반영 | 근거 |
|---|---|---|
| SEO (배점 10) | **10/10 대응** + VEO 에만 47개 | hreflang 은 국내 대상이라 미채택, crawlable-anchors 는 1.7.0 에서 추가 |
| 성능 (배점 5) | **결과 지표만 8.3점** — LCP 3·CLS 2·INP(필드) 2·TBT 0.5 + 원인계열 0.8 | FCP·SI 는 LCP 와 같은 성질을 다시 재는 것이라 미채택. 원인 42개 진단은 구글도 배점 0 |
| 권장사항 (배점 12) | **일부** — HTTPS·인증서·혼합콘텐츠·charset·doctype | 나머지(폐기 API·쿠키·콘솔 오류)는 검색 준비도가 아니다 |
| 접근성 (배점 21) | **불포함** | 검색 순위 신호가 아님. 겹치는 5개(alt·lang·heading·viewport·title)는 SEO 관점으로 이미 있음 |
| 에이전트형 | **불포함 — GEO 준비도가 별도 점수** | "절대 하나의 총점으로 합치지 않는다"(methodology §1). #38 에서 대조 검토 중 |

즉 VEO SEO 점수는 구글 다섯 중 **"검색에 작용하는 것"만 골라 담고 자체 47개를
더한 점수**다. 접근성을 뺀 것은 누락이 아니라 결정이며(§4, LIGHTHOUSE_COMPARISON),
에이전트형은 GEO 준비도와 겹치므로 별도 점수로 분리돼 있다.

## 10. NXT 71점의 해부 — 참사랑한의원 (구글 100 · VEO 76.4 · NXT 71)

NXT 내부 배점은 비공개다. 아래는 화면에서 읽히는 구성과 이전 12항목 나란히
대조(세션 기록)에 근거한다.

**같은 사이트에 세 점수가 나온 이유는 세 도구가 다른 것을 재기 때문이다:**

```
구글 100  = 랜딩 1장 × 배점 감사 10개     → 그 1장은 실제로 깨끗하다
NXT  71   = 랜딩 1장 × 자체 항목 (보안 헤더 포함, 오탐 포함)
VEO  76.4 = 100장 × 59검사 (순위 관련만)
```

NXT 가 구글보다 깎은 27점의 정체 (화면 판독):

1. **보안 헤더 4종 실패** — X-Frame-Options·X-Content-Type-Options·HSTS·
   Referrer-Policy. **검색 순위 요인이 아니다.** 구글의 다섯 카테고리 어디에도
   이 헤더들의 배점은 없다. VEO 가 "점수 밖 추가 항목"으로 설계한 바로 그 영역을
   NXT 는 SEO 점수 안에 합산한다 — `92+8=100 과 100+0=100 이 같아 보이는` 문제의
   실물이다.
2. **혼합 콘텐츠 오탐** — `<a href="http://pf.kakao...">` 를 혼합 콘텐츠로 판정
   (화면에 해당 코드 블록이 보인다). 링크는 리소스가 아니고 브라우저도 차단하지
   않는다. 이전 대조에서 확인된 그 오탐이 그대로 남아 있다.
3. **한 장만 본다** — canonical 40장·title 중복 55장·고아 43장은 NXT 점수에
   존재하지 않는다. 71점을 받고 헤더를 고치면 점수는 오르지만 **검색은 그대로다.**

**숫자가 비슷하다고(71 vs 76.4) 같은 진단이 아니다.** 두 점수가 시키는 일이
다르다 — NXT: 헤더를 넣어라(순위 무관). VEO: canonical·title 템플릿을 고쳐라
(55장짜리 순위 직결 결함).

## 11. NXT 42점 — 그랜드연합의원 (구글 측정불능 · VEO 31.5 · NXT 42)

세 도구가 세 갈래로 갈린 가장 흥미로운 사례다.

- **구글: 아무 점수도 못 냈다** (`lighthouseUserError` — 자체 서명 인증서).
- **NXT: 42점** — 인증서 문제를 항목 하나("HTTPS 인증서 실패")로 취급하고
  나머지를 계속 쟀다. 측정을 완주한 것 자체는 UX 관점에서 배울 점이다.
- **VEO: https 는 정직한 거부(502), http 지정 시 31.5점 + 상한 적용 + 조치 목록.**

VEO 42→31.5 가 NXT 보다 낮은 이유는 가혹해서가 아니라 **범위를 세기 때문이다**:
메타데이터 중복 105/106장, 뷰포트 없음 98/106장 — 한 장 기준으로는 "항목 2개
실패"지만 사이트 기준으로는 살아남은 페이지가 거의 없다. S2(대표 URL) 단계
점수가 8.7/100 인 것이 그 실태다.

### 이 사례가 남긴 제품 개선 후보

인증서가 죽은 사이트에서 지금 VEO 는 사람이 `http://` 로 다시 넣어야 잰다.
NXT 는 자동으로 이어 갔다. **"https 접속 불가 — http 로 측정했으며 이것이 1순위
결함"을 관문 라벨로 명시하는 자동 폴백**이 정직성을 잃지 않으면서 UX 를 따라잡는
길이다. (HANDOFF 남은 일에 추가)

## 12. 세 도구 요약

| | 구글 PSI | NXT | VEO |
|---|---|---|---|
| 측정 범위 | 랜딩 1장 | 랜딩 1장 | 전체 크롤 (상한 100장) |
| 점수 구조 | 5개 분리, 합산 없음 | 단일 합산 | SEO 단일 + GEO 별도 (합산 금지) |
| 비순위 항목 | 별도 카테고리로 분리 | **SEO 점수에 합산** | 점수 밖 (추가 항목 설계) |
| 측정 실패 시 | 점수 없음 (또는 0점 오기록) | 계속 진행 | 측정 불가 명시 + 사유 |
| 봇 차단에 | 오염된 점수 (73점 3건) | (미확인) | 정상 통과 |
| 강점 | 성능 실험실의 표준 | 완주율·화면 친절 | 범위·정직성·재현성 |
