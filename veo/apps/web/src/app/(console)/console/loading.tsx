import { Skeleton } from '@veo/ui';

import styles from '@/styles/page.module.css';

/**
 * 탭을 누른 **직후** 보이는 화면.
 *
 * 콘솔의 모든 화면은 서버에서 그려지고(`force-dynamic`), 안에서 API 를 부른다 — 진단
 * 화면은 아홉 번 부른다. 그런데 이 파일이 없는 동안에는 그 시간에 **아무 일도 일어나지
 * 않았다.** 이전 화면이 그대로 서 있고, 누른 탭에 불도 들어오지 않는다. 눌린 것인지
 * 느린 것인지 구분할 수 없으니 한 번 더 누르게 되고, 그러면 실제로 더 느려진다(사용자 지적).
 *
 * 여기서 고치는 것은 **속도가 아니라 응답**이다. 서버가 걸리는 시간은 그대로다. 다만
 * 화면 전환이 즉시 확정되므로 사이드바가 새 탭을 곧바로 현재로 표시하고, 본문 자리에
 * 이 뼈대가 들어선다.
 *
 * 두 번째 효과가 더 크다: Next 는 동적 화면을 **가장 가까운 이 경계까지만** 미리 받아
 * 둔다. 경계가 하나도 없던 동안에는 미리 받아둘 것도 없었다 — 미리 받기가 꺼진 것과
 * 같았다. 이 파일 하나로 `/console/*` 전체가 그 대상이 된다.
 *
 * 화면마다 두지 않고 이 자리에 하나만 둔다. 열일곱 벌을 만들면 열일곱 벌이 따로 낡는다(0-D).
 * 대신 어느 화면의 뼈대도 되도록 제목·본문 두 덩어리로만 세운다 — 실제와 다른 모양을
 * 정교하게 그리면, 채워질 때 내용이 튀어 오히려 더 어수선하다.
 */
export default function ConsoleLoading() {
  return (
    <div className={styles.page}>
      <div className={styles.header}>
        <Skeleton lines={2} label="화면을 불러오는 중입니다" />
      </div>
      <Skeleton lines={6} variant="block" label="" />
    </div>
  );
}
