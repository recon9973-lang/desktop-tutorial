# RESUME (입지 반경 표 방) — **닫힘. 나갔다** (2026-09-26 확인)

<!-- 가지: claude/adoring-lovelace-qkd1d3 -->

> 이 방 일은 **ANSEO(`veo-platform`)** 에서 했다. 이 저장소(베놈 사이트)는 안 건드렸다.
> 현황 `PROJECT_STATE.md`, 지도 `핵심두뇌_MASTER.md`.

## 한 줄

사장님 물음 **«역 정류장 인구는 왜 안나오지?»** → 「입지」 반경 표의 세 칸이 «—» 였던 것은
자료가 없어서가 아니라 서버가 **고른 반경 하나만** 재고 있었기 때문. 다섯 고리를 다 세게 고쳤다.

## 나간 것

```
0.3.625 (2026-09-19 · ANSEO 방이 냄)  반경 표의 역·정류장·인구를 고리마다
  서버  _ring_axes_of · _population_by_ring — 가장 큰 고리 한 번만 긁어 거리로 가른다
        인구 셈은 반경 인구 카드와 같은 셈·같은 차례(행정동 코드 순)
  계약  ring_axes — 못 잰 값은 null (0 아님)
  화면  표 세 칸이 고리 값을 읽는다 · 「세지 못한 고리」 줄도 · 표 밑 두 줄(잰 것/추정/못 잴 때 까닭)
  시험  API 6 · 웹 6
```

[실측 2026-09-22] `654b7eec` 는 `origin/main` 조상. 코드도 화면도 main 에 있다.

## 남은 것 — **이 방 몫은 없다**

**ANSEO 방이 낼 것 하나**가 대기 중이다(그 방 몫이다):

```
가지  claude/adoring-lovelace-qkd1d3   커밋 2b87e58f  (veo-platform · 올라가 있다)
무엇  변경이력에서 0.3.626 의 기록이 옆 판 글로 덮인 것을 되돌림 (문서만 · 코드 무변경)
      0.3.626 = ANSEO 방의 「작업 대장을 지금 사실로 맞췄습니다」 · 0.3.625 = 이 방 고침(참)
      덮이기 전 글이 0322487f 에 남아 있어 그대로 되살렸다 · 오류 238
적어 둔 자리  대장 §2 배포 대기 표(번호 없는 `—` 줄) · docs/STATE.md 「다른 방에 지시」
검사  [실측 2026-09-24] ci-local 8,254 통과 · pnpm build 통과 · 웹 시험 2,882 초록
```

## 다음 방이 밟을 자리 (이 방이 실제로 밟았다)

- **배포는 이 방에서 못 한다.** `make deploy` 가 첫 줄에서 거부한다 — 사장님 2026-09-20
  «배포를 ANSEO 방으로 넘겨 · 관문으로 만들어»(`deploy_room_gate.sh`). `git config` 로 끄지 않는다.
- **배포를 두 번 겹쳐 걸지 않는다.** 앞 것이 끝난 줄 알고 또 걸었더니 둘이 같은 시험용 DB 를
  서로 지워 검사가 계속 실패했다. 코드 탓으로 보였지만 아니었다.
- **판 번호는 먼저 main 에 닿는 쪽이 갖는다.** 이 방은 0.3.620·0.3.622·0.3.650·0.3.654 로
  네 번 물러났다. 번호를 미리 적지 말고 대기 표에 `—` 로 쌓는다.
- **이 방 PostgreSQL 이 자꾸 죽는다.** `pg_ctlcluster 16 main start` 로 살아난다. 긴 검사에는
  2초마다 살펴 되살리는 감시를 붙여 돌렸다.
- **운영 주소는 이 방에서 못 잰다**(egress 403). 「돌고 있다」를 짐작으로 적지 않는다.
