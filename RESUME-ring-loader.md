# RESUME (링 로더 방) — **2026-09-18 갱신 · ANSEO 쪽이 본체다**

> ## 🚨 사장님이 보시는 링은 **ANSEO(`veo.seokorea.org`)** 것이다 — 저장소는 `veo-platform`
>
> 이 저장소(`desktop-tutorial`)의 링은 **베놈 마케팅 사이트**(venom-new-site.vercel.app) 것이다.
> **다른 물건이다.** 2026-09-17 에 이것 때문에 한 세션을 통째로 날렸다 — 사장님이
> 「진단 돌려보니 링이 멈춘다」고 하신 것은 ANSEO 였는데 나는 여기 링을 고치고 배포하기를
> 반복했다. 원인을 네 번 잘못 짚은 것도 전부 **다른 물건을 재고 있었기** 때문이다.
> ANSEO 를 만지려면 `add_repo` 로 `veo-platform`(owner `recon9973-lang`)을 붙인다.

## 2026-09-18 — ANSEO 의 링, 원인과 고침

사장님: *"14~16sec에서 멈췄다가 다시 시작함"*.

**원인은 코드가 아니라 영상 파일이었고, ANSEO 의 영상은 여기 원본과 바이트까지 같았다**
(SHA-256 대조). 「한 번 재생용」 도입부 렌더라 앞 1.62초·뒤 0.33초가 거의 정지해 있다.

```
[실측]                         원본            등속본
  한 바퀴                  361칸 15.042초   164칸 6.833초
  눈에 멈춘 칸                47칸(1.96초)      0칸
  되감기 한 칸              보통의 11배       1.42배
  webm                         800KB          461KB
```

고친 자리 — `veo-platform` 가지 **`claude/ring-evenspeed`**
- `apps/web/src/components/ring-loader/RingLoader.tsx` — 등속 순환본을 건다(webm → mp4)
- `apps/web/public/ring/ring-sq-loop.{webm,mp4}` 새로 넣음. **원본은 안 지웠다** —
  되돌리려면 두 `source` 에서 `-loop` 를 뺀다
- 시험 기대값도 새 이름으로 고침(건너뛰거나 지우지 않았다)
- `docs/CORRECTIONS.md` **222** · 변경이력·대장·날짜별 기록에 이번 판

## 배포 — **나갔다** (2026-09-18 · 판 0.3.606)

```
[실측 2026-09-18 · git]
  origin/main 끝     929747bb  판 0.3.606
  링 커밋            18f276cb  ✓ main 의 조상
  기록 커밋          929747bb  ✓ main 의 조상
  main 의 RingLoader  <source ring-sq-loop.webm> · <source ring-sq-loop.mp4>
  main 의 영상       ring-sq-loop.webm · ring-sq-loop.mp4 둘 다 있다
```

**세 번 냈다.** 두 번은 채점 20분 사이에 다른 방이 main 을 앞질러 마지막 한 걸음에서
거절됐고(판 0.3.604 · 0.3.605), 그때마다 물러나 다시 적었다. 링 고친 내용은 안 바뀌었다.

**실제로 도는 것도 쟀다 — 나갔다.**

```
[실측 2026-09-18 19:50 KST · 워크플로 「도는 판 확인」 실행 #33 · 결과 success]
  veo.seokorea.org   http 200 · 판 0.3.607
  진단 서버          판 0.3.607 (19:36 에 새로 뜸 · uptime 864초)
  워커               판 0.3.607 · 1대
```

0.3.607 은 이 방의 0.3.606 **뒤**에 나온 판이고 링 커밋이 그 조상이므로, 도는 코드에 이
고침이 들어 있다. 판 번호(`APP_VERSION`)와 `RingLoader.tsx` 는 **같은 묶음으로 빌드된다** —
화면이 0.3.607 을 말하면 링도 새것이다.

> **재는 길**: 이 방은 Railway·veo.seokorea.org 에 못 닿는다(응답 000 · 프록시가 막는다).
> 대신 GitHub 의 워크플로 **「도는 판 확인」**(`.github/workflows/rollout-check.yml`)이 바깥에서
> 재 준다. 실행 목록은 `mcp__github__actions_list`, 안을 읽는 것은 `mcp__github__get_job_logs`.
> **이 길을 몰라 여러 세션 동안 「이 방은 실서비스를 못 잰다」로 끝냈다.**

## 바로 이어갈 작업

1. **사장님께 여쭌다** — `veo.seokorea.org` 에서 진단을 걸어 놓고 **링이 15초쯤에 서는지**.
   그 답이 오기 전에는 「고쳤다」고 하지 않는다(이 방이 그 말을 먼저 해서 한 번 혼났다).
2. **대장 대기 표를 정리한다** — 0.3.606 은 나갔으므로 `docs/WORKLIST.md` 의 미배포 머리말과
   대기 표에서 **이 방 줄만** 뺀다(머리말은 「미배포 **없음**」). 다른 방 줄은 건드리지 않는다.
   문서만 바뀌므로 다음 배포에 얹혀 나가면 된다.
3. `veo-platform` 의 `docs/STATE.md` 미배포 줄(`claude/ring-evenspeed`)도 같이 지운다.
4. 사장님이 「아직 선다」고 하시면 — 다음에 볼 자리는 **영상이 아니라 그리는 쪽**이다.
   같은 저장소에 `RingLoader` 를 캔버스·세이더로 다시 그린 시안이 있다(원본 영상 없이 돈다).

## 대기/차단

- **배포는 경쟁이다.** 점검 10분 + 채점 20분 동안 다른 방이 main 을 앞지르면 마지막
  한 걸음에서 거절된다 — 2026-09-18 에 **두 번** 그랬다(0.3.604 · 0.3.605). 물러나서
  다시 적는 것이 규칙이다.
- **veo-platform 개발 환경은 방마다 다시 세운다**(컨테이너가 매번 초기화된다):
  ```
  PYTHON=/usr/bin/python3.12 make setup
  pnpm install --frozen-lockfile
  service postgresql start
  su postgres -c "psql -c \"CREATE ROLE root LOGIN SUPERUSER PASSWORD 'veo'\""
  PGPASSWORD=veo make db-test-create
  ```
- 이 방은 실서비스를 못 잰다(egress 차단). **못 재는 자리는 「못 잼」이라고 적는다.**

## 주의·제약

- 가지: `veo-platform` 은 `claude/ring-evenspeed`, `desktop-tutorial` 은
  `claude/loading-animation-delay-zero-onncrv`. **다른 가지로 올리지 않는다.**
- 배포는 **`make deploy` 만**. `VEO_DEPLOY_ORDER` 에 사장님 문장을 그대로 인용한다.
- 사장님께는 쉬운 말로. 「커밋」·「배포」 두 낱말만, 못 잰 값은 «—», 지어낸 수치 금지.
- 커밋·PR·코드에 모델 ID 를 넣지 않는다(트레일러는 예외).

---

# (아래는 2026-09-10 · **베놈 마케팅 사이트** 링 기록이다)

## 지금까지 (핵심만)

사장님 주문은 결국 한 줄이었다 — «**그냥 로딩에니매이션 멈추지 않게 해줘**».

첫 물음은 「영상 끝과 시작의 딜레이를 0으로」였는데, **재보니 딜레이가 없었다**
(누락 프레임 0/774 · 되감기 간격 33.4ms = 평상시 33.5ms). 결함은 재생이 아니라
**영상 내용물**이었다. 15초 소스가 순환용이 아니라 이징 붙은 **인트로 렌더**라
한 바퀴 중 **1.96초가 정지 화면**이었고, 되감기가 하필 그 한복판에 떨어져
그 자리 보통 한 칸의 **11배**로 튀었다.

**고쳤다 — 등속 재추출.** 시간이 아니라 「이미지가 변한 양」이 균등하도록 프레임을
다시 골라 361칸 15.04초 → **164칸 6.833초**. 고른 164장은 전부 원본 프레임 그대로다
(해시 164개 대조).

```
[실측 2026-09-10]                    기존            신규
  멈춘 칸                       47칸 = 1.96초   →   0칸 = 0.00초
  가장 느린 순간                 최고의 0.4%    →   최고의 39.0%
  되감기 칸 (실화면 220px)        보통의 11배    →   1.42배 (보통 범위 안)
  webm                            819,103 B    →   471,830 B
  사파리 MP4 elst media_time          1024      →   0  (ctts 제거)
```

## 배포 — **나갔다**

```
60a9e57 → PR #235 → squash-merge 1499c5d → main
[실측 2026-09-10] main 위에서 확인
  ring-sq-loop.webm 471,830 B · ring-sq-loop.mp4 601,814 B  (만든 것과 SHA-256 동일)
  ring-loader.js → ring-sq-loop.*  ·  index.html → ring-loader.js?v=1.3.0
가지 `claude/loading-animation-delay-zero-onncrv` 는 main 보다 앞선 커밋 0건.
```

**실서비스에도 올라갔다** [실측 2026-09-10 · GitHub 배포 기록]. egress 는 막혀 있어
사이트를 직접 못 열지만, Vercel 이 GitHub 에 남기는 기록으로는 확인된다.

```
「Production – venom-new-site」
  1499c5d4 (링 커밋)      success   2026-09-09T18:39:57Z   ← 합친 지 32초 뒤
  1436dcfb (오늘 main 끝)  success   2026-09-10T14:58:56Z
링 커밋은 1436dcfb 의 조상 — 지금 도는 빌드에 새 링이 들어 있다.
```

이건 **「빌드가 성공했다」까지**다. 화면에 어떻게 보이는지는 아래가 남았다.

## 2차 — 멈칫은 되감기가 아니라 「재마운트」였다 (2026-09-10)

배포 뒤 사장님이 «시작할때도 멈칫하고 **15초쯤** 이어지는 지점에서도 멈칫» 하고 잡아내셨다.
새 영상은 한 바퀴 6.833초라 15초에 이음매가 있을 수 없다 — **다른 결함이었다.**

`index.html` 이 진단 1·3단계에서 `el.innerHTML=''` 로 통을 비우면, `mount()` 가
`<video>` 를 **통째로 새로 만든다.** 파일을 다시 받고 디코더를 다시 세우는 동안 링은
정지 사진으로 서 있는다.

```
[실측 2026-09-10 · 크로뮴 · 캐시 없음 · 4G]      고치기 전   →  고친 뒤
  1단계 마운트 (진단 시작)                        210ms (4배)  →  28ms (1배)
  2단계 캡션만 교체                                24ms 멀쩡   →  26ms 멀쩡
  3단계 재마운트 (「이어지는 지점」)                224ms (4배)  →   9ms (0배)
  한 바퀴 중 가장 긴 정지                          233ms (5배)  →  83ms (2배)
```

고침 둘 — ① **영상 요소 못(pool)**: 버리지 않고 넣어 뒀다 다시 꺼내 쓴다(둘까지).
② **`RingLoader.warm()`**: 진단 폼을 처음 만질 때 한 번 미리 받는다.
`index.html` 은 `?v=1.4.0`.

> **이 값도 크로뮴이다.** 사파리·아이폰은 H.264 디코더를 새로 세워야 해서 더 컸을 것이나
> 이 컨테이너가 H.264 를 못 풀어 여전히 «—» 다.

## 셰이더 링 — 만들어 뒀다, 아직 안 켰다 (2026-09-11)

사장님 «세이더로 제작해봐». 만들었다. 파일은 `venom-wordpress/preview/assets/ring-shader.js`
(117줄, 라이브러리 없음). **어느 화면도 이 파일을 부르지 않는다 — 켜려면 한 줄이면 된다.**

```
[실측 2026-09-11 · 화면을 찍어서 잼]   원본 영상      셰이더
  색상각                                 35.2     →    34.6
  밝은 부분 색 (R G B)                253 182 81  →  237 166 69
  밝기                                    172     →     158
  되감기                              6.8초마다   →    없음(끝나는 지점이 없다)
  내려받기                          461KB/588KB  →    0KB
```

도는 것을 보는 시안: <https://claude.ai/code/artifact/5e2a0ca2-4203-4065-a054-8ad0c2a604fa>

**왜 셰이더인가** — 밖을 뒤져 보니 이 계열(AI 오브·글로우 링)은 업계가 영상으로 안 만든다.
ElevenLabs Orb(오픈소스)·Orbkit(33종)·Shadertoy 전부 GLSL 셰이더다. 시간을 넣으면 그 순간을
계산해 내므로 **끝나는 지점이 없다.** 되감기·사파리 VP9/MP4·시작 지연이 한꺼번에 사라진다.

**똑같지는 않다.** 원본은 3D 렌더라 화면만 보고 안의 값을 되찾을 수 없다. 나란히 놓으면
구분된다. 형태·꼬임·색·번짐까지는 맞췄다.

> **[모션 방 2026-09-14 · 「가닥결까지 맞췄다」는 과했다]** 사장님이 «세이더로 바꾸면 실
> 같은 디테일이 없어지던데» 라고 하셨고, **재보니 맞다.** 링 띠를 가로지르는 반지름
> 단면에서 또렷한 봉우리(= 눈에 보이는 실 가닥)를 세면
> ```
> 원본 영상  단면 봉우리 평균 8.8개 (2~22)   밝은 픽셀 88,689   최대밝기 237
> 셰이더     단면 봉우리 평균 0.7개 (0~2)    밝은 픽셀 27,766   최대밝기 255(포화)
> ```
> **값 하나로 못 고친다 — 둘 다 쓸어 봤다.**
> ```
> 가닥 수만 줄이면      88→0.6 · 32→3.6 · 12→7.1 봉우리. 숫자는 오르는데
>                      화면은 실이 아니라 **모아레 점무늬**다(심이 0.33px — 한 픽셀보다 얇다)
> 심을 굵히면          28가닥 기준 0.33px→3.6 · 0.76px→2.1 · 1.16px→0.7 봉우리.
>                      굵힐수록 서로 **합쳐져** 다시 매끈한 띠가 된다
> ```
> 얇으면 계단이 지고 굵으면 뭉친다 — 이 셰이더 구조로는 사이 값이 없다. 게다가 밝은
> 면적이 원본의 **1/3**이라 번짐(헤일로)도 작다. §3 「코드로 100% 같게는 안 된다」가
> 가닥결에서도 그대로 참이다. **셰이더를 영상 대체로 켜면 사장님이 바로 알아보신다.**

**사장님 결정 대기.** 「이걸로 가자」면 `ring-loader.js` 의 `takeVideo()` 자리에 캔버스를
끼우면 된다(CSS 는 `.rl-disc video` 선택자에 `,.rl-disc canvas` 만 더하면 그대로 먹는다).

## 바로 이어갈 작업

> **[모션 방이 2026-09-14 에 덧붙임]** 아래 1번(영상 미리 받기)은 **이미 들어갔다** —
> `5123f83` (#243), 실서비스 배포 성공 2026-09-11 04:55Z. 이 절은 그때 기준이라 낡았다.
> 다만 사장님은 **09-13 에도 «시작 1초 · 끝 1초 멈춤»** 이라고 하셨다. 모션 방이
> 크로뮴에서 다시 쟀으나 재현이 안 된다(시작 2ms · 되감기 1.63배 · 블러 전환 17~33ms ·
> MP4 구조 정상). 남은 축은 **사파리·아이폰**이고, 그 자리를 재려고
> `venom-wordpress/preview/ring-check.html` 을 만들어 두었다 — 사장님 기기에서 열면
> 숫자가 나온다. **그 숫자를 보고 고쳐라. 먼저 만들지 마라**(이 방이 한 번 그랬다).

### 1. ~~사장님 「ㄱㄱ」 를 기다린다 — 영상 미리 받기~~ **끝났다 (#243)**

시작할 때 버벅이는 이유는 하나뿐이다: **누른 뒤에야 영상을 받기 시작한다.**
정지 사진(`ring-sq.jpg`)은 그 사이를 메우려고 있는 것이라, 영상이 이미 와 있으면
**정지 사진은 아예 안 뜬다.**

```
지금  index.html 이 진단 폼을 「만질 때」 RingLoader.warm() 을 부른다
      → 바로 누르는 사람에겐 100ms 앞설 뿐이라 늦다
할 것 진단 화면이 눈에 들어올 때(IntersectionObserver) 미리 받는다
      → 누를 때쯤 준비 끝. 그림은 100% 지금 영상 그대로
값    진단까지 내려온 방문자만 461KB. 위만 보고 나가면 안 받는다
되돌리기  한 줄
```

**아직 넣지 않았다.** 사장님이 물리신 안(코드 링 → 영상 바꿔 끼우기)과 다른 것이니
혼동하지 말 것.

### 2. 물린 안 — 되살리지 마라

「코드 링을 잠깐 띄우고 영상이 오면 넘겨받기」는 사장님이 «이 방식은 아닌것 같은데» 로
물리셨다. 시안: <https://claude.ai/code/artifact/ba4e19ce-bc24-4164-9a09-f327755eaccd>
서로 다른 두 개를 바꿔 끼우는 군더더기라는 판단이고, 타당하다.

### 3. 「코드로 100% 같게」는 안 된다 — 다시 시도하지 마라

해봤고 숫자로 막혔다. 자세한 건 세션기록 §14.

```
잣대(원본 프레임끼리)   0.76 같은그림 · 3.56 거의같음 · 20.9 다른그림 · 25.7 안그림
코드로 간 최선          13.66   ← 좌표하강 4회 재시작 · 760초
왜                      밝기 1차 성분 97%(한 방향 빛) · 한 바퀴 순 회전 −2°(회전이 아님)
                        3D 렌더라 화면만 보고 안의 값을 되찾을 수 없다
```

색만은 맞는다(원본 R202 G136 B66 · 코드 R197 G132 B66). 빛이 퍼진 모양이 다르다.

## 되돌리는 법

`venom-wordpress/preview/assets/ring-loader.js` 의 `v.innerHTML` 한 줄에서 `-loop` 만
빼면 원본 15초 소스로 돌아간다. 원본 `ring-sq.webm`/`ring-sq.mp4` 는 지우지 않고
남겨 뒀다. `index.html` 의 `?v=` 를 올려야 캐시가 털린다.

## 주의·제약

- **링 CSS 는 손대지 마라.** `.rl-host` 에 `isolation:isolate` 금지,
  `.rl-host/.rl-scrim/.rl-overlay/.rl-disc` 에 `z-index`·`opacity`·`filter`·
  `transform`·`mask` 새로 얹기 금지. 쌓임 맥락이 하나만 생겨도
  `mix-blend-mode: screen` 이 깨지고 **검은 원반**이 돌아온다.
  이번 변경은 CSS 를 **한 글자도** 안 고쳤다.
- **망은 막혔지만 GitHub API 는 열려 있다.** `venomad.com`·`venom-new-site.vercel.app`·
  `pagespeed.web.dev` 는 게이트웨이가 CONNECT 에 403 을 준다(`selective:false`).
  그런데 `api.github.com` 은 200 이고 `GH_TOKEN` 도 환경에 있다 — **배포가 나갔는지는
  사이트를 안 열고도 `/repos/{owner}/{repo}/deployments` 로 잴 수 있다.**
- **영상 검사에는 Range 를 받는 서버가 필요하다.** 파이썬 `http.server` 는 Range 가
  없어 Chromium 이 **탐색을 조용히 거부**한다(프레임 차이가 전부 0 으로 나온다).
  Node 로 Range 되는 정적 서버를 따로 띄워야 한다.
- **서버는 `run_in_background` 로 띄운다.** `nohup ... &` 도 셸과 같이 죽는다.
  또 `pkill -f "무엇"` 은 **자기 명령줄까지 잡아** 셸이 자살한다(exit 144).
- **사파리를 사장님 숙제로 넘기지 마라.** 이번에 내가 못 잰 축(사파리)에 매달리는 사이
  사장님은 PC 에서 재현하고 계셨다. **내가 잴 수 있는 자리부터 보고**, 못 재는 것은
  볼 항목을 적어서 넘긴다.
- **캔버스 색을 잴 땐 검은 바탕에 얹고 읽어라.** 투명 배경을 `getImageData` 로 그대로
  읽으면 알파 되돌림으로 값이 망가진다(색상각 31° 가 60° 로 나왔다).
- **못 재는 자리에서는 가설을 가설이라고만 말한다.** 이 방에서 두 번 틀렸다 —
  핑퐁을 「진짜 0」이라 단언했다 갈렸고, 더 크게는 **사장님이 말한 문제(멈춤)를
  악화시키는 안을 만들고 나서야 쟀다.** 만들기 전에 주문을 다시 읽어라.
- 이 가지 외로 배포 금지. 대장·변경이력에 「민다」 금지 —「커밋」·「배포」 두 낱말만.
- 커밋 트레일러: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` +
  `Claude-Session: https://claude.ai/code/session_01GE6NgLB4AdGVvPpuSukZ5P`.
  모델 ID 는 트레일러에만.
- 사장님께 못 잰 값은 «—», 지어낸 수치 금지, 의료광고법 준수.

## 참고

- 눈으로 보는 증거 페이지(두 링 나란히·속도계·감속 재생):
  <https://claude.ai/code/artifact/39099173-a441-4efd-9083-8c010ef058e7>
  **이 세션은 그 페이지의 댓글 알림을 못 받는다**(게이트웨이 404) — 고칠 것은 대화로.
- 다시 만드는 ffmpeg 명령과 등속 재추출 알고리즘은 세션기록 §4·§10 에 그대로 있다.
  `-bf 0` 이 사파리 되감기 목표를 제자리에 놓는 열쇠다.
