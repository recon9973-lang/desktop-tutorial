# RESUME (상권 네비 방) — **닫힘. 전부 나갔다** (2026-09-16 확인)

> 상세는 `docs/session-logs/2026-09-16-nav-local.md`.
> 현황 `PROJECT_STATE.md`, 지도 `핵심두뇌_MASTER.md`.
> **이 방에서 이어갈 작업은 없다.** 아래 「다음 방 몫」만 남았다.

## 한 줄

사장님 「상단 네비에 상권 탭이 없는데」 → 탭 하나가 아니라 **지역·상권 축 자체가 없었다.**
축을 세워 `/local` 허브·상권분석 탭을 만들고, 묻혀 있던 「베놈 체크업」 6종을 본진에 이었다.
**main 에 나갔고 실서비스 반영까지 실측 확인했다.**

## 나간 것

```
70e3136  feat(nav): 상단에 상권분석 탭을 세우고 체크업 6종을 본진에 잇는다      (PR #249)
af0d7e6  fix(nav,seo): 태블릿 헤더 겹침을 걷어내고 지역 글 6편에 제 주소를 준다  (PR #250)
```

둘 다 `origin/main` 조상 [실측 · merge-base]. 작업 가지 `claude/charming-cerf-s4n1ca` 는
main 으로 맞춰 두었고 **앞선 커밋 0건**. 다시 커밋·배포할 일이 없다.

실서비스 실측 — 상단 네비 7탭에 **상권분석** 있음 · `pg-local`·`/local` 각 1건 ·
헤더 CTA → `/contact` · 드로어 분기점 `max-width:1023px` · sitemap **216 URL**
(`/local` + 체크업 5경로 + 지역 글 6/6). CI 「Check Sitemap Drift」·「Update PROJECT_STATE」 초록.

## 다음 방 몫 (이 방은 손대지 않았다)

설계 원본 `홈페이지 제작을 위한 최종 사이트맵 구조 (Sitemap).txt` 대비 **미구현분**이다.
자세한 근거·실측 수치는 세션 기록 「구조 점검」 절에 있다.

**B등급 6건**
1. 검색광고 4종 개별 페이지 — 파워링크·파워컨텐츠·브랜드검색은 본문 언급만, **플레이스검색광고 언급 0**
2. SNS·앱 광고 — **페이스북 페이지 없음**, **당근 언급 0**
3. **브랜드마케팅 4종**(브랜드기획·콘텐츠마케팅·브랜드블로그·영상콘텐츠) 전부 언급 0
4. **테크니컬 SEO·링크빌딩 SEO** 언급 0
5. GEO/AEO/SEO 「~란?·프로세스·효과」 3단 구성 — **못 쟀다**(네비·라우트 층만 실측)
6. **실무 분석·외부 리소스 링크 허브** — 네이버 키워드 도구·약국 키워드 맵·공신력 소스 연동 전무

**C등급 3건** — 조직도 없음 · 본진 미연결 독립 페이지 5개(`/samples/*`·`/growthops.html`·
`/geo-ops.html`·`/ring-check.html`·`/hospital-bot/report.html`) · 모바일 하단바에 SEO 홈페이지 없음

## 주의·제약 (다음 방이 밟을 자리)

- **이 방은 실서비스를 직접 못 잰다** — venomad.com·vercel egress 403.
  대신 **`fetch-file.yml` 을 workflow_dispatch 로 띄우면 러너가 대신 받아 가지에 커밋한다.**
  이 저장소에서는 `actions:write` 가 **된다**(옛 방 인계의 「403」 과 다르다). 확인 후 임시 파일은 지울 것.
- **상단 네비는 `뷰포트 − 321px` 를 넘으면 헤더 우측 버튼과 겹친다.** 탭을 늘리기 전에 이 값부터 재라.
  지금 7탭이고 1024px↑ 에서 겹침 0px, 1023px↓ 는 드로어다.
- **로컬 검증 서버는 `vercel.json` rewrite 를 그대로 흉내낼 것.** 느슨하게 쓰면
  `/seo/seo-engine.js` 가 HTML 로 돌아와 **없는 JS 오류**가 생긴다(이 방이 한 번 속았다).
- **기준선을 같은 조건에 놓기 전에 단언하지 말 것.** 이 방은 세 번 틀렸고 셋 다 그 이유였다
  (세션 기록 「내가 틀린 자리」).
- 정적 지역 글을 더할 때는 `index.html` 의 `slug` 와 `lib/sitemap-builder.js` 의
  `STATIC_POST_SLUGS` **둘 다** 고친다. 한쪽만 고치면 `check-sitemap` 이 표류로 잡는다(정상 동작).
- `PROJECT_STATE.md` 는 자동 생성물 — 손대지 말고 CI 또는 `node scripts/gen-project-state.mjs`.
- 커밋 트레일러: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` +
  `Claude-Session: …`. 모델 ID 는 트레일러에만.
- 사장님께는 「커밋」·「배포」 두 낱말만. 못 잰 값 «—», 지어낸 수치 금지, 의료광고법 준수.

## 참고

- 루트 `RESUME.md` 는 **다른 방(진단 오진 방) 것**이다. 이 방 것은 이 파일이다.
- 이 저장소 인계는 방마다 따로다 — `RESUME-*.md` 목록을 먼저 볼 것.
