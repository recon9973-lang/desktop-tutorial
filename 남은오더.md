# 남은 오더 — 방 스물다섯을 훑어 정리하고, 서 있던 일곱을 전부 풀었다

> 2026-09-16 · 「지도 과별 색상 및 병원명 레이아웃」 방이 **닫히기 전에** 남긴 지도.
> 사장님 지시: *"방이 많아서 하나하나 내가 선택하기 힘드니 진행하고 보고해줘."*
> 아래 상태는 각 방이 스스로 적은 요약이거나, 내가 저장소 원본을 직접 읽어 확인한 것이다
> — **둘을 구분해 적었다.**

## 오늘 나간 것

```
0.3.588   상권 탭 정렬 (같은 과 색 · 경쟁 칸 · 박스 정렬)
0.3.593   AEO 언급현황 — 낱말 구름 · 한 줄 설명 · 8칸+4칸
0.3.599   속도 개선 넷 — AEO 탭 · 큐 검증 · 문서 · /api/queue 지연 고침
PR #250   베놈 상권 탭 누락 — 태블릿 서랍 · 블로그 주소 6건
PR #252   ANSEO 업체 삭제 · 휴지통
2d974896  3km 이상 군집 표기
```

**AEO 가·나·다 셋 다 들어갔다 — 끝난 일이다.** [실측 · main 원본을 직접 읽음]
`c6cdf864` 한 커밋에 셋이 다 있다. 낱말 구름이 `BubbleCloud` 를 대체하고,
`.oneline` + `?` `<details>` 가 네 문단을 한 줄로 줄였고, `.row12`/`.c8`+`.c4` 와
`calc(row*2+gap)=366` · `calc(row*3+gap*2)=556` 이 오른쪽 빈칸을 없앴다.
그 판이 실제로 돈다 — 웹·진단 서버·워커 전부 0.3.593(`afbd343`).

**사장님 화면은 거래처 AEO 탭의 `MentionShareBoard`** 다(오류 216). 딸림 방이 처음에
`MentionRoster` 를 붙들어 보고가 계속 어긋났다. 다음에 「AEO 언급현황」이라 하면 이 부품이다.

## 서 있던 일곱 — **전부 풀어 내려보냈다**

| 방 | 서 있던 자리 | 내려보낸 답 |
|---|---|---|
| 오류 상세 내용 | A안 vs B안 | **A안** — 탭+승격 (사장님 확정) |
| URL 해시 | ①부터? 다른 순서? | **①→②→③ 그대로** |
| SEO/GEO 진단 항목 | 크롤 한도 세 갈래 | **부분 크롤 기록 + 못 잰 것은 «—»** |
| SEO 재진단 텀 | 「고칠까요?」 | **텀은 유지, 벽처럼 안 느껴지게** |
| ANSEO 속도 개선 | 배포 오더 대기 | **나갔다 — 판 0.3.599** (아래) |
| 보관 실행 확인 | 카카오 열쇠 백업 대기 | **열쇠에 막혀 서 있지 마라 — 갈라서 진행** |
| 그랜드아름다운 AEO | 입력 2건 대기 | **코드로 되는 것부터, 필요한 건 한 줄로** |

### 답의 근거 — 지어낸 것이 아니다

- **크롤 한도**: 「한도를 올린다」를 버렸다. 남의 서버를 더 때린다. 「해당 없음」도 버렸다
  — 해당되는데 우리가 못 잰 것이라 **거짓**이다. 오류 135·192 가 바로 그 실수였다
  (못 읽은 응답을 실패로 적어 멀쩡한 사이트를 BLOCKER 로 만들었다).
- **재진단 텀**: s20 진단 비용 보고서가 「30분은 돈이 아니라 **남의 서버 속도와
  「잘린 진단」을 막는 장치**」라고 못 박아 뒀다. 없애면 안전 장치를 걷어내는 것이다.
  진짜 불편은 「언제 되는지 모른다」 쪽이라, 남은 시간을 보여주고 **앞 진단이 끝났으면
  시계와 무관하게 풀어주라**고 했다. 코드가 「시각으로만」 막고 있으면 그것이 결함이다.
- **여드레~열이틀 묵은 방 넷**에는 공통으로 일렀다 — 그때 main 은 0.3.55x 대,
  지금은 **0.3.593** 이다. **현재 main 위에서 다시 보고 시작해라. 이미 들어간 것은
  「이미 됐다」고 적고 버려라** — 같은 것을 두 번 내보내지 않는다.

### 모든 방에 함께 준 것

```
묻고 멈추지 마라 — 갈리면 ㉠사장님 문장에 가까운 쪽 → ㉡안전한 쪽 → ㉢덜 어수선한 쪽.
                  적어두고 계속 간다. 멈춰도 되는 것은 데이터의 진실이 걸린 것 하나뿐.
빈 자리 금지     — 8칸+4칸 · 366+14+176=556 · `6+3+3` 금지
main 직접 커밋 금지 · 배포는 오더 없이 금지
보고는 가지의 RESUME.md 에 적어 커밋 — 요약 한 줄은 정해진 형식으로
```

## 속도 개선 — **나갔다 · 판 0.3.599** (2026-09-17 02:45)

367커밋을 합쳐 시험 7,708건 초록을 만든 뒤, 사장님 오더 「그 방에서 배포해」를 그대로
옮겨 넷 전부 내보냈다.

```
[그 방 보고 · 이 방 실측 아님]
  판 0.3.599 — 진단 서버 · 워커 · 웹 셋 다
  나간 것 4/4   AEO 탭 · 큐 검증 · 문서 · /api/queue 지연 고침
  /api/queue    12.38초 → 0.25초 (운영에서 확인했다고 적혔다)
```

**사장님이 겪으신 그 증상이 이것이었다** — 콘솔 「변경 이력」이 3초에서 끊겨 늘
「진단 못 함」으로 뜨던 것. 원인은 `control.ping`+`broadcast` 를 목적지 없이 불러
답을 받고도 타임아웃을 통째로 기다린 것이고, 캐시 미스마다 무는 구조적 비용이었다.

## 사장님 몫으로 남는 것 — **없다**

일곱 방이 다 풀렸다. 나머지 여섯은 답을 받았으니 스스로 굴러간다. 열쇠 백업·거래처
자료도 「정말 필요한지」부터 다시 따지라고 해 뒀다 — 필요 없다고 나오면 그것도 손이 안 간다.

## 다음 방이 반드시 알아야 할 둘

**하나 — 방과 방 사이 관이 좁다.**
`get_session` 으로 넘어오는 것은 **요약 두 줄뿐**이고 그 두 줄은 **기계가 짓는다.**
표로 달라 두 번, 압축 한 줄로 달라 한 번 청했고 세 번 다 막혔다.
→ **우회 길: 그 방 가지의 `RESUME.md` 에 적게 하고, 이쪽에서 가지를 읽는다.**
   URL 해시 방의 ①②③ 도 이 길로 알아냈다. **모든 방에 이 길을 일러 뒀다.**

**둘 — 저장소를 다시 붙여 봐라.**
「veo-platform 이 안 열린다」고 이 인계에 적혀 있었는데, **오늘 `add_repo` 로 그냥 붙었다.**
붙이자마자 AEO 가·나·다를 원본으로 확인할 수 있었다. **막혔다는 기록을 믿고 재시도를
안 한 것이 잘못이었다.** 막혔다고 적힌 자리도 한 번은 다시 두드려 봐라.

## 못 잰 것

- **실서비스 화면을 이 방에서 못 본다** — `venom-new-site.vercel.app` 이 관문에서
  403(정책 거절)이다. 코드와 도는 판 기록까지가 내가 댈 수 있는 근거다.
