# 남은 오더 — 방 스물다섯 훑어 정리 (2026-09-16 · 한국 24:0x)

> 「지도 과별 색상 및 병원명 레이아웃」 방이 **지워지기 전에** 남긴 지도.
> 이 방이 하던 일은 `RESUME.md` + `RESUME-market-layout.md` 에 따로 있다.
> 아래 상태는 **각 방이 스스로 적은 요약**이다 — 내가 실측한 것이 아니다.

## 오늘 나간 것

```
0.3.588   상권 탭 정렬 (같은 과 색 · 경쟁 칸 · 박스 정렬)
0.3.593   AEO 언급현황 워드클라우드
PR #250   베놈 상권 탭 누락 — 태블릿 서랍 · 블로그 주소 6건
PR #252   ANSEO 업체 삭제 · 휴지통
2d974896  3km 이상 군집 표기 (상권 방 · 내가 미뤄 뒀던 덤)
```

## 사장님 손이 필요한 것 — **여섯**

| 방 | 막힌 자리 | 사장님이 하실 일 |
|---|---|---|
| 오류 상세 내용 파악 | A안(탭+승격) vs B안(목록만) | 한 쪽 고르기 |
| SEO 진단 재진단 텀 제거 | "고칠까요?" | 예/아니오 |
| SEO/GEO 진단 항목 검토 | 6건 남음 · 크롤 한도 방향 | 방향 한 줄 |
| ANSEO 속도 개선·진단 UI | 배포 오더 대기 | 그 방에서 직접 「배포해」 |
| 보관 실행 확인 | 카카오 JS 키 백업 대기 | 키 백업 |
| 그랜드아름다운의원 AEO | 2건 입력 대기 | 자료 주기 |

### AEO 가·나·다 — **셋 다 들어갔다. 끝난 일이다.**

[실측 2026-09-16 · veo-platform main 의 원본을 직접 읽어 확인]

```
가   MentionWordCloud 가 BubbleCloud 를 대체        c6cdf864
     (BubbleCloud 부품은 안 지웠다 — 키워드 화면이 쓴다)
나   .oneline / .onelineSummary / .onelineQ / .onelinePop
     한 줄 + `?` 를 누르면 그 자리에서 펴지는 <details>    같은 커밋
다   .row12(12칸) · .c8 + .c4(8+4) · .fillRow
     .cardM = calc(row*2 + gap) = 366 · .cardL = calc(row*3 + gap*2) = 556
     전폭은 span 12 가 아니라 1 / -1                       같은 커밋
```

**화면이 실제로 그 판을 서비스한다** — [실측 2026-09-16 21:33 KST · 워크플로 실행
35096564436] 웹 0.3.593 · 진단 서버 0.3.593 · 워커 0.3.593 (`afbd343`).

**보신 자리가 어디였나** — 오류 216 에서 밝혀졌다. 사장님 화면은 **거래처 AEO 탭의
`MentionShareBoard`** 다. 딸림 방이 처음에 `MentionRoster` 를 붙들고 있어서
보고가 계속 어긋났다. 다음에 「AEO 언급현황」이라 하면 **`MentionShareBoard`** 다.

좁은 폭도 브라우저로 쟀다 — 1560·1100·700·400 네 폭에서 **낱말 30/30 · 겹침 0 ·
칸 밖 0 · 가로 밀림 0** (`84e7cbfb`). 176px 카드 한가운데가 비던 것도 띠로 채웠다
(`8f49a4f6`).

## 풀어 놓은 것 — URL 해시 방

`session_01E35QUREsyLoHytPUhaKw1w` · 가지 `claude/url-hash-issue-87ol9k`.
「①부터 할까요?」로 서 있던 것을 **①→②→③ 그대로** 가라고 내려보냈다.

```
①  변경이력 우리 몫을 main 에 — 후보 가지 deploy-candidate-claude-changelog-our-release
    의 18fb021 이 채점 대기였다. 판 번호가 안 물리는 경로다(오류 대장 201)
②  venomad 재진단 — 「새 판이 돌면」이라는 조건이 이미 풀렸다(0.3.564 → 0.3.593).
    그 방도 실서비스로 못 나갈 수 있다 — 그러면 사장님이 콘솔에서 돌리신다
③  판 번호 정리 — 다음 판 물릴 때 함께
```

## 지금 도는 방 셋

```
로딩 애니메이션 딜레이 제거
AEO 그래프와 AI별 언급률 차이
지도 과별 색상 및 병원명 레이아웃     ← 이 방 (지워질 예정)
```

## 다음 방이 반드시 알아야 할 둘

**하나 — 방과 방 사이 관이 좁다.**
`get_session` 으로 넘어오는 것은 **요약 두 줄뿐**이고 그 두 줄은 **기계가 짓는다.**
그 방이 아무리 자세히 적어도 안 넘어온다. 표로 달라 두 번, 압축 한 줄로 달라 한 번
청했고 **세 번 다 막혔다.**
→ **우회 길: 그 방 가지의 `RESUME.md` 에 적어 커밋하게 하고, 이쪽에서 가지를 읽는다.**
   (URL 해시 방의 ①②③ 도 이 길로 알아냈다. `desktop-tutorial` 가지는 여기서 읽힌다.)

**둘 — 딸림 방에는 판단 규칙을 먼저 주고 일을 줘라.**
안 주면 한 시간에 세 번 멈춰 묻는다. 이번에 그랬고, 원인은 그 방이 아니라 인계였다 —
시안이 **기존 부품을 모르고** 그려져서 어긋나는 자리마다 올려보내게 돼 있었다.
```
시안이 기준이다. 시안과 기존 부품이 어긋나면 시안이 이긴다.
갈리면 ㉠시안 → ㉡사장님 문장에 가까운 쪽 → ㉢덜 어수선한 쪽. 적어두고 계속 간다.
멈춰도 되는 것은 하나 — 데이터의 진실이 걸린 것.
```
**기존 부품 이름도 같이 넘겨라.**

## 내가 못 잰 것

- **실서비스 화면을 이 방에서 못 본다** — `venom-new-site.vercel.app` 응답 `000`(길 막힘).
  0.3.593 이 실제로 도는지, AEO 화면이 어떻게 보이는지 **전부 «—»** 다.
- **veo-platform 을 이 방에서 못 연다** — `add_repo` 거절 · 조회 거절 · 자격 증명 없음.
  그 저장소가 붙은 방을 쓰거나 새 세션에서 붙여야 한다.
