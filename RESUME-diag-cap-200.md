# RESUME (진단 200장 상한 방) — **닫힘. 나갔다** (0.3.620 · 2026-09-26 확인)

<!-- 가지: claude/wonderful-cori-8z4sqp -->

> **코드는 이 저장소에 없다** — `veo-platform` 이다(`add_repo` 로 붙인다, owner
> `recon9973-lang`). 이 저장소에는 배포 인계서
> (`docs/2026-09-18-ANSEO-배포-인계.md`)만 있다.

## 무엇을 물으셨나

사장님 2026-09-18: *「AnSEO 프로그램에서 지금은 진단을 200개까지 하고 있는데, 확장하거나
줄일 필요가 있는지 서브에이전트와 공식 데이터를 활용해서 종합적으로 검토해줘」*

「200」은 **한 번 진단할 때 훑어보는 홈페이지 장수의 상한**이다
(`console_crawl_max_urls` · 명세 `measurement_scope.max_pages`).

## 검토 결론 — 세 갈래로 나눠 조사했다

**줄일 근거는 하나도 없다.** [실측 2026-08-01] 상한이 100 이던 시절 실도메인 8곳이
**전부** 잘렸고, 그 상태로 「깨진 안쪽 링크 없음」을 8건 전부 거짓 통과시켰다.
100 → 200 은 그 사고가 만든 값이다.

**올릴 이유는 점수다.** 상한에 걸리면 검사 9개가 «—»(모름)로 떨어지고 그 «—»는 분모에
남아 0점이다. [실측 2026-09-16] 전체 89.24 대 부분 80.14 — **9.10점**. 거래처 점수가
우리 설정 때문에 내려간다. 돈은 안 든다 — 진단 1회 외부 청구액 0원, 구글 속도 측정은
장수와 무관하게 고정 5장.

**올리면 치르는 대가는 시간·저장·비교선이다.** 남의 서버에 대한 예의로 초당 2장으로
묶여 있어(실측 장당 0.94초) 400장이면 긁는 데만 6분이 넘고, 「한 판 최악 6.4분」을
전제로 짠 정기 진단 차례가 깨진다. 진단 한 판의 기록이 101킬로바이트라 장수에 비례해
커지는데 그것 때문에 2026-08-23 에 운영이 멈춘 적이 있다. 본 장수가 3배 넘게 차이 나면
지난 진단과 비교할 수 없게 막아 둔 규칙도 있다.

**공식 자료는 「200은 검색엔진 기준이 아니라 우리 표본 크기」라고 말한다.** 구글은 URL
몇천 개 미만이면 크롤 걱정이 필요 없다고 하고, 크롤 예산 안내서는 1만(매일 급변) 또는
100만 페이지부터 읽으라고 한다 — 병원 홈페이지는 전부 그 아래다. 남들과 견주면 200은
무료·체험 등급 한가운데다(세마러시 무료 월 100 → 우리 200 → 스크리밍프로그 무료 1회
500 → 에이치레프스 무료 월 5,000). 네이버 서치어드바이저 쪽 수치는 **«—»** — 네이버가
검색 접근을 막아 1차 자료를 못 봤다. 사람이 직접 열어야 한다.

**권고: 일률적으로 올리지 말고 「200을 넘는 곳만 더 보기」.** 사이트맵으로 크기를 먼저
재면 거의 공짜다. 200 안에 드는 곳은 지금처럼 전부 보고, 넘는 곳만 상한을 푼다
(사이트마다 따로 담는 칸이 이미 있다 — `sites.crawl_settings`). 속도 측정은 어차피
5장 고정이라 요금이 더 들지 않는다. **다만 그 결정 전에 거래처 실제 장수를 재야 한다.**

## 나간 것 — `veo-platform` 판 0.3.620

```
4d960dcc  거래처 홈페이지 장수를 재는 자 — scripts/measure_customer_pages.py
c845c3b7  대장 §4-C 에 한 줄 — 「잴 준비됐다」
c2359d80  배포 대기 표에 번호 없는 줄 · 머리말에 「번호 미정」 꼬리
862a7f97  main(0.3.610) 합침
```

사이트맵으로 장수를 세고, `--crawl` 을 붙이면 제품 크롤러를 상한만 풀어 돌려 실제
장수까지 낸다. 사이트맵 해석은 제품 것(`seo/parsing/sitemap.py`)을 그대로 빌린다 —
새로 지으면 `image:loc` 을 페이지로 세어 제품과 다른 숫자가 나온다.

## 바로 이어갈 작업 — **하나 남았다. 아직 «—» 다**

**거래처 15곳의 실제 장수를 아무도 안 쟀다.** [실측 2026-09-26] main 에
`docs/research/*-customer-page-counts.json` 이 없다. 대장 §4-C 에 그 줄이 살아 있다.

```bash
scripts/measure_customer_pages.py --only 참사랑 --crawl   # 한 곳만, 몇 분
scripts/measure_customer_pages.py --crawl                 # 전부
```

**참사랑한의원부터 재는 것이 급하다.** 172 → 183 → 188 → **195장**(2026-08-02~08-11)
으로 9일에 13%씩 자라 상한 200 의 97.5% 에 붙어 있었고, 그 뒤로 잰 기록이 없다. 넘었다면
그 거래처는 지금 **우리 상한 때문에 9점가량 깎인 점수**를 받고 있다.

이 방은 그 숫자를 못 냈다 — 작업 컨테이너가 거래처 사이트로 못 나가고(프록시 403)
운영 DB 접속 줄도 없다. **운영 DB 와 바깥길이 있는 자리에서 돌려야 한다.**

## 손대지 않은 것

**상한은 지금도 200 이다** [실측 2026-09-26 · `settings.py:645` · 명세 1.15.0].
재기 전에는 바꾸지 말자는 것이 이 방의 결론이고, 사장님도 2026-09-16 에 「올리지
않는다」로 이미 정하셨다.

곁가지로 찾은 것 넷(값과 무관한 어긋남) — 고치지 않고 적어만 둔다.

```
.env.example 의 VEO_CRAWLER_MAX_PAGES=500     읽는 코드가 없는 죽은 줄
요청 max_urls 가 최대 500                     명세는 200 인데 500장 재는 길이 열려 있다
명세 근거란의 「450/h · 240초」                 예산은 2,000 이 됐고 240초 구조는 없어졌다
한 판 최대 요청 211                            실제는 212 (`/llms.txt` 한 번이 빠졌다)
```
