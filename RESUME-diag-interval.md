# RESUME (재진단 텀 방) — **닫힘. 전부 나갔다** (2026-09-18 확인)

<!-- 가지: claude/seo-diagnosis-interval-removal-othn8e -->
<!-- 같은 이름 가지가 veo-platform · desktop-tutorial 둘 다에 있다 -->

> 상세는 `docs/session-logs/2026-09-17-s20.md`.
> 현황 `PROJECT_STATE.md`, 지도 `핵심두뇌_MASTER.md`.
> **이 방에서 이어갈 작업은 없다.** 대장 대기 표에 이 방 줄도 없다.

## 한 줄

사장님 «SEO 진단 30분 텀에서 요금 부담이 없다면 재진단에 텀 없애줘» → **텀은 요금이 아니라
호스트 예산 때문이었다.** 텀을 없애고 예산으로 세게 바꿨고, 그 일을 하다 **공개 약속을
지키는 자리가 아예 없던 것**을 찾아 함께 고쳤다.

## 나간 것 (veo-platform)

```
93628efd  v0.3.556  재진단에 텀이 없다 — 시간이 아니라 판을 센다(예산 ÷ 한 판 = 9판/시간)
                    호스트 예산 450 → 2,000 · 콘솔 크롤이 판마다 새로 만들던 리미터를
                    console_crawler() 한 곳으로 모음 · /bot 공개 약속 2,000 으로
6806d955  v0.3.598  겹친 판 번호 여섯 쌍을 한 항목씩으로 합침(번호는 새로 안 매겼다)
                    claim_version.py 가 겹칠 번호에 서고 관문이 잡는다 ·
                    한 번도 안 그려지던 RescanCountdown 걷어냄
```

**둘 다 main 에 있고 나갔다.** [실측 2026-09-18 16:43 KST · 러너 실행 35320808955]
웹 `veo.seokorea.org` · 진단 서버 `/api/health` · 워커 `/api/queue` 셋 다 `0.3.604` ·
뒤처진 워커 0. (그 뒤 다른 방들이 이어 내 지금은 `0.3.607`.)

## 버린 것 하나 — 그리고 왜

가지에 `0ecd04d2`(대장 실측 줄 고침 · 문서만)가 남아 있었다. 사장님 «다음 코드 판에
같이 태워» 로 대기시켜 둔 것인데, **그 사이 다른 방이 같은 정리를 main 에 했다.**
하려던 일이 이미 되어 있으므로 버린다 — **하지 않은 일을 했다고 적을 수 없다.**

같은 모양이 이 방에서 두 번 났다. 대장은 방 여럿이 함께 만지는 자리라, 문서만으로
20분 창(preflight 12분 + CI 8분)을 걸면 먼저 닿는 쪽이 갖는다.

## 이 방이 남긴 것 — 다음 사람이 쓸 것

- **오류 대장 190** — 콘솔 크롤이 **판마다 리미터를 새로 만들어**, 「동일 호스트 시간당
  요청」이 한 시간의 총량이 아니라 **한 판 안에서의 상한**으로만 돌았다. `/bot` 안내
  페이지가 사이트 주인들께 한 공개 약속을 **지키는 자리가 없었다.**
  내가 그 자리를 두 번 틀리게 말한 것도 함께 적었다 — `cooldown.py` 머리말의 문장을
  **`[실측]` 표시가 없는데도 실측처럼 인용**했고, 그 위에서 사장님이 고르실 선택지까지
  만들었다. **처방: 저장소의 산문을 근거로 쓸 때 `[실측]` 표시가 있는지 먼저 본다.**
- **관문 셋이 실전에서 막았다** — 겹침 관문(`changelog-numbers-are-unique.test.ts`)이
  두 번(합칠 때마다 겹침이 새로 생겨 있었다), `claim_version.py::_refuse_to_relabel` 이
  한 번(문서만 바꾸는 판에도 변경이력 항목이 필요하다), 대장 관문이 한 번.
- **배포는 다섯 번 걸어 한 번 닿았다.** CI 는 걸 때마다 초록이었고 막힌 자리는 늘 마지막
  한 걸음이다. 판이 이 일 하나에 **네 번** 물렸다(554→556 · 595→596 · 596→597 · 597→598).
- **[5/5] 도달 확인은 작업 방에서 늘 실패한다**(egress) — **배포 실패가 아니라 확인을
  못 한 것**이다. 워크플로 「도는 판 확인」(`rollout-check.yml` · `workflow_dispatch`)을
  러너에서 돌려 바깥에서 잰다. 이 방이 쓸 수 있는 유일한 자다.

## 정리 (방을 지울 때)

```
veo-platform      가지 claude/seo-diagnosis-interval-removal-othn8e — 안 나간 커밋 1개,
                  위 까닭으로 버린다. 지워도 된다
desktop-tutorial  가지 같은 이름 — 인계 문서는 이미 main 에 있다(내용 중복). 지워도 된다
작업 트리         깨끗함 · 안 밀린 커밋 0
```
